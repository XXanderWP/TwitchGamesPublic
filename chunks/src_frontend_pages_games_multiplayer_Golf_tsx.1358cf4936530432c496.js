"use strict";
/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
(self["webpackChunk"] = self["webpackChunk"] || []).push([["src_frontend_pages_games_multiplayer_Golf_tsx"],{

/***/ "./src/frontend/styles/golf.less":
/*!***************************************!*\
  !*** ./src/frontend/styles/golf.less ***!
  \***************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n// extracted by mini-css-extract-plugin\n\n\n//# sourceURL=webpack:///./src/frontend/styles/golf.less?");

/***/ }),

/***/ "./src/frontend/pages/games/multiplayer/Golf.tsx":
/*!*******************************************************!*\
  !*** ./src/frontend/pages/games/multiplayer/Golf.tsx ***!
  \*******************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   GolfController: () => (/* binding */ GolfController)\n/* harmony export */ });\n/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react/jsx-runtime */ \"./node_modules/react/jsx-runtime.js\");\n/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! react */ \"./node_modules/react/index.js\");\n/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_1__);\n/* harmony import */ var three__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! three */ \"./node_modules/three/build/three.core.js\");\n/* harmony import */ var three__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! three */ \"./node_modules/three/build/three.module.js\");\n/* harmony import */ var rsuite__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! rsuite */ \"./node_modules/rsuite/esm/Button/index.js\");\n/* harmony import */ var rsuite__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! rsuite */ \"./node_modules/rsuite/esm/InputNumber/index.js\");\n/* harmony import */ var _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../../shared/games_params/golf */ \"./src/shared/games_params/golf.ts\");\n/* harmony import */ var _Controller__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./Controller */ \"./src/frontend/pages/games/multiplayer/Controller.tsx\");\n/* harmony import */ var _api__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../../../api */ \"./src/frontend/api.tsx\");\n/* harmony import */ var _controllers_sounds__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../../../controllers/sounds */ \"./src/frontend/controllers/sounds.ts\");\n/* harmony import */ var _styles_golf_less__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../../../styles/golf.less */ \"./src/frontend/styles/golf.less\");\n/* harmony import */ var _shared_system__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ../../../../shared/system */ \"./src/shared/system.ts\");\n/* harmony import */ var three_addons_geometries_TextGeometry_js__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! three/addons/geometries/TextGeometry.js */ \"./node_modules/three/examples/jsm/geometries/TextGeometry.js\");\n/* harmony import */ var three_examples_jsm_loaders_FontLoader_js__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! three/examples/jsm/loaders/FontLoader.js */ \"./node_modules/three/examples/jsm/loaders/FontLoader.js\");\n/* harmony import */ var three_examples_fonts_helvetiker_regular_typeface_json__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! three/examples/fonts/helvetiker_regular.typeface.json */ \"./node_modules/three/examples/fonts/helvetiker_regular.typeface.json\");\nvar __awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {\n    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }\n    return new (P || (P = Promise))(function (resolve, reject) {\n        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }\n        function rejected(value) { try { step(generator[\"throw\"](value)); } catch (e) { reject(e); } }\n        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }\n        step((generator = generator.apply(thisArg, _arguments || [])).next());\n    });\n};\n\n\n\n\n\n\n\n\n\n\n\n\n\nconst GOLF_SOUND_PATHS = {\n    shot: ['Plonk loud 02'],\n    shotOther: ['Plonk weak 02'],\n    // shot: ['Plonk loud 01', 'Plonk loud 02', 'Plonk loud 03'],\n    // shotOther: ['Plonk weak 01', 'Plonk weak 02', 'Plonk weak 03'],\n    wallBounce: 'BallHit',\n    pitFall: 'BallInWater',\n    hole: 'BallInHole',\n    rolling: '',\n};\nconst CAMERA_MIN_ZOOM = 6;\nconst CAMERA_MAX_ZOOM = 40;\nconst CAMERA_SCROLL_SPEED = 0.03;\nconst SHOT_CANCEL_DISTANCE = 0.3;\nconst SHOT_POWER_MULTIPLIER = 2.8;\nconst NAME_OFFSET = 0.9;\nconst BALL_FALL_DEPTH = 1.85;\nconst BALL_FALL_DURATION_MS = 650;\nconst BALL_RESPAWN_DELAY_MS = 1800;\nconst WATER_WAVE_BASE_AMPLITUDE = 0.018;\nconst WATER_WAVE_BASE_SPEED = 1.25;\nclass GolfController extends _Controller__WEBPACK_IMPORTED_MODULE_3__.MultiplayerGameController {\n    GetContainerViewport() {\n        const container = this.worldRef.current;\n        if (!container) {\n            return {\n                width: 1,\n                height: 1,\n            };\n        }\n        const rect = container.getBoundingClientRect();\n        return {\n            width: Math.max(1, Math.floor(rect.width || container.clientWidth || 1)),\n            height: Math.max(1, Math.floor(rect.height || container.clientHeight || 1)),\n        };\n    }\n    constructor(props) {\n        super(\"107\" /* GameID.GOLF */, _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GolfGameData, _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GolfPlayerData, props);\n        this.worldRef = react__WEBPACK_IMPORTED_MODULE_1___default().createRef();\n        this.raycaster = new three__WEBPACK_IMPORTED_MODULE_8__.Raycaster();\n        this.rayPoint = new three__WEBPACK_IMPORTED_MODULE_8__.Vector2();\n        this.dragPlane = new three__WEBPACK_IMPORTED_MODULE_8__.Plane(new three__WEBPACK_IMPORTED_MODULE_8__.Vector3(0, 1, 0), 0);\n        this.animationFrame = 0;\n        this.lastFrameAt = 0;\n        this.zoom = 18;\n        this.orbitYaw = 0;\n        this.orbitPitch = three__WEBPACK_IMPORTED_MODULE_8__.MathUtils.degToRad(_shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_CAMERA_ANGLE_DEG);\n        this.rightButtonDown = false;\n        this.cameraOffset = { x: 0, y: 0 };\n        this.keysDown = new Set();\n        this.balls_texts = new Map();\n        this.balls_avatars = new Map();\n        this.balls_avatars_textures = new Map();\n        this.balls_additions = new Map();\n        this.balls = new Map();\n        this.ballAnimations = new Map();\n        this.waterSurfaces = [];\n        this.mouseLeftDown = false;\n        this.mouseRightDown = false;\n        this.textMaterial = new three__WEBPACK_IMPORTED_MODULE_8__.MeshStandardMaterial({\n            color: '#ffffff',\n        });\n        this.InitThree = () => {\n            if (this.renderer || !this.worldRef.current)\n                return;\n            const container = this.worldRef.current;\n            const viewport = this.GetContainerViewport();\n            const width = viewport.width;\n            const height = viewport.height;\n            this.scene = new three__WEBPACK_IMPORTED_MODULE_8__.Scene();\n            this.scene.background = new three__WEBPACK_IMPORTED_MODULE_8__.Color('#152b14');\n            this.camera = new three__WEBPACK_IMPORTED_MODULE_8__.PerspectiveCamera(65, width / height, 0.1, 500);\n            const ambient = new three__WEBPACK_IMPORTED_MODULE_8__.AmbientLight(0xffffff, 1.2);\n            this.scene.add(ambient);\n            const dir = new three__WEBPACK_IMPORTED_MODULE_8__.DirectionalLight(0xffffff, 1.0);\n            dir.position.set(18, 30, 14);\n            this.scene.add(dir);\n            this.renderer = new three__WEBPACK_IMPORTED_MODULE_9__.WebGLRenderer({ antialias: true, alpha: false });\n            this.renderer.setSize(width, height);\n            this.renderer.setPixelRatio(Math.min(window.devicePixelRatio || 1, 2));\n            this.renderer.domElement.style.display = 'block';\n            this.renderer.domElement.style.width = '100%';\n            this.renderer.domElement.style.height = '100%';\n            container.appendChild(this.renderer.domElement);\n            container.addEventListener('wheel', this.OnWheel, { passive: false });\n            window.addEventListener('mousemove', this.OnRawMouseMove);\n            window.addEventListener('keydown', this.OnKeyDown);\n            window.addEventListener('keyup', this.OnKeyUp);\n            window.addEventListener('resize', this.OnResize);\n            this.BuildMapMeshes();\n            // The lobby/game layout can finish sizing after mount; force a few recalcs.\n            requestAnimationFrame(this.OnResize);\n            setTimeout(this.OnResize, 120);\n            setTimeout(this.OnResize, 320);\n            this.lastFrameAt = performance.now();\n            this.animationFrame = requestAnimationFrame(this.TickRender);\n        };\n        this.EnsureThreeReady = (attempt = 0) => {\n            if (this.renderer)\n                return;\n            this.InitThree();\n            if (this.renderer)\n                return;\n            if (attempt >= 25)\n                return;\n            if (this.initRetryTimer) {\n                clearTimeout(this.initRetryTimer);\n            }\n            this.initRetryTimer = setTimeout(() => {\n                this.EnsureThreeReady(attempt + 1);\n            }, 40);\n        };\n        this.DisposeThree = () => {\n            if (this.initRetryTimer) {\n                clearTimeout(this.initRetryTimer);\n                this.initRetryTimer = undefined;\n            }\n            if (!this.worldRef.current)\n                return;\n            const container = this.worldRef.current;\n            container.removeEventListener('wheel', this.OnWheel);\n            window.removeEventListener('mousemove', this.OnRawMouseMove);\n            window.removeEventListener('keydown', this.OnKeyDown);\n            window.removeEventListener('keyup', this.OnKeyUp);\n            window.removeEventListener('resize', this.OnResize);\n            if (this.animationFrame) {\n                cancelAnimationFrame(this.animationFrame);\n                this.animationFrame = 0;\n            }\n            if (this.renderer) {\n                const dom = this.renderer.domElement;\n                if (dom.parentElement === container) {\n                    container.removeChild(dom);\n                }\n                this.renderer.dispose();\n                this.renderer = undefined;\n            }\n            this.balls.clear();\n            this.ballAnimations.clear();\n            this.waterSurfaces = [];\n            this.mapRoot = undefined;\n            this.scene = undefined;\n            this.camera = undefined;\n        };\n        this.OnResize = () => {\n            if (!this.renderer || !this.camera || !this.worldRef.current)\n                return;\n            const viewport = this.GetContainerViewport();\n            const width = viewport.width;\n            const height = viewport.height;\n            const currentSize = new three__WEBPACK_IMPORTED_MODULE_8__.Vector2();\n            this.renderer.getSize(currentSize);\n            if (Math.abs(currentSize.x - width) < 1 &&\n                Math.abs(currentSize.y - height) < 1) {\n                return;\n            }\n            this.camera.aspect = width / height;\n            this.camera.updateProjectionMatrix();\n            this.renderer.setSize(width, height);\n        };\n        this.OnWheel = (e) => {\n            e.preventDefault();\n            this.zoom = (0,_shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.Clamp)(this.zoom + e.deltaY * CAMERA_SCROLL_SPEED, CAMERA_MIN_ZOOM, CAMERA_MAX_ZOOM);\n        };\n        this.OnRawMouseMove = (e) => {\n            var _a;\n            if (!this.rightButtonDown)\n                return;\n            if (document.pointerLockElement !== ((_a = this.renderer) === null || _a === void 0 ? void 0 : _a.domElement))\n                return;\n            this.orbitYaw -= e.movementX * 0.004;\n            this.orbitPitch = (0,_shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.Clamp)(this.orbitPitch + e.movementY * 0.004, three__WEBPACK_IMPORTED_MODULE_8__.MathUtils.degToRad(10), three__WEBPACK_IMPORTED_MODULE_8__.MathUtils.degToRad(85));\n        };\n        this.OnKeyDown = (e) => {\n            this.keysDown.add(e.code);\n            if (e.code === 'Space') {\n                this.cameraOffset = { x: 0, y: 0 };\n            }\n            if (e.code !== 'KeyR')\n                return;\n            if (this.state.stage !== 'ingame')\n                return;\n            const me = this.state.players.find(p => p.id === _api__WEBPACK_IMPORTED_MODULE_4__.Api.user_id);\n            if (!me || me.data.finished || me.data.failed)\n                return;\n            this.SendAction('golf.rollback');\n        };\n        this.OnKeyUp = (e) => {\n            this.keysDown.delete(e.code);\n        };\n        this.TickRender = (now) => {\n            this.animationFrame = requestAnimationFrame(this.TickRender);\n            if (!this.renderer || !this.scene || !this.camera)\n                return;\n            const delta = Math.max(0, now - this.lastFrameAt);\n            this.lastFrameAt = now;\n            this.EnsurePlayerBalls();\n            this.UpdateBallAnimations(delta);\n            this.SyncBallPositionsFromState();\n            this.UpdateCameraMovement(delta);\n            this.UpdateCamera();\n            this.UpdateWaterWaves(now);\n            this.UpdateShotPreviewLine();\n            this.OnResize();\n            this.updateTextPositions();\n            this.renderer.render(this.scene, this.camera);\n        };\n        this.OnJoinLobby = () => {\n            this.EnsureThreeReady();\n        };\n        this.OnLeaveLobby = () => {\n            this.DisposeThree();\n        };\n        this.OnUpdateGameData = (oldState, newState) => {\n            var _a, _b, _c, _d;\n            const oldSeed = (_b = (_a = oldState === null || oldState === void 0 ? void 0 : oldState.gameData) === null || _a === void 0 ? void 0 : _a.map) === null || _b === void 0 ? void 0 : _b.seed;\n            const newSeed = (_d = (_c = newState === null || newState === void 0 ? void 0 : newState.gameData) === null || _c === void 0 ? void 0 : _c.map) === null || _d === void 0 ? void 0 : _d.seed;\n            if (oldSeed !== newSeed) {\n                this.BuildMapMeshes();\n            }\n        };\n        this.MainScreen = () => {\n            const me = this.state.players.find(p => p.id === _api__WEBPACK_IMPORTED_MODULE_4__.Api.user_id);\n            const mapReady = !!this.state.gameData.map;\n            const canRegenerate = this.state.stage === 'waiting' &&\n                this.isHost() &&\n                this.state.gameData.canRegenerateMap;\n            const dragPower = this.dragStartBall && this.dragCurrent\n                ? (0,_shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.Clamp)((0,_shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.VecLen)({\n                    x: this.dragStartBall.x - this.dragCurrent.x,\n                    y: this.dragStartBall.y - this.dragCurrent.y,\n                }) * SHOT_POWER_MULTIPLIER, 0, this.state.gameData.maxShotPower || 1)\n                : 0;\n            return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"golfGame\", style: {\n                    minHeight: 420,\n                    height: 'min(72vh, 920px)',\n                }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"golfGame-world\", ref: this.worldRef }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"golfHud topLeft\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"line\", children: [this.LangString('GolfStrokes'), \": \", (me === null || me === void 0 ? void 0 : me.data.strokes) || 0, \" /\", ' ', this.state.gameData.maxStrokes] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"line\", children: [this.LangString('GolfTime'), \":\", ' ', _shared_system__WEBPACK_IMPORTED_MODULE_7__.System.secondsToString(Math.floor(((me === null || me === void 0 ? void 0 : me.data.timeMs) || 0) / 1000))] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"line\", children: [this.LangString('GolfLimitTime'), \":\", ' ', _shared_system__WEBPACK_IMPORTED_MODULE_7__.System.secondsToString(this.state.gameData.timeLimitSeconds || 0)] }), this.dragStartBall && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"line power\", children: [this.LangString('GolfPower'), \": \", Math.round(dragPower * 10) / 10] }))] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"golfHud topRight\", children: this.state.players\n                            .slice()\n                            .sort((a, b) => {\n                            if (a.data.finished !== b.data.finished) {\n                                return a.data.finished ? -1 : 1;\n                            }\n                            if (a.data.strokes !== b.data.strokes) {\n                                return a.data.strokes - b.data.strokes;\n                            }\n                            return a.data.timeMs - b.data.timeMs;\n                        })\n                            .map(player => ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"playerRow\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { className: \"name\", children: player.name }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { className: \"stats\", children: player.data.strokes }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { className: \"stats\", children: _shared_system__WEBPACK_IMPORTED_MODULE_7__.System.secondsToString(Math.floor(player.data.timeMs / 1000)) }), player.data.finished && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { className: \"state success\", children: this.LangString('GolfDone') })), player.data.failed && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { className: \"state fail\", children: this.LangString('GolfFail') }))] }, player.id))) }), !mapReady && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"golfCenterNotice\", children: this.LangString('GolfGenerating') })), canRegenerate && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"golfHud bottomLeft\", children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_10__[\"default\"], { appearance: \"ghost\", color: \"green\", onClick: () => this.SendAction('golf.regenerateMap'), children: this.LangString('GolfRegenerateMap') }) })), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"golfHud bottomRight\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"hintLine\", children: this.LangString('GolfHintShoot') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"hintLine\", children: this.LangString('GolfHintCamera') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"hintLine\", children: this.LangString('GolfHintMove') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"hintLine\", children: this.LangString('GolfHintReset') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"hintLine\", children: this.LangString('GolfHintZoom') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"hintLine\", children: this.LangString('GolfHintRollback') })] })] }));\n        };\n        this.SettingsScreen = () => {\n            const hostWaiting = this.isHost() && this.state.stage === 'waiting';\n            return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"golfSettingsBlock\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"settingRow\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { children: this.LangString('GolfMaxStrokes') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_11__[\"default\"], { min: _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_MIN_MAX_STROKES, max: _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_MAX_MAX_STROKES, step: 1, disabled: !hostWaiting, value: this.state.gameData.maxStrokes, onChange: value => {\n                                    const parsed = Math.round(Number(value));\n                                    if (!hostWaiting || !Number.isFinite(parsed))\n                                        return;\n                                    this.SendAction('golf.updateSettings', {\n                                        maxStrokes: parsed,\n                                    });\n                                } })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"settingRow\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { children: this.LangString('GolfTimeLimit') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_11__[\"default\"], { min: _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_MIN_TIME_LIMIT_SECONDS, max: _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_MAX_TIME_LIMIT_SECONDS, step: 10, disabled: !hostWaiting, value: this.state.gameData.timeLimitSeconds, onChange: value => {\n                                    const parsed = Math.round(Number(value));\n                                    if (!hostWaiting || !Number.isFinite(parsed))\n                                        return;\n                                    this.SendAction('golf.updateSettings', {\n                                        timeLimitSeconds: parsed,\n                                    });\n                                } })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"settingRow\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { children: this.LangString('GolfMaxPower') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_11__[\"default\"], { min: _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_MIN_MAX_SHOT_POWER, max: _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_MAX_MAX_SHOT_POWER, step: 1, disabled: !hostWaiting, value: this.state.gameData.maxShotPower, onChange: value => {\n                                    const parsed = Number(value);\n                                    if (!hostWaiting || !Number.isFinite(parsed))\n                                        return;\n                                    this.SendAction('golf.updateSettings', {\n                                        maxShotPower: parsed,\n                                    });\n                                } })] })] }));\n        };\n        this.OnJoin = () => {\n            this.EnsureThreeReady();\n        };\n        this.OnLeave = () => {\n            this.DisposeThree();\n        };\n        this.socketsListeners.push(this.OnMultiplayerAction('golf.shotApplied', payload => {\n            const playerId = payload === null || payload === void 0 ? void 0 : payload.playerId;\n            const simulation = payload === null || payload === void 0 ? void 0 : payload.simulation;\n            const serverBall = payload === null || payload === void 0 ? void 0 : payload.serverBall;\n            if (!playerId || !simulation || !serverBall)\n                return;\n            this.StartBallAnimation(playerId, simulation, serverBall);\n            this.PlayGolfSound('rolling');\n            (simulation.events || []).forEach(event => {\n                if (event.type === 'wallBounce')\n                    this.PlayGolfSound('wallBounce');\n            });\n        }));\n        this.socketsListeners.push(this.OnMultiplayerAction('golf.rollbackApplied', payload => {\n            const playerId = payload === null || payload === void 0 ? void 0 : payload.playerId;\n            const ball = payload === null || payload === void 0 ? void 0 : payload.ball;\n            if (!playerId || !ball)\n                return;\n            const mesh = this.balls.get(playerId);\n            if (mesh) {\n                mesh.position.set(ball.x, _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_BALL_RADIUS, ball.y);\n            }\n        }));\n    }\n    PlayGolfSound(key) {\n        const sound = GOLF_SOUND_PATHS[key];\n        if (!sound)\n            return;\n        (0,_controllers_sounds__WEBPACK_IMPORTED_MODULE_5__.PlaySound)(sound);\n    }\n    RegisterWaterSurface(mesh, baseY) {\n        this.waterSurfaces.push({\n            mesh,\n            baseY,\n            phase: 0,\n            amplitude: WATER_WAVE_BASE_AMPLITUDE,\n            speed: WATER_WAVE_BASE_SPEED,\n        });\n    }\n    UpdateWaterWaves(nowMs) {\n        if (!this.waterSurfaces.length)\n            return;\n        const t = nowMs / 1000;\n        for (const surface of this.waterSurfaces) {\n            const wave = Math.sin(t * surface.speed + surface.phase);\n            surface.mesh.position.y = surface.baseY + wave * surface.amplitude;\n        }\n    }\n    UpdateCameraMovement(deltaMs) {\n        const shiftMul = this.keysDown.has('ShiftLeft') || this.keysDown.has('ShiftRight') ? 2 : 1;\n        const speed = 0.018 * deltaMs * shiftMul;\n        let dx = 0;\n        let dz = 0;\n        if (this.keysDown.has('KeyW'))\n            dz += 1;\n        if (this.keysDown.has('KeyS'))\n            dz -= 1;\n        if (this.keysDown.has('KeyA'))\n            dx -= 1;\n        if (this.keysDown.has('KeyD'))\n            dx += 1;\n        if (dx === 0 && dz === 0)\n            return;\n        const len = Math.sqrt(dx * dx + dz * dz);\n        dx /= len;\n        dz /= len;\n        const sinY = Math.sin(this.orbitYaw);\n        const cosY = Math.cos(this.orbitYaw);\n        const forwardX = -sinY;\n        const forwardZ = -cosY;\n        const rightX = cosY;\n        const rightZ = -sinY;\n        this.cameraOffset.x += (forwardX * dz + rightX * dx) * speed;\n        this.cameraOffset.y += (forwardZ * dz + rightZ * dx) * speed;\n    }\n    BuildMapMeshes() {\n        if (!this.scene)\n            return;\n        if (this.mapRoot) {\n            this.scene.remove(this.mapRoot);\n        }\n        this.waterSurfaces = [];\n        const map = this.state.gameData.map;\n        if (!map)\n            return;\n        const root = new three__WEBPACK_IMPORTED_MODULE_8__.Group();\n        const grassMat = new three__WEBPACK_IMPORTED_MODULE_8__.MeshStandardMaterial({\n            color: '#3f7f39',\n            roughness: 0.92,\n            metalness: 0.02,\n        });\n        const cellSize = _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_BALL_RADIUS * 10;\n        const cols = Math.ceil(map.width / cellSize);\n        const rows = Math.ceil(map.height / cellSize);\n        const isRectPit = (cx, cy, cw, ch) => {\n            for (const pit of map.pitRects || []) {\n                const ox = Math.max(cx, pit.x);\n                const oy = Math.max(cy, pit.y);\n                const ex = Math.min(cx + cw, pit.x + pit.w);\n                const ey = Math.min(cy + ch, pit.y + pit.h);\n                if (ox < ex && oy < ey) {\n                    const overlapArea = (ex - ox) * (ey - oy);\n                    const cellArea = cw * ch;\n                    if (overlapArea > cellArea * 0.5)\n                        return true;\n                }\n            }\n            return false;\n        };\n        const getCirclePits = (cx, cy, cw, ch) => {\n            const result = [];\n            for (const pit of map.pits) {\n                if (pit.x + pit.r > cx &&\n                    pit.x - pit.r < cx + cw &&\n                    pit.y + pit.r > cy &&\n                    pit.y - pit.r < cy + ch) {\n                    result.push(pit);\n                }\n            }\n            return result;\n        };\n        const isHoleInCell = (cx, cy, cw, ch) => {\n            const hole = map.hole;\n            const holeRadius = _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_HOLE_RADIUS;\n            return (hole.x + holeRadius > cx &&\n                hole.x - holeRadius < cx + cw &&\n                hole.y + holeRadius > cy &&\n                hole.y - holeRadius < cy + ch);\n        };\n        for (let row = 0; row < rows; row++) {\n            for (let col = 0; col < cols; col++) {\n                const cx = col * cellSize;\n                const cy = row * cellSize;\n                const cw = Math.min(cellSize, map.width - cx);\n                const ch = Math.min(cellSize, map.height - cy);\n                if (isRectPit(cx, cy, cw, ch))\n                    continue;\n                const circlePits = getCirclePits(cx, cy, cw, ch);\n                const hasHole = isHoleInCell(cx, cy, cw, ch);\n                if (circlePits.length === 0 && !hasHole) {\n                    const plane = new three__WEBPACK_IMPORTED_MODULE_8__.Mesh(new three__WEBPACK_IMPORTED_MODULE_8__.PlaneGeometry(cw, ch), grassMat);\n                    plane.rotation.x = -Math.PI / 2;\n                    plane.position.set(cx + cw / 2, 0, cy + ch / 2);\n                    root.add(plane);\n                }\n                else if (circlePits.length > 0 || hasHole) {\n                    const shape = new three__WEBPACK_IMPORTED_MODULE_8__.Shape();\n                    shape.moveTo(0, 0);\n                    shape.lineTo(cw, 0);\n                    shape.lineTo(cw, ch);\n                    shape.lineTo(0, ch);\n                    shape.closePath();\n                    circlePits.forEach(pit => {\n                        const localX = pit.x - cx;\n                        const localY = ch - (pit.y - cy);\n                        const path = new three__WEBPACK_IMPORTED_MODULE_8__.Path();\n                        const segs = 28;\n                        for (let i = 0; i <= segs; i++) {\n                            const a = (i / segs) * Math.PI * 2;\n                            const px = localX + Math.cos(a) * pit.r;\n                            const py = localY + Math.sin(a) * pit.r;\n                            if (i === 0)\n                                path.moveTo(px, py);\n                            else\n                                path.lineTo(px, py);\n                        }\n                        path.closePath();\n                        shape.holes.push(path);\n                    });\n                    if (hasHole) {\n                        const localX = map.hole.x - cx;\n                        const localY = ch - (map.hole.y - cy);\n                        const path = new three__WEBPACK_IMPORTED_MODULE_8__.Path();\n                        const segs = 28;\n                        for (let i = 0; i <= segs; i++) {\n                            const a = (i / segs) * Math.PI * 2;\n                            const px = localX + Math.cos(a) * _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_HOLE_RADIUS;\n                            const py = localY + Math.sin(a) * _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_HOLE_RADIUS;\n                            if (i === 0)\n                                path.moveTo(px, py);\n                            else\n                                path.lineTo(px, py);\n                        }\n                        path.closePath();\n                        shape.holes.push(path);\n                    }\n                    const geom = new three__WEBPACK_IMPORTED_MODULE_8__.ShapeGeometry(shape, 4);\n                    const mesh = new three__WEBPACK_IMPORTED_MODULE_8__.Mesh(geom, grassMat);\n                    mesh.rotation.x = -Math.PI / 2;\n                    mesh.position.set(cx, 0, cy + ch);\n                    root.add(mesh);\n                }\n            }\n        }\n        const holeMat = new three__WEBPACK_IMPORTED_MODULE_8__.MeshStandardMaterial({ color: '#151515' });\n        const hole = new three__WEBPACK_IMPORTED_MODULE_8__.Mesh(new three__WEBPACK_IMPORTED_MODULE_8__.CylinderGeometry(_shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_HOLE_RADIUS, _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_HOLE_RADIUS, 0.1, 40), holeMat);\n        hole.position.set(map.hole.x, -1.2, map.hole.y);\n        root.add(hole);\n        const poleYoffset = -2;\n        const poleHeight = 4.0 + poleYoffset * -1;\n        const poleRadius = 0.04;\n        const poleMat = new three__WEBPACK_IMPORTED_MODULE_8__.MeshStandardMaterial({\n            color: '#cccccc',\n            metalness: 0.4,\n            roughness: 0.3,\n        });\n        const pole = new three__WEBPACK_IMPORTED_MODULE_8__.Mesh(new three__WEBPACK_IMPORTED_MODULE_8__.CylinderGeometry(poleRadius, poleRadius, poleHeight, 8), poleMat);\n        pole.position.set(map.hole.x, poleHeight / 2 + poleYoffset, map.hole.y);\n        root.add(pole);\n        const flagWidth = 0.91;\n        const flagHeight = 0.52;\n        const flagGeom = new three__WEBPACK_IMPORTED_MODULE_8__.PlaneGeometry(flagWidth, flagHeight);\n        const flagMat = new three__WEBPACK_IMPORTED_MODULE_8__.MeshStandardMaterial({\n            color: '#e02020',\n            side: three__WEBPACK_IMPORTED_MODULE_8__.DoubleSide,\n        });\n        const flag = new three__WEBPACK_IMPORTED_MODULE_8__.Mesh(flagGeom, flagMat);\n        flag.position.set(map.hole.x + flagWidth / 2, poleHeight - flagHeight / 2 + poleYoffset, map.hole.y);\n        root.add(flag);\n        const wallMat = new three__WEBPACK_IMPORTED_MODULE_8__.MeshStandardMaterial({\n            color: '#7f7f7f',\n            roughness: 0.8,\n        });\n        map.walls.forEach(wall => {\n            const box = new three__WEBPACK_IMPORTED_MODULE_8__.Mesh(new three__WEBPACK_IMPORTED_MODULE_8__.BoxGeometry(wall.w, 2.08, wall.h), wallMat);\n            box.position.set(wall.x + wall.w / 2, 1.04, wall.y + wall.h / 2);\n            root.add(box);\n        });\n        const stoneMat = new three__WEBPACK_IMPORTED_MODULE_8__.MeshStandardMaterial({ color: '#9a9a9a' });\n        map.stones.forEach(stone => {\n            const mesh = new three__WEBPACK_IMPORTED_MODULE_8__.Mesh(new three__WEBPACK_IMPORTED_MODULE_8__.SphereGeometry(stone.r, 12, 12), stoneMat);\n            mesh.position.set(stone.x, stone.r * 0.6, stone.y);\n            root.add(mesh);\n        });\n        const pitDepth = 1.5;\n        const pitWaterLevel = -0.16;\n        const pitDarkMat = new three__WEBPACK_IMPORTED_MODULE_8__.MeshStandardMaterial({ color: '#1a1a1a' });\n        const pitEdgeMat = new three__WEBPACK_IMPORTED_MODULE_8__.MeshStandardMaterial({\n            color: '#2e5a28',\n            roughness: 0.95,\n        });\n        const pitWaterMat = new three__WEBPACK_IMPORTED_MODULE_8__.MeshStandardMaterial({\n            color: '#2e7d8f',\n            roughness: 0.18,\n            metalness: 0.04,\n            transparent: true,\n            opacity: 0.78,\n        });\n        // Merge adjacent pitRects into groups using flood-fill on cell grid\n        const pitCellSet = new Set();\n        const pitCells = [];\n        (map.pitRects || []).forEach(pit => {\n            const col = Math.round(pit.x / cellSize);\n            const row = Math.round(pit.y / cellSize);\n            const k = `${col},${row}`;\n            if (!pitCellSet.has(k)) {\n                pitCellSet.add(k);\n                pitCells.push({ col, row });\n            }\n        });\n        const visitedPits = new Set();\n        const pitGroups = [];\n        pitCells.forEach(cell => {\n            const k = `${cell.col},${cell.row}`;\n            if (visitedPits.has(k))\n                return;\n            const group = [];\n            const stack = [cell];\n            while (stack.length) {\n                const c = stack.pop();\n                const ck = `${c.col},${c.row}`;\n                if (visitedPits.has(ck))\n                    continue;\n                if (!pitCellSet.has(ck))\n                    continue;\n                visitedPits.add(ck);\n                group.push(c);\n                stack.push({ col: c.col + 1, row: c.row });\n                stack.push({ col: c.col - 1, row: c.row });\n                stack.push({ col: c.col, row: c.row + 1 });\n                stack.push({ col: c.col, row: c.row - 1 });\n            }\n            if (group.length)\n                pitGroups.push(group);\n        });\n        pitGroups.forEach(group => {\n            const groupSet = new Set(group.map(c => `${c.col},${c.row}`));\n            // Bottom for each cell\n            group.forEach(c => {\n                const x = c.col * cellSize;\n                const z = c.row * cellSize;\n                const bottom = new three__WEBPACK_IMPORTED_MODULE_8__.Mesh(new three__WEBPACK_IMPORTED_MODULE_8__.PlaneGeometry(cellSize, cellSize), pitDarkMat);\n                bottom.rotation.x = Math.PI / 2;\n                bottom.position.set(x + cellSize / 2, -pitDepth, z + cellSize / 2);\n                root.add(bottom);\n                const water = new three__WEBPACK_IMPORTED_MODULE_8__.Mesh(new three__WEBPACK_IMPORTED_MODULE_8__.PlaneGeometry(cellSize, cellSize), pitWaterMat);\n                water.rotation.x = -Math.PI / 2;\n                water.position.set(x + cellSize / 2, pitWaterLevel, z + cellSize / 2);\n                root.add(water);\n                this.RegisterWaterSurface(water, pitWaterLevel);\n            });\n            // Walls only on outer edges\n            group.forEach(c => {\n                const x = c.col * cellSize;\n                const z = c.row * cellSize;\n                // front (z + cellSize) — no neighbor at row+1\n                if (!groupSet.has(`${c.col},${c.row + 1}`)) {\n                    const w = new three__WEBPACK_IMPORTED_MODULE_8__.Mesh(new three__WEBPACK_IMPORTED_MODULE_8__.PlaneGeometry(cellSize, pitDepth), pitEdgeMat);\n                    w.position.set(x + cellSize / 2, -pitDepth / 2, z + cellSize);\n                    w.rotation.y = Math.PI;\n                    root.add(w);\n                }\n                // back (z) — no neighbor at row-1\n                if (!groupSet.has(`${c.col},${c.row - 1}`)) {\n                    const w = new three__WEBPACK_IMPORTED_MODULE_8__.Mesh(new three__WEBPACK_IMPORTED_MODULE_8__.PlaneGeometry(cellSize, pitDepth), pitEdgeMat);\n                    w.position.set(x + cellSize / 2, -pitDepth / 2, z);\n                    root.add(w);\n                }\n                // left (x) — no neighbor at col-1\n                if (!groupSet.has(`${c.col - 1},${c.row}`)) {\n                    const w = new three__WEBPACK_IMPORTED_MODULE_8__.Mesh(new three__WEBPACK_IMPORTED_MODULE_8__.PlaneGeometry(cellSize, pitDepth), pitEdgeMat);\n                    w.position.set(x, -pitDepth / 2, z + cellSize / 2);\n                    w.rotation.y = Math.PI / 2;\n                    root.add(w);\n                }\n                // right (x + cellSize) — no neighbor at col+1\n                if (!groupSet.has(`${c.col + 1},${c.row}`)) {\n                    const w = new three__WEBPACK_IMPORTED_MODULE_8__.Mesh(new three__WEBPACK_IMPORTED_MODULE_8__.PlaneGeometry(cellSize, pitDepth), pitEdgeMat);\n                    w.position.set(x + cellSize, -pitDepth / 2, z + cellSize / 2);\n                    w.rotation.y = -Math.PI / 2;\n                    root.add(w);\n                }\n            });\n        });\n        map.pits.forEach(pit => {\n            const bottom = new three__WEBPACK_IMPORTED_MODULE_8__.Mesh(new three__WEBPACK_IMPORTED_MODULE_8__.CircleGeometry(pit.r * 0.9, 28), pitDarkMat);\n            bottom.rotation.x = Math.PI / 2;\n            bottom.position.set(pit.x, -pitDepth, pit.y);\n            root.add(bottom);\n            const water = new three__WEBPACK_IMPORTED_MODULE_8__.Mesh(new three__WEBPACK_IMPORTED_MODULE_8__.CircleGeometry(pit.r * 1.05, 28), pitWaterMat);\n            water.rotation.x = -Math.PI / 2;\n            water.position.set(pit.x, pitWaterLevel, pit.y);\n            root.add(water);\n            this.RegisterWaterSurface(water, pitWaterLevel);\n            const wallGeom = new three__WEBPACK_IMPORTED_MODULE_8__.CylinderGeometry(pit.r, pit.r * 0.9, pitDepth, 28, 1, true);\n            const wallMesh = new three__WEBPACK_IMPORTED_MODULE_8__.Mesh(wallGeom, new three__WEBPACK_IMPORTED_MODULE_8__.MeshStandardMaterial({\n                color: '#2e5a28',\n                roughness: 0.95,\n                side: three__WEBPACK_IMPORTED_MODULE_8__.BackSide,\n            }));\n            wallMesh.position.set(pit.x, -pitDepth / 2, pit.y);\n            root.add(wallMesh);\n        });\n        const spawnMarker = new three__WEBPACK_IMPORTED_MODULE_8__.Mesh(new three__WEBPACK_IMPORTED_MODULE_8__.RingGeometry(0.5, 0.9, 24), new three__WEBPACK_IMPORTED_MODULE_8__.MeshBasicMaterial({ color: '#d7f06f', side: three__WEBPACK_IMPORTED_MODULE_8__.DoubleSide }));\n        spawnMarker.rotation.x = -Math.PI / 2;\n        spawnMarker.position.set(map.spawn.x, 0.04, map.spawn.y);\n        root.add(spawnMarker);\n        this.mapRoot = root;\n        this.scene.add(root);\n    }\n    EnsurePlayerBalls() {\n        return __awaiter(this, void 0, void 0, function* () {\n            if (!this.scene)\n                return;\n            const activeIds = new Set(this.state.players.map(p => p.id));\n            [\n                this.balls,\n                this.balls_avatars,\n                this.balls_texts,\n                this.balls_additions,\n            ].forEach(map => {\n                Array.from(map.keys()).forEach(id => {\n                    var _a;\n                    if (activeIds.has(id))\n                        return;\n                    const mesh = map.get(id);\n                    if (mesh) {\n                        (_a = this.scene) === null || _a === void 0 ? void 0 : _a.remove(mesh);\n                    }\n                    map.delete(id);\n                });\n            });\n            this.balls_avatars_textures.forEach((tex, id) => {\n                if (activeIds.has(id))\n                    return;\n                tex.dispose();\n                this.balls_avatars_textures.delete(id);\n            });\n            const loader = new three_examples_jsm_loaders_FontLoader_js__WEBPACK_IMPORTED_MODULE_12__.FontLoader();\n            const font = loader.parse(three_examples_fonts_helvetiker_regular_typeface_json__WEBPACK_IMPORTED_MODULE_13__);\n            this.state.players.forEach(player => {\n                var _a, _b;\n                if (this.balls.has(player.id))\n                    return;\n                const myBall = player.id === _api__WEBPACK_IMPORTED_MODULE_4__.Api.user_id;\n                const mesh = new three__WEBPACK_IMPORTED_MODULE_8__.Mesh(new three__WEBPACK_IMPORTED_MODULE_8__.SphereGeometry(_shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_BALL_RADIUS, 18, 18), new three__WEBPACK_IMPORTED_MODULE_8__.MeshStandardMaterial({\n                    color: myBall ? '#f7f7f7' : '#a3d9ff',\n                    transparent: !myBall,\n                    opacity: myBall ? 1 : 0.45,\n                }));\n                mesh.position.set(player.data.ball.x, _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_BALL_RADIUS, player.data.ball.y);\n                mesh.visible = !player.data.finished;\n                (_a = this.scene) === null || _a === void 0 ? void 0 : _a.add(mesh);\n                this.balls.set(player.id, mesh);\n                if (player.id !== _api__WEBPACK_IMPORTED_MODULE_4__.Api.user_id) {\n                    const nameplate = new three__WEBPACK_IMPORTED_MODULE_8__.Group();\n                    const textGeometry = new three_addons_geometries_TextGeometry_js__WEBPACK_IMPORTED_MODULE_14__.TextGeometry(player.name, {\n                        font: font,\n                        size: 0.5,\n                        depth: 0.1,\n                        curveSegments: 12,\n                    });\n                    textGeometry.computeBoundingBox();\n                    const box = textGeometry.boundingBox;\n                    if (box) {\n                        const offsetX = (box.max.x - box.min.x) / 2;\n                        const offsetY = (box.max.y - box.min.y) / 2;\n                        textGeometry.translate(-offsetX, -offsetY, 0);\n                    }\n                    const textMesh = new three__WEBPACK_IMPORTED_MODULE_8__.Mesh(textGeometry, this.textMaterial);\n                    textMesh.position.set(0, 0, 0);\n                    nameplate.add(textMesh);\n                    this.balls_texts.set(player.id, textMesh);\n                    if (player.avatar) {\n                        const avatarTexture = new three__WEBPACK_IMPORTED_MODULE_8__.TextureLoader().load(player.avatar);\n                        const avatarMaterial = new three__WEBPACK_IMPORTED_MODULE_8__.MeshStandardMaterial({\n                            map: avatarTexture,\n                            roughness: 0.5,\n                            metalness: 0.1,\n                        });\n                        const avatarMesh = new three__WEBPACK_IMPORTED_MODULE_8__.Mesh(new three__WEBPACK_IMPORTED_MODULE_8__.CircleGeometry(0.4, 32), avatarMaterial);\n                        avatarMesh.position.set(0, 0.8, 0);\n                        nameplate.add(avatarMesh);\n                        this.balls_avatars.set(player.id, avatarMesh);\n                        this.balls_avatars_textures.set(player.id, avatarTexture);\n                    }\n                    (_b = this.scene) === null || _b === void 0 ? void 0 : _b.add(nameplate);\n                    this.balls_additions.set(player.id, nameplate);\n                }\n            });\n        });\n    }\n    StartBallAnimation(playerId, simulation, serverBall) {\n        const path = (simulation.path || []).length\n            ? simulation.path\n            : [simulation.safePosition, serverBall];\n        const fallEvent = (simulation.events || []).find(e => e.type === 'pitFall' || e.type === 'hole');\n        const baseDuration = Math.max(400, simulation.durationMs || 800);\n        this.ballAnimations.set(playerId, {\n            path,\n            elapsedMs: 0,\n            moveDurationMs: baseDuration * 2.2,\n            dropDurationMs: fallEvent ? BALL_FALL_DURATION_MS : 0,\n            respawnDelayMs: fallEvent ? BALL_RESPAWN_DELAY_MS : 0,\n            serverBall,\n            fellInHole: simulation.inHole,\n            fallAt: fallEvent ? Object.assign({}, fallEvent.at) : undefined,\n            fallSoundKey: (fallEvent === null || fallEvent === void 0 ? void 0 : fallEvent.type) === 'pitFall' || (fallEvent === null || fallEvent === void 0 ? void 0 : fallEvent.type) === 'hole'\n                ? fallEvent.type\n                : undefined,\n            fallSoundPlayed: false,\n        });\n    }\n    UpdateBallAnimations(deltaMs) {\n        this.ballAnimations.forEach((anim, playerId) => {\n            const mesh = this.balls.get(playerId);\n            if (!mesh)\n                return;\n            anim.elapsedMs += deltaMs;\n            const totalMs = anim.moveDurationMs + anim.dropDurationMs + anim.respawnDelayMs;\n            if (anim.fallAt && anim.elapsedMs > anim.moveDurationMs) {\n                const fallElapsed = anim.elapsedMs - anim.moveDurationMs;\n                if (!anim.fallSoundPlayed && anim.fallSoundKey) {\n                    this.PlayGolfSound(anim.fallSoundKey);\n                    anim.fallSoundPlayed = true;\n                }\n                if (fallElapsed <= anim.dropDurationMs) {\n                    const dropT = (0,_shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.Clamp)(fallElapsed / anim.dropDurationMs, 0, 1);\n                    const easedDrop = dropT * dropT;\n                    mesh.position.set(anim.fallAt.x, _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_BALL_RADIUS - BALL_FALL_DEPTH * easedDrop, anim.fallAt.y);\n                    return;\n                }\n                if (anim.elapsedMs < totalMs) {\n                    mesh.position.set(anim.fallAt.x, _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_BALL_RADIUS - BALL_FALL_DEPTH, anim.fallAt.y);\n                    return;\n                }\n                if (anim.fellInHole) {\n                    mesh.visible = false;\n                    this.ballAnimations.delete(playerId);\n                    return;\n                }\n                mesh.position.set(anim.serverBall.x, _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_BALL_RADIUS, anim.serverBall.y);\n                this.ballAnimations.delete(playerId);\n                return;\n            }\n            const t = (0,_shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.Clamp)(anim.elapsedMs / anim.moveDurationMs, 0, 1);\n            const segmentCount = Math.max(1, anim.path.length - 1);\n            const scaled = t * segmentCount;\n            const index = Math.min(segmentCount - 1, Math.floor(scaled));\n            const localT = scaled - index;\n            const p0 = anim.path[index] || anim.path[0];\n            const p1 = anim.path[index + 1] || p0;\n            const x = p0.x + (p1.x - p0.x) * localT;\n            const y = p0.y + (p1.y - p0.y) * localT;\n            mesh.position.set(x, _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_BALL_RADIUS, y);\n            if (t >= 1) {\n                mesh.position.set(anim.serverBall.x, _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_BALL_RADIUS, anim.serverBall.y);\n                this.ballAnimations.delete(playerId);\n            }\n        });\n    }\n    SyncBallPositionsFromState() {\n        this.state.players.forEach(player => {\n            if (this.ballAnimations.has(player.id))\n                return;\n            const mesh = this.balls.get(player.id);\n            if (!mesh)\n                return;\n            if (player.data.finished) {\n                mesh.visible = false;\n                return;\n            }\n            mesh.visible = true;\n            mesh.position.set(player.data.ball.x, _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_BALL_RADIUS, player.data.ball.y);\n        });\n    }\n    GetGroundPoint(screenX, screenY) {\n        if (!this.camera || !this.renderer)\n            return;\n        const rect = this.renderer.domElement.getBoundingClientRect();\n        if (!rect.width || !rect.height)\n            return;\n        this.rayPoint.x = ((screenX - rect.left) / rect.width) * 2 - 1;\n        this.rayPoint.y = -((screenY - rect.top) / rect.height) * 2 + 1;\n        this.raycaster.setFromCamera(this.rayPoint, this.camera);\n        const hit = new three__WEBPACK_IMPORTED_MODULE_8__.Vector3();\n        const ok = this.raycaster.ray.intersectPlane(this.dragPlane, hit);\n        if (!ok)\n            return;\n        return {\n            x: hit.x,\n            y: hit.z,\n        };\n    }\n    GetMyBallPosition() {\n        const me = this.state.players.find(p => p.id === _api__WEBPACK_IMPORTED_MODULE_4__.Api.user_id);\n        return me === null || me === void 0 ? void 0 : me.data.ball;\n    }\n    CanShoot() {\n        if (this.state.stage !== 'ingame')\n            return false;\n        const me = this.state.players.find(p => p.id === _api__WEBPACK_IMPORTED_MODULE_4__.Api.user_id);\n        if (!me)\n            return false;\n        if (me.data.finished || me.data.failed)\n            return false;\n        return true;\n    }\n    TryStartShot(worldPoint) {\n        if (!worldPoint || !this.CanShoot())\n            return;\n        const myBall = this.GetMyBallPosition();\n        if (!myBall)\n            return;\n        const distance = (0,_shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.VecLen)({\n            x: myBall.x - worldPoint.x,\n            y: myBall.y - worldPoint.y,\n        });\n        if (distance > _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_BALL_RADIUS * 2.0)\n            return;\n        this.dragStartBall = Object.assign({}, myBall);\n        this.dragCurrent = Object.assign({}, worldPoint);\n    }\n    CancelShotDrag() {\n        this.dragStartBall = undefined;\n        this.dragCurrent = undefined;\n    }\n    TryCommitShot() {\n        if (!this.dragStartBall || !this.dragCurrent) {\n            this.CancelShotDrag();\n            return;\n        }\n        const vec = {\n            x: this.dragStartBall.x - this.dragCurrent.x,\n            y: this.dragStartBall.y - this.dragCurrent.y,\n        };\n        const dist = (0,_shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.VecLen)(vec);\n        if (dist <= SHOT_CANCEL_DISTANCE) {\n            this.CancelShotDrag();\n            return;\n        }\n        const maxPower = this.state.gameData.maxShotPower || 22;\n        const power = (0,_shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.Clamp)(dist * SHOT_POWER_MULTIPLIER, 0, maxPower);\n        const direction = {\n            x: vec.x / dist,\n            y: vec.y / dist,\n        };\n        const shot = {\n            shotId: `${Date.now()}_${_shared_system__WEBPACK_IMPORTED_MODULE_7__.System.randomString(4)}`,\n            origin: Object.assign({}, this.dragStartBall),\n            direction,\n            power,\n        };\n        const map = this.state.gameData.map;\n        if (map) {\n            const simulated = (0,_shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.SimulateGolfShot)(map, shot, maxPower);\n            this.StartBallAnimation(_api__WEBPACK_IMPORTED_MODULE_4__.Api.user_id, simulated, simulated.safePosition);\n            this.SendAction('golf.shoot', Object.assign(Object.assign({}, shot), { predictedFinal: simulated.finalPosition }));\n            this.PlayGolfSound('shot');\n        }\n        this.CancelShotDrag();\n    }\n    GetAnimatedBallPosition() {\n        const anim = this.ballAnimations.get(_api__WEBPACK_IMPORTED_MODULE_4__.Api.user_id);\n        if (!anim)\n            return undefined;\n        const mesh = this.balls.get(_api__WEBPACK_IMPORTED_MODULE_4__.Api.user_id);\n        if (!mesh)\n            return undefined;\n        return { x: mesh.position.x, y: mesh.position.z };\n    }\n    UpdateCamera() {\n        if (!this.camera)\n            return;\n        const map = this.state.gameData.map;\n        if (!map)\n            return;\n        const me = this.state.players.find(p => p.id === _api__WEBPACK_IMPORTED_MODULE_4__.Api.user_id);\n        const animPos = this.GetAnimatedBallPosition();\n        const myBall = this.GetMyBallPosition();\n        let baseFocus;\n        if (this.state.stage !== 'ingame' || (me === null || me === void 0 ? void 0 : me.data.finished)) {\n            baseFocus = {\n                x: map.hole.x,\n                y: map.hole.y,\n            };\n        }\n        else if (animPos || myBall) {\n            baseFocus = animPos || myBall;\n        }\n        else {\n            baseFocus = {\n                x: map.hole.x,\n                y: map.hole.y,\n            };\n        }\n        const focus = {\n            x: baseFocus.x + this.cameraOffset.x,\n            y: baseFocus.y + this.cameraOffset.y,\n        };\n        const horizontal = this.zoom * Math.cos(this.orbitPitch);\n        const height = this.zoom * Math.sin(this.orbitPitch);\n        const camX = focus.x + Math.sin(this.orbitYaw) * horizontal;\n        const camZ = focus.y + Math.cos(this.orbitYaw) * horizontal;\n        this.camera.position.set(camX, height, camZ);\n        this.camera.lookAt(focus.x, 0, focus.y);\n    }\n    UpdateShotPreviewLine() {\n        if (!this.scene)\n            return;\n        if (!this.dragStartBall || !this.dragCurrent) {\n            if (this.shotPreviewLine) {\n                this.scene.remove(this.shotPreviewLine);\n                this.shotPreviewLine = undefined;\n            }\n            return;\n        }\n        const vec = {\n            x: this.dragStartBall.x - this.dragCurrent.x,\n            y: this.dragStartBall.y - this.dragCurrent.y,\n        };\n        const dist = (0,_shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.VecLen)(vec);\n        if (dist <= SHOT_CANCEL_DISTANCE) {\n            if (this.shotPreviewLine) {\n                this.scene.remove(this.shotPreviewLine);\n                this.shotPreviewLine = undefined;\n            }\n            return;\n        }\n        const maxPower = this.state.gameData.maxShotPower || 22;\n        const power = (0,_shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.Clamp)(dist * SHOT_POWER_MULTIPLIER, 0, maxPower);\n        const dir = { x: vec.x / dist, y: vec.y / dist };\n        const lineLen = (0,_shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.Clamp)(power * 0.6, 1, 12);\n        const segments = 20;\n        const points = [];\n        for (let i = 0; i <= segments; i++) {\n            const t = i / segments;\n            const d = t * lineLen;\n            points.push(new three__WEBPACK_IMPORTED_MODULE_8__.Vector3(this.dragStartBall.x + dir.x * d, _shared_games_params_golf__WEBPACK_IMPORTED_MODULE_2__.GOLF_BALL_RADIUS + 0.05, this.dragStartBall.y + dir.y * d));\n        }\n        if (this.shotPreviewLine) {\n            this.scene.remove(this.shotPreviewLine);\n        }\n        const geom = new three__WEBPACK_IMPORTED_MODULE_8__.BufferGeometry().setFromPoints(points);\n        const powerRatio = power / maxPower; // 0..1\n        let r = 0, g = 0, b = 0;\n        if (powerRatio < 0.5) {\n            // зелёный → жёлтый\n            const k = powerRatio / 0.5;\n            r = k;\n            g = 1;\n        }\n        else {\n            // жёлтый → красный\n            const k = (powerRatio - 0.5) / 0.5;\n            r = 1;\n            g = 1 - k;\n        }\n        // перевод в THREE.Color\n        const color = new three__WEBPACK_IMPORTED_MODULE_8__.Color(r, g, b);\n        const mat = new three__WEBPACK_IMPORTED_MODULE_8__.LineBasicMaterial({ color: color, linewidth: 2 });\n        this.shotPreviewLine = new three__WEBPACK_IMPORTED_MODULE_8__.Line(geom, mat);\n        this.scene.add(this.shotPreviewLine);\n    }\n    updateTextPositions() {\n        this.balls_additions.forEach((nameplate, playerId) => {\n            nameplate.lookAt(this.camera.position);\n            const ball = this.balls.get(playerId);\n            if (ball) {\n                nameplate.position.set(ball.position.x, ball.position.y + NAME_OFFSET, ball.position.z);\n                nameplate.visible = ball.visible;\n            }\n        });\n    }\n    OnMouse(cursor) {\n        var _a;\n        const worldPoint = this.GetGroundPoint(cursor.x, cursor.y);\n        if (cursor.rightButton && !this.mouseRightDown) {\n            if ((_a = this.renderer) === null || _a === void 0 ? void 0 : _a.domElement.requestPointerLock) {\n                this.renderer.domElement.requestPointerLock();\n            }\n        }\n        if (!cursor.rightButton && this.mouseRightDown) {\n            if (document.pointerLockElement) {\n                document.exitPointerLock();\n            }\n        }\n        this.rightButtonDown = cursor.rightButton;\n        if (cursor.leftButton && !this.mouseLeftDown) {\n            this.TryStartShot(worldPoint);\n        }\n        if (cursor.leftButton && this.dragStartBall && worldPoint) {\n            this.dragCurrent = worldPoint;\n        }\n        if (!cursor.leftButton && this.mouseLeftDown && this.dragStartBall) {\n            this.TryCommitShot();\n        }\n        this.mouseLeftDown = cursor.leftButton;\n        this.mouseRightDown = cursor.rightButton;\n    }\n}\n\n\n//# sourceURL=webpack:///./src/frontend/pages/games/multiplayer/Golf.tsx?");

/***/ }),

/***/ "./src/shared/games_params/golf.ts":
/*!*****************************************!*\
  !*** ./src/shared/games_params/golf.ts ***!
  \*****************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   Clamp: () => (/* binding */ Clamp),\n/* harmony export */   GOLF_BALL_RADIUS: () => (/* binding */ GOLF_BALL_RADIUS),\n/* harmony export */   GOLF_BOUNCE_DAMPING: () => (/* binding */ GOLF_BOUNCE_DAMPING),\n/* harmony export */   GOLF_CAMERA_ANGLE_DEG: () => (/* binding */ GOLF_CAMERA_ANGLE_DEG),\n/* harmony export */   GOLF_DEFAULT_MAX_SHOT_POWER: () => (/* binding */ GOLF_DEFAULT_MAX_SHOT_POWER),\n/* harmony export */   GOLF_DEFAULT_MAX_STROKES: () => (/* binding */ GOLF_DEFAULT_MAX_STROKES),\n/* harmony export */   GOLF_DEFAULT_TIME_LIMIT_SECONDS: () => (/* binding */ GOLF_DEFAULT_TIME_LIMIT_SECONDS),\n/* harmony export */   GOLF_FRICTION: () => (/* binding */ GOLF_FRICTION),\n/* harmony export */   GOLF_GRAVITY: () => (/* binding */ GOLF_GRAVITY),\n/* harmony export */   GOLF_HOLE_DEPTH: () => (/* binding */ GOLF_HOLE_DEPTH),\n/* harmony export */   GOLF_HOLE_FALL_RADIUS: () => (/* binding */ GOLF_HOLE_FALL_RADIUS),\n/* harmony export */   GOLF_HOLE_RADIUS: () => (/* binding */ GOLF_HOLE_RADIUS),\n/* harmony export */   GOLF_LOW_SPEED_THRESHOLD: () => (/* binding */ GOLF_LOW_SPEED_THRESHOLD),\n/* harmony export */   GOLF_LOW_SPEED_TIMEOUT_MS: () => (/* binding */ GOLF_LOW_SPEED_TIMEOUT_MS),\n/* harmony export */   GOLF_MAX_MAX_SHOT_POWER: () => (/* binding */ GOLF_MAX_MAX_SHOT_POWER),\n/* harmony export */   GOLF_MAX_MAX_STROKES: () => (/* binding */ GOLF_MAX_MAX_STROKES),\n/* harmony export */   GOLF_MAX_SIMULATION_STEPS: () => (/* binding */ GOLF_MAX_SIMULATION_STEPS),\n/* harmony export */   GOLF_MAX_TIME_LIMIT_SECONDS: () => (/* binding */ GOLF_MAX_TIME_LIMIT_SECONDS),\n/* harmony export */   GOLF_MIN_MAX_SHOT_POWER: () => (/* binding */ GOLF_MIN_MAX_SHOT_POWER),\n/* harmony export */   GOLF_MIN_MAX_STROKES: () => (/* binding */ GOLF_MIN_MAX_STROKES),\n/* harmony export */   GOLF_MIN_SPEED: () => (/* binding */ GOLF_MIN_SPEED),\n/* harmony export */   GOLF_MIN_TIME_LIMIT_SECONDS: () => (/* binding */ GOLF_MIN_TIME_LIMIT_SECONDS),\n/* harmony export */   GOLF_PIT_DEPTH: () => (/* binding */ GOLF_PIT_DEPTH),\n/* harmony export */   GOLF_PIT_FALL_MARGIN: () => (/* binding */ GOLF_PIT_FALL_MARGIN),\n/* harmony export */   GOLF_STEP_SECONDS: () => (/* binding */ GOLF_STEP_SECONDS),\n/* harmony export */   GenerateGolfMap: () => (/* binding */ GenerateGolfMap),\n/* harmony export */   GolfGameData: () => (/* binding */ GolfGameData),\n/* harmony export */   GolfPlayerData: () => (/* binding */ GolfPlayerData),\n/* harmony export */   IsInPit: () => (/* binding */ IsInPit),\n/* harmony export */   IsInsideHole: () => (/* binding */ IsInsideHole),\n/* harmony export */   IsOutOfBounds: () => (/* binding */ IsOutOfBounds),\n/* harmony export */   Normalize: () => (/* binding */ Normalize),\n/* harmony export */   SimulateGolfShot: () => (/* binding */ SimulateGolfShot),\n/* harmony export */   VecLen: () => (/* binding */ VecLen)\n/* harmony export */ });\nconst GOLF_CAMERA_ANGLE_DEG = 80;\nconst GOLF_DEFAULT_MAX_STROKES = 30;\nconst GOLF_MIN_MAX_STROKES = 10;\nconst GOLF_MAX_MAX_STROKES = 100;\nconst GOLF_DEFAULT_TIME_LIMIT_SECONDS = 300;\nconst GOLF_MIN_TIME_LIMIT_SECONDS = 200;\nconst GOLF_MAX_TIME_LIMIT_SECONDS = 600;\nconst GOLF_DEFAULT_MAX_SHOT_POWER = 22;\nconst GOLF_MIN_MAX_SHOT_POWER = 20;\nconst GOLF_MAX_MAX_SHOT_POWER = 60;\nconst GOLF_BALL_RADIUS = 0.45;\nconst GOLF_HOLE_RADIUS = GOLF_BALL_RADIUS * 1.5;\nconst GOLF_FRICTION = 0.985;\nconst GOLF_MIN_SPEED = 0.025;\nconst GOLF_BOUNCE_DAMPING = 0.72;\nconst GOLF_STEP_SECONDS = 1 / 60;\nconst GOLF_MAX_SIMULATION_STEPS = 2500;\nconst GOLF_GRAVITY = 16;\nconst GOLF_HOLE_DEPTH = 1.55;\nconst GOLF_PIT_DEPTH = 1.95;\nconst GOLF_LOW_SPEED_THRESHOLD = 0.14;\nconst GOLF_LOW_SPEED_TIMEOUT_MS = 1800;\nconst GOLF_HOLE_FALL_RADIUS = GOLF_HOLE_RADIUS + GOLF_BALL_RADIUS * 0.2;\nconst GOLF_PIT_FALL_MARGIN = GOLF_BALL_RADIUS * 0.08;\nconst GolfPlayerData = {\n    strokes: 0,\n    penalties: 0,\n    timeMs: 0,\n    ball: { x: 0, y: 0 },\n    previousBall: { x: 0, y: 0 },\n    finished: false,\n    failed: false,\n    finishTimeMs: 0,\n    finishStrokes: 0,\n};\nconst GolfGameData = {\n    map: undefined,\n    maxStrokes: GOLF_DEFAULT_MAX_STROKES,\n    timeLimitSeconds: GOLF_DEFAULT_TIME_LIMIT_SECONDS,\n    maxShotPower: GOLF_DEFAULT_MAX_SHOT_POWER,\n    canRegenerateMap: true,\n    serverTimeMs: 0,\n};\nfunction Clamp(value, min, max) {\n    return Math.max(min, Math.min(max, value));\n}\nfunction VecLen(v) {\n    return Math.sqrt(v.x * v.x + v.y * v.y);\n}\nfunction Normalize(v) {\n    const len = VecLen(v);\n    if (!len)\n        return { x: 0, y: 0 };\n    return {\n        x: v.x / len,\n        y: v.y / len,\n    };\n}\nfunction DistSq(a, b) {\n    const dx = a.x - b.x;\n    const dy = a.y - b.y;\n    return dx * dx + dy * dy;\n}\nfunction CreateSeededRandom(seed) {\n    let state = Math.max(1, Math.floor(seed)) % 2147483647;\n    return () => {\n        state = (state * 16807) % 2147483647;\n        return (state - 1) / 2147483646;\n    };\n}\nfunction RandomRange(rng, min, max) {\n    return min + (max - min) * rng();\n}\nfunction RandomInt(rng, min, max) {\n    return Math.floor(RandomRange(rng, min, max + 1));\n}\nfunction PushOutsideRect(pos, radius, rect) {\n    const nearestX = Clamp(pos.x, rect.x, rect.x + rect.w);\n    const nearestY = Clamp(pos.y, rect.y, rect.y + rect.h);\n    const dx = pos.x - nearestX;\n    const dy = pos.y - nearestY;\n    const distSq = dx * dx + dy * dy;\n    if (distSq >= radius * radius) {\n        return pos;\n    }\n    if (distSq === 0) {\n        const left = Math.abs(pos.x - rect.x);\n        const right = Math.abs(pos.x - (rect.x + rect.w));\n        const top = Math.abs(pos.y - rect.y);\n        const bottom = Math.abs(pos.y - (rect.y + rect.h));\n        const minDist = Math.min(left, right, top, bottom);\n        if (minDist === left)\n            return { x: rect.x - radius, y: pos.y };\n        if (minDist === right)\n            return { x: rect.x + rect.w + radius, y: pos.y };\n        if (minDist === top)\n            return { x: pos.x, y: rect.y - radius };\n        return { x: pos.x, y: rect.y + rect.h + radius };\n    }\n    const dist = Math.sqrt(distSq);\n    const push = radius - dist;\n    return {\n        x: pos.x + (dx / dist) * push,\n        y: pos.y + (dy / dist) * push,\n    };\n}\nfunction PushOutsideCircle(pos, radius, obstacle) {\n    const dx = pos.x - obstacle.x;\n    const dy = pos.y - obstacle.y;\n    const distSq = dx * dx + dy * dy;\n    const minDistance = radius + obstacle.r;\n    if (distSq >= minDistance * minDistance)\n        return pos;\n    if (distSq === 0) {\n        return {\n            x: obstacle.x + minDistance,\n            y: obstacle.y,\n        };\n    }\n    const dist = Math.sqrt(distSq);\n    const push = minDistance - dist;\n    return {\n        x: pos.x + (dx / dist) * push,\n        y: pos.y + (dy / dist) * push,\n    };\n}\nfunction ReflectVelocityOnRect(pos, vel, rect) {\n    const centerX = rect.x + rect.w / 2;\n    const centerY = rect.y + rect.h / 2;\n    const dx = pos.x - centerX;\n    const dy = pos.y - centerY;\n    if (Math.abs(dx / rect.w) > Math.abs(dy / rect.h)) {\n        return { x: -vel.x * GOLF_BOUNCE_DAMPING, y: vel.y * 0.94 };\n    }\n    return { x: vel.x * 0.94, y: -vel.y * GOLF_BOUNCE_DAMPING };\n}\nfunction ReflectVelocityOnCircle(pos, vel, circle) {\n    const normal = Normalize({ x: pos.x - circle.x, y: pos.y - circle.y });\n    const dot = vel.x * normal.x + vel.y * normal.y;\n    return {\n        x: (vel.x - 2 * dot * normal.x) * GOLF_BOUNCE_DAMPING,\n        y: (vel.y - 2 * dot * normal.y) * GOLF_BOUNCE_DAMPING,\n    };\n}\nfunction IsInsideHole(map, position) {\n    return DistSq(position, map.hole) <= GOLF_HOLE_FALL_RADIUS * GOLF_HOLE_FALL_RADIUS;\n}\nfunction IsOutOfBounds(map, position) {\n    return (position.x < map.safeMargin ||\n        position.y < map.safeMargin ||\n        position.x > map.width - map.safeMargin ||\n        position.y > map.height - map.safeMargin);\n}\nfunction IsInPit(map, position) {\n    for (const pitRect of map.pitRects || []) {\n        const insideX = position.x >= pitRect.x + GOLF_PIT_FALL_MARGIN &&\n            position.x <= pitRect.x + pitRect.w - GOLF_PIT_FALL_MARGIN;\n        const insideY = position.y >= pitRect.y + GOLF_PIT_FALL_MARGIN &&\n            position.y <= pitRect.y + pitRect.h - GOLF_PIT_FALL_MARGIN;\n        if (insideX && insideY) {\n            return true;\n        }\n    }\n    for (const pit of map.pits) {\n        const dx = position.x - pit.x;\n        const dy = position.y - pit.y;\n        if (dx * dx + dy * dy <= Math.pow((pit.r - GOLF_PIT_FALL_MARGIN), 2)) {\n            return true;\n        }\n    }\n    return false;\n}\nfunction ResolveHoleRimInteraction(map, pos, vel) {\n    const dx = pos.x - map.hole.x;\n    const dy = pos.y - map.hole.y;\n    const distSq = dx * dx + dy * dy;\n    const rimOuterRadius = GOLF_HOLE_RADIUS + GOLF_BALL_RADIUS * 0.08;\n    const rimOuterSq = rimOuterRadius * rimOuterRadius;\n    if (distSq <= GOLF_HOLE_FALL_RADIUS * GOLF_HOLE_FALL_RADIUS) {\n        return {\n            pos,\n            vel,\n            fellIn: true,\n            bounced: false,\n        };\n    }\n    if (distSq >= rimOuterSq) {\n        return {\n            pos,\n            vel,\n            fellIn: false,\n            bounced: false,\n        };\n    }\n    const obstacle = {\n        x: map.hole.x,\n        y: map.hole.y,\n        r: rimOuterRadius - GOLF_BALL_RADIUS,\n    };\n    const pushed = PushOutsideCircle(pos, GOLF_BALL_RADIUS, obstacle);\n    if (pushed.x === pos.x && pushed.y === pos.y) {\n        return {\n            pos,\n            vel,\n            fellIn: false,\n            bounced: false,\n        };\n    }\n    return {\n        pos: pushed,\n        vel: ReflectVelocityOnCircle(pushed, vel, obstacle),\n        fellIn: false,\n        bounced: true,\n    };\n}\nfunction IsInsideRect(p, r) {\n    return p.x >= r.x && p.x <= r.x + r.w && p.y >= r.y && p.y <= r.y + r.h;\n}\nfunction ResolvePitInteraction(map, pos, vel) {\n    for (const pitRect of map.pitRects || []) {\n        const fallRect = {\n            x: pitRect.x + GOLF_PIT_FALL_MARGIN,\n            y: pitRect.y + GOLF_PIT_FALL_MARGIN,\n            w: Math.max(0.01, pitRect.w - GOLF_PIT_FALL_MARGIN * 2),\n            h: Math.max(0.01, pitRect.h - GOLF_PIT_FALL_MARGIN * 2),\n        };\n        if (IsInsideRect(pos, fallRect)) {\n            return {\n                pos,\n                vel,\n                fellIn: true,\n                bounced: false,\n            };\n        }\n        if (IsInsideRect(pos, pitRect)) {\n            const pushed = PushOutsideRect(pos, GOLF_BALL_RADIUS * 0.06, pitRect);\n            const reflected = ReflectVelocityOnRect(pushed, vel, pitRect);\n            return {\n                pos: pushed,\n                vel: reflected,\n                fellIn: false,\n                bounced: true,\n            };\n        }\n    }\n    for (const pit of map.pits) {\n        const dx = pos.x - pit.x;\n        const dy = pos.y - pit.y;\n        const distSq = dx * dx + dy * dy;\n        const fallR = Math.max(0.08, pit.r - GOLF_PIT_FALL_MARGIN);\n        if (distSq <= fallR * fallR) {\n            return {\n                pos,\n                vel,\n                fellIn: true,\n                bounced: false,\n            };\n        }\n        const rimOuter = pit.r + GOLF_BALL_RADIUS * 0.08;\n        if (distSq < rimOuter * rimOuter) {\n            const obstacle = {\n                x: pit.x,\n                y: pit.y,\n                r: rimOuter - GOLF_BALL_RADIUS,\n            };\n            const pushed = PushOutsideCircle(pos, GOLF_BALL_RADIUS, obstacle);\n            if (pushed.x !== pos.x || pushed.y !== pos.y) {\n                return {\n                    pos: pushed,\n                    vel: ReflectVelocityOnCircle(pushed, vel, obstacle),\n                    fellIn: false,\n                    bounced: true,\n                };\n            }\n        }\n    }\n    return {\n        pos,\n        vel,\n        fellIn: false,\n        bounced: false,\n    };\n}\nfunction SimulateGolfShot(map, shot, maxPower) {\n    const dir = Normalize(shot.direction);\n    const power = Clamp(shot.power, 0, maxPower);\n    let pos = { x: shot.origin.x, y: shot.origin.y };\n    let safePosition = Object.assign({}, pos);\n    let vel = {\n        x: dir.x * power,\n        y: dir.y * power,\n    };\n    let traveled = 0;\n    let fellIntoPit = false;\n    let inHole = false;\n    let outOfBounds = false;\n    let verticalPos = GOLF_BALL_RADIUS;\n    let verticalVel = 0;\n    let fallingMode;\n    let lowSpeedMs = 0;\n    const events = [];\n    const path = [Object.assign({}, pos)];\n    for (let i = 0; i < GOLF_MAX_SIMULATION_STEPS; i++) {\n        if (fallingMode) {\n            verticalPos += verticalVel * GOLF_STEP_SECONDS;\n            verticalVel -= GOLF_GRAVITY * GOLF_STEP_SECONDS;\n            if (fallingMode === 'hole' && verticalPos <= -GOLF_HOLE_DEPTH) {\n                inHole = true;\n                break;\n            }\n            if (fallingMode === 'pit' && verticalPos <= -GOLF_PIT_DEPTH) {\n                fellIntoPit = true;\n                break;\n            }\n            continue;\n        }\n        const speed = VecLen(vel);\n        if (speed < GOLF_LOW_SPEED_THRESHOLD) {\n            lowSpeedMs += GOLF_STEP_SECONDS * 1000;\n        }\n        else {\n            lowSpeedMs = 0;\n        }\n        if (speed < GOLF_MIN_SPEED || lowSpeedMs >= GOLF_LOW_SPEED_TIMEOUT_MS) {\n            break;\n        }\n        const prev = Object.assign({}, pos);\n        pos.x += vel.x * GOLF_STEP_SECONDS;\n        pos.y += vel.y * GOLF_STEP_SECONDS;\n        traveled += Math.sqrt(Math.pow((pos.x - prev.x), 2) + Math.pow((pos.y - prev.y), 2));\n        let bounced = false;\n        if (pos.x < GOLF_BALL_RADIUS || pos.x > map.width - GOLF_BALL_RADIUS) {\n            vel.x = -vel.x * GOLF_BOUNCE_DAMPING;\n            pos.x = Clamp(pos.x, GOLF_BALL_RADIUS, map.width - GOLF_BALL_RADIUS);\n            bounced = true;\n        }\n        if (pos.y < GOLF_BALL_RADIUS || pos.y > map.height - GOLF_BALL_RADIUS) {\n            vel.y = -vel.y * GOLF_BOUNCE_DAMPING;\n            pos.y = Clamp(pos.y, GOLF_BALL_RADIUS, map.height - GOLF_BALL_RADIUS);\n            bounced = true;\n        }\n        for (const wall of map.walls) {\n            const pushed = PushOutsideRect(pos, GOLF_BALL_RADIUS, wall);\n            if (pushed.x !== pos.x || pushed.y !== pos.y) {\n                pos = pushed;\n                vel = ReflectVelocityOnRect(pos, vel, wall);\n                bounced = true;\n            }\n        }\n        for (const stone of map.stones) {\n            const pushed = PushOutsideCircle(pos, GOLF_BALL_RADIUS, stone);\n            if (pushed.x !== pos.x || pushed.y !== pos.y) {\n                pos = pushed;\n                vel = ReflectVelocityOnCircle(pos, vel, stone);\n                bounced = true;\n            }\n        }\n        const holeInteraction = ResolveHoleRimInteraction(map, pos, vel);\n        pos = holeInteraction.pos;\n        vel = holeInteraction.vel;\n        if (holeInteraction.bounced) {\n            bounced = true;\n        }\n        if (holeInteraction.fellIn) {\n            fallingMode = 'hole';\n            verticalVel = -0.45;\n            vel = { x: 0, y: 0 };\n            events.push({\n                type: 'hole',\n                at: Object.assign({}, pos),\n                timeMs: Math.floor((i + 1) * GOLF_STEP_SECONDS * 1000),\n            });\n            continue;\n        }\n        const pitInteraction = ResolvePitInteraction(map, pos, vel);\n        pos = pitInteraction.pos;\n        vel = pitInteraction.vel;\n        if (pitInteraction.bounced) {\n            bounced = true;\n        }\n        if (pitInteraction.fellIn) {\n            fallingMode = 'pit';\n            verticalVel = -0.35;\n            vel = { x: 0, y: 0 };\n            events.push({\n                type: 'pitFall',\n                at: Object.assign({}, pos),\n                timeMs: Math.floor((i + 1) * GOLF_STEP_SECONDS * 1000),\n            });\n            continue;\n        }\n        if (bounced) {\n            events.push({\n                type: 'wallBounce',\n                at: Object.assign({}, pos),\n                timeMs: Math.floor((i + 1) * GOLF_STEP_SECONDS * 1000),\n            });\n        }\n        if (IsOutOfBounds(map, pos)) {\n            outOfBounds = true;\n            break;\n        }\n        safePosition = Object.assign({}, pos);\n        if (i % 2 === 0) {\n            path.push(Object.assign({}, safePosition));\n        }\n        vel.x *= GOLF_FRICTION;\n        vel.y *= GOLF_FRICTION;\n    }\n    const durationMs = Math.max(200, Math.floor((traveled / Math.max(1, power)) * 750 + events.length * 60));\n    return {\n        finalPosition: pos,\n        safePosition,\n        fellIntoPit,\n        inHole,\n        outOfBounds,\n        traveled,\n        durationMs,\n        events,\n        path,\n    };\n}\nfunction BuildGridCorridorGolfMap(seed) {\n    const rng = CreateSeededRandom(seed);\n    let Tile;\n    (function (Tile) {\n        Tile[Tile[\"Empty\"] = 0] = \"Empty\";\n        Tile[Tile[\"Ground\"] = 1] = \"Ground\";\n        Tile[Tile[\"Wall\"] = 2] = \"Wall\";\n        Tile[Tile[\"Pit\"] = 3] = \"Pit\";\n        Tile[Tile[\"Spawn\"] = 4] = \"Spawn\";\n        Tile[Tile[\"Hole\"] = 5] = \"Hole\";\n        Tile[Tile[\"Stone\"] = 6] = \"Stone\";\n    })(Tile || (Tile = {}));\n    const tileSize = GOLF_BALL_RADIUS * 10;\n    const cols = 45;\n    const rows = 33;\n    const width = cols * tileSize;\n    const height = rows * tileSize;\n    const safeMargin = 1.2;\n    const grid = Array.from({ length: rows }, () => Array(cols).fill(Tile.Empty));\n    const RIGHT = { dx: 1, dy: 0 };\n    const LEFT = { dx: -1, dy: 0 };\n    const DOWN = { dx: 0, dy: 1 };\n    const UP = { dx: 0, dy: -1 };\n    function perpDirs(d) {\n        if (d.dx !== 0)\n            return [UP, DOWN];\n        return [LEFT, RIGHT];\n    }\n    const margin = 3;\n    const path = [];\n    const visited = new Set();\n    const key = (cx, cy) => `${cx},${cy}`;\n    let cx = margin;\n    let cy = RandomInt(rng, margin + 1, rows - margin - 2);\n    path.push({ x: cx, y: cy });\n    visited.add(key(cx, cy));\n    let dir = RIGHT;\n    const totalSegments = RandomInt(rng, 18, 27);\n    for (let seg = 0; seg < totalSegments; seg++) {\n        // Each segment runs straight for a random length, then turns\n        const segLen = RandomInt(rng, 3, 8);\n        for (let step = 0; step < segLen; step++) {\n            const nx = cx + dir.dx;\n            const ny = cy + dir.dy;\n            if (nx < margin || nx >= cols - margin || ny < margin || ny >= rows - margin)\n                break;\n            cx = nx;\n            cy = ny;\n            if (!visited.has(key(cx, cy))) {\n                path.push({ x: cx, y: cy });\n                visited.add(key(cx, cy));\n            }\n        }\n        // Force a turn: pick a perpendicular direction\n        const perps = perpDirs(dir);\n        const candidates = [];\n        for (const pd of perps) {\n            const testX = cx + pd.dx * 3;\n            const testY = cy + pd.dy * 3;\n            if (testX >= margin && testX < cols - margin && testY >= margin && testY < rows - margin) {\n                candidates.push(pd);\n            }\n        }\n        if (candidates.length > 0) {\n            dir = candidates[RandomInt(rng, 0, candidates.length - 1)];\n        }\n        else {\n            // Reverse if stuck\n            dir = { dx: -dir.dx, dy: -dir.dy };\n        }\n    }\n    // Final stretch: push toward the far edge to add length\n    const finalDir = cx < cols / 2 ? RIGHT : LEFT;\n    for (let i = 0; i < 10; i++) {\n        const nx = cx + finalDir.dx;\n        const ny = cy + finalDir.dy;\n        if (nx < margin || nx >= cols - margin || ny < margin || ny >= rows - margin)\n            break;\n        cx = nx;\n        cy = ny;\n        if (!visited.has(key(cx, cy))) {\n            path.push({ x: cx, y: cy });\n            visited.add(key(cx, cy));\n        }\n    }\n    // --- Carve corridors (narrow: half-width of 1) ---\n    const corridorHalf = 1;\n    for (const p of path) {\n        for (let dx = -corridorHalf; dx <= corridorHalf; dx++) {\n            for (let dy = -corridorHalf; dy <= corridorHalf; dy++) {\n                const nx = p.x + dx;\n                const ny = p.y + dy;\n                if (nx < 0 || ny < 0 || nx >= cols || ny >= rows)\n                    continue;\n                if (grid[ny][nx] === Tile.Empty) {\n                    grid[ny][nx] = Tile.Ground;\n                }\n            }\n        }\n    }\n    // --- Occasionally widen some sections to create small open areas ---\n    for (let i = 0; i < path.length; i++) {\n        if (rng() < 0.12) {\n            const p = path[i];\n            for (let dx = -2; dx <= 2; dx++) {\n                for (let dy = -2; dy <= 2; dy++) {\n                    if (Math.abs(dx) === 2 && Math.abs(dy) === 2)\n                        continue; // diamond shape\n                    const nx = p.x + dx;\n                    const ny = p.y + dy;\n                    if (nx < 0 || ny < 0 || nx >= cols || ny >= rows)\n                        continue;\n                    if (grid[ny][nx] === Tile.Empty) {\n                        grid[ny][nx] = Tile.Ground;\n                    }\n                }\n            }\n        }\n    }\n    // --- Add walls around ground tiles ---\n    for (let gy = 0; gy < rows; gy++) {\n        for (let gx = 0; gx < cols; gx++) {\n            if (grid[gy][gx] !== Tile.Empty)\n                continue;\n            let nearGround = false;\n            for (let dx = -1; dx <= 1; dx++) {\n                for (let dy = -1; dy <= 1; dy++) {\n                    if (dx === 0 && dy === 0)\n                        continue;\n                    const nx = gx + dx;\n                    const ny = gy + dy;\n                    if (nx >= 0 && ny >= 0 && nx < cols && ny < rows) {\n                        if (grid[ny][nx] === Tile.Ground) {\n                            nearGround = true;\n                        }\n                    }\n                }\n            }\n            if (nearGround) {\n                grid[gy][gx] = Tile.Wall;\n            }\n        }\n    }\n    const start = path[0];\n    // Pick the hole as the path point farthest from spawn (Euclidean)\n    let bestDist = -1;\n    let bestIdx = path.length - 1;\n    for (let i = Math.floor(path.length * 0.5); i < path.length; i++) {\n        const dx = path[i].x - start.x;\n        const dy = path[i].y - start.y;\n        const d = dx * dx + dy * dy;\n        if (d > bestDist) {\n            bestDist = d;\n            bestIdx = i;\n        }\n    }\n    const end = path[bestIdx];\n    // --- Place pits along the corridor (avoid start/end) ---\n    for (let i = 0; i < path.length; i++) {\n        const p = path[i];\n        const dStart = Math.abs(p.x - start.x) + Math.abs(p.y - start.y);\n        const dEnd = Math.abs(p.x - end.x) + Math.abs(p.y - end.y);\n        if (dStart <= 3 || dEnd <= 3)\n            continue;\n        if (rng() < 0.09) {\n            grid[p.y][p.x] = Tile.Pit;\n        }\n    }\n    // --- Place stones (circular obstacles) inside the corridors ---\n    for (let i = 0; i < path.length; i++) {\n        const p = path[i];\n        const dStart = Math.abs(p.x - start.x) + Math.abs(p.y - start.y);\n        const dEnd = Math.abs(p.x - end.x) + Math.abs(p.y - end.y);\n        if (dStart <= 3 || dEnd <= 3)\n            continue;\n        if (rng() < 0.10) {\n            // Only place stone if the tile is ground and not already occupied\n            if (grid[p.y][p.x] === Tile.Ground) {\n                grid[p.y][p.x] = Tile.Stone;\n            }\n        }\n    }\n    // --- Place extra wall bumps protruding into wider sections ---\n    for (let i = 0; i < path.length; i++) {\n        const p = path[i];\n        const dStart = Math.abs(p.x - start.x) + Math.abs(p.y - start.y);\n        const dEnd = Math.abs(p.x - end.x) + Math.abs(p.y - end.y);\n        if (dStart <= 4 || dEnd <= 4)\n            continue;\n        if (rng() < 0.06) {\n            // Check if this is an interior ground tile (all 4 neighbours are ground)\n            const allNeighboursGround = p.x > 0 && p.x < cols - 1 && p.y > 0 && p.y < rows - 1 &&\n                grid[p.y - 1][p.x] === Tile.Ground &&\n                grid[p.y + 1][p.x] === Tile.Ground &&\n                grid[p.y][p.x - 1] === Tile.Ground &&\n                grid[p.y][p.x + 1] === Tile.Ground;\n            if (allNeighboursGround && grid[p.y][p.x] === Tile.Ground) {\n                grid[p.y][p.x] = Tile.Wall;\n            }\n        }\n    }\n    grid[start.y][start.x] = Tile.Spawn;\n    grid[end.y][end.x] = Tile.Hole;\n    // --- Convert grid to map data ---\n    const walls = [];\n    const stones = [];\n    const pits = [];\n    const pitRects = [];\n    const spawn = {\n        x: (start.x + 0.5) * tileSize,\n        y: (start.y + 0.5) * tileSize,\n    };\n    const hole = {\n        x: (end.x + 0.5) * tileSize,\n        y: (end.y + 0.5) * tileSize,\n    };\n    for (let r = 0; r < rows; r++) {\n        for (let c = 0; c < cols; c++) {\n            const tile = grid[r][c];\n            const tx = c * tileSize;\n            const ty = r * tileSize;\n            if (tile === Tile.Wall) {\n                walls.push({ x: tx, y: ty, w: tileSize, h: tileSize });\n            }\n            if (tile === Tile.Pit) {\n                pitRects.push({\n                    x: tx + tileSize * 0.1,\n                    y: ty + tileSize * 0.1,\n                    w: tileSize * 0.8,\n                    h: tileSize * 0.8,\n                });\n            }\n            if (tile === Tile.Stone) {\n                stones.push({\n                    x: tx + tileSize * 0.5,\n                    y: ty + tileSize * 0.5,\n                    r: tileSize * (0.2 + rng() * 0.15),\n                });\n            }\n            // Don't place stones or obstacles at hole location\n            if (tile === Tile.Hole) {\n                // Clear any stones in this tile area\n                const holeArea = tileSize * 0.8;\n                for (let i = stones.length - 1; i >= 0; i--) {\n                    const s = stones[i];\n                    if (Math.abs(s.x - (tx + tileSize * 0.5)) < holeArea &&\n                        Math.abs(s.y - (ty + tileSize * 0.5)) < holeArea) {\n                        stones.splice(i, 1);\n                    }\n                }\n            }\n            if (tile === Tile.Spawn) {\n                spawn.x = tx + tileSize / 2;\n                spawn.y = ty + tileSize / 2;\n            }\n            if (tile === Tile.Hole) {\n                hole.x = tx + tileSize / 2;\n                hole.y = ty + tileSize / 2;\n            }\n        }\n    }\n    // --- Add a few extra round pits at random ground tiles for variety ---\n    for (let i = 0; i < 3; i++) {\n        const idx = RandomInt(rng, Math.floor(path.length * 0.2), Math.floor(path.length * 0.8));\n        const p = path[idx];\n        if (grid[p.y][p.x] === Tile.Ground) {\n            pits.push({\n                x: (p.x + 0.5) * tileSize + RandomRange(rng, -0.5, 0.5),\n                y: (p.y + 0.5) * tileSize + RandomRange(rng, -0.5, 0.5),\n                r: tileSize * (0.25 + rng() * 0.1),\n            });\n        }\n    }\n    return {\n        seed,\n        width,\n        height,\n        spawn,\n        hole,\n        walls,\n        stones,\n        pits,\n        pitRects,\n        safeMargin,\n    };\n}\nfunction GenerateGolfMap(seed) {\n    return BuildGridCorridorGolfMap(seed);\n}\n\n\n//# sourceURL=webpack:///./src/shared/games_params/golf.ts?");

/***/ })

}]);