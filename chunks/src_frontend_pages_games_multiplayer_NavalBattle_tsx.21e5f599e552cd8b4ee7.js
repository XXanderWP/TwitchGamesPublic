"use strict";
/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
(self["webpackChunk"] = self["webpackChunk"] || []).push([["src_frontend_pages_games_multiplayer_NavalBattle_tsx"],{

/***/ "./src/frontend/styles/navalbattle.less":
/*!**********************************************!*\
  !*** ./src/frontend/styles/navalbattle.less ***!
  \**********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n// extracted by mini-css-extract-plugin\n\n\n//# sourceURL=webpack:///./src/frontend/styles/navalbattle.less?");

/***/ }),

/***/ "./src/frontend/pages/games/multiplayer/NavalBattle.tsx":
/*!**************************************************************!*\
  !*** ./src/frontend/pages/games/multiplayer/NavalBattle.tsx ***!
  \**************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   NavalBattleController: () => (/* binding */ NavalBattleController)\n/* harmony export */ });\n/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react/jsx-runtime */ \"./node_modules/react/jsx-runtime.js\");\n/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! react */ \"./node_modules/react/index.js\");\n/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_1__);\n/* harmony import */ var three__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! three */ \"./node_modules/three/build/three.module.js\");\n/* harmony import */ var three__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! three */ \"./node_modules/three/build/three.core.js\");\n/* harmony import */ var three_examples_jsm_loaders_FBXLoader_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! three/examples/jsm/loaders/FBXLoader.js */ \"./node_modules/three/examples/jsm/loaders/FBXLoader.js\");\n/* harmony import */ var three_examples_jsm_objects_Water_js__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! three/examples/jsm/objects/Water.js */ \"./node_modules/three/examples/jsm/objects/Water.js\");\n/* harmony import */ var three_examples_jsm_geometries_TextGeometry_js__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! three/examples/jsm/geometries/TextGeometry.js */ \"./node_modules/three/examples/jsm/geometries/TextGeometry.js\");\n/* harmony import */ var three_examples_jsm_loaders_FontLoader_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! three/examples/jsm/loaders/FontLoader.js */ \"./node_modules/three/examples/jsm/loaders/FontLoader.js\");\n/* harmony import */ var three_examples_fonts_helvetiker_regular_typeface_json__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! three/examples/fonts/helvetiker_regular.typeface.json */ \"./node_modules/three/examples/fonts/helvetiker_regular.typeface.json\");\n/* harmony import */ var rsuite__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! rsuite */ \"./node_modules/rsuite/esm/Button/index.js\");\n/* harmony import */ var rsuite__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! rsuite */ \"./node_modules/rsuite/esm/Row/index.js\");\n/* harmony import */ var rsuite__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! rsuite */ \"./node_modules/rsuite/esm/Col/index.js\");\n/* harmony import */ var rsuite__WEBPACK_IMPORTED_MODULE_17__ = __webpack_require__(/*! rsuite */ \"./node_modules/rsuite/esm/InputNumber/index.js\");\n/* harmony import */ var rsuite__WEBPACK_IMPORTED_MODULE_18__ = __webpack_require__(/*! rsuite */ \"./node_modules/rsuite/esm/Toggle/index.js\");\n/* harmony import */ var _api__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../api */ \"./src/frontend/api.tsx\");\n/* harmony import */ var _Controller__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./Controller */ \"./src/frontend/pages/games/multiplayer/Controller.tsx\");\n/* harmony import */ var _shared_games_params_navalbattle__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../../../../shared/games_params/navalbattle */ \"./src/shared/games_params/navalbattle.ts\");\n/* harmony import */ var _shared_system__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../../../../shared/system */ \"./src/shared/system.ts\");\n/* harmony import */ var _styles_navalbattle_less__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../../../styles/navalbattle.less */ \"./src/frontend/styles/navalbattle.less\");\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n// ─────────────────────────────────────────────────────────────────────────────\n//  CAMERA CONFIG\n//  Измени CAMERA_MODE, чтобы переключиться между режимами\n// ─────────────────────────────────────────────────────────────────────────────\nconst CAMERA_MODE = 'orbit';\n// Top-down настройки\nconst TOPDOWN_FOV = 55; // угол обзора\nconst TOPDOWN_HEIGHT = 1400; // высота камеры над водой (в юнитах)\nconst TOPDOWN_TILT_DEG = 20; // наклон камеры вперёд от вертикали (0 = прямо вниз)\nconst TOPDOWN_AIM_OFFSET = 180; // смещение камеры в сторону прицела\n// Orbit (3D от третьего лица) настройки\nconst ORBIT_DEFAULT_RADIUS = 900;\nconst ORBIT_MIN_PHI_DEG = 10; // минимальный угол от зенита\nconst ORBIT_MAX_PHI_DEG = 80; // максимальный угол (почти горизонталь)\nconst ORBIT_SENSITIVITY_X = 0.003;\nconst ORBIT_SENSITIVITY_Y = 0.003;\n// Путь к текстуре нормалей воды (стандартная из three/examples)\nconst WATER_NORMALS_URL = '/images/waternormals_512w.webp';\n// ─────────────────────────────────────────────────────────────────────────────\nconst PLAYER_COLORS = [\n    '#4dd7b4',\n    '#ff6b6b',\n    '#ffd93d',\n    '#6bcbff',\n    '#c06bff',\n    '#ff9b6b',\n    '#6bff9b',\n    '#ff6bcb',\n    '#b4d74d',\n    '#e87dfb',\n];\nconst MODIFIER_ICON = {\n    [\"stealth\" /* NavalBattleModifierType.STEALTH */]: 'STEALTH',\n    [\"damage_x2\" /* NavalBattleModifierType.DAMAGE_X2 */]: 'DMG X2',\n    [\"shield\" /* NavalBattleModifierType.SHIELD */]: 'SHIELD',\n    [\"repair\" /* NavalBattleModifierType.REPAIR */]: 'REPAIR',\n    [\"speed_x2\" /* NavalBattleModifierType.SPEED_X2 */]: 'SPEED',\n};\n// ─────────────────────────────────────────────────────────────────────────────\n//  Контроллер\n// ─────────────────────────────────────────────────────────────────────────────\nclass NavalBattleController extends _Controller__WEBPACK_IMPORTED_MODULE_3__.MultiplayerGameController {\n    constructor(props) {\n        super(\"105\" /* GameID.NAVAL_BATTLE */, _shared_games_params_navalbattle__WEBPACK_IMPORTED_MODULE_4__.NavalBattleGameData, _shared_games_params_navalbattle__WEBPACK_IMPORTED_MODULE_4__.NavalBattlePlayerData, props);\n        // ── Refs ────────────────────────────────────────────────────────────────────\n        this.canvasRef = react__WEBPACK_IMPORTED_MODULE_1___default().createRef();\n        this.boardRef = react__WEBPACK_IMPORTED_MODULE_1___default().createRef();\n        this.offscreenLayerRef = react__WEBPACK_IMPORTED_MODULE_1___default().createRef();\n        // ── Three.js core ───────────────────────────────────────────────────────────\n        this.renderer = null;\n        this.scene = null;\n        this.camera = null;\n        this.water = null; // Water\n        // ── Scene objects ───────────────────────────────────────────────────────────\n        this.shipMeshes = new Map();\n        this.aimMeshes = new Map();\n        this.hpBarObjects = new Map();\n        this.wreck3Ds = [];\n        this.modifierMeshes = new Map();\n        // Particle pool (GPU Points)\n        this.particleGeo = null;\n        this.particleMat = null;\n        this.particlePoints = null;\n        this.particles3D = [];\n        // Damage floats\n        this.damageLabels = [];\n        // FBX cache\n        this.fbxCache = new Map();\n        this.fbxLoader = new three_examples_jsm_loaders_FBXLoader_js__WEBPACK_IMPORTED_MODULE_7__.FBXLoader();\n        // Projectiles\n        this.projectiles = [];\n        this.mainFont = new three_examples_jsm_loaders_FontLoader_js__WEBPACK_IMPORTED_MODULE_8__.FontLoader().parse(three_examples_fonts_helvetiker_regular_typeface_json__WEBPACK_IMPORTED_MODULE_9__);\n        // ── Camera orbit state ──────────────────────────────────────────────────────\n        this.orbitTheta = 0;\n        this.orbitPhi = (40 * Math.PI) / 180;\n        this.orbitR = ORBIT_DEFAULT_RADIUS;\n        this.isDragging = false;\n        this.lastMX = 0;\n        this.lastMY = 0;\n        // ── Игровое состояние ───────────────────────────────────────────────────────\n        this.frameReq = 0;\n        this.pressed = { w: false, s: false, a: false, d: false };\n        this.shotHeld = {\n            [\"left\" /* NavalBattleWeaponType.LEFT */]: false,\n            [\"right\" /* NavalBattleWeaponType.RIGHT */]: false,\n            [\"front\" /* NavalBattleWeaponType.FRONT */]: false,\n        };\n        this.renderStates = new Map();\n        this.lastFrameMS = 0;\n        this.lastSentInput = '';\n        this.prevHpMap = new Map();\n        this.prevVisibleMap = new Map();\n        this.prevCooldownMap = new Map();\n        this.zoom = 1.0;\n        this.screenShake = 0;\n        this.activeHeldWeapon = '';\n        this.cameraOffset = { x: 0, y: 0 };\n        this.backgroundMusic = 'spacepower.background';\n        // ── Клавиатурный ввод (без изменений) ────────────────────────────────────\n        this.OnKeyDown = (e) => {\n            const target = e.target;\n            if (target) {\n                const tag = (target.tagName || '').toLowerCase();\n                if (tag === 'input' || tag === 'textarea' || target.isContentEditable)\n                    return;\n            }\n            let changed = false;\n            if (e.code === 'KeyW' && !this.pressed.w) {\n                this.pressed.w = true;\n                changed = true;\n            }\n            if (e.code === 'KeyS' && !this.pressed.s) {\n                this.pressed.s = true;\n                changed = true;\n            }\n            if (e.code === 'KeyA' && !this.pressed.a) {\n                this.pressed.a = true;\n                changed = true;\n            }\n            if (e.code === 'KeyD' && !this.pressed.d) {\n                this.pressed.d = true;\n                changed = true;\n            }\n            if (e.code === 'KeyQ') {\n                e.preventDefault();\n                this.HandleWeaponStart(\"left\" /* NavalBattleWeaponType.LEFT */);\n            }\n            if (e.code === 'KeyE') {\n                e.preventDefault();\n                this.HandleWeaponStart(\"right\" /* NavalBattleWeaponType.RIGHT */);\n            }\n            if (e.code === 'Space' && !e.repeat) {\n                e.preventDefault();\n                this.HandleWeaponStart(\"front\" /* NavalBattleWeaponType.FRONT */);\n            }\n            // Zoom / orbit radius\n            if (e.code === 'Minus' || e.code === 'NumpadSubtract') {\n                e.preventDefault();\n                if (CAMERA_MODE === 'topdown')\n                    this.zoom = Math.max(0.35, this.zoom - 0.1);\n                else\n                    this.orbitR = Math.min(2500, this.orbitR + 80);\n            }\n            if (e.code === 'Equal' ||\n                e.code === 'NumpadAdd' ||\n                e.code === 'BracketRight') {\n                e.preventDefault();\n                if (CAMERA_MODE === 'topdown')\n                    this.zoom = Math.min(2.5, this.zoom + 0.1);\n                else\n                    this.orbitR = Math.max(200, this.orbitR - 80);\n            }\n            if (changed)\n                this.SendInput();\n        };\n        this.OnKeyUp = (e) => {\n            let changed = false;\n            if (e.code === 'KeyW' && this.pressed.w) {\n                this.pressed.w = false;\n                changed = true;\n            }\n            if (e.code === 'KeyS' && this.pressed.s) {\n                this.pressed.s = false;\n                changed = true;\n            }\n            if (e.code === 'KeyA' && this.pressed.a) {\n                this.pressed.a = false;\n                changed = true;\n            }\n            if (e.code === 'KeyD' && this.pressed.d) {\n                this.pressed.d = false;\n                changed = true;\n            }\n            if (e.code === 'KeyQ') {\n                e.preventDefault();\n                this.HandleWeaponRelease(\"left\" /* NavalBattleWeaponType.LEFT */);\n            }\n            if (e.code === 'KeyE') {\n                e.preventDefault();\n                this.HandleWeaponRelease(\"right\" /* NavalBattleWeaponType.RIGHT */);\n            }\n            if (e.code === 'Space') {\n                e.preventDefault();\n                this.HandleWeaponRelease(\"front\" /* NavalBattleWeaponType.FRONT */);\n            }\n            if (changed)\n                this.SendInput();\n        };\n        // ── RAF ───────────────────────────────────────────────────────────────────\n        this.TickFrame = () => {\n            const nowMS = _shared_system__WEBPACK_IMPORTED_MODULE_5__.System.timestampMS;\n            const deltaMS = this.lastFrameMS ? nowMS - this.lastFrameMS : 16;\n            this.lastFrameMS = nowMS;\n            if (this.state.inLobby && this.IsInCombat()) {\n                this.DrawCanvas3D(deltaMS);\n            }\n            this.frameReq = requestAnimationFrame(this.TickFrame);\n        };\n    }\n    // ════════════════════════════════════════════════════════════════════════════\n    //  Three.js: инициализация\n    // ════════════════════════════════════════════════════════════════════════════\n    InitThreeJS() {\n        if (this.renderer)\n            return;\n        const canvas = this.canvasRef.current;\n        if (!canvas) {\n            setTimeout(() => {\n                this.InitThreeJS();\n            }, 100);\n            return;\n        }\n        // ── WebGL renderer ────────────────────────────────────────────────────────\n        this.renderer = new three__WEBPACK_IMPORTED_MODULE_10__.WebGLRenderer({\n            canvas,\n            antialias: true,\n            alpha: false,\n        });\n        this.renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));\n        this.renderer.shadowMap.enabled = true;\n        this.renderer.shadowMap.type = three__WEBPACK_IMPORTED_MODULE_11__.PCFSoftShadowMap;\n        this.renderer.toneMapping = three__WEBPACK_IMPORTED_MODULE_11__.ACESFilmicToneMapping;\n        this.renderer.toneMappingExposure = 0.9;\n        // ── Сцена ────────────────────────────────────────────────────────────────\n        this.scene = new three__WEBPACK_IMPORTED_MODULE_11__.Scene();\n        this.scene.background = new three__WEBPACK_IMPORTED_MODULE_11__.Color(0x0a2437);\n        this.scene.fog = new three__WEBPACK_IMPORTED_MODULE_11__.Fog(0x0a2437, 4000, 9000);\n        // ── Камера ───────────────────────────────────────────────────────────────\n        this.camera = new three__WEBPACK_IMPORTED_MODULE_11__.PerspectiveCamera(TOPDOWN_FOV, 16 / 9, 1, 14000);\n        // ── Освещение ────────────────────────────────────────────────────────────\n        this.scene.add(new three__WEBPACK_IMPORTED_MODULE_11__.AmbientLight(0x6688aa, 0.65));\n        const sun = new three__WEBPACK_IMPORTED_MODULE_11__.DirectionalLight(0xffeedd, 1.5);\n        sun.position.set(900, 1400, 700);\n        sun.castShadow = true;\n        sun.shadow.mapSize.set(2048, 2048);\n        sun.shadow.camera.near = 10;\n        sun.shadow.camera.far = 5000;\n        sun.shadow.camera.left = sun.shadow.camera.bottom = -2500;\n        sun.shadow.camera.right = sun.shadow.camera.top = 2500;\n        this.scene.add(sun);\n        // Дополнительный мягкий fill-свет с другой стороны\n        const fill = new three__WEBPACK_IMPORTED_MODULE_11__.DirectionalLight(0x4466aa, 0.4);\n        fill.position.set(-500, 800, -400);\n        this.scene.add(fill);\n        // ── Вода ─────────────────────────────────────────────────────────────────\n        this.InitWater();\n        // ── Геометрия карты (сетка, границы) ─────────────────────────────────────\n        this.InitMapGeometry();\n        // ── Система частиц ────────────────────────────────────────────────────────\n        this.InitParticleSystem();\n        this.SetupOrbitControls();\n        const int = setInterval(() => {\n            const clear = () => {\n                clearInterval(int);\n                this.DestroyThreeJS();\n            };\n            if (!this.renderer) {\n                clear();\n                return;\n            }\n            const canvas = this.canvasRef.current;\n            if (!canvas) {\n                clear();\n                return;\n            }\n        }, 100);\n    }\n    // ─────────────────────────────────────────────────────────────────────────────\n    InitWater() {\n        if (!this.scene)\n            return;\n        const { mapWidth: W, mapHeight: H } = this.state.gameData;\n        const geo = new three__WEBPACK_IMPORTED_MODULE_11__.PlaneGeometry(W, H, 128, 128);\n        const waterNormals = new three__WEBPACK_IMPORTED_MODULE_11__.TextureLoader().load(WATER_NORMALS_URL, tex => {\n            tex.wrapS = tex.wrapT = three__WEBPACK_IMPORTED_MODULE_11__.RepeatWrapping;\n        });\n        this.water = new three_examples_jsm_objects_Water_js__WEBPACK_IMPORTED_MODULE_12__.Water(geo, {\n            textureWidth: 512,\n            textureHeight: 512,\n            waterNormals,\n            sunDirection: new three__WEBPACK_IMPORTED_MODULE_11__.Vector3(0.6, 1, 0.4).normalize(),\n            sunColor: 0xffeedd,\n            waterColor: 0x0b2d45,\n            distortionScale: 3.7,\n            fog: true,\n        });\n        this.water.rotation.x = -Math.PI / 2;\n        this.water.position.set(W / 2, 0, H / 2);\n        this.scene.add(this.water);\n    }\n    // ─────────────────────────────────────────────────────────────────────────────\n    InitMapGeometry() {\n        if (!this.scene)\n            return;\n        const { mapWidth: W, mapHeight: H } = this.state.gameData;\n        const Y = 3;\n        const step = 180;\n        // Сетка\n        const gridVerts = [];\n        for (let x = 0; x <= W; x += step) {\n            gridVerts.push(x, Y, 0, x, Y, H);\n        }\n        for (let z = 0; z <= H; z += step) {\n            gridVerts.push(0, Y, z, W, Y, z);\n        }\n        const gridGeo = new three__WEBPACK_IMPORTED_MODULE_11__.BufferGeometry();\n        gridGeo.setAttribute('position', new three__WEBPACK_IMPORTED_MODULE_11__.Float32BufferAttribute(gridVerts, 3));\n        this.scene.add(new three__WEBPACK_IMPORTED_MODULE_11__.LineSegments(gridGeo, new three__WEBPACK_IMPORTED_MODULE_11__.LineBasicMaterial({\n            color: 0xffffff,\n            transparent: true,\n            opacity: 0.055,\n        })));\n        // Граница карты\n        const bVerts = [\n            0,\n            Y,\n            0,\n            W,\n            Y,\n            0,\n            W,\n            Y,\n            0,\n            W,\n            Y,\n            H,\n            W,\n            Y,\n            H,\n            0,\n            Y,\n            H,\n            0,\n            Y,\n            H,\n            0,\n            Y,\n            0,\n        ];\n        const borderGeo = new three__WEBPACK_IMPORTED_MODULE_11__.BufferGeometry();\n        borderGeo.setAttribute('position', new three__WEBPACK_IMPORTED_MODULE_11__.Float32BufferAttribute(bVerts, 3));\n        this.scene.add(new three__WEBPACK_IMPORTED_MODULE_11__.LineSegments(borderGeo, new three__WEBPACK_IMPORTED_MODULE_11__.LineBasicMaterial({\n            color: 0xfafaff,\n            transparent: true,\n            opacity: 0.38,\n        })));\n    }\n    // ─────────────────────────────────────────────────────────────────────────────\n    InitParticleSystem() {\n        if (!this.scene)\n            return;\n        const MAX = 2000;\n        this.particleGeo = new three__WEBPACK_IMPORTED_MODULE_11__.BufferGeometry();\n        const positions = new Float32Array(MAX * 3);\n        const colors = new Float32Array(MAX * 3);\n        const sizes = new Float32Array(MAX);\n        this.particleGeo.setAttribute('position', new three__WEBPACK_IMPORTED_MODULE_11__.BufferAttribute(positions, 3));\n        this.particleGeo.setAttribute('color', new three__WEBPACK_IMPORTED_MODULE_11__.BufferAttribute(colors, 3));\n        this.particleGeo.setAttribute('size', new three__WEBPACK_IMPORTED_MODULE_11__.BufferAttribute(sizes, 1));\n        this.particleGeo.setDrawRange(0, 0);\n        // Шейдер: мягкие круглые пятна с аддитивным смешением\n        this.particleMat = new three__WEBPACK_IMPORTED_MODULE_11__.ShaderMaterial({\n            uniforms: {},\n            vertexShader: /* glsl */ `\n        attribute float size;\n        attribute vec3 color;\n        varying vec3 vColor;\n        void main() {\n          vColor = color;\n          vec4 mv = modelViewMatrix * vec4(position, 1.0);\n          gl_PointSize = size * (280.0 / -mv.z);\n          gl_Position  = projectionMatrix * mv;\n        }\n      `,\n            fragmentShader: /* glsl */ `\n        varying vec3 vColor;\n        void main() {\n          vec2  uv = gl_PointCoord - 0.5;\n          float r  = dot(uv, uv);\n          if (r > 0.25) discard;\n          float alpha = 1.0 - r * 4.0;\n          gl_FragColor = vec4(vColor, alpha);\n        }\n      `,\n            transparent: true,\n            depthWrite: false,\n            blending: three__WEBPACK_IMPORTED_MODULE_11__.AdditiveBlending,\n            vertexColors: true,\n        });\n        this.particlePoints = new three__WEBPACK_IMPORTED_MODULE_11__.Points(this.particleGeo, this.particleMat);\n        this.scene.add(this.particlePoints);\n    }\n    // ════════════════════════════════════════════════════════════════════════════\n    //  Three.js: уничтожение\n    // ════════════════════════════════════════════════════════════════════════════\n    DestroyThreeJS() {\n        var _a, _b, _c;\n        console.log(`DestroyThreeJS`);\n        // Мышиные события\n        const canvas = this.canvasRef.current;\n        if (canvas && this._onMouseDown)\n            canvas.removeEventListener('mousedown', this._onMouseDown);\n        if (this._onMouseMove)\n            window.removeEventListener('mousemove', this._onMouseMove);\n        if (this._onMouseUp)\n            window.removeEventListener('mouseup', this._onMouseUp);\n        if (canvas && this._onWheel)\n            canvas.removeEventListener('wheel', this._onWheel);\n        // Объекты сцены\n        this.shipMeshes.forEach(g => { var _a; return (_a = this.scene) === null || _a === void 0 ? void 0 : _a.remove(g); });\n        this.shipMeshes.clear();\n        this.aimMeshes.forEach(m => {\n            var _a;\n            (_a = this.scene) === null || _a === void 0 ? void 0 : _a.remove(m);\n            m.geometry.dispose();\n        });\n        this.aimMeshes.clear();\n        this.wreck3Ds.forEach(w => { var _a; return (_a = this.scene) === null || _a === void 0 ? void 0 : _a.remove(w.group); });\n        this.wreck3Ds = [];\n        this.hpBarObjects.clear();\n        this.damageLabels.forEach(d => {\n            var _a;\n            (_a = d.mesh.parent) === null || _a === void 0 ? void 0 : _a.remove(d.mesh);\n            d.mesh.traverse(child => {\n                if (child.isMesh) {\n                    child.geometry.dispose();\n                    child.material.dispose();\n                }\n            });\n        });\n        this.damageLabels = [];\n        this.modifierMeshes.forEach(m => {\n            var _a;\n            (_a = this.scene) === null || _a === void 0 ? void 0 : _a.remove(m);\n            m.geometry.dispose();\n        });\n        this.modifierMeshes.clear();\n        if (this.particlePoints)\n            (_a = this.scene) === null || _a === void 0 ? void 0 : _a.remove(this.particlePoints);\n        this.particleGeo = null;\n        this.particleMat = null;\n        this.particlePoints = null;\n        this.particles3D = [];\n        this.projectiles.forEach(p => {\n            var _a;\n            (_a = this.scene) === null || _a === void 0 ? void 0 : _a.remove(p.mesh);\n            p.mesh.geometry.dispose();\n            p.mesh.material.dispose();\n        });\n        this.projectiles = [];\n        if (this.water)\n            (_b = this.scene) === null || _b === void 0 ? void 0 : _b.remove(this.water);\n        this.water = null;\n        (_c = this.renderer) === null || _c === void 0 ? void 0 : _c.dispose();\n        this.renderer = null;\n        this.scene = null;\n        this.camera = null;\n        this.InitThreeJS();\n    }\n    // ════════════════════════════════════════════════════════════════════════════\n    //  Корабли: меши\n    // ════════════════════════════════════════════════════════════════════════════\n    /**\n     * Примитив-заглушка: коробка (корпус) + конус (нос) + надстройка.\n     * Нос смотрит в +X, что соответствует angle=0 в игровой логике.\n     */\n    MakePrimitiveShip(shipId, color) {\n        const ship = (0,_shared_games_params_navalbattle__WEBPACK_IMPORTED_MODULE_4__.GetNavalBattleShip)(shipId);\n        const L = ship.length;\n        const W = ship.width;\n        const col = new three__WEBPACK_IMPORTED_MODULE_11__.Color(color);\n        const group = new three__WEBPACK_IMPORTED_MODULE_11__.Group();\n        // Корпус\n        const hull = new three__WEBPACK_IMPORTED_MODULE_11__.Mesh(new three__WEBPACK_IMPORTED_MODULE_11__.BoxGeometry(L, W * 0.25, W), new three__WEBPACK_IMPORTED_MODULE_11__.MeshStandardMaterial({\n            color: col,\n            roughness: 0.55,\n            metalness: 0.35,\n        }));\n        hull.position.y = W * 0.1;\n        hull.castShadow = hull.receiveShadow = true;\n        group.add(hull);\n        // Нос (конус повёрнут по Z — острие в +X)\n        const bow = new three__WEBPACK_IMPORTED_MODULE_11__.Mesh(new three__WEBPACK_IMPORTED_MODULE_11__.ConeGeometry(W * 0.38, L * 0.32, 6), new three__WEBPACK_IMPORTED_MODULE_11__.MeshStandardMaterial({\n            color: col.clone().multiplyScalar(0.8),\n            roughness: 0.5,\n            metalness: 0.4,\n        }));\n        bow.rotation.z = -Math.PI / 2;\n        bow.position.set(L * 0.56, W * 0.14, 0);\n        bow.castShadow = true;\n        group.add(bow);\n        // Надстройка\n        const superStr = new three__WEBPACK_IMPORTED_MODULE_11__.Mesh(new three__WEBPACK_IMPORTED_MODULE_11__.BoxGeometry(L * 0.3, W * 0.32, W * 0.52), new three__WEBPACK_IMPORTED_MODULE_11__.MeshStandardMaterial({\n            color: col.clone().multiplyScalar(1.2),\n            roughness: 0.4,\n            metalness: 0.55,\n        }));\n        superStr.position.set(-L * 0.08, W * 0.29, 0);\n        superStr.castShadow = true;\n        group.add(superStr);\n        // Трубы дыма (небольшие цилиндры)\n        for (let i = 0; i < 2; i++) {\n            const chimney = new three__WEBPACK_IMPORTED_MODULE_11__.Mesh(new three__WEBPACK_IMPORTED_MODULE_11__.CylinderGeometry(W * 0.06, W * 0.07, W * 0.4, 8), new three__WEBPACK_IMPORTED_MODULE_11__.MeshStandardMaterial({ color: 0x333333, roughness: 0.9 }));\n            chimney.position.set(-L * 0.06 + i * L * 0.1, W * 0.52, W * 0.14 - i * W * 0.28);\n            chimney.castShadow = true;\n            group.add(chimney);\n        }\n        return group;\n    }\n    GetOrCreateShipMesh(player) {\n        var _a;\n        if (this.shipMeshes.has(player.id))\n            return this.shipMeshes.get(player.id);\n        const ship = (0,_shared_games_params_navalbattle__WEBPACK_IMPORTED_MODULE_4__.GetNavalBattleShip)(player.data.shipId);\n        const color = this.GetPlayerColor(player.id);\n        const group = new three__WEBPACK_IMPORTED_MODULE_11__.Group();\n        (_a = this.scene) === null || _a === void 0 ? void 0 : _a.add(group);\n        this.shipMeshes.set(player.id, group);\n        if (ship.modelPath) {\n            // Пока модель грузится — показываем примитив\n            const placeholder = this.MakePrimitiveShip(player.data.shipId, color);\n            group.add(placeholder);\n            const cached = this.fbxCache.get(ship.modelPath);\n            if (cached) {\n                group.remove(placeholder);\n                group.add(cached.clone());\n            }\n            else {\n                this.fbxLoader.load(ship.modelPath, (fbx) => {\n                    this.fbxCache.set(ship.modelPath, fbx);\n                    group.remove(placeholder);\n                    fbx.traverse(child => {\n                        if (child.isMesh) {\n                            const m = child;\n                            m.castShadow = m.receiveShadow = true;\n                        }\n                    });\n                    group.add(fbx.clone());\n                }, undefined, () => {\n                    /* оставляем примитив при ошибке */\n                });\n            }\n        }\n        else {\n            group.add(this.MakePrimitiveShip(player.data.shipId, color));\n        }\n        // ── Имя игрока (TextGeometry) ──────────────────────────────────────────\n        if (player.id !== _api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id && this.mainFont) {\n            const textGeo = new three_examples_jsm_geometries_TextGeometry_js__WEBPACK_IMPORTED_MODULE_13__.TextGeometry(player.name, {\n                font: this.mainFont,\n                size: 14,\n                curveSegments: 2,\n                depth: 0,\n            });\n            textGeo.computeBoundingBox();\n            const cx = -0.5 * (textGeo.boundingBox.max.x - textGeo.boundingBox.min.x);\n            const nameMat = new three__WEBPACK_IMPORTED_MODULE_11__.MeshBasicMaterial({ color: 0xffffff });\n            const nameMesh = new three__WEBPACK_IMPORTED_MODULE_11__.Mesh(textGeo, nameMat);\n            nameMesh.position.set(cx, 0, 0);\n            const nameGrp = new three__WEBPACK_IMPORTED_MODULE_11__.Group();\n            nameGrp.name = 'FaceCamera';\n            nameGrp.position.set(0, (0,_shared_games_params_navalbattle__WEBPACK_IMPORTED_MODULE_4__.GetNavalBattleShip)(player.data.shipId).width * 0.9 + 20, 0);\n            nameGrp.add(nameMesh);\n            group.add(nameGrp);\n        }\n        // ── HP-бар (PlaneGeometry) ──────────────────────────────────────────────\n        const hpGrp = new three__WEBPACK_IMPORTED_MODULE_11__.Group();\n        hpGrp.name = 'FaceCamera';\n        hpGrp.position.set(0, (0,_shared_games_params_navalbattle__WEBPACK_IMPORTED_MODULE_4__.GetNavalBattleShip)(player.data.shipId).width * 0.9, 0);\n        const bgGeo = new three__WEBPACK_IMPORTED_MODULE_11__.PlaneGeometry(60, 6);\n        const bgMat = new three__WEBPACK_IMPORTED_MODULE_11__.MeshBasicMaterial({ color: 0x08101e });\n        const bgMesh = new three__WEBPACK_IMPORTED_MODULE_11__.Mesh(bgGeo, bgMat);\n        hpGrp.add(bgMesh);\n        const fillGeo = new three__WEBPACK_IMPORTED_MODULE_11__.PlaneGeometry(60, 6);\n        fillGeo.translate(30, 0, 0);\n        const fillMat = new three__WEBPACK_IMPORTED_MODULE_11__.MeshBasicMaterial({ color: 0x53e17f });\n        const fillMesh = new three__WEBPACK_IMPORTED_MODULE_11__.Mesh(fillGeo, fillMat);\n        fillMesh.position.set(-30, 0, 0.1); // Z=0.1 чтобы быть над фоном\n        hpGrp.add(fillMesh);\n        group.add(hpGrp);\n        this.hpBarObjects.set(player.id, { fillPath: fillMesh });\n        return group;\n    }\n    UpdateShipMesh(player, pos, nowMS) {\n        const group = this.GetOrCreateShipMesh(player);\n        const isMe = player.id === _api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id;\n        const dead = player.data.hp <= 0 || player.data.respawnAtMS > nowMS;\n        const stealthOff = !player.data.visible && !isMe;\n        const blink = player.data.invulnerableUntilMS > nowMS &&\n            Math.floor(nowMS / 140) % 2 === 0;\n        group.visible = !dead && !stealthOff && !blink;\n        if (!group.visible)\n            return;\n        // Покачивание на волнах\n        const t = nowMS * 0.001;\n        const bx = pos.x * 0.004;\n        const bz = pos.y * 0.004;\n        const bob = Math.sin(t + bx) * 2.8 + Math.sin(t * 0.73 + bz) * 1.8;\n        const roll = Math.sin(t * 0.82 + bx) * 0.022;\n        const pitch = Math.sin(t * 0.61 + bz) * 0.016;\n        // Игровые координаты XY → Three.js XZ (Y — вверх)\n        group.position.set(pos.x, bob, pos.y);\n        // В игре angle=0 → направление +X, в Three.js rotation.y CCW\n        group.rotation.set(pitch, -pos.angle, roll, 'YXZ');\n        // Прозрачность при невидимости (только себе)\n        const stealth = isMe && player.data.stealthUntilMS > nowMS;\n        group.traverse(child => {\n            if (!child.isMesh)\n                return;\n            const mat = child.material;\n            if (mat.transparent !== stealth || (stealth && mat.opacity !== 0.38)) {\n                mat.transparent = stealth;\n                mat.opacity = stealth ? 0.38 : 1;\n                mat.needsUpdate = true;\n            }\n        });\n        // HP-бар\n        const hpObj = this.hpBarObjects.get(player.id);\n        if (hpObj && player.data.maxHp > 0) {\n            const ratio = Math.max(0, player.data.hp / player.data.maxHp);\n            hpObj.fillPath.scale.x = Math.max(0.001, ratio);\n            const barColor = ratio > 0.5 ? 0x53e17f : ratio > 0.25 ? 0xffd93d : 0xff6b6b;\n            const mat = hpObj.fillPath.material;\n            if (mat.color.getHex() !== barColor)\n                mat.color.setHex(barColor);\n        }\n    }\n    // ════════════════════════════════════════════════════════════════════════════\n    //  Зона прицела\n    // ════════════════════════════════════════════════════════════════════════════\n    GetOrCreateAimMesh(playerId) {\n        var _a;\n        if (this.aimMeshes.has(playerId))\n            return this.aimMeshes.get(playerId);\n        // Трапеция из 2 треугольников; вершины обновляются каждый кадр\n        const geo = new three__WEBPACK_IMPORTED_MODULE_11__.BufferGeometry();\n        geo.setAttribute('position', new three__WEBPACK_IMPORTED_MODULE_11__.BufferAttribute(new Float32Array(4 * 3), 3));\n        geo.setIndex([0, 1, 2, 0, 2, 3]);\n        const mat = new three__WEBPACK_IMPORTED_MODULE_11__.MeshBasicMaterial({\n            color: 0xff7878,\n            transparent: true,\n            opacity: 0.18,\n            side: three__WEBPACK_IMPORTED_MODULE_11__.DoubleSide,\n            depthWrite: false,\n        });\n        const mesh = new three__WEBPACK_IMPORTED_MODULE_11__.Mesh(geo, mat);\n        mesh.visible = false;\n        (_a = this.scene) === null || _a === void 0 ? void 0 : _a.add(mesh);\n        this.aimMeshes.set(playerId, mesh);\n        return mesh;\n    }\n    UpdateAimMesh(player, state) {\n        const mesh = this.GetOrCreateAimMesh(player.id);\n        const weapon = player.data.aimingWeapon;\n        if (!weapon) {\n            mesh.visible = false;\n            return;\n        }\n        mesh.visible = true;\n        mesh.material.opacity =\n            player.id === _api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id ? 0.18 : 0.13;\n        const ship = (0,_shared_games_params_navalbattle__WEBPACK_IMPORTED_MODULE_4__.GetNavalBattleShip)(player.data.shipId);\n        const cos = Math.cos(state.angle);\n        const sin = Math.sin(state.angle);\n        // Направление и ширина в зависимости от типа оружия\n        let dirX = cos, dirZ = sin;\n        let normalX = -sin, normalZ = cos;\n        let originX = state.x + dirX * ship.length * 0.5;\n        let originZ = state.y + dirZ * ship.length * 0.5;\n        let nearW = ship.splashForwardWidth;\n        let range = _shared_games_params_navalbattle__WEBPACK_IMPORTED_MODULE_4__.NAVAL_BATTLE_SHOT_FORWARD_RANGE;\n        if (weapon === \"left\" /* NavalBattleWeaponType.LEFT */) {\n            dirX = sin;\n            dirZ = -cos;\n            normalX = cos;\n            normalZ = sin;\n            originX = state.x + dirX * ship.width * 0.5;\n            originZ = state.y + dirZ * ship.width * 0.5;\n            nearW = ship.splashSideWidth;\n            range = _shared_games_params_navalbattle__WEBPACK_IMPORTED_MODULE_4__.NAVAL_BATTLE_SHOT_SIDE_RANGE;\n        }\n        else if (weapon === \"right\" /* NavalBattleWeaponType.RIGHT */) {\n            dirX = -sin;\n            dirZ = cos;\n            normalX = cos;\n            normalZ = sin;\n            originX = state.x + dirX * ship.width * 0.5;\n            originZ = state.y + dirZ * ship.width * 0.5;\n            nearW = ship.splashSideWidth;\n            range = _shared_games_params_navalbattle__WEBPACK_IMPORTED_MODULE_4__.NAVAL_BATTLE_SHOT_SIDE_RANGE;\n        }\n        const farW = nearW * 1.4;\n        const farX = originX + dirX * range;\n        const farZ = originZ + dirZ * range;\n        const Y = 5;\n        const pos = mesh.geometry.attributes.position;\n        pos.setXYZ(0, originX + normalX * nearW * 0.5, Y, originZ + normalZ * nearW * 0.5);\n        pos.setXYZ(1, originX - normalX * nearW * 0.5, Y, originZ - normalZ * nearW * 0.5);\n        pos.setXYZ(2, farX - normalX * farW * 0.5, Y, farZ - normalZ * farW * 0.5);\n        pos.setXYZ(3, farX + normalX * farW * 0.5, Y, farZ + normalZ * farW * 0.5);\n        pos.needsUpdate = true;\n        mesh.geometry.computeBoundingSphere();\n    }\n    // ════════════════════════════════════════════════════════════════════════════\n    //  Обломки\n    // ════════════════════════════════════════════════════════════════════════════\n    SpawnWreck3D(x, z, angle, shipId) {\n        if (!this.scene)\n            return;\n        const ship = (0,_shared_games_params_navalbattle__WEBPACK_IMPORTED_MODULE_4__.GetNavalBattleShip)(shipId);\n        const group = new three__WEBPACK_IMPORTED_MODULE_11__.Group();\n        const hullMesh = new three__WEBPACK_IMPORTED_MODULE_11__.Mesh(new three__WEBPACK_IMPORTED_MODULE_11__.BoxGeometry(ship.length, ship.width * 0.12, ship.width), new three__WEBPACK_IMPORTED_MODULE_11__.MeshStandardMaterial({\n            color: 0x2e343b,\n            roughness: 0.92,\n            metalness: 0.15,\n            transparent: true,\n            opacity: 0.7,\n        }));\n        hullMesh.receiveShadow = true;\n        group.add(hullMesh);\n        group.position.set(x, -2, z);\n        group.rotation.y = -angle;\n        this.scene.add(group);\n        this.wreck3Ds.push({\n            group,\n            expiresAtMS: _shared_system__WEBPACK_IMPORTED_MODULE_5__.System.timestampMS + 3200,\n            fireTimer: 0,\n        });\n        // Большой взрыв при потоплении\n        for (let i = 0; i < 30; i++) {\n            const ang = Math.random() * Math.PI * 2;\n            const spd = 80 + Math.random() * 220;\n            this.SpawnParticle3D(x + (Math.random() - 0.5) * ship.length * 0.6, z + (Math.random() - 0.5) * ship.width * 0.6, i < 15 ? '#ff7a28' : '#ffee44', 8 + Math.random() * 12, 0.0015 + Math.random() * 0.002, Math.cos(ang) * spd, Math.sin(ang) * spd, 60 + Math.random() * 120);\n        }\n    }\n    UpdateWrecks3D(deltaMS, nowMS) {\n        var _a;\n        const next = [];\n        for (const w of this.wreck3Ds) {\n            if (w.expiresAtMS <= nowMS) {\n                (_a = this.scene) === null || _a === void 0 ? void 0 : _a.remove(w.group);\n                continue;\n            }\n            next.push(w);\n            const life = Math.max(0, (w.expiresAtMS - nowMS) / 3200);\n            w.group.traverse(child => {\n                if (child.isMesh) {\n                    const mat = child\n                        .material;\n                    mat.opacity = 0.35 + life * 0.35;\n                }\n            });\n            // Огонь\n            w.fireTimer += deltaMS;\n            if (w.fireTimer > 45) {\n                w.fireTimer = 0;\n                const p = w.group.position;\n                this.SpawnParticle3D(p.x + (Math.random() - 0.5) * 22, p.z + (Math.random() - 0.5) * 22, life > 0.5 ? '#ff7a28' : '#ff4400', 10 + Math.random() * 14, 0.003 + Math.random() * 0.002, 0, 0, 30 + Math.random() * 50);\n                // Дым\n                this.SpawnParticle3D(p.x + (Math.random() - 0.5) * 15, p.z + (Math.random() - 0.5) * 15, '#555566', 14 + Math.random() * 10, 0.0012, (Math.random() - 0.5) * 20, (Math.random() - 0.5) * 20, 25 + Math.random() * 40);\n            }\n        }\n        this.wreck3Ds = next;\n    }\n    // ════════════════════════════════════════════════════════════════════════════\n    //  Частицы\n    // ════════════════════════════════════════════════════════════════════════════\n    SpawnParticle3D(x, z, color, size, decay, vx = 0, vz = 0, vy = 0) {\n        const c = new three__WEBPACK_IMPORTED_MODULE_11__.Color(color);\n        this.particles3D.push({\n            x,\n            z,\n            y: 6 + Math.random() * 4,\n            vx: vx + (Math.random() - 0.5) * 20,\n            vz: vz + (Math.random() - 0.5) * 20,\n            vy: vy,\n            life: 1,\n            decay,\n            r: c.r,\n            g: c.g,\n            b: c.b,\n            size,\n        });\n    }\n    SpawnHitParticles3D(x, z, color, own) {\n        const count = own ? 35 : 20;\n        for (let i = 0; i < count; i++) {\n            const ang = Math.random() * Math.PI * 2;\n            const spd = 70 + Math.random() * (own ? 220 : 140);\n            this.SpawnParticle3D(x + (Math.random() - 0.5) * 10, z + (Math.random() - 0.5) * 10, own ? '#ff4444' : color, own ? 6 + Math.random() * 6 : 3 + Math.random() * 4, 0.0018 + Math.random() * 0.002, Math.cos(ang) * spd, Math.sin(ang) * spd, 30 + Math.random() * 70);\n        }\n        // Искры / заabayание\n        for (let i = 0; i < count; i++) {\n            const ang = Math.random() * Math.PI * 2;\n            const spd = 40 + Math.random() * (own ? 180 : 100);\n            this.SpawnParticle3D(x + (Math.random() - 0.5) * 8, z + (Math.random() - 0.5) * 8, own ? '#ffff88' : '#ffdd88', 2 + Math.random() * 3, 0.0025 + Math.random() * 0.002, Math.cos(ang) * spd, Math.sin(ang) * spd, 20 + Math.random() * 50);\n        }\n        // Вспышка воды\n        for (let i = 0; i < 12; i++) {\n            const ang = Math.random() * Math.PI * 2;\n            this.SpawnParticle3D(x, z, '#99ccff', 5 + Math.random() * 8, 0.003, Math.cos(ang) * (40 + Math.random() * 80), Math.sin(ang) * (40 + Math.random() * 80), 50 + Math.random() * 80);\n        }\n        // Дым\n        for (let i = 0; i < 6; i++) {\n            const ang = Math.random() * Math.PI * 2;\n            this.SpawnParticle3D(x + (Math.random() - 0.5) * 15, z + (Math.random() - 0.5) * 15, '#333344', 10 + Math.random() * 8, 0.0008, (Math.random() - 0.5) * 30, (Math.random() - 0.5) * 30, 15 + Math.random() * 25);\n        }\n        this.screenShake = Math.max(this.screenShake, own ? 12 : 6);\n    }\n    SpawnShotParticles3D(x, z, dirX, dirZ, color) {\n        // Дульные газы\n        for (let i = 0; i < 14; i++) {\n            const spread = (Math.random() - 0.5) * 1.8;\n            const spd = 160 + Math.random() * 240;\n            this.SpawnParticle3D(x, z, color, 3 + Math.random() * 4, 0.004 + Math.random() * 0.003, (dirX - spread * dirZ) * spd, (dirZ + spread * dirX) * spd, 50 + Math.random() * 80);\n        }\n        // Снарядный всплеск вдоль траектории\n        for (let i = 0; i < 6; i++) {\n            this.SpawnParticle3D(x + dirX * (30 + i * 20), z + dirZ * (30 + i * 20), '#ffffff', 2 + Math.random() * 3, 0.006, 0, 0, 20 + Math.random() * 30);\n        }\n    }\n    UpdateParticles3D(deltaMS) {\n        const dt = deltaMS / 1000;\n        const alive = [];\n        for (const p of this.particles3D) {\n            p.x += p.vx * dt;\n            p.z += p.vz * dt;\n            p.y += p.vy * dt;\n            p.vy -= 80 * dt; // гравитация\n            p.vx *= 0.88;\n            p.vz *= 0.88;\n            if (p.y < 0) {\n                p.y = 0;\n                p.vy *= -0.3;\n            }\n            p.life -= p.decay * deltaMS;\n            if (p.life > 0)\n                alive.push(p);\n        }\n        // Ограничиваем размер пула\n        this.particles3D = alive.length > 2000 ? alive.slice(-2000) : alive;\n        if (!this.particleGeo)\n            return;\n        const posAttr = this.particleGeo.attributes\n            .position;\n        const colAttr = this.particleGeo.attributes.color;\n        const sizeAttr = this.particleGeo.attributes.size;\n        const n = this.particles3D.length;\n        for (let i = 0; i < n; i++) {\n            const p = this.particles3D[i];\n            posAttr.setXYZ(i, p.x, p.y, p.z);\n            colAttr.setXYZ(i, p.r * p.life, p.g * p.life, p.b * p.life);\n            sizeAttr.setX(i, p.size * p.life);\n        }\n        posAttr.needsUpdate = true;\n        colAttr.needsUpdate = true;\n        sizeAttr.needsUpdate = true;\n        this.particleGeo.setDrawRange(0, n);\n    }\n    SpawnProjectile(x, z, dirX, dirZ, speed, colorHex) {\n        if (!this.scene)\n            return;\n        const geo = new three__WEBPACK_IMPORTED_MODULE_11__.CylinderGeometry(1.5, 1.5, 20, 5);\n        geo.rotateZ(Math.PI / 2);\n        const color = new three__WEBPACK_IMPORTED_MODULE_11__.Color(colorHex).multiplyScalar(1.5);\n        const mat = new three__WEBPACK_IMPORTED_MODULE_11__.MeshBasicMaterial({ color });\n        const mesh = new three__WEBPACK_IMPORTED_MODULE_11__.Mesh(geo, mat);\n        mesh.position.set(x, 5, z);\n        mesh.rotation.y = -Math.atan2(dirZ, dirX);\n        this.scene.add(mesh);\n        this.projectiles.push({\n            mesh,\n            vx: dirX * speed,\n            vz: dirZ * speed,\n            life: 0.3,\n        });\n    }\n    UpdateProjectiles3D(deltaMS) {\n        var _a;\n        const dt = deltaMS / 1000;\n        const alive = [];\n        for (const p of this.projectiles) {\n            p.life -= dt;\n            if (p.life <= 0) {\n                (_a = this.scene) === null || _a === void 0 ? void 0 : _a.remove(p.mesh);\n                p.mesh.geometry.dispose();\n                p.mesh.material.dispose();\n            }\n            else {\n                p.mesh.position.x += p.vx * dt;\n                p.mesh.position.z += p.vz * dt;\n                alive.push(p);\n            }\n        }\n        this.projectiles = alive;\n    }\n    // ════════════════════════════════════════════════════════════════════════════\n    //  Цифры урона (CSS2D)\n    // ════════════════════════════════════════════════════════════════════════════\n    SpawnDamageText3D(x, z, value, own) {\n        if (value <= 0 || !this.scene || !this.mainFont)\n            return;\n        const textGeo = new three_examples_jsm_geometries_TextGeometry_js__WEBPACK_IMPORTED_MODULE_13__.TextGeometry(`-${Math.round(value)}`, {\n            font: this.mainFont,\n            size: 16,\n            curveSegments: 2,\n            depth: 0,\n        });\n        textGeo.computeBoundingBox();\n        const cx = -0.5 * (textGeo.boundingBox.max.x - textGeo.boundingBox.min.x);\n        const mat = new three__WEBPACK_IMPORTED_MODULE_11__.MeshBasicMaterial({\n            color: own ? 0xff6d6d : 0xffd7a6,\n            transparent: true,\n            opacity: 1,\n        });\n        const mesh = new three__WEBPACK_IMPORTED_MODULE_11__.Mesh(textGeo, mat);\n        // Обернем в группу чтобы центрировать локально\n        const group = new three__WEBPACK_IMPORTED_MODULE_11__.Group();\n        mesh.position.set(cx, 0, 0);\n        group.add(mesh);\n        group.position.set(x, 50, z);\n        group.name = 'FaceCamera';\n        this.scene.add(group);\n        this.damageLabels.push({ mesh: group, life: 1, vy: 42 });\n    }\n    UpdateDamageTexts(deltaMS) {\n        var _a;\n        const dt = deltaMS / 1000;\n        const next = [];\n        for (const d of this.damageLabels) {\n            d.mesh.position.y += d.vy * dt;\n            d.life -= 0.0018 * deltaMS;\n            if (d.life <= 0) {\n                (_a = this.scene) === null || _a === void 0 ? void 0 : _a.remove(d.mesh);\n                const child = d.mesh.children[0];\n                child.geometry.dispose();\n                child.material.dispose();\n                continue;\n            }\n            const child = d.mesh.children[0];\n            child.material.opacity = Math.max(0, d.life);\n            // Look at camera\n            if (this.camera)\n                d.mesh.quaternion.copy(this.camera.quaternion);\n            next.push(d);\n        }\n        this.damageLabels = next;\n    }\n    // ════════════════════════════════════════════════════════════════════════════\n    //  Модификаторы\n    // ════════════════════════════════════════════════════════════════════════════\n    UpdateModifiers(nowMS) {\n        if (!this.scene)\n            return;\n        const mods = this.state.gameData.modifiers;\n        const modSet = new Set(mods.map(m => `${m.x}_${m.y}`));\n        // Удаляем подобранные\n        this.modifierMeshes.forEach((mesh, key) => {\n            if (!modSet.has(key)) {\n                this.scene.remove(mesh);\n                mesh.geometry.dispose();\n                this.modifierMeshes.delete(key);\n            }\n        });\n        // Создаём новые\n        for (const mod of mods) {\n            const key = `${mod.x}_${mod.y}`;\n            if (this.modifierMeshes.has(key))\n                continue;\n            const mesh = new three__WEBPACK_IMPORTED_MODULE_11__.Mesh(new three__WEBPACK_IMPORTED_MODULE_11__.BoxGeometry(28, 28, 28), new three__WEBPACK_IMPORTED_MODULE_11__.MeshStandardMaterial({\n                color: 0x162434,\n                roughness: 0.4,\n                metalness: 0.65,\n                transparent: true,\n                opacity: 0.9,\n                emissive: new three__WEBPACK_IMPORTED_MODULE_11__.Color(0x112233),\n                emissiveIntensity: 0.5,\n            }));\n            mesh.position.set(mod.x, 14, mod.y);\n            mesh.castShadow = true;\n            this.scene.add(mesh);\n            // Иконка текста поверх куба\n            if (this.mainFont) {\n                const textGeo = new three_examples_jsm_geometries_TextGeometry_js__WEBPACK_IMPORTED_MODULE_13__.TextGeometry(MODIFIER_ICON[mod.type], {\n                    font: this.mainFont,\n                    size: 8,\n                    curveSegments: 2,\n                    depth: 0,\n                });\n                textGeo.computeBoundingBox();\n                const cx = -0.5 * (textGeo.boundingBox.max.x - textGeo.boundingBox.min.x);\n                const mat = new three__WEBPACK_IMPORTED_MODULE_11__.MeshBasicMaterial({ color: 0xffffff });\n                const txtMesh = new three__WEBPACK_IMPORTED_MODULE_11__.Mesh(textGeo, mat);\n                txtMesh.position.set(cx, 0, 0);\n                const grp = new three__WEBPACK_IMPORTED_MODULE_11__.Group();\n                grp.name = 'FaceCamera';\n                grp.position.set(0, 26, 0);\n                grp.add(txtMesh);\n                mesh.add(grp);\n            }\n            this.modifierMeshes.set(key, mesh);\n        }\n        // Анимация вращения + плавание\n        this.modifierMeshes.forEach(mesh => {\n            mesh.rotation.y = nowMS * 0.0011;\n            mesh.position.y = 14 + Math.sin(nowMS * 0.002) * 5;\n        });\n    }\n    // ════════════════════════════════════════════════════════════════════════════\n    //  Индикаторы за экраном\n    // ════════════════════════════════════════════════════════════════════════════\n    UpdateOffscreenIndicators(width, height) {\n        const container = this.offscreenLayerRef.current;\n        if (!container || !this.camera)\n            return;\n        const cx = width * 0.5;\n        const cy = height * 0.5;\n        const pad = 28;\n        const meId = _api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id;\n        let html = '';\n        for (const player of this.state.players) {\n            if (player.id === meId || player.data.eliminated)\n                continue;\n            if (player.data.hp <= 0 || player.data.respawnAtMS > _shared_system__WEBPACK_IMPORTED_MODULE_5__.System.timestampMS)\n                continue;\n            if (!player.data.visible)\n                continue;\n            const state = this.renderStates.get(player.id);\n            if (!state)\n                continue;\n            // Проецируем мировую позицию в NDC\n            const v = new three__WEBPACK_IMPORTED_MODULE_11__.Vector3(state.x, 0, state.y).project(this.camera);\n            const sx = (v.x * 0.5 + 0.5) * width;\n            const sy = (v.y * -0.5 + 0.5) * height;\n            if (sx >= pad && sx <= width - pad && sy >= pad && sy <= height - pad)\n                continue;\n            const dx = sx - cx;\n            const dy = sy - cy;\n            const ang = Math.atan2(dy, dx) * (180 / Math.PI);\n            const t = Math.min((cx - pad) / Math.max(1e-6, Math.abs(dx)), (cy - pad) / Math.max(1e-6, Math.abs(dy)));\n            const ex = cx + dx * t;\n            const ey = cy + dy * t;\n            const col = this.GetPlayerColor(player.id);\n            html +=\n                `<div style=\"position:absolute;left:${ex}px;top:${ey}px;transform:translate(-50%,-50%) rotate(${ang}deg);` +\n                    `width:0;height:0;border-top:7px solid transparent;border-bottom:7px solid transparent;` +\n                    `border-left:13px solid ${col};opacity:0.95;\"></div>`;\n        }\n        container.innerHTML = html;\n    }\n    // ════════════════════════════════════════════════════════════════════════════\n    //  Камера\n    // ════════════════════════════════════════════════════════════════════════════\n    SetupOrbitControls() {\n        const canvas = this.canvasRef.current;\n        if (!canvas)\n            return;\n        this._onMouseDown = (e) => {\n            if (CAMERA_MODE !== 'orbit')\n                return;\n            if (e.button === 0 || e.button === 2) {\n                this.isDragging = true;\n                this.lastMX = e.clientX;\n                this.lastMY = e.clientY;\n            }\n        };\n        this._onMouseMove = (e) => {\n            if (!this.isDragging || CAMERA_MODE !== 'orbit')\n                return;\n            const dx = e.clientX - this.lastMX;\n            const dy = e.clientY - this.lastMY;\n            this.lastMX = e.clientX;\n            this.lastMY = e.clientY;\n            this.orbitTheta -= dx * ORBIT_SENSITIVITY_X;\n            this.orbitPhi = Math.max((ORBIT_MIN_PHI_DEG * Math.PI) / 180, Math.min((ORBIT_MAX_PHI_DEG * Math.PI) / 180, this.orbitPhi - dy * ORBIT_SENSITIVITY_Y));\n        };\n        this._onMouseUp = () => {\n            this.isDragging = false;\n        };\n        this._onWheel = (e) => {\n            if (CAMERA_MODE !== 'orbit')\n                return;\n            e.preventDefault();\n            this.orbitR = Math.max(200, Math.min(2500, this.orbitR + e.deltaY * 0.6));\n        };\n        canvas.addEventListener('mousedown', this._onMouseDown);\n        window.addEventListener('mousemove', this._onMouseMove);\n        window.addEventListener('mouseup', this._onMouseUp);\n        canvas.addEventListener('wheel', this._onWheel, { passive: false });\n        canvas.addEventListener('contextmenu', e => e.preventDefault());\n    }\n    UpdateCamera(deltaMS, meState, width, height) {\n        if (!this.camera || !this.renderer)\n            return;\n        const canvas = this.canvasRef.current;\n        if (canvas && (canvas.width !== width || canvas.height !== height)) {\n            this.renderer.setSize(width, height, false);\n            this.camera.aspect = width / height;\n            this.camera.updateProjectionMatrix();\n        }\n        this.screenShake = Math.max(0, this.screenShake - deltaMS * 0.025);\n        const shX = this.screenShake > 0.5 ? (Math.random() - 0.5) * this.screenShake : 0;\n        const shZ = this.screenShake > 0.5 ? (Math.random() - 0.5) * this.screenShake : 0;\n        const tx = meState.x;\n        const tz = meState.y;\n        if (CAMERA_MODE === 'topdown') {\n            // Смещение камеры в сторону прицела\n            const aimWeapon = this.activeHeldWeapon;\n            if (aimWeapon) {\n                const dir = this.GetWeaponAimDir(aimWeapon, meState.angle);\n                const blend = Math.min(0.25, deltaMS / 120);\n                this.cameraOffset.x = _shared_system__WEBPACK_IMPORTED_MODULE_5__.System.lerp(this.cameraOffset.x, dir.x * TOPDOWN_AIM_OFFSET, blend);\n                this.cameraOffset.y = _shared_system__WEBPACK_IMPORTED_MODULE_5__.System.lerp(this.cameraOffset.y, dir.y * TOPDOWN_AIM_OFFSET, blend);\n            }\n            else {\n                const blend = Math.min(0.08, deltaMS / 200);\n                this.cameraOffset.x = _shared_system__WEBPACK_IMPORTED_MODULE_5__.System.lerp(this.cameraOffset.x, 0, blend);\n                this.cameraOffset.y = _shared_system__WEBPACK_IMPORTED_MODULE_5__.System.lerp(this.cameraOffset.y, 0, blend);\n            }\n            // Высота масштабируется через zoom (меньше zoom → выше камера → больше видно)\n            const camH = TOPDOWN_HEIGHT / this.zoom;\n            const tiltRad = (TOPDOWN_TILT_DEG * Math.PI) / 180;\n            const lookX = tx + this.cameraOffset.x;\n            const lookZ = tz + this.cameraOffset.y;\n            this.camera.position.set(lookX + shX, camH, lookZ - camH * Math.tan(tiltRad) + shZ);\n            this.camera.lookAt(lookX, 0, lookZ);\n        }\n        else {\n            // Orbit вокруг корабля игрока\n            const sp = Math.sin(this.orbitPhi);\n            const cp = Math.cos(this.orbitPhi);\n            const st = Math.sin(this.orbitTheta);\n            const ct = Math.cos(this.orbitTheta);\n            this.camera.position.set(tx + this.orbitR * sp * st + shX, this.orbitR * cp, tz + this.orbitR * sp * ct + shZ);\n            this.camera.lookAt(tx, 30, tz);\n        }\n    }\n    // ════════════════════════════════════════════════════════════════════════════\n    //  Главный рендер кадра\n    // ════════════════════════════════════════════════════════════════════════════\n    DrawCanvas3D(deltaMS) {\n        if (!this.renderer || !this.scene || !this.camera)\n            return;\n        const me = this.GetMe();\n        if (!me)\n            return;\n        const nowMS = _shared_system__WEBPACK_IMPORTED_MODULE_5__.System.timestampMS;\n        const meState = this.UpdateRenderState(me, deltaMS);\n        // Размер вьюпорта\n        const board = this.boardRef.current;\n        const width = Math.max(640, Math.min(1180, (board === null || board === void 0 ? void 0 : board.clientWidth) || 960));\n        const height = Math.max(360, Math.min(640, Math.floor(width * 0.56)));\n        // Камера\n        this.UpdateCamera(deltaMS, meState, width, height);\n        // Все корабли\n        const activeIds = new Set(this.state.players.map(p => p.id));\n        for (const player of this.state.players) {\n            const state = player.id === _api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id\n                ? meState\n                : this.UpdateRenderState(player, deltaMS);\n            this.UpdateShipMesh(player, state, nowMS);\n            this.UpdateAimMesh(player, state);\n        }\n        // Убираем меши отключившихся игроков\n        this.shipMeshes.forEach((g, id) => {\n            if (!activeIds.has(id)) {\n                this.scene.remove(g);\n                this.shipMeshes.delete(id);\n                this.hpBarObjects.delete(id);\n            }\n        });\n        // Модификаторы\n        this.UpdateModifiers(nowMS);\n        // Ориентация на камеру всех вложенных FaceCamera-групп\n        if (this.camera) {\n            const camQ = this.camera.quaternion;\n            // Для кораблей (hp, names)\n            this.shipMeshes.forEach(ship => {\n                const invQ = ship.quaternion.clone().invert();\n                ship.children.forEach(c => {\n                    if (c.name === 'FaceCamera')\n                        c.quaternion.copy(camQ).premultiply(invQ);\n                });\n            });\n            // Для модификаторов\n            this.modifierMeshes.forEach(mesh => {\n                const invQ = mesh.quaternion.clone().invert();\n                mesh.children.forEach(c => {\n                    if (c.name === 'FaceCamera')\n                        c.quaternion.copy(camQ).premultiply(invQ);\n                });\n            });\n        }\n        // Обломки\n        this.UpdateWrecks3D(deltaMS, nowMS);\n        // Частицы\n        this.UpdateParticles3D(deltaMS);\n        this.UpdateProjectiles3D(deltaMS);\n        // Цифры урона\n        this.UpdateDamageTexts(deltaMS);\n        // Индикаторы за экраном\n        this.UpdateOffscreenIndicators(width, height);\n        // Анимация воды\n        if (this.water) {\n            this.water.material.uniforms['time'].value += deltaMS * 0.00028;\n        }\n        this.renderer.render(this.scene, this.camera);\n    }\n    // ════════════════════════════════════════════════════════════════════════════\n    //  Вспомогательные методы (без изменений из оригинала)\n    // ════════════════════════════════════════════════════════════════════════════\n    GetPlayerColor(id) {\n        const idx = this.state.players.findIndex(p => p.id === id);\n        return PLAYER_COLORS[Math.abs(idx) % PLAYER_COLORS.length];\n    }\n    NormalizeAngle(a) {\n        const pi2 = Math.PI * 2;\n        let r = a % pi2;\n        if (r <= -Math.PI)\n            r += pi2;\n        if (r > Math.PI)\n            r -= pi2;\n        return r;\n    }\n    GetMe() {\n        return this.state.players.find(p => p.id === _api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id);\n    }\n    IsInCombat() {\n        return (this.state.stage === 'ingame' &&\n            this.state.gameData.phase === \"combat\" /* NavalBattlePhase.COMBAT */);\n    }\n    IsInSelection() {\n        return (this.state.stage === 'ingame' &&\n            this.state.gameData.phase === \"ship_select\" /* NavalBattlePhase.SHIP_SELECT */);\n    }\n    GetSelectionRemainingSeconds() {\n        if (!this.IsInSelection())\n            return 0;\n        return Math.max(0, Math.ceil((this.state.gameData.phaseEndsAtMS - _shared_system__WEBPACK_IMPORTED_MODULE_5__.System.timestampMS) / 1000));\n    }\n    GetShipStatPercent(kind, value) {\n        const all = _shared_games_params_navalbattle__WEBPACK_IMPORTED_MODULE_4__.NAVAL_BATTLE_SHIPS.map(s => s[kind]);\n        const min = Math.min(...all);\n        const max = Math.max(...all);\n        if (max === min)\n            return 100;\n        return Math.round(((value - min) / (max - min)) * 100);\n    }\n    CooldownEndsAt(player, weapon) {\n        if (weapon === \"left\" /* NavalBattleWeaponType.LEFT */)\n            return player.cooldownLeftEndsAtMS;\n        if (weapon === \"right\" /* NavalBattleWeaponType.RIGHT */)\n            return player.cooldownRightEndsAtMS;\n        return player.cooldownFrontEndsAtMS;\n    }\n    CooldownRemaining(player, weapon) {\n        return Math.max(0, this.CooldownEndsAt(player, weapon) - _shared_system__WEBPACK_IMPORTED_MODULE_5__.System.timestampMS);\n    }\n    CanShoot(weapon) {\n        const me = this.GetMe();\n        if (!me)\n            return false;\n        if (!this.IsInCombat())\n            return false;\n        if (me.data.eliminated)\n            return false;\n        if (me.data.hp <= 0 || me.data.respawnAtMS > _shared_system__WEBPACK_IMPORTED_MODULE_5__.System.timestampMS)\n            return false;\n        if (me.data.invulnerableUntilMS > _shared_system__WEBPACK_IMPORTED_MODULE_5__.System.timestampMS)\n            return false;\n        if (this.CooldownRemaining(me.data, weapon) > 0)\n            return false;\n        return true;\n    }\n    WeaponLabel(weapon) {\n        if (weapon === \"left\" /* NavalBattleWeaponType.LEFT */)\n            return 'Q';\n        if (weapon === \"right\" /* NavalBattleWeaponType.RIGHT */)\n            return 'E';\n        return 'SPACE';\n    }\n    GetWeaponAimDir(weapon, angle) {\n        const cos = Math.cos(angle);\n        const sin = Math.sin(angle);\n        if (weapon === \"left\" /* NavalBattleWeaponType.LEFT */)\n            return { x: sin, y: -cos };\n        if (weapon === \"right\" /* NavalBattleWeaponType.RIGHT */)\n            return { x: -sin, y: cos };\n        return { x: cos, y: sin };\n    }\n    SendInput(force = false) {\n        const next = JSON.stringify(this.pressed);\n        if (!force && this.lastSentInput === next)\n            return;\n        this.lastSentInput = next;\n        this.SendAction('naval.input', this.pressed);\n    }\n    HandleWeaponStart(weapon) {\n        if (!this.CanShoot(weapon))\n            return;\n        if (this.activeHeldWeapon && this.activeHeldWeapon !== weapon)\n            return;\n        if (this.shotHeld[weapon])\n            return;\n        this.shotHeld[weapon] = true;\n        this.activeHeldWeapon = weapon;\n        this.SendAction('naval.aimStart', { weapon });\n    }\n    HandleWeaponRelease(weapon) {\n        if (!this.shotHeld[weapon])\n            return;\n        this.shotHeld[weapon] = false;\n        if (this.activeHeldWeapon === weapon)\n            this.activeHeldWeapon = '';\n        if (!this.IsInCombat()) {\n            this.SendAction('naval.aimCancel');\n            return;\n        }\n        this.SendAction('naval.fire', { weapon });\n        const me = this.GetMe();\n        const state = me ? this.renderStates.get(me.id) : undefined;\n        if (me && state) {\n            const ship = (0,_shared_games_params_navalbattle__WEBPACK_IMPORTED_MODULE_4__.GetNavalBattleShip)(me.data.shipId);\n            const cos = Math.cos(state.angle);\n            const sin = Math.sin(state.angle);\n            let dirX = cos, dirZ = sin;\n            let offX = 0, offZ = 0;\n            let isSide = false;\n            if (weapon === \"left\" /* NavalBattleWeaponType.LEFT */) {\n                dirX = sin;\n                dirZ = -cos;\n                offX = dirX * ship.width * 0.5;\n                offZ = dirZ * ship.width * 0.5;\n                isSide = true;\n            }\n            else if (weapon === \"right\" /* NavalBattleWeaponType.RIGHT */) {\n                dirX = -sin;\n                dirZ = cos;\n                offX = dirX * ship.width * 0.5;\n                offZ = dirZ * ship.width * 0.5;\n                isSide = true;\n            }\n            else {\n                offX = cos * ship.length * 0.5;\n                offZ = sin * ship.length * 0.5;\n            }\n            const px = state.x + offX;\n            const pz = state.y + offZ;\n            const colorHex = this.GetPlayerColor(me.id);\n            this.SpawnShotParticles3D(px, pz, dirX, dirZ, colorHex);\n            const range = isSide\n                ? _shared_games_params_navalbattle__WEBPACK_IMPORTED_MODULE_4__.NAVAL_BATTLE_SHOT_SIDE_RANGE\n                : _shared_games_params_navalbattle__WEBPACK_IMPORTED_MODULE_4__.NAVAL_BATTLE_SHOT_FORWARD_RANGE;\n            const speed = range / 0.3;\n            const normX = -dirZ;\n            const normZ = dirX;\n            if (isSide) {\n                const spread = ship.splashSideWidth * 0.4;\n                this.SpawnProjectile(px, pz, dirX, dirZ, speed, colorHex);\n                this.SpawnProjectile(px + normX * spread, pz + normZ * spread, dirX, dirZ, speed, colorHex);\n                this.SpawnProjectile(px - normX * spread, pz - normZ * spread, dirX, dirZ, speed, colorHex);\n            }\n            else {\n                const spread = ship.splashForwardWidth * 0.4;\n                this.SpawnProjectile(px + normX * spread, pz + normZ * spread, dirX, dirZ, speed, colorHex);\n                this.SpawnProjectile(px - normX * spread, pz - normZ * spread, dirX, dirZ, speed, colorHex);\n            }\n        }\n    }\n    // ── Интерполяция позиций (без изменений) ─────────────────────────────────\n    UpdateRenderState(player, deltaMS) {\n        const nowMS = _shared_system__WEBPACK_IMPORTED_MODULE_5__.System.timestampMS;\n        const dt = Math.max(0, Math.min(0.08, deltaMS / 1000));\n        const existing = this.renderStates.get(player.id);\n        if (player.id !== _api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id &&\n            (!player.data.visible || player.data.x < -90000 || player.data.y < -90000)) {\n            if (existing)\n                return existing;\n            const fallback = {\n                x: 0,\n                y: 0,\n                angle: player.data.angle,\n                updatedAtMS: nowMS,\n            };\n            this.renderStates.set(player.id, fallback);\n            return fallback;\n        }\n        const base = existing || {\n            x: player.data.x,\n            y: player.data.y,\n            angle: player.data.angle,\n            updatedAtMS: nowMS,\n        };\n        const predictedAngle = base.angle + player.data.turnInput * dt;\n        const predictedX = base.x + Math.cos(predictedAngle) * player.data.speedForward * dt;\n        const predictedY = base.y + Math.sin(predictedAngle) * player.data.speedForward * dt;\n        const blend = player.id === _api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id ? 0.07 : 0.05;\n        const x = _shared_system__WEBPACK_IMPORTED_MODULE_5__.System.lerp(predictedX, player.data.x, blend);\n        const y = _shared_system__WEBPACK_IMPORTED_MODULE_5__.System.lerp(predictedY, player.data.y, blend);\n        const angleDiff = this.NormalizeAngle(player.data.angle - predictedAngle);\n        const angle = predictedAngle + angleDiff * blend;\n        const clamped = {\n            x: Math.max(0, Math.min(this.state.gameData.mapWidth, x)),\n            y: Math.max(0, Math.min(this.state.gameData.mapHeight, y)),\n            angle,\n            updatedAtMS: nowMS,\n        };\n        this.renderStates.set(player.id, clamped);\n        return clamped;\n    }\n    ResetInputState() {\n        this.pressed = { w: false, s: false, a: false, d: false };\n        this.shotHeld = {\n            [\"left\" /* NavalBattleWeaponType.LEFT */]: false,\n            [\"right\" /* NavalBattleWeaponType.RIGHT */]: false,\n            [\"front\" /* NavalBattleWeaponType.FRONT */]: false,\n        };\n        this.activeHeldWeapon = '';\n        this.lastSentInput = '';\n    }\n    // ════════════════════════════════════════════════════════════════════════════\n    //  React: экраны (HUD/UI — без изменений из оригинала)\n    // ════════════════════════════════════════════════════════════════════════════\n    RenderWaiting() {\n        return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"naval-waiting\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"naval-waiting-title\", children: this.LangString('gameNavalBattle') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"naval-waiting-subtitle\", children: this.LangString('NavalBattleWaitingPlayers') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"naval-waiting-list\", children: this.state.players.map((player, idx) => ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"naval-waiting-player\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { className: \"naval-waiting-dot\", style: {\n                                    background: PLAYER_COLORS[idx % PLAYER_COLORS.length],\n                                } }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"img\", { className: \"naval-waiting-avatar\", src: player.avatar, alt: player.name, onClick: () => {\n                                    if (!player.bot)\n                                        _api__WEBPACK_IMPORTED_MODULE_2__.Api.ShowProfile(player.id);\n                                } }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"span\", { className: `naval-waiting-name ${player.bot ? '' : 'clickable'}`, onClick: () => {\n                                    if (!player.bot)\n                                        _api__WEBPACK_IMPORTED_MODULE_2__.Api.ShowProfile(player.id);\n                                }, children: [player.bot ? 'BOT | ' : '', player.name, !player.bot\n                                        ? ` | ${Math.max(0, Math.round(player.ping || 0))}ms`\n                                        : ''] }), player.id === _api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"span\", { className: \"naval-waiting-you\", children: [' ', \"(\", this.LangString('NavalBattleYou'), \")\"] }))] }, player.id))) })] }));\n    }\n    RenderShipSelect() {\n        const me = this.GetMe();\n        if (!me)\n            return (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.Fragment, {});\n        const remain = this.GetSelectionRemainingSeconds();\n        const selectedShip = me.data.shipId;\n        return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"naval-select-screen\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"select-head\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"h2\", { children: this.LangString('NavalBattleSelectTitle') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"timer\", children: this.LangString('NavalBattleSelectTimer', remain) })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"ship-cards\", children: _shared_games_params_navalbattle__WEBPACK_IMPORTED_MODULE_4__.NAVAL_BATTLE_SHIPS.map(ship => ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: `ship-card ${selectedShip === ship.id ? 'selected' : ''}`, onClick: () => this.SendAction('naval.selectShip', { shipId: ship.id }), children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"ship-title\", children: this.LangString(ship.name) }), ['hp', 'damage', 'speed'].map(stat => ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"stat-row\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { children: this.LangString(`NavalBattleStat${stat.charAt(0).toUpperCase() + stat.slice(1)}`) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"bar\", children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { style: {\n                                                width: `${Math.max(12, this.GetShipStatPercent(stat, ship[stat]))}%`,\n                                            } }) })] }, stat)))] }, ship.id))) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"select-bottom\", children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_14__[\"default\"], { appearance: \"primary\", color: \"green\", disabled: me.data.shipConfirmed, onClick: () => this.SendAction('naval.confirmShip'), children: me.data.shipConfirmed\n                            ? this.LangString('NavalBattleShipConfirmed')\n                            : this.LangString('NavalBattleConfirmShip') }) })] }));\n    }\n    RenderWeaponCooldown(weapon) {\n        const me = this.GetMe();\n        if (!me)\n            return (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.Fragment, {});\n        const remaining = this.CooldownRemaining(me.data, weapon);\n        const ratio = 1 - remaining / _shared_games_params_navalbattle__WEBPACK_IMPORTED_MODULE_4__.NAVAL_BATTLE_WEAPON_COOLDOWN_MS;\n        return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"weapon-cd\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"label\", children: this.WeaponLabel(weapon) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"bar\", children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { style: { width: `${Math.max(0, Math.min(100, ratio * 100))}%` } }) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"value\", children: remaining > 0 ? `${(remaining / 1000).toFixed(1)}s` : 'READY' })] }, weapon));\n    }\n    RenderCombatHud() {\n        const me = this.GetMe();\n        if (!me)\n            return (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.Fragment, {});\n        const alive = this.state.players.filter(p => !p.data.eliminated).length;\n        const invulRemain = Math.max(0, Math.ceil((me.data.invulnerableUntilMS - _shared_system__WEBPACK_IMPORTED_MODULE_5__.System.timestampMS) / 1000));\n        return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"naval-hud\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"info-row\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { children: this.LangString('NavalBattleLives', me.data.lives) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { children: this.LangString('NavalBattleAlivePlayers', alive) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { children: this.LangString('NavalBattleKills', me.data.kills) })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"info-row\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { children: [this.LangString('NavalBattleHp'), \": \", Math.round(me.data.hp), \"/\", Math.round(me.data.maxHp)] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { children: [this.LangString('NavalBattleShield'), \": \", Math.round(me.data.shield), \"/\", Math.round(me.data.shieldMax)] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { children: invulRemain > 0\n                                ? this.LangString('NavalBattleInvulnerable', invulRemain)\n                                : this.LangString('NavalBattleControlsHint') })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"weapons-row\", children: [this.RenderWeaponCooldown(\"left\" /* NavalBattleWeaponType.LEFT */), this.RenderWeaponCooldown(\"front\" /* NavalBattleWeaponType.FRONT */), this.RenderWeaponCooldown(\"right\" /* NavalBattleWeaponType.RIGHT */)] }), CAMERA_MODE === 'orbit' && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"info-row\", style: { fontSize: 11, opacity: 0.6 }, children: this.LangString('NavalBattleCameraHint') }))] }));\n    }\n    RenderActiveModifiersOverlay() {\n        const me = this.GetMe();\n        if (!me)\n            return (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.Fragment, {});\n        const now = _shared_system__WEBPACK_IMPORTED_MODULE_5__.System.timestampMS;\n        const active = [];\n        if (me.data.stealthUntilMS > now)\n            active.push({\n                label: this.LangString('NavalBattleBuffStealth'),\n                remainMS: me.data.stealthUntilMS - now,\n            });\n        if (me.data.damageBoostUntilMS > now)\n            active.push({\n                label: this.LangString('NavalBattleBuffDamage'),\n                remainMS: me.data.damageBoostUntilMS - now,\n            });\n        if (me.data.speedBoostUntilMS > now)\n            active.push({\n                label: this.LangString('NavalBattleBuffSpeed'),\n                remainMS: me.data.speedBoostUntilMS - now,\n            });\n        if (!active.length)\n            return (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.Fragment, {});\n        return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"naval-active-mods\", \"aria-live\": \"polite\", children: active.map(item => ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"naval-active-mod\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { className: \"name\", children: item.label }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"span\", { className: \"time\", children: [Math.ceil(item.remainMS / 1000), \"s\"] })] }, item.label))) }));\n    }\n    // ════════════════════════════════════════════════════════════════════════════\n    //  Публичный API (жизненный цикл)\n    // ════════════════════════════════════════════════════════════════════════════\n    MainScreen() {\n        if (!this.state.inLobby)\n            return (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.Fragment, {});\n        return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"naval-battle-main\", ref: this.boardRef, children: [this.state.stage === 'waiting' && this.RenderWaiting(), this.IsInSelection() && this.RenderShipSelect(), this.IsInCombat() && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.Fragment, { children: [this.RenderCombatHud(), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"naval-canvas-wrap\", style: { position: 'relative' }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"canvas\", { ref: this.canvasRef, className: \"naval-canvas\" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { ref: this.offscreenLayerRef, style: {\n                                        position: 'absolute',\n                                        top: 0,\n                                        left: 0,\n                                        width: '100%',\n                                        height: '100%',\n                                        pointerEvents: 'none',\n                                        overflow: 'hidden',\n                                    } }), this.RenderActiveModifiersOverlay()] })] }))] }));\n    }\n    SettingsScreen() {\n        if (!this.isHost()) {\n            return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"naval-settings-readonly\", children: [this.LangString('NavalBattleLives', this.state.gameData.livesPerPlayer), ' ', \"|\", ' ', this.LangString(this.state.gameData.allowModifiers\n                        ? 'NavalBattleModifiersEnabled'\n                        : 'NavalBattleModifiersDisabled')] }));\n        }\n        return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.Fragment, { children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_15__[\"default\"], { children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_16__[\"default\"], { sm: 24, children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_17__[\"default\"], { size: \"lg\", value: this.state.gameData.livesPerPlayer, min: 1, max: 10, step: 1, onChange: value => {\n                                let lives = parseInt(String(value || 0), 10);\n                                if (!Number.isFinite(lives))\n                                    lives = 3;\n                                lives = Math.max(1, Math.min(10, lives));\n                                this.SendAction('set.livesPerPlayer', lives);\n                            }, formatter: value => `${this.LangString('NavalBattleLivesSetting')}: ${value}` }) }) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_15__[\"default\"], { style: { marginTop: 10 }, children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_16__[\"default\"], { sm: 24, children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"naval-toggle-row\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { children: this.LangString('NavalBattleModifiersSetting') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_18__[\"default\"], { checked: this.state.gameData.allowModifiers, onChange: checked => this.SendAction('set.allowModifiers', { enabled: checked }) })] }) }) })] }));\n    }\n    OnUpdateGameData() {\n        var _a;\n        const seen = new Set();\n        for (const player of this.state.players) {\n            seen.add(player.id);\n            const hiddenSentinel = player.data.x < -90000 || player.data.y < -90000;\n            const isVisibleToMe = player.id === _api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id || (player.data.visible && !hiddenSentinel);\n            const wasVisible = (_a = this.prevVisibleMap.get(player.id)) !== null && _a !== void 0 ? _a : isVisibleToMe;\n            if (!this.renderStates.has(player.id) && isVisibleToMe) {\n                this.renderStates.set(player.id, {\n                    x: player.data.x,\n                    y: player.data.y,\n                    angle: player.data.angle,\n                    updatedAtMS: _shared_system__WEBPACK_IMPORTED_MODULE_5__.System.timestampMS,\n                });\n            }\n            // Выход из невидимости — снапаем позицию\n            if (isVisibleToMe && !wasVisible) {\n                this.renderStates.set(player.id, {\n                    x: player.data.x,\n                    y: player.data.y,\n                    angle: player.data.angle,\n                    updatedAtMS: _shared_system__WEBPACK_IMPORTED_MODULE_5__.System.timestampMS,\n                });\n            }\n            this.prevVisibleMap.set(player.id, isVisibleToMe);\n        }\n        // HP delta → частицы + цифры\n        for (const player of this.state.players) {\n            const prevHp = this.prevHpMap.get(player.id);\n            const currHp = player.data.hp;\n            if (prevHp !== undefined && currHp < prevHp && currHp > 0) {\n                const state = this.renderStates.get(player.id);\n                if (state) {\n                    const ownHit = player.id === _api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id;\n                    this.SpawnHitParticles3D(state.x, state.y, this.GetPlayerColor(player.id), ownHit);\n                    this.SpawnDamageText3D(state.x, state.y, prevHp - currHp, ownHit);\n                }\n            }\n            if (prevHp !== undefined && prevHp > 0 && currHp <= 0) {\n                const state = this.renderStates.get(player.id);\n                if (state)\n                    this.SpawnWreck3D(state.x, state.y, state.angle, player.data.shipId);\n            }\n            this.prevHpMap.set(player.id, currHp);\n        }\n        // Cooldown delta → выстрелы других игроков\n        for (const player of this.state.players) {\n            const prevCd = this.prevCooldownMap.get(player.id);\n            const currCdLeft = player.data.cooldownLeftEndsAtMS;\n            const currCdRight = player.data.cooldownRightEndsAtMS;\n            const currCdFront = player.data.cooldownFrontEndsAtMS;\n            const nowMS = _shared_system__WEBPACK_IMPORTED_MODULE_5__.System.timestampMS;\n            if (prevCd && player.id !== _api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id) {\n                const state = this.renderStates.get(player.id);\n                if (state && player.data.hp > 0) {\n                    const ship = (0,_shared_games_params_navalbattle__WEBPACK_IMPORTED_MODULE_4__.GetNavalBattleShip)(player.data.shipId);\n                    const cos = Math.cos(state.angle);\n                    const sin = Math.sin(state.angle);\n                    if (prevCd.left > nowMS && currCdLeft < prevCd.left - 100) {\n                        const dirX = sin;\n                        const dirZ = -cos;\n                        const offX = dirX * ship.width * 0.5;\n                        const offZ = dirZ * ship.width * 0.5;\n                        this.SpawnShotParticles3D(state.x + offX, state.y + offZ, dirX, dirZ, this.GetPlayerColor(player.id));\n                    }\n                    if (prevCd.right > nowMS && currCdRight < prevCd.right - 100) {\n                        const dirX = -sin;\n                        const dirZ = cos;\n                        const offX = dirX * ship.width * 0.5;\n                        const offZ = dirZ * ship.width * 0.5;\n                        this.SpawnShotParticles3D(state.x + offX, state.y + offZ, dirX, dirZ, this.GetPlayerColor(player.id));\n                    }\n                    if (prevCd.front > nowMS && currCdFront < prevCd.front - 100) {\n                        const offX = cos * ship.length * 0.5;\n                        const offZ = sin * ship.length * 0.5;\n                        this.SpawnShotParticles3D(state.x + offX, state.y + offZ, cos, sin, this.GetPlayerColor(player.id));\n                    }\n                }\n            }\n            this.prevCooldownMap.set(player.id, {\n                left: currCdLeft,\n                right: currCdRight,\n                front: currCdFront,\n            });\n        }\n        // Чистим устаревшие записи\n        [\n            this.renderStates,\n            this.prevHpMap,\n            this.prevVisibleMap,\n            this.prevCooldownMap,\n        ].forEach(map => {\n            map.forEach((_, id) => {\n                if (!seen.has(id))\n                    map.delete(id);\n            });\n        });\n    }\n    OnJoinLobby() {\n        this.prevHpMap.clear();\n        this.prevVisibleMap.clear();\n        this.prevCooldownMap.clear();\n        this.particles3D = [];\n        this.projectiles = [];\n        this.damageLabels = [];\n        this.wreck3Ds = [];\n        this.zoom = 1.0;\n        this.screenShake = 0;\n        this.cameraOffset = { x: 0, y: 0 };\n        this.ResetInputState();\n        this.SendInput(true);\n    }\n    OnLeaveLobby() {\n        this.activeHeldWeapon = '';\n        this.ResetInputState();\n        this.SendInput(true);\n    }\n    OnJoin() {\n        // Клавиатура\n        this.keyDownHandler = this.OnKeyDown;\n        this.keyUpHandler = this.OnKeyUp;\n        window.addEventListener('keydown', this.keyDownHandler);\n        window.addEventListener('keyup', this.keyUpHandler);\n        // Периодическая отправка инпута\n        if (this.inputTick)\n            clearInterval(this.inputTick);\n        this.inputTick = setInterval(() => {\n            if (this.state.inLobby)\n                this.SendInput(false);\n        }, 100);\n        // Three.js инициализируем после рендера React (чтобы canvas был в DOM)\n        requestAnimationFrame(() => {\n            this.InitThreeJS();\n        });\n        // RAF\n        if (this.frameReq)\n            cancelAnimationFrame(this.frameReq);\n        this.lastFrameMS = 0;\n        this.frameReq = requestAnimationFrame(this.TickFrame);\n    }\n    OnLeave() {\n        if (this.keyDownHandler)\n            window.removeEventListener('keydown', this.keyDownHandler);\n        if (this.keyUpHandler)\n            window.removeEventListener('keyup', this.keyUpHandler);\n        if (this.inputTick) {\n            clearInterval(this.inputTick);\n            this.inputTick = undefined;\n        }\n        if (this.frameReq) {\n            cancelAnimationFrame(this.frameReq);\n            this.frameReq = 0;\n        }\n        this.DestroyThreeJS();\n        this.renderStates.clear();\n        this.prevVisibleMap.clear();\n        this.prevCooldownMap.clear();\n        this.projectiles = [];\n        this.damageLabels = [];\n        this.wreck3Ds = [];\n        this.cameraOffset = { x: 0, y: 0 };\n        this.ResetInputState();\n    }\n}\n\n\n//# sourceURL=webpack:///./src/frontend/pages/games/multiplayer/NavalBattle.tsx?");

/***/ }),

/***/ "./src/shared/games_params/navalbattle.ts":
/*!************************************************!*\
  !*** ./src/shared/games_params/navalbattle.ts ***!
  \************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   GetNavalBattleShip: () => (/* binding */ GetNavalBattleShip),\n/* harmony export */   NAVAL_BATTLE_DAMAGE_X2_MS: () => (/* binding */ NAVAL_BATTLE_DAMAGE_X2_MS),\n/* harmony export */   NAVAL_BATTLE_MAP_HEIGHT: () => (/* binding */ NAVAL_BATTLE_MAP_HEIGHT),\n/* harmony export */   NAVAL_BATTLE_MAP_WIDTH: () => (/* binding */ NAVAL_BATTLE_MAP_WIDTH),\n/* harmony export */   NAVAL_BATTLE_MODIFIER_SPAWN_MS: () => (/* binding */ NAVAL_BATTLE_MODIFIER_SPAWN_MS),\n/* harmony export */   NAVAL_BATTLE_MODIFIER_TTL_MS: () => (/* binding */ NAVAL_BATTLE_MODIFIER_TTL_MS),\n/* harmony export */   NAVAL_BATTLE_RESPAWN_MS: () => (/* binding */ NAVAL_BATTLE_RESPAWN_MS),\n/* harmony export */   NAVAL_BATTLE_SELECTION_SECONDS: () => (/* binding */ NAVAL_BATTLE_SELECTION_SECONDS),\n/* harmony export */   NAVAL_BATTLE_SHIPS: () => (/* binding */ NAVAL_BATTLE_SHIPS),\n/* harmony export */   NAVAL_BATTLE_SHOT_FORWARD_RANGE: () => (/* binding */ NAVAL_BATTLE_SHOT_FORWARD_RANGE),\n/* harmony export */   NAVAL_BATTLE_SHOT_SIDE_RANGE: () => (/* binding */ NAVAL_BATTLE_SHOT_SIDE_RANGE),\n/* harmony export */   NAVAL_BATTLE_SPAWN_PROTECTION_MS: () => (/* binding */ NAVAL_BATTLE_SPAWN_PROTECTION_MS),\n/* harmony export */   NAVAL_BATTLE_SPEED_X2_MS: () => (/* binding */ NAVAL_BATTLE_SPEED_X2_MS),\n/* harmony export */   NAVAL_BATTLE_STEALTH_MS: () => (/* binding */ NAVAL_BATTLE_STEALTH_MS),\n/* harmony export */   NAVAL_BATTLE_SYNC_INTERVAL_MS: () => (/* binding */ NAVAL_BATTLE_SYNC_INTERVAL_MS),\n/* harmony export */   NAVAL_BATTLE_TICK_MS: () => (/* binding */ NAVAL_BATTLE_TICK_MS),\n/* harmony export */   NAVAL_BATTLE_WEAPON_COOLDOWN_MS: () => (/* binding */ NAVAL_BATTLE_WEAPON_COOLDOWN_MS),\n/* harmony export */   NavalBattleGameData: () => (/* binding */ NavalBattleGameData),\n/* harmony export */   NavalBattleModifierTypes: () => (/* binding */ NavalBattleModifierTypes),\n/* harmony export */   NavalBattlePlayerData: () => (/* binding */ NavalBattlePlayerData),\n/* harmony export */   NavalBattleWeaponTypes: () => (/* binding */ NavalBattleWeaponTypes)\n/* harmony export */ });\nconst NavalBattleWeaponTypes = [\n    \"left\" /* NavalBattleWeaponType.LEFT */,\n    \"front\" /* NavalBattleWeaponType.FRONT */,\n    \"right\" /* NavalBattleWeaponType.RIGHT */,\n];\nconst NAVAL_BATTLE_WEAPON_COOLDOWN_MS = 5000;\nconst NAVAL_BATTLE_SHIPS = [\n    {\n        id: \"small\" /* NavalBattleShipClass.SMALL */,\n        name: 'NavalBattleShipSmall',\n        hp: 90,\n        speed: 280,\n        turnSpeed: 2.6,\n        damage: 16,\n        width: 34,\n        length: 64,\n        splashForwardWidth: 24,\n        splashSideWidth: 34,\n        imagePath: '',\n    },\n    {\n        id: \"medium\" /* NavalBattleShipClass.MEDIUM */,\n        name: 'NavalBattleShipMedium',\n        hp: 130,\n        speed: 220,\n        turnSpeed: 2.1,\n        damage: 23,\n        width: 42,\n        length: 78,\n        splashForwardWidth: 30,\n        splashSideWidth: 42,\n        imagePath: '',\n    },\n    {\n        id: \"large\" /* NavalBattleShipClass.LARGE */,\n        name: 'NavalBattleShipLarge',\n        hp: 190,\n        speed: 155,\n        turnSpeed: 1.55,\n        damage: 31,\n        width: 54,\n        length: 98,\n        splashForwardWidth: 36,\n        splashSideWidth: 54,\n        imagePath: '',\n    },\n];\nconst NAVAL_BATTLE_SELECTION_SECONDS = 15;\nconst NAVAL_BATTLE_RESPAWN_MS = 2600;\nconst NAVAL_BATTLE_SPAWN_PROTECTION_MS = 3200;\nconst NAVAL_BATTLE_SYNC_INTERVAL_MS = 80;\nconst NAVAL_BATTLE_TICK_MS = 50;\nconst NAVAL_BATTLE_MAP_WIDTH = 3000;\nconst NAVAL_BATTLE_MAP_HEIGHT = 1900;\nconst NAVAL_BATTLE_SHOT_FORWARD_RANGE = 360;\nconst NAVAL_BATTLE_SHOT_SIDE_RANGE = 250;\nconst NavalBattleModifierTypes = [\n    \"stealth\" /* NavalBattleModifierType.STEALTH */,\n    \"damage_x2\" /* NavalBattleModifierType.DAMAGE_X2 */,\n    \"shield\" /* NavalBattleModifierType.SHIELD */,\n    \"repair\" /* NavalBattleModifierType.REPAIR */,\n    \"speed_x2\" /* NavalBattleModifierType.SPEED_X2 */,\n];\nconst NAVAL_BATTLE_MODIFIER_SPAWN_MS = 8500;\nconst NAVAL_BATTLE_MODIFIER_TTL_MS = 20000;\nconst NAVAL_BATTLE_STEALTH_MS = 20000;\nconst NAVAL_BATTLE_DAMAGE_X2_MS = 30000;\nconst NAVAL_BATTLE_SPEED_X2_MS = 20000;\nconst NavalBattlePlayerData = {\n    shipId: \"medium\" /* NavalBattleShipClass.MEDIUM */,\n    shipConfirmed: false,\n    eliminated: false,\n    lives: 3,\n    kills: 0,\n    deaths: 0,\n    x: 0,\n    y: 0,\n    angle: 0,\n    speedForward: 0,\n    turnInput: 0,\n    hp: 130,\n    maxHp: 130,\n    shield: 0,\n    shieldMax: 13,\n    pressingW: false,\n    pressingS: false,\n    pressingA: false,\n    pressingD: false,\n    aimingWeapon: '',\n    cooldownLeftEndsAtMS: 0,\n    cooldownRightEndsAtMS: 0,\n    cooldownFrontEndsAtMS: 0,\n    invulnerableUntilMS: 0,\n    respawnAtMS: 0,\n    stealthUntilMS: 0,\n    damageBoostUntilMS: 0,\n    speedBoostUntilMS: 0,\n    visible: true,\n    lastInputAtMS: 0,\n};\nconst NavalBattleGameData = {\n    phase: \"ship_select\" /* NavalBattlePhase.SHIP_SELECT */,\n    phaseEndsAtMS: 0,\n    mapWidth: NAVAL_BATTLE_MAP_WIDTH,\n    mapHeight: NAVAL_BATTLE_MAP_HEIGHT,\n    syncIntervalMS: NAVAL_BATTLE_SYNC_INTERVAL_MS,\n    livesPerPlayer: 3,\n    allowModifiers: true,\n    modifiers: [],\n    serverTick: 0,\n    startedAtMS: 0,\n};\nconst GetNavalBattleShip = (id) => {\n    return NAVAL_BATTLE_SHIPS.find(ship => ship.id === id) || NAVAL_BATTLE_SHIPS[1];\n};\n\n\n//# sourceURL=webpack:///./src/shared/games_params/navalbattle.ts?");

/***/ })

}]);