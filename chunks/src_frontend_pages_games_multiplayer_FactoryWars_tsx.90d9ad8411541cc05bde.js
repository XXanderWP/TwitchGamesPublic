"use strict";(self.webpackChunk=self.webpackChunk||[]).push([["src_frontend_pages_games_multiplayer_FactoryWars_tsx"],{"./src/frontend/styles/factorywars.less":()=>{eval("// extracted by mini-css-extract-plugin\n\n\n//# sourceURL=webpack:///./src/frontend/styles/factorywars.less?")},"./src/frontend/pages/games/multiplayer/FactoryWars.tsx":(__unused_webpack_module,__webpack_exports__,__webpack_require__)=>{eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   FactoryWarsController: () => (/* binding */ FactoryWarsController)\n/* harmony export */ });\n/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react/jsx-runtime */ \"./node_modules/react/jsx-runtime.js\");\n/* harmony import */ var _Controller__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./Controller */ \"./src/frontend/pages/games/multiplayer/Controller.tsx\");\n/* harmony import */ var _api__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../api */ \"./src/frontend/api.tsx\");\n/* harmony import */ var rsuite__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! rsuite */ \"./node_modules/rsuite/esm/Button/index.js\");\n/* harmony import */ var rsuite__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! rsuite */ \"./node_modules/rsuite/esm/Progress/index.js\");\n/* harmony import */ var rsuite__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! rsuite */ \"./node_modules/rsuite/esm/InputNumber/index.js\");\n/* harmony import */ var rsuite__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! rsuite */ \"./node_modules/rsuite/esm/SelectPicker/index.js\");\n/* harmony import */ var rsuite__WEBPACK_IMPORTED_MODULE_17__ = __webpack_require__(/*! rsuite */ \"./node_modules/rsuite/esm/Slider/index.js\");\n/* harmony import */ var rsuite__WEBPACK_IMPORTED_MODULE_18__ = __webpack_require__(/*! rsuite */ \"./node_modules/rsuite/esm/Row/index.js\");\n/* harmony import */ var rsuite__WEBPACK_IMPORTED_MODULE_19__ = __webpack_require__(/*! rsuite */ \"./node_modules/rsuite/esm/Col/index.js\");\n/* harmony import */ var _styles_factorywars_less__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../../../styles/factorywars.less */ \"./src/frontend/styles/factorywars.less\");\n/* harmony import */ var _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../../../../shared/games_params/factory */ \"./src/shared/games_params/factory.ts\");\n/* harmony import */ var three__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! three */ \"./node_modules/three/build/three.core.js\");\n/* harmony import */ var three__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! three */ \"./node_modules/three/build/three.module.js\");\n/* harmony import */ var three_examples_jsm_loaders_FBXLoader_js__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! three/examples/jsm/loaders/FBXLoader.js */ \"./node_modules/three/examples/jsm/loaders/FBXLoader.js\");\n/* harmony import */ var three_examples_jsm_utils_SkeletonUtils_js__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! three/examples/jsm/utils/SkeletonUtils.js */ \"./node_modules/three/examples/jsm/utils/SkeletonUtils.js\");\n/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! react */ \"./node_modules/react/index.js\");\n/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_5__);\n/* harmony import */ var _shared_system__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../../../../shared/system */ \"./src/shared/system.ts\");\n/* harmony import */ var _controllers_state__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ../../../controllers/state */ \"./src/frontend/controllers/state.ts\");\n/* harmony import */ var _components_tooltip__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ../../components/tooltip */ \"./src/frontend/pages/components/tooltip.tsx\");\nvar __awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {\n    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }\n    return new (P || (P = Promise))(function (resolve, reject) {\n        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }\n        function rejected(value) { try { step(generator[\"throw\"](value)); } catch (e) { reject(e); } }\n        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }\n        step((generator = generator.apply(thisArg, _arguments || [])).next());\n    });\n};\n\n\n\n\n\n\n\n\n\n\n\n\n\nclass FactoryWarsController extends _Controller__WEBPACK_IMPORTED_MODULE_1__.MultiplayerGameController {\n    get minersIds() {\n        const res = [];\n        _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.minersData.forEach(data => {\n            res.push(...data.ids);\n        });\n        return res\n            .filter(q => !!q)\n            .filter((id, index, self) => self.indexOf(id) === index);\n    }\n    get openedInterfacesBlock() {\n        return this.openedInterfaces.find(q => q !== 'player');\n    }\n    get gameMode() {\n        var _a, _b;\n        return (_b = (_a = this.state) === null || _a === void 0 ? void 0 : _a.gameData) === null || _b === void 0 ? void 0 : _b.gamemode;\n    }\n    get pveState() {\n        var _a, _b;\n        return (_b = (_a = this.state) === null || _a === void 0 ? void 0 : _a.gameData) === null || _b === void 0 ? void 0 : _b.pve;\n    }\n    LocalizeConfigName(name, nameLang) {\n        if (nameLang) {\n            const localized = this.LangString(nameLang);\n            if (localized)\n                return localized;\n        }\n        return name || '';\n    }\n    GetTeamDisplayName(teamId) {\n        var _a, _b;\n        const teamOrder = this.GetTeamOrder(teamId);\n        if (teamOrder < 0) {\n            return this.LangString('FactoryWarsTeamNameFallback', teamId + 1);\n        }\n        const teams = ((_b = (_a = this.config) === null || _a === void 0 ? void 0 : _a.teamsConfig) === null || _b === void 0 ? void 0 : _b.teams) || [];\n        if (!teams.length) {\n            return this.LangString('FactoryWarsTeamNameFallback', teamId + 1);\n        }\n        const cfg = teams[teamOrder % teams.length];\n        return this.LocalizeConfigName(cfg === null || cfg === void 0 ? void 0 : cfg.name, cfg === null || cfg === void 0 ? void 0 : cfg.name);\n    }\n    GetTeamColorByOrder(teamOrder) {\n        var _a, _b, _c;\n        const teams = ((_b = (_a = this.config) === null || _a === void 0 ? void 0 : _a.teamsConfig) === null || _b === void 0 ? void 0 : _b.teams) || [];\n        if (!teams.length || teamOrder < 0)\n            return '#ffffff';\n        return ((_c = teams[teamOrder % teams.length]) === null || _c === void 0 ? void 0 : _c.color) || '#ffffff';\n    }\n    GetTeamColorById(teamId) {\n        const teamOrder = this.GetTeamOrder(teamId);\n        return this.GetTeamColorByOrder(teamOrder >= 0 ? teamOrder : teamId);\n    }\n    GetPveWaveStatusText(now = Date.now()) {\n        if (this.gameMode !== 'pve')\n            return undefined;\n        const pve = this.pveState;\n        if (!pve)\n            return undefined;\n        const directionLabel = pve.direction\n            ? this.LocalizeConfigName(pve.direction, _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsPveDirectionLangKeys[pve.direction])\n            : this.LangString('FactoryWarsPveDirectionUnknown');\n        if (pve.status === 'wave') {\n            this.frozenPveCountdownSeconds = undefined;\n            return this.LangString('FactoryWarsPveWaveNow', Math.max(0, pve.aliveAutomatons), Math.max(0, pve.allocatedAutomatons));\n        }\n        const shouldFreezeCountdown = this.state.stage !== 'ingame' ||\n            this.state.gamePaused ||\n            this.state.pauseResumeSeconds > 0;\n        if (shouldFreezeCountdown) {\n            if (this.state.stage !== 'ingame') {\n                this.frozenPveCountdownSeconds =\n                    _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsPveConfig.firstWaveDelaySeconds;\n            }\n            else if (this.frozenPveCountdownSeconds === undefined) {\n                this.frozenPveCountdownSeconds = Math.max(0, Math.ceil((Number(pve.countdownEndsAt || 0) - now) / 1000));\n            }\n            return this.LangString('FactoryWarsPveWaveIn', _shared_system__WEBPACK_IMPORTED_MODULE_6__.System.secondsToString(this.frozenPveCountdownSeconds || 0), directionLabel);\n        }\n        this.frozenPveCountdownSeconds = undefined;\n        const secondsLeft = Math.max(0, Math.ceil((Number(pve.countdownEndsAt || 0) - now) / 1000));\n        return this.LangString('FactoryWarsPveWaveIn', _shared_system__WEBPACK_IMPORTED_MODULE_6__.System.secondsToString(secondsLeft), directionLabel);\n    }\n    GetPveVoteState() {\n        var _a, _b;\n        const votedPlayerIds = Array.from(new Set(((_a = this.pveState) === null || _a === void 0 ? void 0 : _a.votedPlayerIds) || []));\n        const totalPlayers = this.state.players.filter(player => player.online).length;\n        return {\n            votes: votedPlayerIds.length,\n            totalPlayers,\n            hasVoted: votedPlayerIds.includes(_api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id),\n            canVote: this.gameMode === 'pve' && ((_b = this.pveState) === null || _b === void 0 ? void 0 : _b.status) === 'countdown',\n        };\n    }\n    NewBuilds(...builds) {\n        const allowed = builds.filter(build => this.CanPlaceBuild(build));\n        if (allowed.length === 0)\n            return;\n        this.SendAction('builds.new', allowed);\n    }\n    DestroyBuilds(...uids) {\n        this.SendAction('builds.destroy', uids);\n    }\n    IsConveyorGhostSelected() {\n        return !!this.ghostObject && this.ghostBuildId === 'conveyor';\n    }\n    IsUndergroundConveyorGhostSelected() {\n        return !!this.ghostObject && this.ghostBuildId === 'underground_conveyor';\n    }\n    RotationByDirection(dx, dz) {\n        if (dz > 0)\n            return 0;\n        if (dz < 0)\n            return -2;\n        if (dx < 0)\n            return -1;\n        if (dx > 0)\n            return -3;\n        return this.ghostObjectRotation;\n    }\n    NormalizeRotation(rotation) {\n        const normalized = Math.round(Number(rotation) || 0) % 4;\n        return normalized < 0 ? normalized + 4 : normalized;\n    }\n    GetOppositeRotation(rotation) {\n        const normalized = this.NormalizeRotation(rotation);\n        const opposite = (normalized + 2) % 4;\n        if (opposite === 0)\n            return 0;\n        if (opposite === 1)\n            return -1;\n        if (opposite === 2)\n            return -2;\n        return -3;\n    }\n    ToggleFullScreen() {\n        if (!document.fullscreenElement) {\n            const el = document.getElementById('factorywars-main');\n            el === null || el === void 0 ? void 0 : el.requestFullscreen();\n        }\n        else {\n            document.exitFullscreen();\n        }\n        this.forceUpdate();\n    }\n    GetUndergroundExitForEntry(entry) {\n        if (entry.id !== 'underground_conveyor')\n            return undefined;\n        const nextCell = (0,_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsFaceCell)(entry.pos, entry.rotation);\n        const dirX = nextCell.x - entry.pos.x;\n        const dirZ = nextCell.z - entry.pos.z;\n        if (dirX === 0 && dirZ === 0)\n            return undefined;\n        const expectedExitRotation = entry.rotation;\n        const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(build => build.id === entry.id);\n        const maxSteps = Math.max(1, Math.floor(Number(cfg === null || cfg === void 0 ? void 0 : cfg.maxCells) ||\n            Math.max(this.state.gameData.mapSize.x, this.state.gameData.mapSize.z)));\n        for (let step = 1; step <= maxSteps; step++) {\n            const cell = {\n                x: entry.pos.x + dirX * step,\n                z: entry.pos.z + dirZ * step,\n            };\n            if (!(0,_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsIsPointInsideMap)(cell, this.state.gameData.mapSize)) {\n                break;\n            }\n            const candidate = this.state.gameData.buildings.find(build => build.uid !== entry.uid &&\n                build.id === 'underground_conveyor' &&\n                build.team === entry.team &&\n                build.pos.x === cell.x &&\n                build.pos.z === cell.z &&\n                build.rotation === expectedExitRotation);\n            if (candidate)\n                return candidate;\n        }\n        return undefined;\n    }\n    GetUndergroundConnection(build) {\n        if (build.id !== 'underground_conveyor')\n            return undefined;\n        const directExit = this.GetUndergroundExitForEntry(build);\n        if (directExit) {\n            return { entry: build, exit: directExit };\n        }\n        const linkedEntry = this.state.gameData.buildings.find(candidate => {\n            if (candidate.uid === build.uid ||\n                candidate.id !== 'underground_conveyor' ||\n                candidate.team !== build.team)\n                return false;\n            const candidateExit = this.GetUndergroundExitForEntry(candidate);\n            return (candidateExit === null || candidateExit === void 0 ? void 0 : candidateExit.uid) === build.uid;\n        });\n        if (!linkedEntry)\n            return undefined;\n        return { entry: linkedEntry, exit: build };\n    }\n    ConveyorRotationByDirection(dx, dz) {\n        return this.RotationByDirection(dx, dz);\n    }\n    ConveyorDragBuildKey(cell) {\n        return `${cell.x}:${cell.z}`;\n    }\n    MarkObjectAsGhost(object, opacity = 0.5) {\n        object.traverse((obj) => {\n            if (obj.isMesh && obj.material) {\n                const mats = Array.isArray(obj.material)\n                    ? obj.material\n                    : [obj.material];\n                mats.forEach(m => {\n                    var _a;\n                    m.transparent = true;\n                    m.opacity = opacity;\n                    m.depthWrite = false;\n                    if (m instanceof three__WEBPACK_IMPORTED_MODULE_9__.ShaderMaterial && ((_a = m.uniforms) === null || _a === void 0 ? void 0 : _a.uOpacity)) {\n                        m.uniforms.uOpacity.value = opacity;\n                        m.uniformsNeedUpdate = true;\n                    }\n                });\n            }\n        });\n    }\n    UnmarkObjectAsGhost(object) {\n        object.traverse((obj) => {\n            if (obj.isMesh && obj.material) {\n                const mats = Array.isArray(obj.material)\n                    ? obj.material\n                    : [obj.material];\n                mats.forEach(m => {\n                    var _a;\n                    m.transparent = false;\n                    m.opacity = 1;\n                    m.depthWrite = true;\n                    if (m instanceof three__WEBPACK_IMPORTED_MODULE_9__.ShaderMaterial && ((_a = m.uniforms) === null || _a === void 0 ? void 0 : _a.uOpacity)) {\n                        m.uniforms.uOpacity.value = 1;\n                        m.uniformsNeedUpdate = true;\n                    }\n                });\n            }\n        });\n    }\n    SyncConveyorDragGhosts() {\n        if (!this.scene)\n            return;\n        const shouldExist = new Set(this.conveyorDragBuilds.map(build => this.ConveyorDragBuildKey(build.position)));\n        this.conveyorDragGhostObjects.forEach((ghost, key) => {\n            if (shouldExist.has(key))\n                return;\n            this.DisposeObject(ghost);\n            this.conveyorDragGhostObjects.delete(key);\n        });\n        this.conveyorDragBuilds.forEach(build => {\n            const key = this.ConveyorDragBuildKey(build.position);\n            let ghost = this.conveyorDragGhostObjects.get(key);\n            if (!ghost) {\n                ghost = this.GetModel(build.id);\n                if (!ghost)\n                    return;\n                this.MarkObjectAsGhost(ghost, 0.75);\n                this.scene.add(ghost);\n                this.conveyorDragGhostObjects.set(key, ghost);\n            }\n            const pos = this.SnapToGrid({\n                x: build.position.x * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize,\n                z: build.position.z * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize,\n            });\n            ghost.visible = this.cursorInsideGame;\n            ghost.position.set(pos.x, 0, pos.z);\n            ghost.rotation.y = build.rotation * (Math.PI / 2);\n            ghost.scale.set(1, 1, 1);\n        });\n    }\n    ClearConveyorDragGhosts() {\n        this.conveyorDragGhostObjects.forEach(ghost => {\n            this.DisposeObject(ghost);\n        });\n        this.conveyorDragGhostObjects.clear();\n    }\n    BeginConveyorDragBuild(startCell) {\n        this.conveyorDragBuildActive = true;\n        this.conveyorDragLastCell = Object.assign({}, startCell);\n        this.conveyorDragBuilds = [\n            {\n                id: this.ghostBuildId,\n                position: Object.assign({}, startCell),\n                rotation: this.ghostObjectRotation,\n            },\n        ];\n        this.SyncConveyorDragGhosts();\n    }\n    ExtendConveyorDragBuild(targetCell) {\n        if (!this.conveyorDragBuildActive || !this.conveyorDragLastCell)\n            return;\n        let current = Object.assign({}, this.conveyorDragLastCell);\n        while (current.x !== targetCell.x || current.z !== targetCell.z) {\n            const dxRemain = targetCell.x - current.x;\n            const dzRemain = targetCell.z - current.z;\n            const stepX = Math.abs(dxRemain) >= Math.abs(dzRemain) && dxRemain !== 0\n                ? Math.sign(dxRemain)\n                : 0;\n            const stepZ = stepX === 0 && dzRemain !== 0 ? Math.sign(dzRemain) : 0;\n            if (stepX === 0 && stepZ === 0)\n                break;\n            const nextCell = {\n                x: current.x + stepX,\n                z: current.z + stepZ,\n            };\n            const rotation = this.ConveyorRotationByDirection(stepX, stepZ);\n            const nextKey = this.ConveyorDragBuildKey(nextCell);\n            const existingIndex = this.conveyorDragBuilds.findIndex(b => this.ConveyorDragBuildKey(b.position) === nextKey);\n            if (existingIndex > -1) {\n                this.conveyorDragBuilds = this.conveyorDragBuilds.slice(0, existingIndex + 1);\n            }\n            else {\n                const lastBuild = this.conveyorDragBuilds[this.conveyorDragBuilds.length - 1];\n                if (lastBuild) {\n                    lastBuild.rotation = rotation;\n                }\n                this.conveyorDragBuilds.push({\n                    id: this.ghostBuildId,\n                    position: Object.assign({}, nextCell),\n                    rotation,\n                });\n            }\n            this.ghostObjectRotation = rotation;\n            if (this.ghostObject) {\n                this.ghostObject.rotation.y = rotation * (Math.PI / 2);\n            }\n            current = nextCell;\n        }\n        this.conveyorDragLastCell = Object.assign({}, current);\n        this.SyncConveyorDragGhosts();\n    }\n    FinishConveyorDragBuild() {\n        if (!this.conveyorDragBuildActive) {\n            this.conveyorDragCandidateStart = undefined;\n            return false;\n        }\n        this.conveyorDragBuildActive = false;\n        this.conveyorDragCandidateStart = undefined;\n        this.conveyorDragLastCell = undefined;\n        const builds = [...this.conveyorDragBuilds];\n        this.conveyorDragBuilds = [];\n        this.ClearConveyorDragGhosts();\n        if (builds.length === 0)\n            return false;\n        this.NewBuilds(...builds);\n        if (!this.keysPressed.has('ShiftLeft') &&\n            !this.keysPressed.has('ShiftRight')) {\n            this.CancelGhost();\n        }\n        return true;\n    }\n    CancelConveyorDragBuild() {\n        this.conveyorDragCandidateStart = undefined;\n        this.conveyorDragLastCell = undefined;\n        this.conveyorDragBuilds = [];\n        this.conveyorDragBuildActive = false;\n        this.ClearConveyorDragGhosts();\n    }\n    HandleCanvasMouseDown(e) {\n        if (this.graphicsMenuOpen)\n            return;\n        if (e.button === 2) {\n            this.UpdateHover({ x: e.clientX, y: e.clientY });\n            const build = this.GetBuildingInPoint(this.hoverCell);\n            if (build &&\n                build.team === this.myTeamId &&\n                this.IsBuildingUiAvailable(build)) {\n                this.OpenBuildingUi(build.uid);\n                this.CancelConveyorDragBuild();\n                return;\n            }\n            this.TryPickupDropByCursor({ x: e.clientX, y: e.clientY });\n            this.CancelConveyorDragBuild();\n            return;\n        }\n        this.UpdateHover({ x: e.clientX, y: e.clientY });\n        if (this.editBuildMode && !this.ghostObject && this.isValidPoint) {\n            this.editSelectionActive = true;\n            this.editBuildRange = [Object.assign({}, this.hoverCell), Object.assign({}, this.hoverCell)];\n            this.CancelConveyorDragBuild();\n            return;\n        }\n        // Start automaton drag-select when no ghost and no edit mode\n        if (!this.ghostObject && !this.editBuildMode && this.isValidPoint) {\n            this.automatonDragSelectActive = true;\n            this.automatonDragSelectStart = Object.assign({}, this.hoverCell);\n            this.editBuildRange = [Object.assign({}, this.hoverCell), Object.assign({}, this.hoverCell)];\n            this.automatonDragSelectScreenStart = { x: e.clientX, y: e.clientY };\n            this.automatonDragSelectScreenEnd = { x: e.clientX, y: e.clientY };\n            this.CancelConveyorDragBuild();\n            this.forceUpdate();\n            return;\n        }\n        if (!this.IsConveyorGhostSelected() ||\n            this.editBuildMode ||\n            !this.isValidPoint) {\n            this.CancelConveyorDragBuild();\n            return;\n        }\n        this.UpdateHover({ x: e.clientX, y: e.clientY });\n        this.conveyorDragCandidateStart = Object.assign({}, this.hoverCell);\n        this.conveyorDragLastCell = undefined;\n        this.conveyorDragBuilds = [];\n    }\n    HandleCanvasMouseMove(e) {\n        if (this.graphicsMenuOpen)\n            return;\n        this.hoverCursorPos = { x: e.clientX, y: e.clientY };\n        this.UpdateMinimapResourceHover(e.clientX, e.clientY);\n        // Automaton drag-select range update\n        if (this.automatonDragSelectActive && this.automatonDragSelectStart) {\n            this.UpdateHover({ x: e.clientX, y: e.clientY });\n            if (this.isValidPoint) {\n                this.editBuildRange[1] = Object.assign({}, this.hoverCell);\n                this.automatonDragSelectScreenEnd = { x: e.clientX, y: e.clientY };\n                this.forceUpdate();\n            }\n            return;\n        }\n        if (this.editSelectionActive && this.editBuildMode && !this.ghostObject) {\n            this.UpdateHover({ x: e.clientX, y: e.clientY });\n            if (!this.isValidPoint)\n                return;\n            this.editBuildRange[1] = Object.assign({}, this.hoverCell);\n            return;\n        }\n        if (!this.conveyorDragCandidateStart && !this.conveyorDragBuildActive)\n            return;\n        if (!this.IsConveyorGhostSelected() || this.editBuildMode)\n            return;\n        this.UpdateHover({ x: e.clientX, y: e.clientY });\n        if (!this.isValidPoint)\n            return;\n        const start = this.conveyorDragCandidateStart;\n        if (!this.conveyorDragBuildActive && start) {\n            if (start.x === this.hoverCell.x && start.z === this.hoverCell.z)\n                return;\n            this.BeginConveyorDragBuild(start);\n        }\n        this.ExtendConveyorDragBuild(this.hoverCell);\n    }\n    HandleCanvasMouseUp() {\n        if (this.graphicsMenuOpen)\n            return;\n        // Finish automaton drag-select\n        if (this.automatonDragSelectActive) {\n            this.automatonDragSelectActive = false;\n            const didDrag = this.automatonDragSelectStart &&\n                (this.editBuildRange[0].x !== this.editBuildRange[1].x ||\n                    this.editBuildRange[0].z !== this.editBuildRange[1].z);\n            this.automatonDragSelectStart = undefined;\n            this.forceUpdate();\n            if (didDrag) {\n                const range = this.NormalizeBuildRange(this.editBuildRange[0], this.editBuildRange[1]);\n                const minWx = range[0].x * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n                const maxWx = range[1].x * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n                const minWz = range[0].z * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n                const maxWz = range[1].z * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n                const found = this.state.gameData.automatons.filter(a => a.team === this.myTeamId &&\n                    a.position.x >= minWx &&\n                    a.position.x <= maxWx &&\n                    a.position.z >= minWz &&\n                    a.position.z <= maxWz);\n                if (found.length > 0) {\n                    this.selectedAutomatonUids = found.map(a => a.uid);\n                    this.suppressNextCanvasClick = true;\n                    return;\n                }\n                // No automatons found — suppress click to avoid accidental building\n                this.suppressNextCanvasClick = true;\n                return;\n            }\n            // Single click — fall through to normal click handling\n        }\n        const processedEdit = this.ClickUpGame();\n        const sentBatch = this.FinishConveyorDragBuild();\n        if (processedEdit || sentBatch) {\n            this.suppressNextCanvasClick = true;\n        }\n    }\n    HandleCanvasClick() {\n        if (this.graphicsMenuOpen)\n            return;\n        if (this.suppressNextCanvasClick) {\n            this.suppressNextCanvasClick = false;\n            return;\n        }\n        this.ClickInGame();\n    }\n    GetDropUidByCursor(cursor) {\n        var _a;\n        if (!this.renderer || !this.camera)\n            return undefined;\n        const canvas = this.renderer.domElement;\n        const rect = canvas.getBoundingClientRect();\n        const x = (cursor.x - rect.left) / rect.width;\n        const y = (cursor.y - rect.top) / rect.height;\n        if (x < 0 || x > 1 || y < 0 || y > 1)\n            return undefined;\n        this.mouse.set(x * 2 - 1, -y * 2 + 1);\n        this.raycaster.setFromCamera(this.mouse, this.camera);\n        const intersects = this.raycaster.intersectObjects([...this.dropItems.values()], true);\n        for (const hit of intersects) {\n            let target = hit.object;\n            while (target) {\n                const uid = (_a = target.userData) === null || _a === void 0 ? void 0 : _a.dropUid;\n                if (typeof uid === 'string') {\n                    return uid;\n                }\n                target = target.parent;\n            }\n        }\n        return undefined;\n    }\n    TryPickupDropByCursor(cursor) {\n        if (!this.VerifyCanControl())\n            return;\n        const dropUid = this.GetDropUidByCursor(cursor);\n        if (!dropUid)\n            return;\n        this.SendActionSocket('drop.pickup', { uid: dropUid });\n    }\n    TryPickupNearestByHold(now = Date.now()) {\n        if (!this.VerifyCanControl())\n            return;\n        if (!this.keysPressed.has('KeyV'))\n            return;\n        if (now - this.lastPickupNearestAt < _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.pickupIntervalMS)\n            return;\n        this.lastPickupNearestAt = now;\n        this.SendActionSocket('drop.pickupNearest');\n    }\n    UpdateHoveredDrop(cursor) {\n        this.hoveredDropUid = this.GetDropUidByCursor(cursor);\n    }\n    get isValidPoint() {\n        if (!this.hoverCell)\n            return false;\n        if (this.ghostBuildId && this.ghostSelectionBuilds.length === 0) {\n            return this.CanPlaceBuild({\n                id: this.ghostBuildId,\n                position: this.hoverCell,\n                rotation: this.ghostObjectRotation,\n            });\n        }\n        return this.IsCellInMyTeamZone(this.hoverCell);\n    }\n    get activeTeamIndexes() {\n        return Array.from(new Set(this.state.players\n            .map(player => Number(this.GetTeam(player.id)))\n            .filter(team => Number.isInteger(team) && team >= 0))).sort((a, b) => a - b);\n    }\n    get teamCount() {\n        return this.activeTeamIndexes.length;\n    }\n    get myTeamOrder() {\n        return this.activeTeamIndexes.indexOf(this.myTeamId);\n    }\n    GetTeamOrder(teamId) {\n        return this.activeTeamIndexes.indexOf(teamId);\n    }\n    IsCellInTeamZone(cell, teamOrder) {\n        if (teamOrder < 0 || this.teamCount <= 0)\n            return false;\n        if (!(0,_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsIsPointInsideMap)(cell, this.state.gameData.mapSize))\n            return false;\n        return ((0,_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsGetTeamZoneIndexForPoint)(cell, this.state.gameData.mapSize, this.teamCount) === teamOrder);\n    }\n    IsCellInMyTeamZone(cell) {\n        return this.IsCellInTeamZone(cell, this.myTeamOrder);\n    }\n    IsWorldPositionInTeamZone(position, teamOrder) {\n        const gamePos = this.ToGameCoords(position);\n        return this.IsCellInTeamZone(gamePos, teamOrder);\n    }\n    CanPlayerStandAtWorldPosition(playerId, position) {\n        var _a;\n        const teamId = (_a = this.GetTeam(playerId)) !== null && _a !== void 0 ? _a : -1;\n        const teamOrder = this.GetTeamOrder(teamId);\n        return (this.IsWorldPositionInTeamZone(position, teamOrder) &&\n            (0,_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsCanPlayerStandAt)(this.state.gameData, position));\n    }\n    get builds() {\n        return _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds;\n    }\n    get myTeamId() {\n        var _a;\n        return (_a = this.GetTeam(_api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id)) !== null && _a !== void 0 ? _a : -1;\n    }\n    get myTeamData() {\n        return this.state.gameData.teams[this.myTeamId];\n    }\n    GetTeamData(teamId) {\n        return this.state.gameData.teams[teamId];\n    }\n    GetTeamHub(teamId) {\n        const teamData = this.GetTeamData(teamId);\n        if (!teamData)\n            return undefined;\n        return this.state.gameData.buildings.find(b => b.team === teamId && b.id === 'hub');\n    }\n    GetTeamHp(teamId) {\n        const teamData = this.GetTeamData(teamId);\n        if (!teamData)\n            return {\n                current: 0,\n                max: 2000,\n            };\n        const hub = this.GetTeamHub(teamId);\n        if (hub) {\n            return {\n                current: hub.data.hp || 0,\n                max: hub.data.maxHp || 2000,\n            };\n        }\n        else {\n            return {\n                current: 0,\n                max: 2000,\n            };\n        }\n    }\n    get me() {\n        return this.state.players.find(p => p.id === _api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id);\n    }\n    get myInventory() {\n        var _a;\n        return ((_a = this.me) === null || _a === void 0 ? void 0 : _a.data.inventory) || {};\n    }\n    get openedBuilding() {\n        if (!this.openedBuildingUid)\n            return undefined;\n        return this.state.gameData.buildings.find(b => b.uid === this.openedBuildingUid);\n    }\n    get openedBuildingConfig() {\n        if (!this.openedBuilding)\n            return undefined;\n        return _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(b => b.id === this.openedBuilding.id);\n    }\n    IsBuildingUiAvailable(build) {\n        if (!build)\n            return false;\n        const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(q => q.id === build.id);\n        if (!cfg)\n            return false;\n        if (build.id === 'conveyor' || build.id === 'underground_conveyor')\n            return false;\n        if (build.id === 'laboratory')\n            return true;\n        return !!(cfg.recipes || cfg.input || cfg.output || cfg.progressTick > 0);\n    }\n    SyncOpenedInterfaces() {\n        const opened = [];\n        if (this.showPlayerUi)\n            opened.push('player');\n        if (this.openedBuildingUid)\n            opened.push('build');\n        this.openedInterfaces = opened;\n        if (this.openedInterfacesBlock) {\n            this.players_velocities.set(_api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id, {\n                x: 0,\n                z: 0,\n                shift: false,\n            });\n            this.VerifyLocalVelocityAndPos();\n        }\n    }\n    GetMyWorldPosition() {\n        var _a, _b;\n        const modelPos = (_a = this.players_models.get(_api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id)) === null || _a === void 0 ? void 0 : _a.position;\n        if (modelPos) {\n            return { x: modelPos.x, z: modelPos.z };\n        }\n        const syncedPos = this.players_positions.get(_api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id);\n        if (syncedPos)\n            return syncedPos;\n        if (this.state.stage !== 'ingame') {\n            const spawn = (_b = this.myTeamData) === null || _b === void 0 ? void 0 : _b.spawnPoint;\n            if (spawn) {\n                return {\n                    x: (spawn.x - 1) * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize,\n                    z: spawn.z * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize,\n                };\n            }\n        }\n        return undefined;\n    }\n    IsBuildingInPickupRange(build) {\n        if (!build)\n            return false;\n        const myPos = this.GetMyWorldPosition();\n        if (!myPos)\n            return false;\n        const interactionRange = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.pickupRange * 3;\n        const maxDistanceSq = interactionRange * interactionRange;\n        return this.GetBuildOccupiedCells(build).some(cell => {\n            const dx = cell.x * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize - myPos.x;\n            const dz = cell.z * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize - myPos.z;\n            return dx * dx + dz * dz <= maxDistanceSq;\n        });\n    }\n    CloseAllInterfaces(forceUpdate = true) {\n        this.openedBuildingUid = undefined;\n        this.showPlayerUi = false;\n        this.ResetInventoryDrag();\n        this.dragAmountModal = { open: false };\n        this.SyncOpenedInterfaces();\n        if (forceUpdate)\n            this.forceUpdate();\n    }\n    ValidateOpenedBuildingDistance() {\n        if (!this.openedBuildingUid)\n            return;\n        const build = this.openedBuilding;\n        if (!build || !this.IsBuildingInPickupRange(build)) {\n            this.CloseAllInterfaces();\n        }\n    }\n    UpdateCursorInsideGame(cursor) {\n        const canvas = this.canvasRef.current;\n        if (!canvas)\n            return;\n        const rect = canvas.getBoundingClientRect();\n        const insideRect = cursor.x >= rect.left &&\n            cursor.x <= rect.right &&\n            cursor.y >= rect.top &&\n            cursor.y <= rect.bottom;\n        if (!insideRect) {\n            this.cursorInsideGame = false;\n            return;\n        }\n        const elementAtCursor = typeof document !== 'undefined'\n            ? document.elementFromPoint(cursor.x, cursor.y)\n            : null;\n        this.cursorInsideGame = elementAtCursor === canvas;\n    }\n    TogglePlayerUi() {\n        this.showPlayerUi = !this.showPlayerUi;\n        this.SyncOpenedInterfaces();\n        this.forceUpdate();\n    }\n    OpenBuildingUi(buildUid) {\n        const build = this.state.gameData.buildings.find(b => b.uid === buildUid);\n        if (!build)\n            return;\n        if (build.team !== this.myTeamId)\n            return;\n        if (!this.IsBuildingUiAvailable(build))\n            return;\n        if (!this.IsBuildingInPickupRange(build))\n            return;\n        this.openedBuildingUid = build.uid;\n        this.showPlayerUi = true;\n        this.SyncOpenedInterfaces();\n        this.forceUpdate();\n    }\n    CloseBuildingUi() {\n        this.openedBuildingUid = undefined;\n        this.ResetInventoryDrag();\n        this.dragAmountModal = { open: false };\n        this.SyncOpenedInterfaces();\n        this.forceUpdate();\n    }\n    GetAutomatonUidAtCursor(cursor) {\n        var _a;\n        if (!this.renderer || !this.camera)\n            return undefined;\n        const canvas = this.renderer.domElement;\n        const rect = canvas.getBoundingClientRect();\n        const x = (cursor.x - rect.left) / rect.width;\n        const y = (cursor.y - rect.top) / rect.height;\n        if (x < 0 || x > 1 || y < 0 || y > 1)\n            return undefined;\n        const ndcX = x * 2 - 1;\n        const ndcY = -y * 2 + 1;\n        const tmpRay = new three__WEBPACK_IMPORTED_MODULE_9__.Raycaster();\n        tmpRay.setFromCamera(new three__WEBPACK_IMPORTED_MODULE_9__.Vector2(ndcX, ndcY), this.camera);\n        const intersects = tmpRay.intersectObjects([...this.automatons_models.values()], true);\n        for (const hit of intersects) {\n            let target = hit.object;\n            while (target) {\n                const uid = (_a = target.userData) === null || _a === void 0 ? void 0 : _a.automatonUid;\n                if (uid)\n                    return uid;\n                target = target.parent;\n            }\n        }\n        return undefined;\n    }\n    GetAutomatonUidByMouse() {\n        var _a;\n        if (!this.camera)\n            return undefined;\n        const raycaster = new three__WEBPACK_IMPORTED_MODULE_9__.Raycaster();\n        raycaster.setFromCamera(this.mouse, this.camera);\n        const intersects = raycaster.intersectObjects([...this.automatons_models.values()], true);\n        for (const hit of intersects) {\n            let target = hit.object;\n            while (target) {\n                const uid = (_a = target.userData) === null || _a === void 0 ? void 0 : _a.automatonUid;\n                if (uid)\n                    return uid;\n                target = target.parent;\n            }\n        }\n        return undefined;\n    }\n    GetBuildingUidByMouse() {\n        var _a;\n        if (!this.camera)\n            return undefined;\n        const raycaster = new three__WEBPACK_IMPORTED_MODULE_9__.Raycaster();\n        raycaster.setFromCamera(this.mouse, this.camera);\n        const intersects = raycaster.intersectObjects([...this.buildings.values()], true);\n        for (const hit of intersects) {\n            let target = hit.object;\n            while (target) {\n                const uid = (_a = target.userData) === null || _a === void 0 ? void 0 : _a.buildingUid;\n                if (uid)\n                    return uid;\n                target = target.parent;\n            }\n        }\n        return undefined;\n    }\n    EnsureCombatHoverObjects() {\n        if (!this.scene)\n            return;\n        if (!this.hoverTargetLine) {\n            const geometry = new three__WEBPACK_IMPORTED_MODULE_9__.BufferGeometry().setFromPoints([\n                new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(),\n                new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(),\n            ]);\n            const material = new three__WEBPACK_IMPORTED_MODULE_9__.LineBasicMaterial({\n                color: '#ffd16b',\n                transparent: true,\n                opacity: 0.9,\n            });\n            this.hoverTargetLine = new three__WEBPACK_IMPORTED_MODULE_9__.Line(geometry, material);\n            this.hoverTargetLine.visible = false;\n            this.scene.add(this.hoverTargetLine);\n        }\n        if (!this.hoverUndergroundLinkArrow) {\n            this.hoverUndergroundLinkArrow = new three__WEBPACK_IMPORTED_MODULE_9__.ArrowHelper(new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(1, 0, 0), new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(0, 0, 0), _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize * 0.45, 0xffd16b, _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize * 0.22, _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize * 0.12);\n            this.hoverUndergroundLinkArrow.visible = false;\n            this.scene.add(this.hoverUndergroundLinkArrow);\n        }\n        if (!this.hoverTurretRangeRing) {\n            const geometry = new three__WEBPACK_IMPORTED_MODULE_9__.RingGeometry(0.98, 1, 96);\n            const material = new three__WEBPACK_IMPORTED_MODULE_9__.MeshBasicMaterial({\n                color: '#ffcc66',\n                transparent: true,\n                opacity: 0.35,\n                side: three__WEBPACK_IMPORTED_MODULE_9__.DoubleSide,\n            });\n            this.hoverTurretRangeRing = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(geometry, material);\n            this.hoverTurretRangeRing.rotation.x = -Math.PI / 2;\n            this.hoverTurretRangeRing.visible = false;\n            this.scene.add(this.hoverTurretRangeRing);\n        }\n    }\n    HideCombatHoverObjects() {\n        if (this.hoverTargetLine)\n            this.hoverTargetLine.visible = false;\n        if (this.hoverUndergroundLinkArrow)\n            this.hoverUndergroundLinkArrow.visible = false;\n        if (this.hoverTurretRangeRing)\n            this.hoverTurretRangeRing.visible = false;\n    }\n    GetAutomatonFireHeight(unitId) {\n        const unitCfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsAutomatonUnits.find(unit => unit.id === unitId);\n        return Math.max(24, Number((unitCfg === null || unitCfg === void 0 ? void 0 : unitCfg.fly) || 0) + 24);\n    }\n    DistanceSq(a, b) {\n        const dx = a.x - b.x;\n        const dz = a.z - b.z;\n        return dx * dx + dz * dz;\n    }\n    GetNearestBuildCellToWorldPos(worldPos, build) {\n        const occupied = this.GetBuildOccupiedCells(build);\n        if (occupied.length === 0) {\n            return { x: build.pos.x, z: build.pos.z };\n        }\n        const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n        let nearest = occupied[0];\n        let nearestDistSq = Number.POSITIVE_INFINITY;\n        for (const cell of occupied) {\n            const cellWorld = {\n                x: cell.x * grid,\n                z: cell.z * grid,\n            };\n            const distSq = this.DistanceSq(worldPos, cellWorld);\n            if (distSq < nearestDistSq) {\n                nearestDistSq = distSq;\n                nearest = cell;\n            }\n        }\n        return nearest;\n    }\n    DistanceSqToNearestBuildCell(worldPos, build) {\n        const nearestCell = this.GetNearestBuildCellToWorldPos(worldPos, build);\n        const nearestWorld = {\n            x: nearestCell.x * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize,\n            z: nearestCell.z * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize,\n        };\n        return this.DistanceSq(worldPos, nearestWorld);\n    }\n    ResolveAutomatonAttackTargetBuild(automaton) {\n        const lastShotTargetUid = this.automatonLastShotTargetByUid.get(automaton.uid);\n        if (lastShotTargetUid) {\n            const lastShotTarget = this.state.gameData.buildings.find(build => build.uid === lastShotTargetUid);\n            if (lastShotTarget)\n                return lastShotTarget;\n            this.automatonLastShotTargetByUid.delete(automaton.uid);\n        }\n        const unitCfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsAutomatonUnits.find(u => u.id === automaton.id);\n        if (!unitCfg)\n            return undefined;\n        const explicitTarget = this.state.gameData.buildings.find(build => build.uid === automaton.targetUid);\n        const ownHub = this.state.gameData.buildings.find(build => {\n            if (build.team !== automaton.team)\n                return false;\n            const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(q => q.id === build.id);\n            return !!(cfg === null || cfg === void 0 ? void 0 : cfg.isTeamCore);\n        });\n        const enemyTurrets = this.state.gameData.buildings.filter(build => {\n            if (build.team === automaton.team)\n                return false;\n            const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(q => q.id === build.id);\n            if (!(cfg === null || cfg === void 0 ? void 0 : cfg.turret))\n                return false;\n            const hasAmmo = (build.data.input || []).some(slot => slot && slot.id === cfg.turret.ammoItemId && Number(slot.amount) > 0);\n            if (!hasAmmo)\n                return false;\n            const turretRangeSq = cfg.turret.range * cfg.turret.range;\n            const distSq = this.DistanceSqToNearestBuildCell(automaton.position, build);\n            return distSq <= turretRangeSq;\n        });\n        const attackRangeSq = unitCfg.range * unitCfg.range;\n        const enemyBuildingsInRange = this.state.gameData.buildings.filter(build => {\n            if (build.team === automaton.team)\n                return false;\n            const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(q => q.id === build.id);\n            if (!cfg)\n                return false;\n            if (_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryAutomatonAttackIgnores.includes(cfg.category))\n                return false;\n            const distSq = this.DistanceSqToNearestBuildCell(automaton.position, build);\n            return distSq <= attackRangeSq;\n        });\n        let targetBuild;\n        let targetBuildDist = Number.POSITIVE_INFINITY;\n        for (const turretBuild of enemyTurrets) {\n            const distSq = this.DistanceSqToNearestBuildCell(automaton.position, turretBuild);\n            if (distSq <= attackRangeSq && distSq < targetBuildDist) {\n                targetBuild = turretBuild;\n                targetBuildDist = distSq;\n            }\n        }\n        if (!targetBuild) {\n            for (const building of enemyBuildingsInRange) {\n                const distSq = this.DistanceSqToNearestBuildCell(automaton.position, building);\n                if (distSq < targetBuildDist) {\n                    targetBuild = building;\n                    targetBuildDist = distSq;\n                }\n            }\n        }\n        return targetBuild || explicitTarget || ownHub;\n    }\n    UpdateHoverCombatVisuals() {\n        var _a;\n        this.EnsureCombatHoverObjects();\n        this.HideCombatHoverObjects();\n        const automatonUid = this.GetAutomatonUidByMouse();\n        if (automatonUid) {\n            const automaton = this.state.gameData.automatons.find(a => a.uid === automatonUid);\n            const sourceModel = this.automatons_models.get(automatonUid);\n            if (automaton && sourceModel && this.hoverTargetLine) {\n                const targetBuild = this.ResolveAutomatonAttackTargetBuild(automaton);\n                if (targetBuild) {\n                    const nearestCell = this.GetNearestBuildCellToWorldPos(automaton.position, targetBuild);\n                    const targetPos = this.SnapToGrid({\n                        x: nearestCell.x * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize,\n                        z: nearestCell.z * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize,\n                    });\n                    const fireHeight = this.GetAutomatonFireHeight(automaton.id);\n                    const from = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(sourceModel.position.x, fireHeight, sourceModel.position.z);\n                    const to = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(targetPos.x, 18, targetPos.z);\n                    this.hoverTargetLine.geometry.setFromPoints([from, to]);\n                    this.hoverTargetLine.visible = true;\n                }\n            }\n            return;\n        }\n        const buildingUid = this.GetBuildingUidByMouse();\n        if (!buildingUid)\n            return;\n        const build = this.state.gameData.buildings.find(b => b.uid === buildingUid);\n        if (!build)\n            return;\n        const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(b => b.id === build.id);\n        if (!cfg)\n            return;\n        if (build.id === 'underground_conveyor') {\n            const connection = this.GetUndergroundConnection(build);\n            if (!connection || !this.hoverTargetLine)\n                return;\n            const from = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(connection.entry.pos.x * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize, 14, connection.entry.pos.z * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize);\n            const to = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(connection.exit.pos.x * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize, 14, connection.exit.pos.z * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize);\n            this.hoverTargetLine.geometry.setFromPoints([from, to]);\n            this.hoverTargetLine.visible = true;\n            if (this.hoverUndergroundLinkArrow) {\n                const direction = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3().subVectors(to, from);\n                const length = direction.length();\n                if (length > 1e-3) {\n                    direction.normalize();\n                    const travelT = (((Date.now() * 0.0011) % 1) + 1) % 1;\n                    const animatedPos = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(from.x + (to.x - from.x) * travelT, 14, from.z + (to.z - from.z) * travelT);\n                    this.hoverUndergroundLinkArrow.position.copy(animatedPos);\n                    this.hoverUndergroundLinkArrow.setDirection(direction);\n                    this.hoverUndergroundLinkArrow.setLength(Math.max(_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize * 0.36, length * 0.22), _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize * 0.24, _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize * 0.12);\n                    this.hoverUndergroundLinkArrow.visible = true;\n                }\n            }\n            return;\n        }\n        if (!cfg.turret)\n            return;\n        const turretModel = this.buildings.get(build.uid);\n        if (!turretModel)\n            return;\n        const buildPos = this.SnapToGrid({\n            x: build.pos.x * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize,\n            z: build.pos.z * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize,\n        });\n        if (this.hoverTurretRangeRing) {\n            this.hoverTurretRangeRing.position.set(buildPos.x, 3, buildPos.z);\n            this.hoverTurretRangeRing.scale.set(cfg.turret.range, cfg.turret.range, 1);\n            this.hoverTurretRangeRing.visible = true;\n        }\n        const targetPos = (_a = build.data.additional) === null || _a === void 0 ? void 0 : _a.turretTargetPos;\n        if (targetPos && this.hoverTargetLine) {\n            const from = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(buildPos.x, 24, buildPos.z);\n            const to = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(targetPos.x, 18, targetPos.z);\n            this.hoverTargetLine.geometry.setFromPoints([from, to]);\n            this.hoverTargetLine.visible = true;\n        }\n    }\n    OpenAutomatonUi(uid) {\n        const automaton = this.state.gameData.automatons.find(a => a.uid === uid);\n        if (!automaton)\n            return;\n        if (automaton.team !== this.myTeamId)\n            return;\n        this.openedAutomatonUid = uid;\n        this.UpdateAutomatonSelectionRing(uid);\n        this.forceUpdate();\n    }\n    CloseAutomatonUi() {\n        this.openedAutomatonUid = undefined;\n        this.RemoveAutomatonSelectionRing();\n        this.forceUpdate();\n    }\n    UpdateAutomatonSelectionRing(uid) {\n        this.RemoveAutomatonSelectionRing();\n        const model = this.automatons_models.get(uid);\n        if (!model || !this.scene)\n            return;\n        const ring = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.TorusGeometry(_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize * 0.38, _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize * 0.025, 8, 32), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n            color: '#ffffff',\n            emissive: '#88ccff',\n            emissiveIntensity: 1.2,\n            roughness: 0.4,\n            metalness: 0.2,\n        }));\n        ring.rotation.x = Math.PI / 2;\n        ring.position.set(model.position.x, 4, model.position.z);\n        this.scene.add(ring);\n        this.automatonSelectionRing = ring;\n    }\n    RemoveAutomatonSelectionRing() {\n        if (this.automatonSelectionRing && this.scene) {\n            this.scene.remove(this.automatonSelectionRing);\n        }\n        this.automatonSelectionRing = undefined;\n    }\n    SendAutomatonTarget(automatonUid, targetUid) {\n        this.SendActionSocket('automaton.set_target', { automatonUid, targetUid });\n    }\n    UpdateAutomatonTooltip() {\n        const tooltip = this.automatonTooltipRef.current;\n        if (!tooltip)\n            return;\n        const uid = this.GetAutomatonUidByMouse();\n        if (!uid) {\n            tooltip.style.display = 'none';\n            return;\n        }\n        const automaton = this.state.gameData.automatons.find(a => a.uid === uid);\n        if (!automaton) {\n            tooltip.style.display = 'none';\n            return;\n        }\n        const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsAutomatonUnits.find(u => u.id === automaton.id);\n        const targetTeam = automaton.targetUid\n            ? (() => {\n                const tb = this.state.gameData.buildings.find(b => b.uid === automaton.targetUid);\n                return tb != null ? tb.team : undefined;\n            })()\n            : undefined;\n        const statusText = targetTeam !== undefined\n            ? this.LangString('FactoryWarsAutomatonStatusAttack', this.GetTeamDisplayName(targetTeam))\n            : this.LangString('FactoryWarsAutomatonStatusIdle');\n        tooltip.innerHTML = `<strong>${this.LocalizeConfigName(cfg === null || cfg === void 0 ? void 0 : cfg.name, cfg === null || cfg === void 0 ? void 0 : cfg.nameLang) || automaton.id}</strong><br/>${this.LangString('FactoryWarsHpCompact', Math.floor(automaton.hp), Math.floor(automaton.maxHp))}<br/>${statusText}`;\n        tooltip.style.display = 'block';\n        tooltip.style.left = `${this.hoverCursorPos.x + 14}px`;\n        tooltip.style.top = `${this.hoverCursorPos.y - 10}px`;\n    }\n    GetItemLockingRecipe(itemId) {\n        var _a;\n        const openedRecipes = ((_a = this.myTeamData) === null || _a === void 0 ? void 0 : _a.openedRecipes) || [];\n        return _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.repicePermissions.find(recipe => {\n            if (!recipe.items.includes(itemId))\n                return false;\n            return !openedRecipes.includes(recipe.id);\n        });\n    }\n    IsItemLockedByRecipe(itemId) {\n        return !!this.GetItemLockingRecipe(itemId);\n    }\n    IsRecipeResearchable(recipeId) {\n        const teamData = this.myTeamData;\n        if (!teamData)\n            return false;\n        if (teamData.openedRecipes.includes(recipeId))\n            return false;\n        const recipe = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.repicePermissions.find(r => r.id === recipeId);\n        if (!recipe)\n            return false;\n        if (recipe.requireRecipes) {\n            return recipe.requireRecipes.every(rId => teamData.openedRecipes.includes(rId));\n        }\n        return true;\n    }\n    SelectLabResearch(recipeId) {\n        this.SendActionSocket('lab.research.select', { recipeId });\n    }\n    GetItemName(itemId) {\n        const item = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.items.find(cfg => cfg.id === itemId);\n        return this.LocalizeConfigName(item === null || item === void 0 ? void 0 : item.name, item === null || item === void 0 ? void 0 : item.nameLang) || itemId;\n    }\n    GetInventoryItemAmount(itemId, inventory = this.myInventory) {\n        return Object.values(inventory || {}).reduce((sum, item) => {\n            if ((item === null || item === void 0 ? void 0 : item.id) !== itemId)\n                return sum;\n            return sum + Number(item.amount || 0);\n        }, 0);\n    }\n    GetBuildInventoryAmount(buildId, inventory = this.myInventory) {\n        return this.GetInventoryItemAmount(`build.${buildId}`, inventory);\n    }\n    CanStartBuildCraft(buildId) {\n        var _a, _b;\n        const craft = (_b = (_a = this.me) === null || _a === void 0 ? void 0 : _a.data) === null || _b === void 0 ? void 0 : _b.craft;\n        if (craft)\n            return false;\n        const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(b => b.id === buildId);\n        if (!(cfg === null || cfg === void 0 ? void 0 : cfg.craft))\n            return false;\n        if (this.IsItemLockedByRecipe(`build.${buildId}`))\n            return false;\n        return cfg.craft.input.every(req => {\n            return this.GetInventoryItemAmount(req.id) >= req.amount;\n        });\n    }\n    StartBuildCraft(buildId) {\n        var _a, _b;\n        const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(b => b.id === buildId);\n        if (!(cfg === null || cfg === void 0 ? void 0 : cfg.craft)) {\n            this.PushGameNotification(this.LangString('FactoryWarsNotifyCraftStartImpossible'), 'error');\n            return;\n        }\n        if ((_b = (_a = this.me) === null || _a === void 0 ? void 0 : _a.data) === null || _b === void 0 ? void 0 : _b.craft) {\n            this.PushGameNotification(this.LangString('FactoryWarsNotifyCraftAlreadyActive'), 'warning');\n            return;\n        }\n        const buildItemId = `build.${buildId}`;\n        const lockingRecipe = this.GetItemLockingRecipe(buildItemId);\n        if (lockingRecipe) {\n            this.PushGameNotification(this.LangString('FactoryWarsNotifyCraftBlockedByResearch', this.LocalizeConfigName(lockingRecipe.name, lockingRecipe.nameLang) ||\n                lockingRecipe.name), 'warning');\n            return;\n        }\n        if (!this.CanStartBuildCraft(buildId)) {\n            this.PushGameNotification(this.LangString('FactoryWarsNotifyCraftNotEnoughResources'), 'warning');\n            return;\n        }\n        this.SendAction('player.build.craft', { buildId });\n    }\n    PushGameNotification(text, type = 'info', ttlMs = 2600) {\n        if (!text)\n            return;\n        const now = Date.now();\n        const next = {\n            id: ++this.gameNotificationSeq,\n            text,\n            type,\n            expiresAt: now + Math.max(800, ttlMs),\n        };\n        this.gameNotifications = [...this.gameNotifications, next].slice(-5);\n        this.forceUpdate();\n    }\n    TickGameNotifications(now = Date.now()) {\n        if (this.gameNotifications.length === 0)\n            return;\n        const next = this.gameNotifications.filter(item => item.expiresAt > now);\n        if (next.length === this.gameNotifications.length)\n            return;\n        this.gameNotifications = next;\n        this.forceUpdate();\n    }\n    // ─────────────────────────────────────────────────────────────────────────────\n    // Category Hotkey System\n    GetBuildsInCategory(categoryId) {\n        const category = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryBuildCategories.find(q => q.id === categoryId);\n        if (!category)\n            return [];\n        return _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.filter(build => build.category === categoryId &&\n            !(category.hideBuilds || []).includes(build.id));\n    }\n    CloseCategoryMenu() {\n        if (this.openCategoryMenuIndex !== undefined) {\n            this.openCategoryMenuIndex = undefined;\n            this.forceUpdate();\n        }\n    }\n    SelectBuildFromCategory(categoryIndex, buildIndexInCategory) {\n        if (categoryIndex < 0 || categoryIndex >= _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryBuildCategories.length)\n            return;\n        const category = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryBuildCategories[categoryIndex];\n        const builds = this.GetBuildsInCategory(category.id);\n        if (buildIndexInCategory < 0 || buildIndexInCategory >= builds.length)\n            return;\n        const build = builds[buildIndexInCategory];\n        this.SelectGhostBuild(build.id);\n        this.CloseCategoryMenu();\n    }\n    TryOpenCategoryMenu(categoryIndex) {\n        if (categoryIndex < 0 || categoryIndex >= _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryBuildCategories.length)\n            return;\n        if (this.openCategoryMenuIndex === categoryIndex) {\n            this.CloseCategoryMenu();\n            return;\n        }\n        this.openCategoryMenuIndex = categoryIndex;\n        this.forceUpdate();\n    }\n    GetMissingBuildItemsForPlacement(buildIds) {\n        if (buildIds.length === 0)\n            return [];\n        const required = new Map();\n        buildIds.forEach(buildId => {\n            required.set(buildId, (required.get(buildId) || 0) + 1);\n        });\n        return Array.from(required.entries())\n            .map(([buildId, amount]) => {\n            const have = this.GetBuildInventoryAmount(buildId);\n            const missing = Math.max(0, amount - have);\n            return { buildId, missing };\n        })\n            .filter(item => item.missing > 0);\n    }\n    GetPlayerCraftProgressPercent() {\n        var _a, _b;\n        const craft = (_b = (_a = this.me) === null || _a === void 0 ? void 0 : _a.data) === null || _b === void 0 ? void 0 : _b.craft;\n        if (!craft)\n            return 0;\n        const duration = Math.max(1, Number(craft.durationMS) || 1);\n        const elapsed = Date.now() - Number(craft.startedAt || 0);\n        return Math.max(0, Math.min(100, (elapsed / duration) * 100));\n    }\n    GetPlayerInventorySlots() {\n        return Array.from({ length: _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.playerMaxSlots }).map((_, slot) => {\n            const item = this.myInventory[slot];\n            return {\n                slot,\n                item,\n            };\n        });\n    }\n    GetBuildingSlots(type) {\n        const cfg = this.openedBuildingConfig;\n        const build = this.openedBuilding;\n        if (!cfg || !build)\n            return [];\n        const source = type === 'input' ? build.data.input || [] : build.data.output || [];\n        const max = type === 'input'\n            ? cfg.maxInputSlots || source.length\n            : cfg.maxOutputSlots || source.length;\n        const visibleSlots = Math.max(max || 0, source.length, 1);\n        return Array.from({ length: visibleSlots }).map((_, slot) => ({\n            slot,\n            item: source[slot],\n        }));\n    }\n    OnInventoryDragStart(source, e, preview) {\n        this.dragSource = Object.assign(Object.assign({}, source), { splitWithCtrl: !!e.ctrlKey ||\n                this.keysPressed.has('ControlLeft') ||\n                this.keysPressed.has('ControlRight') });\n        this.dragPreview = {\n            itemId: source.itemId,\n            amount: source.amount,\n            itemName: (preview === null || preview === void 0 ? void 0 : preview.itemName) || this.GetItemName(source.itemId),\n            itemPreview: preview === null || preview === void 0 ? void 0 : preview.itemPreview,\n        };\n        this.UpdateInventoryDragCursor(e.clientX, e.clientY);\n        const transparentImage = this.GetTransparentDragImage();\n        if (transparentImage) {\n            e.dataTransfer.setDragImage(transparentImage, 0, 0);\n        }\n        // Allow both inventory move and hotkey assignment drop targets.\n        e.dataTransfer.effectAllowed = 'copyMove';\n        e.dataTransfer.setData('text/plain', JSON.stringify(this.dragSource));\n        this.forceUpdate();\n    }\n    GetTransparentDragImage() {\n        if (this.transparentDragImage)\n            return this.transparentDragImage;\n        if (typeof document === 'undefined')\n            return undefined;\n        const pixel = document.createElement('canvas');\n        pixel.width = 1;\n        pixel.height = 1;\n        const image = new Image();\n        image.src = pixel.toDataURL('image/png');\n        this.transparentDragImage = image;\n        return this.transparentDragImage;\n    }\n    UpdateInventoryDragCursor(x, y) {\n        this.dragCursorPos = { x, y };\n        if (this.dragPreview) {\n            this.forceUpdate();\n        }\n    }\n    ResetInventoryDrag() {\n        this.dragSource = undefined;\n        this.dragPreview = undefined;\n    }\n    GetDragSourceFromEvent(e) {\n        if (this.dragSource)\n            return this.dragSource;\n        const raw = e.dataTransfer.getData('text/plain');\n        if (!raw)\n            return undefined;\n        try {\n            const parsed = JSON.parse(raw);\n            if (!parsed ||\n                typeof parsed.buildingUid !== 'string' ||\n                typeof parsed.type !== 'string' ||\n                typeof parsed.slot !== 'number' ||\n                typeof parsed.itemId !== 'string' ||\n                typeof parsed.amount !== 'number') {\n                return undefined;\n            }\n            return parsed;\n        }\n        catch (_a) {\n            return undefined;\n        }\n    }\n    CanDropToTarget(target, source) {\n        const effectiveSource = source || this.dragSource;\n        const sourceData = effectiveSource;\n        if (!sourceData)\n            return false;\n        if (sourceData.buildingUid !== target.buildingUid)\n            return false;\n        if (sourceData.type === target.type && sourceData.slot === target.slot)\n            return false;\n        if (sourceData.type === 'player' && target.type === 'player')\n            return true;\n        if (sourceData.type === 'player' && target.type === 'input')\n            return true;\n        if (sourceData.type === 'input' && target.type === 'player')\n            return true;\n        if (sourceData.type === 'output' && target.type === 'player')\n            return true;\n        return false;\n    }\n    SendInventoryMove(source, target, amount) {\n        if (!Number.isFinite(amount) || amount <= 0)\n            return;\n        if (source.type === 'player' &&\n            target.type === 'player' &&\n            source.buildingUid === 'self-panel') {\n            this.SendActionSocket('player.inventory.move', {\n                fromSlot: source.slot,\n                toSlot: target.slot,\n                amount,\n            });\n            return;\n        }\n        this.SendActionSocket('build.inventory.move', {\n            uid: source.buildingUid,\n            from: {\n                type: source.type,\n                slot: source.slot,\n            },\n            to: {\n                type: target.type,\n                slot: target.slot,\n            },\n            amount,\n        });\n    }\n    HandleInventoryDrop(target, e) {\n        e.preventDefault();\n        const source = this.GetDragSourceFromEvent(e);\n        if (!source)\n            return false;\n        this.ResetInventoryDrag();\n        if (!source)\n            return;\n        if (!this.CanDropToTarget(target, source))\n            return;\n        const maxAmount = source.amount;\n        if (source.splitWithCtrl && maxAmount > 1) {\n            this.dragAmountModal = {\n                open: true,\n                source,\n                target,\n                amount: maxAmount,\n                max: maxAmount,\n            };\n            this.forceUpdate();\n            return;\n        }\n        this.SendInventoryMove(source, target, maxAmount);\n        this.forceUpdate();\n    }\n    ConfirmDragAmount() {\n        if (!this.dragAmountModal.open)\n            return;\n        const amount = Math.max(1, Math.min(this.dragAmountModal.max, Math.floor(this.dragAmountModal.amount)));\n        this.SendInventoryMove(this.dragAmountModal.source, this.dragAmountModal.target, amount);\n        this.dragAmountModal = { open: false };\n        this.forceUpdate();\n    }\n    SelectNextBuildingRecipe() {\n        var _a;\n        const build = this.openedBuilding;\n        const cfg = this.openedBuildingConfig;\n        if (!build || !(cfg === null || cfg === void 0 ? void 0 : cfg.recipes) || cfg.recipes.length <= 1)\n            return;\n        const selectedIndex = Number.isInteger((_a = build.data.additional) === null || _a === void 0 ? void 0 : _a.recipeIndex)\n            ? Number(build.data.additional.recipeIndex)\n            : 0;\n        const next = (selectedIndex + 1) % cfg.recipes.length;\n        this.SendActionSocket('build.recipe.select', {\n            uid: build.uid,\n            recipeIndex: next,\n        });\n    }\n    get threeJsReady() {\n        return (this.threeJsInitialized &&\n            !!this.scene &&\n            !!this.camera &&\n            !!this.cameraMini &&\n            !!this.renderer &&\n            !!this.rendererMini);\n    }\n    GetBuildingInPoint(pos) {\n        return (0,_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsGetBuildingInPoint)(this.state.gameData, pos);\n    }\n    GetBuildOccupiedCells(build) {\n        return (0,_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsGetBuildingOccupiedCells)('pos' in build\n            ? build\n            : {\n                id: build.id,\n                position: build.position,\n                rotation: build.rotation,\n            });\n    }\n    GetBuildWorldCenter(build) {\n        return (0,_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsGetBuildingWorldCenter)('pos' in build\n            ? build\n            : {\n                id: build.id,\n                position: build.position,\n                rotation: build.rotation,\n            });\n    }\n    CanPlaceBuild(build, ignoredUids = new Set()) {\n        const cells = this.GetBuildOccupiedCells(build);\n        if (cells.length === 0)\n            return false;\n        if (!cells.every(cell => this.IsCellInMyTeamZone(cell)))\n            return false;\n        return cells.every(cell => {\n            const occupied = this.state.gameData.buildings.find(current => !ignoredUids.has(current.uid) &&\n                (0,_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsDoesBuildingOccupyCell)(current, cell));\n            return !occupied;\n        });\n    }\n    GetBuildingsBetweenPoints(pos1, pos2) {\n        const minX = Math.min(pos1.x, pos2.x);\n        const maxX = Math.max(pos1.x, pos2.x);\n        const minZ = Math.min(pos1.z, pos2.z);\n        const maxZ = Math.max(pos1.z, pos2.z);\n        return this.state.gameData.buildings.filter(p => {\n            const isInsideRange = this.GetBuildOccupiedCells(p).some(cell => cell.x >= minX && cell.x <= maxX && cell.z >= minZ && cell.z <= maxZ);\n            const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(build => build.id === p.id);\n            const canManualEdit = (cfg === null || cfg === void 0 ? void 0 : cfg.allowManualDestroy) !== false;\n            return isInsideRange && canManualEdit;\n        });\n    }\n    NormalizeBuildRange(pos1, pos2) {\n        return [\n            {\n                x: Math.min(pos1.x, pos2.x),\n                z: Math.min(pos1.z, pos2.z),\n            },\n            {\n                x: Math.max(pos1.x, pos2.x),\n                z: Math.max(pos1.z, pos2.z),\n            },\n        ];\n    }\n    GetRangeCenterCell(pos1, pos2) {\n        const [from, to] = this.NormalizeBuildRange(pos1, pos2);\n        return {\n            x: Math.floor((from.x + to.x) / 2),\n            z: Math.floor((from.z + to.z) / 2),\n        };\n    }\n    CreateGhostSelection(builds, centerCell, isCut) {\n        this.CancelGhost();\n        if (builds.length === 0)\n            return false;\n        const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n        group.position.set(this.hoverPoint.x, 0, this.hoverPoint.z);\n        group.scale.set(0, 0, 0);\n        const selectionOriginWorld = {\n            x: centerCell.x * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize,\n            z: centerCell.z * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize,\n        };\n        this.ghostSelectionBuilds = builds\n            .map(build => {\n            const ghostModel = this.GetModel(build.id);\n            if (!ghostModel)\n                return undefined;\n            const offset = {\n                x: build.pos.x - centerCell.x,\n                z: build.pos.z - centerCell.z,\n            };\n            const buildCenter = this.GetBuildWorldCenter(build);\n            ghostModel.position.set(buildCenter.x - selectionOriginWorld.x, 0, buildCenter.z - selectionOriginWorld.z);\n            ghostModel.rotation.y = build.rotation * (Math.PI / 2);\n            this.MarkObjectAsGhost(ghostModel);\n            group.add(ghostModel);\n            if (isCut) {\n                const originalModel = this.buildings.get(build.uid);\n                if (originalModel)\n                    this.MarkObjectAsGhost(originalModel, 0.55);\n            }\n            return {\n                uid: build.uid,\n                id: build.id,\n                rotation: build.rotation,\n                offset,\n            };\n        })\n            .filter(Boolean);\n        if (this.ghostSelectionBuilds.length === 0)\n            return false;\n        this.cutBuildUids = isCut ? builds.map(build => build.uid) : [];\n        this.hoverGroup.visible = false;\n        this.ghostBuildId = undefined;\n        this.ghostObjectRotation = 0;\n        this.ghostObject = group;\n        this.scene.add(group);\n        this.forceUpdate();\n        return true;\n    }\n    GetGhostSelectionTargets(anchorCell = this.hoverCell) {\n        return this.ghostSelectionBuilds.map(build => ({\n            uid: build.uid,\n            id: build.id,\n            rotation: build.rotation,\n            position: {\n                x: anchorCell.x + build.offset.x,\n                z: anchorCell.z + build.offset.z,\n            },\n        }));\n    }\n    CanPlaceGhostSelection(anchorCell = this.hoverCell) {\n        const targets = this.GetGhostSelectionTargets(anchorCell);\n        const movingUids = new Set(this.cutBuildUids);\n        const targetKeys = new Set();\n        for (const target of targets) {\n            const cells = this.GetBuildOccupiedCells(target);\n            if (!cells.every(cell => this.IsCellInMyTeamZone(cell)))\n                return false;\n            for (const cell of cells) {\n                const key = `${cell.x}:${cell.z}`;\n                if (targetKeys.has(key))\n                    return false;\n                targetKeys.add(key);\n                const occupied = this.state.gameData.buildings.find(build => !movingUids.has(build.uid) &&\n                    (0,_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsDoesBuildingOccupyCell)(build, cell));\n                if (occupied)\n                    return false;\n            }\n        }\n        return true;\n    }\n    GetResouceInPoint(pos) {\n        var _a;\n        return (_a = this.state.gameData.map.find(p => p.x === pos.x && p.z === pos.z)) === null || _a === void 0 ? void 0 : _a.resource;\n    }\n    GetModel(id, color) {\n        let model = this.GenerateModel(id, color);\n        if (!model) {\n            model = this.CloneObject(this.models.get(id));\n            if (color) {\n                this.ApplyResouceColor(model, color);\n            }\n        }\n        if (!model)\n            return undefined;\n        return model;\n    }\n    CalculateVectorOffset(position, centeringStrength = 1) {\n        return (0,_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsConveyorMoveVelocity)(position, this.state.gameData, undefined, centeringStrength);\n    }\n    GenerateModel(id, color) {\n        const cacheKey = `${id}:${color || ''}`;\n        const buildCfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(build => build.id === id || build.model === id);\n        const storeModel = (model) => {\n            if (this.GenerateModelStorage.has(cacheKey))\n                return;\n            // Store a pristine template in the cache: shares geometry (one GPU buffer\n            // for all instances) but has independent materials, so per-instance\n            // mutations (resource colours, smoke opacity, emissive pulses, etc.)\n            // never bleed back into the cache and corrupt future clones.\n            const template = (0,three_examples_jsm_utils_SkeletonUtils_js__WEBPACK_IMPORTED_MODULE_10__.clone)(model);\n            template.traverse((child) => {\n                if (child.isMesh && child.material) {\n                    child.material = Array.isArray(child.material)\n                        ? child.material.map((m) => m.clone())\n                        : child.material.clone();\n                }\n            });\n            this.GenerateModelStorage.set(cacheKey, template);\n            // Mark the first returned instance's geometry as shared so that\n            // DisposeObject won't free the buffer still referenced by the\n            // template and all future clones.\n            model.traverse((child) => {\n                if (child.isMesh && child.geometry) {\n                    child.userData.sharedGeometry = true;\n                }\n            });\n        };\n        const finalizeModel = (model) => {\n            var _a, _b;\n            if (!model)\n                return undefined;\n            storeModel(model);\n            if (!buildCfg)\n                return model;\n            const sizeX = Math.max(1, Number((_a = buildCfg.size) === null || _a === void 0 ? void 0 : _a.x) || 1);\n            const sizeZ = Math.max(1, Number((_b = buildCfg.size) === null || _b === void 0 ? void 0 : _b.y) || 1);\n            const bounds = new three__WEBPACK_IMPORTED_MODULE_9__.Box3().setFromObject(model);\n            const size = bounds.getSize(new three__WEBPACK_IMPORTED_MODULE_9__.Vector3());\n            const targetWidth = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize * sizeX;\n            const targetDepth = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize * sizeZ;\n            if (size.x > 0) {\n                model.scale.x *= targetWidth / size.x;\n            }\n            if (size.z > 0) {\n                model.scale.z *= targetDepth / size.z;\n            }\n            return model;\n        };\n        if (this.GenerateModelStorage.has(cacheKey)) {\n            return finalizeModel(this.CloneObject(this.GenerateModelStorage.get(cacheKey)));\n        }\n        // Модель жидкостей: колба\n        if (id === 'liquidResource') {\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            // Основание колбы\n            const base = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.18, grid * 0.22, grid * 0.12, 24), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#b8dcff',\n                roughness: 0.12,\n                metalness: 0.18,\n                transparent: true,\n                opacity: 0.7,\n            }));\n            base.position.y = grid * 0.06;\n            group.add(base);\n            // Шар жидкости\n            const liquid = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.SphereGeometry(grid * 0.16, 24, 16), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: color || '#378ADD',\n                roughness: 0.18,\n                metalness: 0.08,\n                transparent: true,\n                opacity: 0.85,\n            }));\n            liquid.position.y = grid * 0.18;\n            group.add(liquid);\n            // Горлышко\n            const neck = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.07, grid * 0.09, grid * 0.18, 16), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#e0f6ff',\n                roughness: 0.09,\n                metalness: 0.18,\n                transparent: true,\n                opacity: 0.5,\n            }));\n            neck.position.y = grid * 0.32;\n            group.add(neck);\n            // Пробка\n            const cork = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.06, grid * 0.08, grid * 0.04, 12), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#a67c52',\n                roughness: 0.7,\n                metalness: 0.05,\n            }));\n            cork.position.y = grid * 0.37;\n            group.add(cork);\n            return finalizeModel(group);\n        }\n        // Модель скважины (well)\n        if (id === 'well') {\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            // Основание\n            const base = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.22, grid * 0.25, grid * 0.12, 18), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#888',\n                roughness: 0.8,\n                metalness: 0.2,\n            }));\n            base.position.y = grid * 0.06;\n            group.add(base);\n            // Внутренняя вода\n            const water = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.16, grid * 0.18, grid * 0.06, 18), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#378ADD',\n                roughness: 0.18,\n                metalness: 0.08,\n                transparent: true,\n                opacity: 0.7,\n            }));\n            water.position.y = grid * 0.09;\n            group.add(water);\n            // Стойка\n            const pole = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.03, grid * 0.04, grid * 0.32, 10), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#a67c52',\n                roughness: 0.6,\n                metalness: 0.1,\n            }));\n            pole.position.y = grid * 0.22;\n            group.add(pole);\n            // Ручка\n            const handle = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.TorusGeometry(grid * 0.09, grid * 0.012, 8, 16, Math.PI), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#b8b8b8',\n                roughness: 0.4,\n                metalness: 0.3,\n            }));\n            handle.name = 'MinerTopRotor';\n            handle.position.set(0, grid * 0.36, grid * 0.09);\n            handle.rotation.x = Math.PI / 2;\n            handle.userData.minerHitBaseY = handle.position.y;\n            handle.userData.minerHitAmplitude = grid * 0.1;\n            group.add(handle);\n            return finalizeModel(group);\n        }\n        // Модель лесопилки (lumberjack)\n        if (id === 'lumberjack') {\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            // Основание\n            const base = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.7, grid * 0.12, grid * 0.7), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#7a4d2a',\n                roughness: 0.8,\n                metalness: 0.1,\n            }));\n            base.position.y = grid * 0.06;\n            group.add(base);\n            // Пила\n            const saw = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.13, grid * 0.13, grid * 0.04, 24), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#cccccc',\n                roughness: 0.3,\n                metalness: 0.7,\n                emissive: '#888',\n            }));\n            saw.name = 'MinerTopRotor';\n            saw.position.set(0, grid * 0.14, 0);\n            saw.rotation.x = Math.PI / 2;\n            saw.userData.minerHitBaseY = saw.position.y;\n            saw.userData.minerHitAmplitude = grid * 0.08;\n            group.add(saw);\n            // Дерево-бревно\n            const log = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.07, grid * 0.09, grid * 0.38, 14), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#ba8b5a',\n                roughness: 0.7,\n                metalness: 0.1,\n            }));\n            log.position.set(grid * 0.18, grid * 0.13, 0);\n            log.rotation.z = Math.PI / 2;\n            group.add(log);\n            return finalizeModel(group);\n        }\n        if (id === 'resource') {\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            const rockMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: color || '#7f7f7f',\n                roughness: 0.92,\n                metalness: 0.08,\n            });\n            const chunks = [\n                { x: 0, y: grid * 0.15, z: 0, sx: 0.24, sy: 0.3, sz: 0.22 },\n                {\n                    x: grid * 0.16,\n                    y: grid * 0.1,\n                    z: grid * 0.05,\n                    sx: 0.17,\n                    sy: 0.2,\n                    sz: 0.16,\n                },\n                {\n                    x: -grid * 0.12,\n                    y: grid * 0.09,\n                    z: -grid * 0.08,\n                    sx: 0.15,\n                    sy: 0.18,\n                    sz: 0.14,\n                },\n            ];\n            chunks.forEach(chunk => {\n                const rock = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.DodecahedronGeometry(grid * 0.35, 0), rockMat);\n                rock.scale.set(chunk.sx, chunk.sy, chunk.sz);\n                rock.position.set(chunk.x, chunk.y, chunk.z);\n                group.add(rock);\n            });\n            return finalizeModel(group);\n        }\n        if (id === 'liquid') {\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            const puddleColor = color || '#378ADD';\n            const puddleCore = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.34, grid * 0.37, grid * 0.04, 28), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: puddleColor,\n                roughness: 0.22,\n                metalness: 0.04,\n                transparent: true,\n                opacity: 0.9,\n            }));\n            puddleCore.position.y = grid * 0.02;\n            group.add(puddleCore);\n            const puddleShine = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.22, grid * 0.24, grid * 0.012, 24), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#b8dcff',\n                roughness: 0.08,\n                metalness: 0.1,\n                transparent: true,\n                opacity: 0.5,\n            }));\n            puddleShine.position.set(grid * 0.04, grid * 0.045, -grid * 0.03);\n            group.add(puddleShine);\n            const rimStoneMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#6e6a66',\n                roughness: 0.93,\n                metalness: 0.05,\n            });\n            rimStoneMat.userData.protectColor = true;\n            const stoneCount = 14;\n            for (let i = 0; i < stoneCount; i++) {\n                const angle = (Math.PI * 2 * i) / stoneCount;\n                const radius = grid * (0.42 + (i % 3) * 0.012);\n                const stone = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.DodecahedronGeometry(grid * 0.11, 0), rimStoneMat);\n                const sx = 0.45 + ((i * 7) % 4) * 0.08 * 3;\n                const sy = 0.34 + ((i * 5) % 3) * 0.09 * 3;\n                const sz = 0.42 + ((i * 3) % 4) * 0.07 * 3;\n                stone.scale.set(sx, sy, sz);\n                stone.position.set(Math.cos(angle) * radius, grid * (0.03 + (i % 2) * 0.01), Math.sin(angle) * radius);\n                stone.rotation.set(((i * 13) % 10) * 0.08, angle + ((i * 3) % 5) * 0.1, ((i * 11) % 8) * 0.07);\n                group.add(stone);\n            }\n            return finalizeModel(group);\n        }\n        if (id === 'wood') {\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            const bark = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: color || '#7a4d2a',\n                roughness: 0.9,\n                metalness: 0.05,\n            });\n            const core = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#ba8b5a',\n                roughness: 0.88,\n                metalness: 0.02,\n            });\n            const log = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.11, grid * 0.13, grid * 0.62, 14), bark);\n            log.rotation.z = Math.PI / 2;\n            log.position.y = grid * 0.12;\n            group.add(log);\n            for (const side of [-1, 1]) {\n                const cut = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.1, grid * 0.1, grid * 0.02, 14), core);\n                cut.rotation.z = Math.PI / 2;\n                cut.position.set(side * grid * 0.31, grid * 0.12, 0);\n                group.add(cut);\n            }\n            return finalizeModel(group);\n        }\n        if (id === 'woodResource') {\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            const trunkMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#6d4a2e',\n                roughness: 0.93,\n                metalness: 0.03,\n            });\n            const treeOffsets = [\n                { x: 0, z: 0, h: 0.7 },\n                { x: grid * 0.2, z: grid * 0.08, h: 0.62 },\n                { x: -grid * 0.22, z: grid * 0.12, h: 0.58 },\n                { x: grid * 0.12, z: -grid * 0.18, h: 0.55 },\n                { x: -grid * 0.14, z: -grid * 0.2, h: 0.6 },\n                { x: grid * 0.28, z: -grid * 0.06, h: 0.5 },\n            ];\n            treeOffsets.forEach((tree, index) => {\n                const trunkHeight = grid * tree.h;\n                const trunkRadius = grid * (0.032 + (index % 3) * 0.004);\n                const trunk = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(trunkRadius * 0.85, trunkRadius, trunkHeight, 10), trunkMat);\n                trunk.position.set(tree.x, trunkHeight * 0.5, tree.z);\n                group.add(trunk);\n                const branchCount = 3 + (index % 2);\n                for (let i = 0; i < branchCount; i++) {\n                    const angle = (Math.PI * 2 * i) / branchCount + index * 0.35;\n                    const length = grid * (0.17 + i * 0.02);\n                    const branch = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(trunkRadius * 0.28, trunkRadius * 0.42, length, 8), trunkMat);\n                    branch.position.set(tree.x + Math.cos(angle) * grid * 0.06, trunkHeight * (0.58 + i * 0.1), tree.z + Math.sin(angle) * grid * 0.06);\n                    branch.rotation.z = Math.PI / 2.7;\n                    branch.rotation.y = angle;\n                    group.add(branch);\n                }\n            });\n            return finalizeModel(group);\n        }\n        if (id === 'coal') {\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            const coalMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: color || '#1e1e1e',\n                roughness: 0.95,\n                metalness: 0.05,\n            });\n            const shinyMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#3a3a3a',\n                roughness: 0.4,\n                metalness: 0.25,\n            });\n            const chunks = [\n                {\n                    x: 0,\n                    y: grid * 0.13,\n                    z: 0,\n                    sx: 0.22,\n                    sy: 0.26,\n                    sz: 0.2,\n                    shiny: false,\n                },\n                {\n                    x: grid * 0.14,\n                    y: grid * 0.09,\n                    z: grid * 0.06,\n                    sx: 0.15,\n                    sy: 0.18,\n                    sz: 0.14,\n                    shiny: true,\n                },\n                {\n                    x: -grid * 0.1,\n                    y: grid * 0.08,\n                    z: -grid * 0.07,\n                    sx: 0.13,\n                    sy: 0.16,\n                    sz: 0.12,\n                    shiny: false,\n                },\n                {\n                    x: grid * 0.05,\n                    y: grid * 0.05,\n                    z: -grid * 0.14,\n                    sx: 0.1,\n                    sy: 0.12,\n                    sz: 0.1,\n                    shiny: true,\n                },\n            ];\n            chunks.forEach(chunk => {\n                const geo = new three__WEBPACK_IMPORTED_MODULE_9__.DodecahedronGeometry(grid * chunk.sx, 0);\n                const mesh = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(geo, chunk.shiny ? shinyMat : coalMat);\n                mesh.scale.set(1, chunk.sy / chunk.sx, chunk.sz / chunk.sx);\n                mesh.position.set(chunk.x, chunk.y, chunk.z);\n                mesh.rotation.set(Math.random() * Math.PI, Math.random() * Math.PI, Math.random() * Math.PI);\n                group.add(mesh);\n            });\n            return finalizeModel(group);\n        }\n        if (id === 'crystal') {\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            const crystalMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: color || '#7de8e8',\n                roughness: 0.08,\n                metalness: 0.1,\n                transparent: true,\n                opacity: 0.82,\n            });\n            const crystalCoreMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#c0f8f8',\n                roughness: 0.05,\n                metalness: 0.2,\n                transparent: true,\n                opacity: 0.55,\n            });\n            const spikes = [\n                { x: 0, z: 0, h: 0.48, r: 0.07, angle: 0 },\n                { x: grid * 0.12, z: grid * 0.05, h: 0.36, r: 0.055, angle: 0.3 },\n                { x: -grid * 0.1, z: grid * 0.08, h: 0.32, r: 0.05, angle: -0.25 },\n                { x: grid * 0.06, z: -grid * 0.13, h: 0.28, r: 0.045, angle: 0.15 },\n                { x: -grid * 0.13, z: -grid * 0.06, h: 0.3, r: 0.048, angle: -0.4 },\n            ];\n            spikes.forEach((spike, i) => {\n                const geo = new three__WEBPACK_IMPORTED_MODULE_9__.ConeGeometry(grid * spike.r, grid * spike.h, 6);\n                const mesh = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(geo, i === 0 ? crystalCoreMat : crystalMat);\n                mesh.position.set(spike.x, grid * spike.h * 0.5, spike.z);\n                mesh.rotation.set(spike.angle, (i * Math.PI * 2) / spikes.length, 0);\n                group.add(mesh);\n            });\n            return finalizeModel(group);\n        }\n        if (id === 'conveyor') {\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            const base = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid, 8, grid), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#4f555d',\n                metalness: 0.3,\n                roughness: 0.72,\n            }));\n            base.position.y = 4;\n            group.add(base);\n            const arrowMaterial = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#d3a44f',\n                emissive: '#5f3f0e',\n                emissiveIntensity: 0.3,\n                metalness: 0.35,\n                roughness: 0.45,\n            });\n            const arrowCount = 4;\n            const arrowTravel = this.conveyorArrowTravel;\n            const arrowStep = arrowTravel / arrowCount;\n            const arrowWingLength = grid * 0.24;\n            const arrowWingWidth = grid * 0.05;\n            const arrowWingHeight = 2;\n            const arrowHalfSpread = grid * 0.12;\n            for (let i = 0; i < arrowCount; i++) {\n                const arrow = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n                arrow.userData.conveyorArrowBaseZ =\n                    -arrowTravel * 0.5 + arrowStep * i + arrowStep * 0.5;\n                arrow.position.y = 11.4;\n                const leftWing = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(arrowWingWidth, arrowWingHeight, arrowWingLength), arrowMaterial);\n                leftWing.position.x = -arrowHalfSpread;\n                leftWing.rotation.y = Math.PI / 4;\n                arrow.add(leftWing);\n                const rightWing = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(arrowWingWidth, arrowWingHeight, arrowWingLength), arrowMaterial);\n                rightWing.position.x = arrowHalfSpread;\n                rightWing.rotation.y = -Math.PI / 4;\n                arrow.add(rightWing);\n                group.add(arrow);\n            }\n            group.userData.conveyorArrowTravel = arrowTravel;\n            return finalizeModel(group);\n        }\n        if (id === 'underground_conveyor') {\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            const frame = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.96, 10, grid * 0.96), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#50545f',\n                metalness: 0.28,\n                roughness: 0.72,\n            }));\n            frame.position.y = 5;\n            group.add(frame);\n            const shaft = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.62, 14, grid * 0.82), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#22262c',\n                metalness: 0.12,\n                roughness: 0.88,\n            }));\n            shaft.position.y = 6;\n            group.add(shaft);\n            // const chute = new THREE.Mesh(\n            //   new THREE.BoxGeometry(grid * 0.5, 8, grid * 0.64),\n            //   new THREE.MeshStandardMaterial({\n            //     color: '#b08b52',\n            //     emissive: '#3a280f',\n            //     emissiveIntensity: 0.22,\n            //     metalness: 0.22,\n            //     roughness: 0.52,\n            //   })\n            // );\n            // chute.position.set(0, 10, grid * 0.06);\n            // chute.rotation.x = -Math.PI / 4.2;\n            // group.add(chute);\n            const sideWallMaterial = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#6f5a3a',\n                roughness: 0.66,\n                metalness: 0.16,\n            });\n            const leftWall = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.05, 16, grid * 0.58), sideWallMaterial);\n            leftWall.position.set(-grid * 0.23, 11, grid * 0.1);\n            leftWall.rotation.x = -Math.PI / 4.2;\n            group.add(leftWall);\n            const rightWall = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.05, 16, grid * 0.58), sideWallMaterial);\n            rightWall.position.set(grid * 0.23, 11, grid * 0.1);\n            rightWall.rotation.x = -Math.PI / 4.2;\n            group.add(rightWall);\n            const mouthLip = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.54, 5, grid * 0.06), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#a77e44',\n                metalness: 0.36,\n                roughness: 0.48,\n            }));\n            mouthLip.position.set(0, 4, grid * 0.34);\n            group.add(mouthLip);\n            const descentArrow = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.ConeGeometry(grid * 0.055, grid * 0.16, 4), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#d7b066',\n                emissive: '#5a3d12',\n                emissiveIntensity: 0.35,\n                roughness: 0.4,\n                metalness: 0.45,\n            }));\n            descentArrow.rotation.x = Math.PI;\n            descentArrow.position.set(0, 16, grid * 0.28);\n            group.add(descentArrow);\n            const indicatorPivot = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            indicatorPivot.name = 'UndergroundDirectionIndicator';\n            indicatorPivot.position.set(0, 22, 0);\n            const indicatorArrow = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            indicatorArrow.name = 'UndergroundDirectionIndicatorArrow';\n            const shaftMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#6fd6ff',\n                emissive: '#1c5f7d',\n                emissiveIntensity: 0.45,\n                roughness: 0.38,\n                metalness: 0.2,\n            });\n            const directionShaft = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.07, 3.5, grid * 0.2), shaftMat);\n            directionShaft.position.set(0, 0, -grid * 0.07);\n            indicatorArrow.add(directionShaft);\n            const head = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.ConeGeometry(grid * 0.065, grid * 0.15, 6), shaftMat);\n            head.rotation.x = -Math.PI / 2;\n            head.position.set(0, 0, grid * 0.07);\n            indicatorArrow.add(head);\n            indicatorPivot.add(indicatorArrow);\n            const indicatorCross = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            indicatorCross.name = 'UndergroundDirectionIndicatorCross';\n            const crossMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#ff7a7a',\n                emissive: '#7d1f1f',\n                emissiveIntensity: 0.5,\n                roughness: 0.45,\n                metalness: 0.15,\n            });\n            const crossBarA = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.2, 3.5, grid * 0.05), crossMat);\n            crossBarA.rotation.y = Math.PI / 4;\n            indicatorCross.add(crossBarA);\n            const crossBarB = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.2, 3.5, grid * 0.05), crossMat);\n            crossBarB.rotation.y = -Math.PI / 4;\n            indicatorCross.add(crossBarB);\n            indicatorPivot.add(indicatorCross);\n            indicatorArrow.visible = false;\n            indicatorCross.visible = true;\n            group.add(indicatorPivot);\n            return finalizeModel(group);\n        }\n        if (id === 'chest') {\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            // Keep the model inside one cell: make body slightly smaller while preserving lid size.\n            const lidWidth = grid * 0.68;\n            const lidDepth = grid * 0.5;\n            const bodyWidth = grid * 0.62;\n            const bodyHeight = grid * 0.2;\n            const bodyDepth = grid * 0.44;\n            const lidHeight = grid * 0.14;\n            const woodMaterial = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#7b4f26',\n                roughness: 0.86,\n                metalness: 0.08,\n            });\n            const woodDarkMaterial = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#593516',\n                roughness: 0.9,\n                metalness: 0.05,\n            });\n            const metalMaterial = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#c9a24b',\n                roughness: 0.35,\n                metalness: 0.72,\n            });\n            const body = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(bodyWidth, bodyHeight, bodyDepth), woodMaterial);\n            body.position.y = bodyHeight * 0.5;\n            group.add(body);\n            const lid = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(lidWidth, lidHeight, lidDepth), woodDarkMaterial);\n            lid.position.y = bodyHeight + lidHeight * 0.5 + grid * 0.005;\n            group.add(lid);\n            const ribSize = grid * 0.045;\n            const ribHeight = bodyHeight + lidHeight * 0.9;\n            const ribOffsets = [\n                { x: bodyWidth * 0.5 - ribSize * 0.5, z: 0 },\n                { x: -bodyWidth * 0.5 + ribSize * 0.5, z: 0 },\n                { x: 0, z: bodyDepth * 0.5 - ribSize * 0.5 },\n                { x: 0, z: -bodyDepth * 0.5 + ribSize * 0.5 },\n            ];\n            ribOffsets.forEach(offset => {\n                const rib = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(offset.x === 0 ? bodyWidth * 0.92 : ribSize, ribHeight, offset.z === 0 ? bodyDepth * 0.92 : ribSize), metalMaterial);\n                rib.position.set(offset.x, ribHeight * 0.5, offset.z);\n                group.add(rib);\n            });\n            const lockPlate = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.12, grid * 0.14, grid * 0.03), metalMaterial);\n            lockPlate.position.set(0, bodyHeight * 0.62, lidDepth * 0.5 + grid * 0.01);\n            group.add(lockPlate);\n            const lockCore = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.018, grid * 0.018, grid * 0.035, 12), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#5f3a12',\n                roughness: 0.55,\n                metalness: 0.15,\n            }));\n            lockCore.rotation.x = Math.PI / 2;\n            lockCore.position.set(0, bodyHeight * 0.62, lidDepth * 0.5 + grid * 0.03);\n            group.add(lockCore);\n            return finalizeModel(group);\n        }\n        if (id === 'miner') {\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            const base = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.92, grid * 0.12, grid * 0.92), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#5d5147',\n                metalness: 0.18,\n                roughness: 0.82,\n            }));\n            base.position.y = grid * 0.06;\n            group.add(base);\n            const tower = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.26, grid * 0.52, grid * 0.26), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#8d744f',\n                metalness: 0.28,\n                roughness: 0.58,\n            }));\n            tower.position.set(0, grid * 0.32, 0);\n            group.add(tower);\n            const drill = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.06, grid * 0.12, grid * 0.5, 12), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#c99a45',\n                metalness: 0.55,\n                roughness: 0.34,\n            }));\n            drill.position.set(0, grid * 0.13, grid * 0.18);\n            drill.rotation.x = Math.PI / 2;\n            group.add(drill);\n            const cap = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.17, grid * 0.2, grid * 0.09, 18), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#4e4a46',\n                metalness: 0.44,\n                roughness: 0.42,\n            }));\n            cap.name = 'MinerTopRotor';\n            cap.position.y = grid * 0.6;\n            cap.userData.minerHitBaseY = cap.position.y;\n            cap.userData.minerHitAmplitude = grid * 0.12;\n            group.add(cap);\n            const oreGlow = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.18, grid * 0.08, grid * 0.18), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#d9683a',\n                emissive: '#7a2d12',\n                emissiveIntensity: 0.5,\n                metalness: 0.15,\n                roughness: 0.7,\n            }));\n            oreGlow.position.set(0, grid * 0.08, -grid * 0.16);\n            group.add(oreGlow);\n            return finalizeModel(group);\n        }\n        if (id === 'smelter') {\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            const hearth = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.9, grid * 0.22, grid * 0.82), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#5a4a42',\n                metalness: 0.14,\n                roughness: 0.88,\n            }));\n            hearth.position.y = grid * 0.11;\n            group.add(hearth);\n            const furnaceBody = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.66, grid * 0.42, grid * 0.58), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#78655a',\n                metalness: 0.2,\n                roughness: 0.76,\n            }));\n            furnaceBody.position.set(0, grid * 0.34, -grid * 0.04);\n            group.add(furnaceBody);\n            const chimney = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.11, grid * 0.14, grid * 0.46, 18), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#60544d',\n                metalness: 0.18,\n                roughness: 0.82,\n            }));\n            chimney.name = 'SmelterChimney';\n            chimney.position.set(grid * 0.18, grid * 0.58, -grid * 0.14);\n            group.add(chimney);\n            const mouth = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.24, grid * 0.16, grid * 0.04), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#ffb25a',\n                emissive: '#b34a12',\n                emissiveIntensity: 1.0,\n                metalness: 0.05,\n                roughness: 0.45,\n            }));\n            mouth.name = 'SmelterWindow';\n            mouth.position.set(0, grid * 0.28, grid * 0.26);\n            group.add(mouth);\n            const sidePipe = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.045, grid * 0.045, grid * 0.42, 12), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#9a7a52',\n                metalness: 0.48,\n                roughness: 0.34,\n            }));\n            sidePipe.rotation.z = Math.PI / 2;\n            sidePipe.position.set(-grid * 0.18, grid * 0.4, -grid * 0.08);\n            group.add(sidePipe);\n            for (let i = 0; i < 3; i++) {\n                const smoke = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.SphereGeometry(grid * 0.08, 10, 10), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                    color: '#8a8a8a',\n                    transparent: true,\n                    opacity: 0,\n                    depthWrite: false,\n                    roughness: 1,\n                    metalness: 0,\n                }));\n                smoke.name = `SmelterSmoke${i}`;\n                smoke.position.set(grid * 0.18, grid * (0.78 + i * 0.06), -grid * 0.14);\n                smoke.userData.smokeBaseY = smoke.position.y;\n                smoke.userData.smokeBaseScale = 0.8 + i * 0.14;\n                smoke.userData.smokePhase = i / 3;\n                smoke.scale.setScalar(smoke.userData.smokeBaseScale);\n                group.add(smoke);\n            }\n            return finalizeModel(group);\n        }\n        if (id === 'fabric') {\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            const floor = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.94, grid * 0.12, grid * 0.94), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#4e5660',\n                metalness: 0.22,\n                roughness: 0.78,\n            }));\n            floor.position.y = grid * 0.06;\n            group.add(floor);\n            const hall = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.78, grid * 0.34, grid * 0.56), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#798592',\n                metalness: 0.3,\n                roughness: 0.55,\n            }));\n            hall.position.set(0, grid * 0.29, 0);\n            group.add(hall);\n            const beltHousing = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.72, grid * 0.12, grid * 0.16), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#2f3338',\n                metalness: 0.42,\n                roughness: 0.48,\n            }));\n            beltHousing.position.set(0, grid * 0.18, grid * 0.24);\n            group.add(beltHousing);\n            const stackOffsets = [-grid * 0.22, 0, grid * 0.22];\n            stackOffsets.forEach(offsetX => {\n                const stack = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.06, grid * 0.075, grid * 0.34, 14), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                    color: '#b98a46',\n                    metalness: 0.55,\n                    roughness: 0.3,\n                }));\n                stack.name = `FabricStack${offsetX}`;\n                stack.position.set(offsetX, grid * 0.35, grid * 0.22);\n                group.add(stack);\n                for (let i = 0; i < 2; i++) {\n                    const smoke = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.SphereGeometry(grid * 0.06, 10, 10), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                        color: '#909090',\n                        transparent: true,\n                        opacity: 0,\n                        depthWrite: false,\n                        roughness: 1,\n                        metalness: 0,\n                    }));\n                    smoke.name = `FabricSmoke${Math.round(offsetX)}_${i}`;\n                    smoke.position.set(offsetX, grid * (0.52 + i * 0.06), grid * 0.22);\n                    smoke.userData.smokeBaseY = smoke.position.y;\n                    smoke.userData.smokeBaseScale = 0.65 + i * 0.15;\n                    smoke.userData.smokePhase = (Math.round(offsetX) + i * 0.5) * 0.17;\n                    smoke.scale.setScalar(smoke.userData.smokeBaseScale);\n                    group.add(smoke);\n                }\n            });\n            const windowStrip = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.52, grid * 0.1, grid * 0.03), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#8de0ff',\n                emissive: '#1c6b89',\n                emissiveIntensity: 0.7,\n                metalness: 0.1,\n                roughness: 0.25,\n            }));\n            windowStrip.name = 'FabricWindow';\n            windowStrip.position.set(0, grid * 0.34, grid * 0.29);\n            group.add(windowStrip);\n            return finalizeModel(group);\n        }\n        if (id === 'manipulator') {\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const platformHeight = 14;\n            const mastHeight = grid * 0.85;\n            const mastRadius = grid * 0.05;\n            const railLength = grid;\n            const hangerHeight = mastHeight * 0.5;\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            const platform = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid, platformHeight, grid), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#5f5f63',\n                metalness: 0.3,\n                roughness: 0.7,\n            }));\n            platform.position.y = platformHeight * 0.5;\n            group.add(platform);\n            const mast = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(mastRadius, mastRadius * 1.2, mastHeight, 24), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#d4922f',\n                metalness: 0.25,\n                roughness: 0.65,\n            }));\n            mast.position.y = platformHeight + mastHeight * 0.5;\n            group.add(mast);\n            const armPivot = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            armPivot.name = 'ManipulatorArmPivot';\n            armPivot.position.y = platformHeight + mastHeight;\n            group.add(armPivot);\n            const rail = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.09, grid * 0.08, railLength), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#b8812a',\n                metalness: 0.2,\n                roughness: 0.7,\n            }));\n            rail.position.z = railLength * 0.5;\n            armPivot.add(rail);\n            const hanger = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.03, grid * 0.03, hangerHeight, 16), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#e2b661',\n                metalness: 0.2,\n                roughness: 0.7,\n            }));\n            hanger.position.set(0, -hangerHeight * 0.5, railLength);\n            armPivot.add(hanger);\n            const hookBase = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.TorusGeometry(grid * 0.06, grid * 0.012, 12, 20), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#f4c065',\n                metalness: 0.35,\n                roughness: 0.5,\n            }));\n            hookBase.rotation.x = Math.PI / 2;\n            hookBase.position.set(0, -hangerHeight, railLength);\n            armPivot.add(hookBase);\n            const claw = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.05, grid * 0.14, grid * 0.04), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#ecb85f',\n                metalness: 0.2,\n                roughness: 0.7,\n            }));\n            claw.position.set(0, -hangerHeight - grid * 0.07, railLength);\n            armPivot.add(claw);\n            const hookAnchor = new three__WEBPACK_IMPORTED_MODULE_9__.Object3D();\n            hookAnchor.name = 'ManipulatorHook';\n            hookAnchor.position.set(0, -hangerHeight - grid * 0.14, railLength);\n            armPivot.add(hookAnchor);\n            return buildCfg &&\n                buildCfg.size &&\n                (buildCfg.size.x > 1 || buildCfg.size.y > 1)\n                ? finalizeModel(group)\n                : group;\n        }\n        if (id === 'ingot') {\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const ingotLength = grid * 0.9;\n            const ingotWidth = grid * 0.52;\n            const ingotHeight = grid * 0.28;\n            const topLength = ingotLength * 0.62;\n            const shape = new three__WEBPACK_IMPORTED_MODULE_9__.Shape();\n            shape.moveTo(-ingotLength / 2, 0);\n            shape.lineTo(ingotLength / 2, 0);\n            shape.lineTo(topLength / 2, ingotHeight);\n            shape.lineTo(-topLength / 2, ingotHeight);\n            shape.closePath();\n            const geometry = new three__WEBPACK_IMPORTED_MODULE_9__.ExtrudeGeometry(shape, {\n                depth: ingotWidth,\n                bevelEnabled: false,\n            });\n            geometry.translate(0, 0, -ingotWidth / 2);\n            const material = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: color || '#d2a24c',\n                metalness: 0.55,\n                roughness: 0.35,\n            });\n            const mesh = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(geometry, material);\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            group.add(mesh);\n            return finalizeModel(group);\n        }\n        if (id === 'laboratory') {\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            // Base platform\n            const platformH = grid * 0.18;\n            const platform = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.9, platformH, grid * 0.9), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#2e2e52',\n                metalness: 0.35,\n                roughness: 0.65,\n            }));\n            platform.position.y = platformH * 0.5;\n            group.add(platform);\n            // Corner pillars\n            const pillarH = grid * 0.55;\n            const pillarR = grid * 0.04;\n            const pillarMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#5050a0',\n                metalness: 0.5,\n                roughness: 0.45,\n            });\n            const pillarOffsets = [\n                { x: grid * 0.33, z: grid * 0.33 },\n                { x: -grid * 0.33, z: grid * 0.33 },\n                { x: grid * 0.33, z: -grid * 0.33 },\n                { x: -grid * 0.33, z: -grid * 0.33 },\n            ];\n            for (const off of pillarOffsets) {\n                const pillar = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(pillarR, pillarR * 1.2, pillarH, 12), pillarMat);\n                pillar.position.set(off.x, platformH + pillarH * 0.5, off.z);\n                group.add(pillar);\n            }\n            // Ring connecting pillars at top\n            const ringY = platformH + pillarH;\n            const ringGeo = new three__WEBPACK_IMPORTED_MODULE_9__.TorusGeometry(grid * 0.35, grid * 0.025, 8, 32);\n            const ringMesh = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(ringGeo, new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#7070c8',\n                metalness: 0.6,\n                roughness: 0.35,\n            }));\n            ringMesh.rotation.x = Math.PI / 2;\n            ringMesh.position.y = ringY;\n            group.add(ringMesh);\n            // Dome (hemisphere)\n            const domeR = grid * 0.38;\n            const domeMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#5566ee',\n                metalness: 0.15,\n                roughness: 0.25,\n                transparent: true,\n                opacity: 0.75,\n            });\n            const dome = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.SphereGeometry(domeR, 32, 16, 0, Math.PI * 2, 0, Math.PI / 2), domeMat);\n            dome.position.y = ringY;\n            group.add(dome);\n            // Glowing central orb\n            const orb = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.SphereGeometry(grid * 0.07, 16, 16), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#aabbff',\n                emissive: '#3355ff',\n                emissiveIntensity: 1.2,\n            }));\n            orb.name = 'LaboratoryCoreOrb';\n            orb.position.y = ringY + domeR * 0.45;\n            group.add(orb);\n            return finalizeModel(group);\n        }\n        if (id === 'hub') {\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            const base = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.48, grid * 0.54, grid * 0.24, 28), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#4b5266',\n                metalness: 0.35,\n                roughness: 0.62,\n            }));\n            base.position.y = grid * 0.12;\n            group.add(base);\n            const spire = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.22, grid * 0.27, grid * 2.4, 28), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#7aa2e0',\n                emissive: '#1f3f80',\n                emissiveIntensity: 0.62,\n                metalness: 0.25,\n                roughness: 0.45,\n            }));\n            spire.position.y = grid * 1.35;\n            group.add(spire);\n            const crown = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.42, grid * 0.32, grid * 0.34, 24), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#94b7ff',\n                emissive: '#315ec2',\n                emissiveIntensity: 0.72,\n                metalness: 0.35,\n                roughness: 0.38,\n            }));\n            crown.position.y = grid * 2.63;\n            group.add(crown);\n            const antennaOffsets = [\n                { x: grid * 0.27, z: grid * 0.27 },\n                { x: -grid * 0.27, z: grid * 0.27 },\n                { x: grid * 0.27, z: -grid * 0.27 },\n                { x: -grid * 0.27, z: -grid * 0.27 },\n            ];\n            for (const off of antennaOffsets) {\n                const antenna = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.05, grid * 2.2, grid * 0.05), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                    color: '#8f9ab2',\n                    metalness: 0.4,\n                    roughness: 0.45,\n                }));\n                antenna.position.set(off.x, grid * 1.25, off.z);\n                group.add(antenna);\n            }\n            const apex = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.SphereGeometry(grid * 0.16, 20, 16), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#d8e7ff',\n                emissive: '#5f8dff',\n                emissiveIntensity: 1.1,\n                metalness: 0.25,\n                roughness: 0.2,\n            }));\n            apex.position.y = grid * 2.96;\n            group.add(apex);\n            return finalizeModel(group);\n        }\n        if (id === 'turret') {\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            const platform = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.34, grid * 0.38, grid * 0.12, 20), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#40444f',\n                metalness: 0.4,\n                roughness: 0.62,\n            }));\n            platform.position.y = grid * 0.06;\n            group.add(platform);\n            const body = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.42, grid * 0.22, grid * 0.32), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#69707e',\n                metalness: 0.35,\n                roughness: 0.5,\n            }));\n            body.position.y = grid * 0.18;\n            group.add(body);\n            const barrel = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.04, grid * 0.04, grid * 0.42, 18), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#9f7f4f',\n                metalness: 0.5,\n                roughness: 0.32,\n            }));\n            barrel.rotation.x = Math.PI / 2;\n            barrel.position.set(0, grid * 0.2, grid * 0.34);\n            group.add(barrel);\n            const sensor = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.SphereGeometry(grid * 0.05, 16, 16), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#f2d087',\n                emissive: '#7d4d00',\n                emissiveIntensity: 0.65,\n                roughness: 0.35,\n                metalness: 0.2,\n            }));\n            sensor.position.set(0, grid * 0.24, grid * 0.12);\n            group.add(sensor);\n            return finalizeModel(group);\n        }\n        if (id === 'automaton_factory') {\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            const floor = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.94, grid * 0.12, grid * 0.94), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#3f4d44',\n                roughness: 0.75,\n                metalness: 0.22,\n            }));\n            floor.position.y = grid * 0.06;\n            group.add(floor);\n            const chamber = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.58, grid * 0.46, grid * 0.46), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#7f8f83',\n                roughness: 0.58,\n                metalness: 0.35,\n            }));\n            chamber.position.y = grid * 0.3;\n            group.add(chamber);\n            const glass = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.34, grid * 0.24, grid * 0.34), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#81d9be',\n                emissive: '#1f7a66',\n                emissiveIntensity: 0.45,\n                roughness: 0.08,\n                metalness: 0.2,\n                transparent: true,\n                opacity: 0.62,\n            }));\n            glass.position.y = grid * 0.33;\n            group.add(glass);\n            const pipe = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.TorusGeometry(grid * 0.2, grid * 0.03, 10, 26), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#556f66',\n                roughness: 0.45,\n                metalness: 0.44,\n            }));\n            pipe.rotation.x = Math.PI / 2;\n            pipe.position.y = grid * 0.44;\n            group.add(pipe);\n            return finalizeModel(group);\n        }\n        if (id === 'scout') {\n            // Quadcopter hovering in the air\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            const frameMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#3a4a5c',\n                metalness: 0.5,\n                roughness: 0.45,\n            });\n            const rotorMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#8fb0d0',\n                metalness: 0.3,\n                roughness: 0.55,\n            });\n            const bodyMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#2e3a4a',\n                metalness: 0.4,\n                roughness: 0.5,\n            });\n            const sensorMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#e0403a',\n                emissive: '#a01010',\n                emissiveIntensity: 0.8,\n            });\n            // Central body\n            const body = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.28, grid * 0.08, grid * 0.28), bodyMat);\n            body.position.y = 0;\n            group.add(body);\n            // Sensor dome on top\n            const sensor = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.SphereGeometry(grid * 0.055, 12, 8), sensorMat);\n            sensor.position.y = grid * 0.07;\n            group.add(sensor);\n            // 4 arms + rotors\n            const armOffsets = [\n                { x: grid * 0.22, z: grid * 0.22 },\n                { x: -grid * 0.22, z: grid * 0.22 },\n                { x: grid * 0.22, z: -grid * 0.22 },\n                { x: -grid * 0.22, z: -grid * 0.22 },\n            ];\n            for (const off of armOffsets) {\n                // arm\n                const arm = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.05, grid * 0.025, Math.hypot(off.x, off.z) * 2), frameMat);\n                arm.position.set(off.x * 0.5, 0, off.z * 0.5);\n                arm.rotation.y = Math.atan2(off.x, off.z);\n                group.add(arm);\n                // rotor disc\n                const rotor = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.1, grid * 0.1, grid * 0.012, 18), rotorMat);\n                rotor.position.set(off.x, grid * 0.025, off.z);\n                group.add(rotor);\n                // rotor hub\n                const hub = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.018, grid * 0.018, grid * 0.035, 10), frameMat);\n                hub.position.set(off.x, grid * 0.038, off.z);\n                group.add(hub);\n            }\n            // Thin landing legs\n            for (const off of armOffsets) {\n                const leg = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.012, grid * 0.012, grid * 0.18, 8), frameMat);\n                leg.position.set(off.x * 0.55, -grid * 0.08, off.z * 0.55);\n                group.add(leg);\n            }\n            return finalizeModel(group);\n        }\n        if (id === 'assault') {\n            // Creeper-like body, wheels at bottom, gun barrel instead of head\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            const bodyMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#3d6640',\n                metalness: 0.3,\n                roughness: 0.6,\n            });\n            const darkMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#253527',\n                metalness: 0.35,\n                roughness: 0.55,\n            });\n            const wheelMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#1a1a1a',\n                metalness: 0.2,\n                roughness: 0.8,\n            });\n            const rimMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#6e6e6e',\n                metalness: 0.6,\n                roughness: 0.35,\n            });\n            const gunMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#9a9a7a',\n                metalness: 0.55,\n                roughness: 0.38,\n            });\n            const eyeMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#ff2222',\n                emissive: '#cc0000',\n                emissiveIntensity: 1.0,\n            });\n            // Main torso (Creeper-like tall box)\n            const torso = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.38, grid * 0.36, grid * 0.28), bodyMat);\n            torso.position.y = grid * 0.28;\n            group.add(torso);\n            // Chest armor stripe\n            const chest = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.4, grid * 0.06, grid * 0.05), darkMat);\n            chest.position.set(0, grid * 0.28, grid * 0.145);\n            group.add(chest);\n            // 4 wheels (2 per side)\n            const wheelPositions = [\n                { x: grid * 0.22, z: grid * 0.07 },\n                { x: grid * 0.22, z: -grid * 0.07 },\n                { x: -grid * 0.22, z: grid * 0.07 },\n                { x: -grid * 0.22, z: -grid * 0.07 },\n            ];\n            for (const wp of wheelPositions) {\n                const wheel = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.1, grid * 0.1, grid * 0.06, 16), wheelMat);\n                wheel.rotation.z = Math.PI / 2;\n                wheel.position.set(wp.x, grid * 0.1, wp.z);\n                group.add(wheel);\n                const rim = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.048, grid * 0.048, grid * 0.065, 6), rimMat);\n                rim.rotation.z = Math.PI / 2;\n                rim.position.set(wp.x, grid * 0.1, wp.z);\n                group.add(rim);\n            }\n            // Gun mount (neck stump) instead of head\n            const neck = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.22, grid * 0.14, grid * 0.22), darkMat);\n            neck.position.y = grid * 0.53;\n            group.add(neck);\n            // Gun barrel (main)\n            const barrel = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.055, grid * 0.065, grid * 0.46, 14), gunMat);\n            barrel.rotation.x = Math.PI / 2;\n            barrel.position.set(0, grid * 0.56, grid * 0.28);\n            group.add(barrel);\n            // Barrel shroud\n            const shroud = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.085, grid * 0.085, grid * 0.14, 14), darkMat);\n            shroud.rotation.x = Math.PI / 2;\n            shroud.position.set(0, grid * 0.56, grid * 0.075);\n            group.add(shroud);\n            // Glowing eyes on the torso front (where head would be)\n            for (const ex of [-grid * 0.08, grid * 0.08]) {\n                const eye = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.SphereGeometry(grid * 0.028, 10, 8), eyeMat);\n                eye.position.set(ex, grid * 0.44, grid * 0.145);\n                group.add(eye);\n            }\n            return finalizeModel(group);\n        }\n        if (id === 'siege') {\n            // Large intimidating bipedal robot with huge rocket launcher\n            const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            const armorMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#4a3830',\n                metalness: 0.45,\n                roughness: 0.55,\n            });\n            const darkArmorMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#2a2018',\n                metalness: 0.5,\n                roughness: 0.5,\n            });\n            const pipeMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#7a6048',\n                metalness: 0.6,\n                roughness: 0.35,\n            });\n            const glowMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#ff6600',\n                emissive: '#cc4400',\n                emissiveIntensity: 1.2,\n            });\n            const eyeMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: '#ff2200',\n                emissive: '#ff2200',\n                emissiveIntensity: 1.5,\n            });\n            // Legs\n            const legOffsets = [\n                { x: grid * 0.14, z: 0 },\n                { x: -grid * 0.14, z: 0 },\n            ];\n            for (const lo of legOffsets) {\n                // Upper leg\n                const upperLeg = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.14, grid * 0.22, grid * 0.14), armorMat);\n                upperLeg.position.set(lo.x, grid * 0.19, 0);\n                group.add(upperLeg);\n                // Lower leg (angled out)\n                const lowerLeg = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.11, grid * 0.2, grid * 0.11), darkArmorMat);\n                lowerLeg.position.set(lo.x * 1.1, grid * 0.05, grid * 0.04);\n                group.add(lowerLeg);\n                // Foot\n                const foot = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.16, grid * 0.055, grid * 0.2), armorMat);\n                foot.position.set(lo.x * 1.1, grid * 0.028, grid * 0.05);\n                group.add(foot);\n            }\n            // Waist / pelvis\n            const pelvis = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.36, grid * 0.1, grid * 0.26), darkArmorMat);\n            pelvis.position.y = grid * 0.33;\n            group.add(pelvis);\n            // Main torso\n            const torso = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.44, grid * 0.3, grid * 0.3), armorMat);\n            torso.position.y = grid * 0.53;\n            group.add(torso);\n            // Shoulder pads\n            for (const sx of [grid * 0.28, -grid * 0.28]) {\n                const shoulder = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.12, grid * 0.13, grid * 0.24), armorMat);\n                shoulder.position.set(sx, grid * 0.62, 0);\n                group.add(shoulder);\n            }\n            // Head\n            const head = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.22, grid * 0.18, grid * 0.2), darkArmorMat);\n            head.position.y = grid * 0.77;\n            group.add(head);\n            // Eyes\n            for (const ex of [-grid * 0.065, grid * 0.065]) {\n                const eye = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.SphereGeometry(grid * 0.03, 10, 8), eyeMat);\n                eye.position.set(ex, grid * 0.78, grid * 0.1);\n                group.add(eye);\n            }\n            // Right arm (normal) — short stub\n            const armR = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.1, grid * 0.22, grid * 0.1), darkArmorMat);\n            armR.position.set(-grid * 0.29, grid * 0.52, 0);\n            group.add(armR);\n            // Left side — HUGE rocket launcher arm\n            const launchArm = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.13, grid * 0.24, grid * 0.13), armorMat);\n            launchArm.position.set(grid * 0.32, grid * 0.54, 0);\n            group.add(launchArm);\n            // Launcher body (the big box)\n            const launcherBody = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.22, grid * 0.22, grid * 0.22), darkArmorMat);\n            launcherBody.position.set(grid * 0.38, grid * 0.62, 0);\n            group.add(launcherBody);\n            // 4 rocket tubes (2x2 cluster)\n            const tubeOffsets = [\n                { y: grid * 0.06, z: grid * 0.06 },\n                { y: grid * 0.06, z: -grid * 0.06 },\n                { y: -grid * 0.06, z: grid * 0.06 },\n                { y: -grid * 0.06, z: -grid * 0.06 },\n            ];\n            for (const to of tubeOffsets) {\n                const tube = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.CylinderGeometry(grid * 0.038, grid * 0.038, grid * 0.44, 12), pipeMat);\n                tube.rotation.x = Math.PI / 2;\n                tube.position.set(grid * 0.38, grid * 0.62 + to.y, grid * 0.28 + to.z * 0.2);\n                group.add(tube);\n                // Rocket tip glow\n                const tip = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.SphereGeometry(grid * 0.022, 8, 6), glowMat);\n                tip.position.set(grid * 0.38, grid * 0.62 + to.y, grid * 0.52 + to.z * 0.2);\n                group.add(tip);\n            }\n            // Exhaust vents on torso\n            for (const vx of [-grid * 0.12, grid * 0.12]) {\n                const vent = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(grid * 0.06, grid * 0.04, grid * 0.02), darkArmorMat);\n                vent.position.set(vx, grid * 0.59, grid * 0.16);\n                group.add(vent);\n            }\n            return finalizeModel(group);\n        }\n        return finalizeModel(undefined);\n    }\n    ActionManipulator(data) {\n        const buildModel = this.buildings.get(data.buildingUid);\n        const dropModel = this.dropItems.get(data.dropUid);\n        if (!buildModel)\n            return;\n        const armPivot = buildModel.getObjectByName('ManipulatorArmPivot');\n        if (armPivot) {\n            this.manipulatorAnimations.set(data.buildingUid, {\n                elapsed: 0,\n                duration: Math.max(0.2, data.durationMS / 1000),\n                armPivot,\n            });\n        }\n        // Play manipulator custom sound on each arm movement\n        this.PlayBuildingCustomSound(data.buildingUid);\n        if (!dropModel)\n            return;\n        const start = dropModel.position.clone();\n        const target = this.SnapToGrid({ x: data.to.x, z: data.to.z });\n        const end = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(target.x, 4, target.z);\n        this.dropManipulatorAnimations.set(data.dropUid, {\n            elapsed: 0,\n            duration: Math.max(0.2, data.durationMS / 1000),\n            start,\n            end,\n            buildingUid: data.buildingUid,\n        });\n    }\n    TickManipulatorAnimations(delta) {\n        if (this.manipulatorAnimations.size === 0)\n            return;\n        this.manipulatorAnimations.forEach((animation, uid) => {\n            animation.elapsed = Math.min(animation.elapsed + delta, animation.duration);\n            const p = animation.duration > 0 ? animation.elapsed / animation.duration : 1;\n            let angle = 0;\n            if (p < 0.2) {\n                angle = 0;\n            }\n            else if (p < 0.75) {\n                const t = (p - 0.2) / 0.55;\n                angle = three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.lerp(0, Math.PI, t);\n            }\n            else if (p < 0.92) {\n                angle = Math.PI;\n            }\n            else {\n                const t = (p - 0.92) / 0.08;\n                angle = three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.lerp(Math.PI, 0, three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.clamp(t, 0, 1));\n            }\n            animation.armPivot.rotation.y = angle;\n            if (p >= 1) {\n                animation.armPivot.rotation.y = 0;\n                this.manipulatorAnimations.delete(uid);\n            }\n        });\n    }\n    TickDropManipulatorAnimations(delta) {\n        if (this.dropManipulatorAnimations.size === 0)\n            return;\n        this.dropManipulatorAnimations.forEach((animation, uid) => {\n            const model = this.dropItems.get(uid);\n            if (!model) {\n                this.dropManipulatorAnimations.delete(uid);\n                return;\n            }\n            const manipulatorModel = this.buildings.get(animation.buildingUid);\n            const hook = manipulatorModel === null || manipulatorModel === void 0 ? void 0 : manipulatorModel.getObjectByName('ManipulatorHook');\n            if (!hook) {\n                this.dropManipulatorAnimations.delete(uid);\n                return;\n            }\n            animation.elapsed = Math.min(animation.elapsed + delta, animation.duration);\n            const p = animation.duration > 0 ? animation.elapsed / animation.duration : 1;\n            const hookWorldPos = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3();\n            hook.getWorldPosition(hookWorldPos);\n            // 0-20%: pull the item to the hook\n            // 20-75%: keep item glued to hook while arm rotates\n            // 75-92%: release and lower to target cell\n            // 92-100%: settle on target\n            if (p < 0.2) {\n                const t = p / 0.2;\n                model.position.lerpVectors(animation.start, hookWorldPos, t);\n            }\n            else if (p < 0.75) {\n                model.position.copy(hookWorldPos);\n            }\n            else if (p < 0.92) {\n                const t = (p - 0.75) / 0.17;\n                model.position.lerpVectors(hookWorldPos, animation.end, t);\n            }\n            else {\n                model.position.copy(animation.end);\n            }\n            if (p >= 1) {\n                model.position.copy(animation.end);\n                this.dropManipulatorAnimations.delete(uid);\n            }\n        });\n    }\n    GeneratePreviewImages() {\n        return __awaiter(this, void 0, void 0, function* () {\n            const renderer = new three__WEBPACK_IMPORTED_MODULE_11__.WebGLRenderer({ antialias: true, alpha: true });\n            try {\n                for (const build of _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds) {\n                    if (!build.model)\n                        continue;\n                    const blob = yield this.renderModelToWebM(this.GetModel(build.model), undefined, renderer);\n                    if (blob)\n                        this.imgObjects.set(build.id, blob);\n                }\n                for (const item of _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.items) {\n                    if (!item.model)\n                        continue;\n                    const blob = yield this.renderModelToWebM(this.GetModel(item.model, item.color), undefined, renderer);\n                    if (blob)\n                        this.imgObjects.set(item.id, blob);\n                }\n            }\n            finally {\n                renderer.forceContextLoss();\n                renderer.dispose();\n                renderer.domElement.width = 0;\n                renderer.domElement.height = 0;\n            }\n        });\n    }\n    renderModelToWebM(model_1) {\n        return __awaiter(this, arguments, void 0, function* (model, options = { size: 100, duration: 2000, fps: 30 }, renderer) {\n            const disableAnimation = true; // всегда генерируем статичное изображение\n            const { size } = options;\n            const previewRenderer = renderer || new three__WEBPACK_IMPORTED_MODULE_11__.WebGLRenderer({ antialias: true, alpha: true });\n            // Важно: передаём size и size без третьего параметра, чтобы обновились и CSS-размеры\n            previewRenderer.setSize(size, size);\n            previewRenderer.setPixelRatio(window.devicePixelRatio);\n            // Вычисляем ограничивающий бокс модели и центрируем её\n            const box = new three__WEBPACK_IMPORTED_MODULE_9__.Box3().setFromObject(model);\n            const center = box.getCenter(new three__WEBPACK_IMPORTED_MODULE_9__.Vector3());\n            const sphere = box.getBoundingSphere(new three__WEBPACK_IMPORTED_MODULE_9__.Sphere());\n            const radius = sphere.radius;\n            // Сохраняем исходную позицию, чтобы вернуть модель в исходное состояние\n            const originalPos = model.position.clone();\n            model.position.sub(center);\n            // Настраиваем камеру так, чтобы модель помещалась в кадр\n            const camera = new three__WEBPACK_IMPORTED_MODULE_9__.PerspectiveCamera(75, 1, 0.1, 1000);\n            const fov = camera.fov * (Math.PI / 180);\n            const distance = (radius / Math.sin(fov / 2)) * 1.05;\n            camera.position.copy(new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(1, 1, 1).normalize().multiplyScalar(distance));\n            camera.lookAt(0, 0, 0);\n            camera.near = Math.max(0.01, distance - radius * 1.5);\n            camera.far = distance + radius * 1.5;\n            camera.updateProjectionMatrix();\n            // Освещение\n            const light = new three__WEBPACK_IMPORTED_MODULE_9__.DirectionalLight(0xffffff, 1);\n            light.position.set(0, distance * 2, distance * 0.1);\n            const ambient = new three__WEBPACK_IMPORTED_MODULE_9__.AmbientLight(0xffffff, 0.5);\n            // Создаём временную сцену\n            const scene = new three__WEBPACK_IMPORTED_MODULE_9__.Scene();\n            scene.add(light, ambient, model);\n            // Рендерим статичное изображение\n            previewRenderer.render(scene, camera);\n            const dataUrl = previewRenderer.domElement.toDataURL('image/png');\n            // Возвращаем модель на исходную позицию\n            model.position.copy(originalPos);\n            // Очистка\n            scene.clear();\n            if (!renderer) {\n                previewRenderer.forceContextLoss();\n                previewRenderer.dispose();\n                previewRenderer.domElement.width = 0;\n                previewRenderer.domElement.height = 0;\n            }\n            return dataUrl;\n        });\n    }\n    GetWorldPoint(cursor) {\n        const canvas = this.renderer.domElement;\n        const rect = canvas.getBoundingClientRect();\n        const x = (cursor.x - rect.left) / rect.width;\n        const y = (cursor.y - rect.top) / rect.height;\n        if (x < 0 || x > 1 || y < 0 || y > 1)\n            return null;\n        this.mouse.set(x * 2 - 1, -y * 2 + 1);\n        this.raycaster.setFromCamera(this.mouse, this.camera);\n        const point = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3();\n        this.raycaster.ray.intersectPlane(new three__WEBPACK_IMPORTED_MODULE_9__.Plane(new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(0, 1, 0), 0), point);\n        return point;\n    }\n    UpdateHover(cursor) {\n        const point = this.GetWorldPoint(cursor);\n        if (!point)\n            return;\n        const snapped = this.SnapToGrid(point);\n        this.hoverPoint.set(snapped.x, 0, snapped.z);\n        this.hoverCell = {\n            x: Math.round(snapped.x / _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize),\n            z: Math.round(snapped.z / _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize),\n        };\n    }\n    UpdateHoverMesh() {\n        const hoveredBuild = this.GetBuildingInPoint(this.hoverCell);\n        const hoveredDropModel = this.hoveredDropUid\n            ? this.dropItems.get(this.hoveredDropUid)\n            : undefined;\n        const useDropHover = !!hoveredDropModel &&\n            !this.ghostObject &&\n            !this.editSelectionActive &&\n            this.cursorInsideGame;\n        this.hoverGroup.visible =\n            (!this.ghostObject && this.isValidPoint && this.cursorInsideGame) ||\n                useDropHover;\n        const useCellRangeSelection = (this.editSelectionActive && !!this.editBuildMode) ||\n            this.automatonDragSelectActive;\n        if (useCellRangeSelection) {\n            const [from, to] = this.NormalizeBuildRange(this.editBuildRange[0], this.editBuildRange[1]);\n            const width = to.x - from.x + 1;\n            const height = to.z - from.z + 1;\n            const center = {\n                x: ((from.x + to.x) / 2) * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize,\n                z: ((from.z + to.z) / 2) * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize,\n            };\n            this.hoverGroup.position.lerp(new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(center.x, 10, center.z), 0.5);\n            this.hoverGroup.scale.set(width * 1.01, height * 1.01, 1.01);\n        }\n        else if (useDropHover && hoveredDropModel) {\n            const box = new three__WEBPACK_IMPORTED_MODULE_9__.Box3().setFromObject(hoveredDropModel);\n            const center = box.getCenter(new three__WEBPACK_IMPORTED_MODULE_9__.Vector3());\n            const size = box.getSize(new three__WEBPACK_IMPORTED_MODULE_9__.Vector3());\n            const scaleX = Math.max(0.25, (size.x / _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize) * 1.2);\n            const scaleY = Math.max(0.25, (size.z / _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize) * 1.2);\n            this.hoverGroup.position.lerp(new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(center.x, 10, center.z), 0.5);\n            this.hoverGroup.scale.set(scaleX, scaleY, 1.01);\n        }\n        else if (hoveredBuild) {\n            const center = this.GetBuildWorldCenter(hoveredBuild);\n            const size = (0,_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsGetBuildingFootprintSize)(hoveredBuild.id, hoveredBuild.rotation);\n            this.hoverGroup.position.lerp(new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(center.x, 10, center.z), 0.5);\n            this.hoverGroup.scale.set(size.x * 1.01, size.z * 1.01, 1.01);\n        }\n        else {\n            this.hoverGroup.position.lerp(new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(this.hoverPoint.x, 10, this.hoverPoint.z), 0.5);\n            this.hoverGroup.scale.set(1.01, 1.01, 1.01);\n        }\n        const materials = this.hoverBorder.material instanceof Array\n            ? this.hoverBorder.material\n            : [this.hoverBorder.material];\n        materials.forEach((m) => {\n            const color = this.automatonDragSelectActive\n                ? '#2e8fff'\n                : this.editBuildMode === 'delete'\n                    ? '#cc0606'\n                    : this.editBuildMode === 'cut'\n                        ? '#ccbf06'\n                        : this.editBuildMode === 'copy'\n                            ? '#0655cc'\n                            : '#c2c2c2';\n            m.color.set(color);\n        });\n    }\n    RotateGhost() {\n        if (this.ghostSelectionBuilds.length > 0)\n            return false;\n        if (!this.ghostObject)\n            return false;\n        this.ghostObjectRotation = (this.ghostObjectRotation - 1) % 4;\n        this.ghostObject.rotation.y = this.ghostObjectRotation * (Math.PI / 2);\n        return true;\n    }\n    CancelGhost() {\n        this.CancelConveyorDragBuild();\n        this.undergroundConveyorPlacementStart = undefined;\n        this.undergroundConveyorPlacementStartRotation = undefined;\n        this.undergroundConveyorPlacementFirstBuildUid = undefined;\n        if (this.cutBuildUids.length > 0) {\n            this.cutBuildUids.forEach(uid => {\n                const originalModel = this.buildings.get(uid);\n                if (originalModel)\n                    this.UnmarkObjectAsGhost(originalModel);\n            });\n            this.cutBuildUids = [];\n        }\n        this.ghostSelectionBuilds = [];\n        this.editSelectionActive = false;\n        if (this.ghostObject) {\n            this.DisposeObject(this.ghostObject);\n            this.ghostObject = undefined;\n            this.ghostBuildId = undefined;\n            this.ghostObjectBaseScale.set(1, 1, 1);\n            this.hoverGroup.visible = this.cursorInsideGame && this.isValidPoint;\n            this.forceUpdate();\n        }\n    }\n    SetGhost(id) {\n        this.CancelGhost();\n        const model = this.GetModel(id);\n        if (!model)\n            return;\n        this.hoverGroup.visible = false;\n        this.ghostObjectRotation = 0;\n        this.ghostSelectionBuilds = [];\n        this.ghostObject = model;\n        this.ghostBuildId = id;\n        this.ghostObjectBaseScale.copy(this.ghostObject.scale);\n        const center = this.GetBuildWorldCenter({\n            id,\n            position: this.hoverCell,\n            rotation: this.ghostObjectRotation,\n        });\n        this.ghostObject.position.set(center.x, 0, center.z);\n        this.ghostObject.scale.set(0, 0, 0);\n        console.log(this.ghostObject);\n        this.MarkObjectAsGhost(this.ghostObject);\n        this.scene.add(this.ghostObject);\n        this.forceUpdate();\n    }\n    SelectGhostBuild(id) {\n        if (this.ghostBuildId === id)\n            return;\n        const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(q => q.id === id);\n        if (!cfg)\n            return;\n        this.SetGhost(cfg.id);\n    }\n    UpdateGhost() {\n        this.conveyorDragGhostObjects.forEach(ghost => {\n            ghost.visible = this.cursorInsideGame;\n        });\n        if (!this.ghostObject)\n            return;\n        this.ghostObject.visible = this.isValidPoint && this.cursorInsideGame;\n        const center = this.GetBuildWorldCenter({\n            id: this.ghostBuildId,\n            position: this.hoverCell,\n            rotation: this.ghostObjectRotation,\n        });\n        this.ghostObject.position.lerp(new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(center.x, this.hoverPoint.y, center.z), 0.5);\n        this.ghostObject.scale.lerp(this.ghostObjectBaseScale, 0.8);\n    }\n    ClickUpGame() {\n        console.log(`ClickUpGame`);\n        this.editBuildRange[1] = Object.assign({}, this.hoverCell);\n        if (this.editSelectionActive && this.editBuildMode) {\n            this.editSelectionActive = false;\n            const range = this.NormalizeBuildRange(this.editBuildRange[0], this.editBuildRange[1]);\n            this.editBuildRange = [...range];\n            if (this.editBuildMode === 'delete') {\n                if (!this.keysPressed.has('ShiftLeft') &&\n                    !this.keysPressed.has('ShiftRight')) {\n                    this.editBuildMode = undefined;\n                }\n                const builds = this.GetBuildingsBetweenPoints(range[0], range[1]);\n                this.DestroyBuilds(...builds.map(b => b.uid));\n                return true;\n            }\n            if (this.editBuildMode === 'copy') {\n                this.editBuildMode = undefined;\n                const builds = this.GetBuildingsBetweenPoints(range[0], range[1]);\n                const centerCell = this.GetRangeCenterCell(range[0], range[1]);\n                return this.CreateGhostSelection(builds, centerCell, false);\n            }\n            if (this.editBuildMode === 'cut') {\n                this.editBuildMode = undefined;\n                const builds = this.GetBuildingsBetweenPoints(range[0], range[1]);\n                const centerCell = this.GetRangeCenterCell(range[0], range[1]);\n                return this.CreateGhostSelection(builds, centerCell, true);\n            }\n        }\n        return false;\n    }\n    BuildGhostSend() {\n        if (this.ghostSelectionBuilds.length > 0) {\n            const targets = this.GetGhostSelectionTargets();\n            if (this.cutBuildUids.length > 0) {\n                this.cutBuildUids.forEach(uid => {\n                    const originalModel = this.buildings.get(uid);\n                    if (originalModel)\n                        this.UnmarkObjectAsGhost(originalModel);\n                });\n                this.SendAction('builds.move', targets.map(target => ({\n                    uid: target.uid,\n                    position: target.position,\n                    rotation: target.rotation,\n                })));\n                this.cutBuildUids = [];\n            }\n            else {\n                const missing = this.GetMissingBuildItemsForPlacement(targets.map(target => target.id));\n                if (missing.length > 0) {\n                    const missingList = missing\n                        .map(item => {\n                        const buildCfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(build => build.id === item.buildId);\n                        const name = this.LocalizeConfigName(buildCfg === null || buildCfg === void 0 ? void 0 : buildCfg.name, buildCfg === null || buildCfg === void 0 ? void 0 : buildCfg.nameLang) ||\n                            item.buildId;\n                        return `${name} x${item.missing}`;\n                    })\n                        .join(', ');\n                    this.PushGameNotification(this.LangString('FactoryWarsNotifyNotEnoughBuildItems', missingList), 'warning');\n                }\n                this.NewBuilds(...targets.map(target => ({\n                    id: target.id,\n                    position: target.position,\n                    rotation: target.rotation,\n                })));\n            }\n            this.ghostSelectionBuilds = [];\n        }\n        else {\n            const missing = this.GetMissingBuildItemsForPlacement([\n                this.ghostBuildId,\n            ]);\n            if (missing.length > 0) {\n                const buildCfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(build => build.id === this.ghostBuildId);\n                const buildName = this.LocalizeConfigName(buildCfg === null || buildCfg === void 0 ? void 0 : buildCfg.name, buildCfg === null || buildCfg === void 0 ? void 0 : buildCfg.nameLang) ||\n                    this.ghostBuildId;\n                this.PushGameNotification(this.LangString('FactoryWarsNotifyNotEnoughItemsForBuild', buildName), 'warning');\n                return;\n            }\n            this.NewBuilds({\n                id: this.ghostBuildId,\n                position: this.hoverCell,\n                rotation: this.ghostObjectRotation,\n            });\n        }\n    }\n    ClickInGame() {\n        this.editBuildRange[0] = Object.assign({}, this.hoverCell);\n        // Single-click on automaton opens the same group UI (with 1 unit)\n        if (!this.ghostObject) {\n            const automatonUid = this.GetAutomatonUidByMouse();\n            if (automatonUid) {\n                const automaton = this.state.gameData.automatons.find(a => a.uid === automatonUid);\n                if (automaton && automaton.team === this.myTeamId) {\n                    this.selectedAutomatonUids = [automatonUid];\n                    this.forceUpdate();\n                    return;\n                }\n            }\n            else if (this.selectedAutomatonUids.length > 0) {\n                this.selectedAutomatonUids = [];\n                this.forceUpdate();\n                return;\n            }\n        }\n        if (this.ghostObject) {\n            if (this.IsUndergroundConveyorGhostSelected()) {\n                const currentCell = Object.assign({}, this.hoverCell);\n                const startCell = this.undergroundConveyorPlacementStart;\n                const buildCfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(q => q.id === 'underground_conveyor');\n                const maxCells = Math.max(1, Math.floor(Number(buildCfg === null || buildCfg === void 0 ? void 0 : buildCfg.maxCells) || 1));\n                if (!startCell) {\n                    const rotation = this.ghostObjectRotation;\n                    const canStart = this.CanPlaceBuild({\n                        id: this.ghostBuildId,\n                        position: currentCell,\n                        rotation,\n                    });\n                    if (!canStart) {\n                        console.log('Cannot place underground conveyor start point here');\n                        return;\n                    }\n                    this.SendAction('build', {\n                        id: this.ghostBuildId,\n                        position: currentCell,\n                        rotation,\n                    });\n                    this.undergroundConveyorPlacementStart = currentCell;\n                    this.undergroundConveyorPlacementStartRotation = rotation;\n                    this.undergroundConveyorPlacementFirstBuildUid = undefined;\n                    this.ghostObjectRotation = rotation;\n                    if (this.ghostObject) {\n                        this.ghostObject.rotation.y =\n                            this.ghostObjectRotation * (Math.PI / 2);\n                    }\n                    return;\n                }\n                const startRotation = typeof this.undergroundConveyorPlacementStartRotation === 'number'\n                    ? this.undergroundConveyorPlacementStartRotation\n                    : this.ghostObjectRotation;\n                this.ghostObjectRotation = startRotation;\n                if (this.ghostObject) {\n                    this.ghostObject.rotation.y =\n                        this.ghostObjectRotation * (Math.PI / 2);\n                }\n                const startBuild = this.state.gameData.buildings.find(b => b.id === 'underground_conveyor' &&\n                    b.team === this.myTeamId &&\n                    b.pos.x === startCell.x &&\n                    b.pos.z === startCell.z &&\n                    b.rotation === startRotation);\n                if (startBuild) {\n                    this.undergroundConveyorPlacementFirstBuildUid = startBuild.uid;\n                }\n                if (startCell.x === currentCell.x && startCell.z === currentCell.z) {\n                    if (this.undergroundConveyorPlacementFirstBuildUid) {\n                        this.CancelGhost();\n                    }\n                    return;\n                }\n                const axisAligned = startCell.x === currentCell.x || startCell.z === currentCell.z;\n                if (!axisAligned) {\n                    console.log('Underground conveyor must be placed in a straight line');\n                    return;\n                }\n                const cellsDistance = Math.abs(currentCell.x - startCell.x) +\n                    Math.abs(currentCell.z - startCell.z);\n                if (cellsDistance > maxCells) {\n                    console.log(`Underground conveyor max distance is ${maxCells} cells`);\n                    return;\n                }\n                const entryForward = (0,_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsFaceCell)(startCell, startRotation);\n                const dirX = entryForward.x - startCell.x;\n                const dirZ = entryForward.z - startCell.z;\n                const onForwardRay = dirX === 0\n                    ? currentCell.x === startCell.x &&\n                        Math.sign(currentCell.z - startCell.z) === Math.sign(dirZ)\n                    : currentCell.z === startCell.z &&\n                        Math.sign(currentCell.x - startCell.x) === Math.sign(dirX);\n                if (!onForwardRay) {\n                    console.log('Exit underground conveyor must be in front of entry direction');\n                    return;\n                }\n                const existingBuildAtCell = this.state.gameData.buildings.find(b => b.pos.x === currentCell.x && b.pos.z === currentCell.z);\n                if (existingBuildAtCell &&\n                    existingBuildAtCell.id === 'underground_conveyor' &&\n                    existingBuildAtCell.team === this.myTeamId &&\n                    existingBuildAtCell.rotation === startRotation &&\n                    existingBuildAtCell.uid !==\n                        this.undergroundConveyorPlacementFirstBuildUid) {\n                    this.CancelGhost();\n                    return;\n                }\n                const endBuild = {\n                    id: this.ghostBuildId,\n                    position: currentCell,\n                    rotation: startRotation,\n                };\n                if (!this.CanPlaceBuild(endBuild)) {\n                    console.log('Cannot place underground conveyor exit here');\n                    return;\n                }\n                this.SendAction('build', endBuild);\n                this.CancelGhost();\n                return;\n            }\n            if (this.ghostSelectionBuilds.length > 0) {\n                if (!this.CanPlaceGhostSelection()) {\n                    console.log('There is already a building here');\n                    return;\n                }\n                this.BuildGhostSend();\n                if (!this.keysPressed.has('ShiftLeft') &&\n                    !this.keysPressed.has('ShiftRight')) {\n                    this.CancelGhost();\n                }\n                return;\n            }\n            const findExist = this.GetBuildingInPoint(this.hoverCell);\n            if (findExist) {\n                if (findExist.id === this.ghostBuildId) {\n                    // Copy-placed ghost on same-type building → rotate\n                    this.SendAction('build.rotate', {\n                        uid: findExist.uid,\n                        rotation: this.ghostObjectRotation,\n                    });\n                }\n                else {\n                    console.log('There is already a building here');\n                }\n            }\n            else {\n                this.BuildGhostSend();\n            }\n            if (!this.keysPressed.has('ShiftLeft') &&\n                !this.keysPressed.has('ShiftRight')) {\n                this.CancelGhost();\n            }\n        }\n    }\n    ApplyResouceColor(mesh, color) {\n        if (!color)\n            return;\n        mesh === null || mesh === void 0 ? void 0 : mesh.traverse((child) => {\n            var _a, _b, _c, _d;\n            if (child.isMesh) {\n                if (child.material instanceof Array) {\n                    child.material.forEach((m) => {\n                        var _a;\n                        if (!m.userData.protectColor && ((_a = m === null || m === void 0 ? void 0 : m.color) === null || _a === void 0 ? void 0 : _a.set))\n                            m.color.set(color);\n                    });\n                }\n                else {\n                    if (!((_b = (_a = child.material) === null || _a === void 0 ? void 0 : _a.userData) === null || _b === void 0 ? void 0 : _b.protectColor) &&\n                        ((_d = (_c = child.material) === null || _c === void 0 ? void 0 : _c.color) === null || _d === void 0 ? void 0 : _d.set))\n                        child.material.color.set(color);\n                }\n            }\n        });\n    }\n    DrawMapPoint(ids, mapData = this.state.gameData.map) {\n        var _a;\n        const point = mapData.find(p => p.ids === ids);\n        if (!point)\n            return;\n        if (!point.resource)\n            return;\n        const item = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.items.find(i => i.id === point.resource.id);\n        if (!item)\n            return;\n        const resourceModelId = item.modelResource || item.model;\n        const applyColor = !item.modelResource;\n        if (this.mapItems.has(ids)) {\n            (_a = this.mapItems.get(ids)) === null || _a === void 0 ? void 0 : _a.forEach(obj => {\n                if (applyColor) {\n                    this.ApplyResouceColor(obj, item.color);\n                }\n                obj.visible = true;\n            });\n            return; // уже нарисовано\n        }\n        const coords = this.SnapToGrid({\n            x: point.x * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize,\n            z: point.z * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize,\n        });\n        const mesh1 = this.GetModel(resourceModelId, applyColor ? item.color : undefined);\n        if (!mesh1)\n            return;\n        mesh1.userData.pointIds = ids;\n        mesh1.position.set(coords.x, 4, coords.z);\n        this.ApplyShadowForObject(mesh1);\n        // mesh1.scale.set(1.1, 1.1, 1.1);\n        // // mesh1.rotateOnAxis(new THREE.Vector3(-1, -1, -1), Math.PI / 4);\n        this.scene.add(mesh1);\n        // const mesh2 = new THREE.Mesh(\n        //   this.resouceGeometry,\n        //   this.resouceMaterials.get(item.id)\n        // );\n        // mesh2.userData.pointIds = ids;\n        // mesh2.scale.set(0.8, 0.8, 0.8);\n        // // mesh2.rotateOnAxis(new THREE.Vector3(1, 1, 1), Math.PI / 4);\n        // mesh2.position.set(coords.x, 7, coords.z);\n        // this.scene.add(mesh2);\n        this.mapItems.set(ids, [mesh1]);\n    }\n    RemoveMapPoint(...ids) {\n        ids.forEach(id => {\n            const objs = this.mapItems.get(id);\n            if (objs) {\n                objs.forEach(obj => {\n                    obj.visible = false;\n                });\n            }\n        });\n    }\n    PickMapPoints(position, radius) {\n        var _a;\n        if (position === void 0) { position = (_a = this.players_models.get(_api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id)) === null || _a === void 0 ? void 0 : _a.position; }\n        if (radius === void 0) { radius = 2000; }\n        if (!position)\n            return undefined;\n        const points = [];\n        this.state.gameData.map.forEach(point => {\n            if (this.distanceTo({\n                x: point.x * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize,\n                z: point.z * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize,\n            }, position) <= radius) {\n                const findBuild = this.GetBuildingInPoint(point);\n                if (!findBuild || !this.minersIds.includes(findBuild.id)) {\n                    points.push(point);\n                }\n            }\n        });\n        return points;\n    }\n    distanceTo(pos1, pos2) {\n        return Math.sqrt(Math.pow((pos1.x - pos2.x), 2) + Math.pow((pos1.z - pos2.z), 2));\n    }\n    EnsureFogOfWarMesh(mapSize) {\n        if (!this.scene)\n            return;\n        const signature = `${mapSize.x}:${mapSize.z}`;\n        if (signature === this.fogOfWarMapSignature && this.fogOfWarMesh)\n            return;\n        if (this.fogOfWarMesh) {\n            this.DisposeObject(this.fogOfWarMesh);\n            this.fogOfWarMesh = undefined;\n        }\n        const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n        const capacity = Math.max(1, mapSize.x * mapSize.z);\n        const geometry = new three__WEBPACK_IMPORTED_MODULE_9__.PlaneGeometry(grid * 0.98, grid * 0.98);\n        geometry.rotateX(-Math.PI / 2);\n        const material = new three__WEBPACK_IMPORTED_MODULE_9__.MeshBasicMaterial({\n            color: '#060b12',\n            transparent: true,\n            opacity: 0.84,\n            depthWrite: false,\n            depthTest: false,\n        });\n        const mesh = new three__WEBPACK_IMPORTED_MODULE_9__.InstancedMesh(geometry, material, capacity);\n        mesh.instanceMatrix.setUsage(three__WEBPACK_IMPORTED_MODULE_9__.DynamicDrawUsage);\n        mesh.frustumCulled = false;\n        mesh.position.y = 8;\n        mesh.renderOrder = 950;\n        mesh.layers.enable(0);\n        mesh.layers.enable(2);\n        // Fog is visual-only; gameplay picking/raycast should ignore it.\n        mesh.raycast = () => { };\n        mesh.count = 0;\n        this.scene.add(mesh);\n        this.fogOfWarMesh = mesh;\n        this.fogOfWarMapSignature = signature;\n        this.fogOfWarMapSize = { x: mapSize.x, z: mapSize.z };\n        this.fogOfWarInstanceCapacity = capacity;\n        this.fogOfWarVisibility = new Uint8Array(capacity);\n        this.fogOfWarVisibilityStamp = 0;\n        this.fogOfWarVisibilityStampApplied = -1;\n    }\n    MarkFogVisibleCircle(visibility, mapSize, centerWorld, radiusWorld) {\n        if (radiusWorld <= 0)\n            return;\n        const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n        const cx = centerWorld.x / grid;\n        const cz = centerWorld.z / grid;\n        const radiusCells = Math.max(0, radiusWorld / grid);\n        const radiusSq = radiusCells * radiusCells;\n        const minX = Math.max(0, Math.floor(cx - radiusCells));\n        const maxX = Math.min(mapSize.x - 1, Math.ceil(cx + radiusCells));\n        const minZ = Math.max(0, Math.floor(cz - radiusCells));\n        const maxZ = Math.min(mapSize.z - 1, Math.ceil(cz + radiusCells));\n        for (let z = minZ; z <= maxZ; z++) {\n            const dz = z - cz;\n            const dzSq = dz * dz;\n            if (dzSq > radiusSq)\n                continue;\n            const dxMax = Math.sqrt(radiusSq - dzSq);\n            const rowMinX = Math.max(minX, Math.ceil(cx - dxMax));\n            const rowMaxX = Math.min(maxX, Math.floor(cx + dxMax));\n            const rowOffset = z * mapSize.x;\n            for (let x = rowMinX; x <= rowMaxX; x++) {\n                visibility[rowOffset + x] = 1;\n            }\n        }\n    }\n    GetFogVisionScale() {\n        return Math.max(0, Number(this.state.gameData.visibleScale || 1));\n    }\n    UpdateFogOfWar(now = performance.now(), force = false) {\n        if (!this.scene)\n            return;\n        const mapSize = this.state.gameData.mapSize;\n        if (!mapSize || mapSize.x <= 0 || mapSize.z <= 0)\n            return;\n        const fogEnabled = this.state.gameData.warFog !== false;\n        this.EnsureFogOfWarMesh(mapSize);\n        const mesh = this.fogOfWarMesh;\n        if (!mesh)\n            return;\n        if (!fogEnabled) {\n            mesh.visible = false;\n            mesh.count = 0;\n            mesh.instanceMatrix.needsUpdate = true;\n            if (this.fogOfWarVisibility.length !== mapSize.x * mapSize.z) {\n                this.fogOfWarVisibility = new Uint8Array(mapSize.x * mapSize.z);\n            }\n            this.fogOfWarVisibility.fill(1);\n            this.fogOfWarVisibilityStamp++;\n            this.ApplyFogVisibilityToScene();\n            return;\n        }\n        mesh.visible = true;\n        if (!force &&\n            now - this.fogOfWarLastUpdateAt < this.fogOfWarTickIntervalMS) {\n            if (this.fogOfWarVisibilityStampApplied !== this.fogOfWarVisibilityStamp) {\n                this.ApplyFogVisibilityToScene();\n            }\n            return;\n        }\n        this.fogOfWarLastUpdateAt = now;\n        const visibility = this.fogOfWarVisibility;\n        visibility.fill(0);\n        const visionSources = [];\n        const myTeamId = this.myTeamId;\n        const visionScale = this.GetFogVisionScale();\n        if (myTeamId === undefined || myTeamId === null || myTeamId < 0) {\n            visibility.fill(1);\n        }\n        else {\n            const playerRange = Math.max(0, _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.playerVisibleRange * visionScale);\n            this.state.players.forEach(player => {\n                if (this.GetTeam(player.id) !== myTeamId)\n                    return;\n                const model = this.players_models.get(player.id);\n                const pos = (model === null || model === void 0 ? void 0 : model.position) || this.players_positions.get(player.id);\n                if (!pos)\n                    return;\n                visionSources.push({ x: pos.x, z: pos.z, radius: playerRange });\n                this.MarkFogVisibleCircle(visibility, mapSize, pos, playerRange);\n            });\n            this.state.gameData.buildings.forEach(build => {\n                if (build.team !== myTeamId)\n                    return;\n                const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(q => q.id === build.id);\n                const range = Math.max(0, Number((cfg === null || cfg === void 0 ? void 0 : cfg.visibleRange) || 0) * visionScale);\n                if (range <= 0)\n                    return;\n                const center = this.GetBuildWorldCenter(build);\n                visionSources.push({ x: center.x, z: center.z, radius: range });\n                this.MarkFogVisibleCircle(visibility, mapSize, center, range);\n            });\n            this.state.gameData.automatons.forEach(automaton => {\n                if (automaton.team !== myTeamId)\n                    return;\n                const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsAutomatonUnits.find(q => q.id === automaton.id);\n                const range = Math.max(0, Number((cfg === null || cfg === void 0 ? void 0 : cfg.visibleRange) || 0) * visionScale);\n                if (range <= 0)\n                    return;\n                visionSources.push({\n                    x: automaton.position.x,\n                    z: automaton.position.z,\n                    radius: range,\n                });\n                this.MarkFogVisibleCircle(visibility, mapSize, automaton.position, range);\n            });\n        }\n        const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n        const edgeSoftness = grid * 1.45;\n        const sampleOffsets = [\n            { x: 0, z: 0 },\n            { x: -0.34 * grid, z: -0.34 * grid },\n            { x: 0.34 * grid, z: -0.34 * grid },\n            { x: -0.34 * grid, z: 0.34 * grid },\n            { x: 0.34 * grid, z: 0.34 * grid },\n        ];\n        let hiddenCount = 0;\n        for (let z = 0; z < mapSize.z; z++) {\n            for (let x = 0; x < mapSize.x; x++) {\n                let fogStrength = visibility[z * mapSize.x + x] === 1 ? 0 : 1;\n                if (visionSources.length > 0) {\n                    const worldX = x * grid;\n                    const worldZ = z * grid;\n                    let accumFog = 0;\n                    for (let s = 0; s < sampleOffsets.length; s++) {\n                        const sample = sampleOffsets[s];\n                        let nearestEdgeDistance = Number.POSITIVE_INFINITY;\n                        for (let i = 0; i < visionSources.length; i++) {\n                            const src = visionSources[i];\n                            const dx = worldX + sample.x - src.x;\n                            const dz = worldZ + sample.z - src.z;\n                            const distanceToCenter = Math.sqrt(dx * dx + dz * dz);\n                            const edgeDistance = distanceToCenter - src.radius;\n                            if (edgeDistance < nearestEdgeDistance) {\n                                nearestEdgeDistance = edgeDistance;\n                            }\n                        }\n                        accumFog += three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.clamp(nearestEdgeDistance / edgeSoftness, 0, 1);\n                    }\n                    fogStrength = accumFog / sampleOffsets.length;\n                }\n                if (fogStrength <= 0.02)\n                    continue;\n                const easedFog = three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.smoothstep(fogStrength, 0, 1);\n                const scale = three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.lerp(0.12, 1.02, easedFog);\n                this.fogOfWarTempMatrix.makeScale(scale, scale, scale);\n                this.fogOfWarTempMatrix.setPosition(x * grid, 0, z * grid);\n                mesh.setMatrixAt(hiddenCount, this.fogOfWarTempMatrix);\n                hiddenCount++;\n            }\n        }\n        mesh.count = hiddenCount;\n        mesh.instanceMatrix.needsUpdate = true;\n        this.fogOfWarVisibilityStamp++;\n        this.ApplyFogVisibilityToScene();\n    }\n    IsCellVisibleInFog(cell) {\n        if (this.state.gameData.warFog === false)\n            return true;\n        const mapSize = this.fogOfWarMapSize;\n        const x = Math.round(cell.x);\n        const z = Math.round(cell.z);\n        if (x < 0 ||\n            z < 0 ||\n            x >= mapSize.x ||\n            z >= mapSize.z ||\n            this.fogOfWarVisibility.length !== mapSize.x * mapSize.z) {\n            return false;\n        }\n        return this.fogOfWarVisibility[z * mapSize.x + x] === 1;\n    }\n    IsWorldPositionVisibleInFog(position) {\n        const cell = this.ToGameCoords(position);\n        return this.IsCellVisibleInFog(cell);\n    }\n    IsMapPointVisibleInFog(point) {\n        return this.IsCellVisibleInFog({ x: point.x, z: point.z });\n    }\n    IsBuildingVisibleInFog(build) {\n        if (build.team === this.myTeamId)\n            return true;\n        if (build.id === 'hub')\n            return true;\n        const cells = this.GetBuildOccupiedCells(build);\n        return cells.some(cell => this.IsCellVisibleInFog(cell));\n    }\n    IsAutomatonVisibleInFog(auto) {\n        if (auto.team === this.myTeamId)\n            return true;\n        return this.IsWorldPositionVisibleInFog(auto.position);\n    }\n    IsPlayerVisibleInFog(playerId) {\n        if (playerId === _api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id)\n            return true;\n        const player = this.state.players.find(q => q.id === playerId);\n        if (!player)\n            return false;\n        if (this.GetTeam(player.id) === this.myTeamId)\n            return true;\n        const model = this.players_models.get(playerId);\n        const pos = (model === null || model === void 0 ? void 0 : model.position) || this.players_positions.get(playerId);\n        if (!pos)\n            return false;\n        return this.IsWorldPositionVisibleInFog(pos);\n    }\n    ApplyFogVisibilityToScene() {\n        this.fogOfWarVisibilityStampApplied = this.fogOfWarVisibilityStamp;\n        this.buildings.forEach((model, uid) => {\n            const build = this.state.gameData.buildings.find(q => q.uid === uid);\n            if (!build) {\n                model.visible = false;\n                return;\n            }\n            model.visible = this.IsBuildingVisibleInFog(build);\n        });\n        this.automatons_models.forEach((model, uid) => {\n            const automaton = this.state.gameData.automatons.find(q => q.uid === uid);\n            if (!automaton) {\n                model.visible = false;\n                return;\n            }\n            model.visible = this.IsAutomatonVisibleInFog(automaton);\n        });\n        this.dropItems.forEach((model, uid) => {\n            const drop = this.state.gameData.drop.find(q => q.uid === uid);\n            if (!drop) {\n                model.visible = false;\n                return;\n            }\n            model.visible = this.IsWorldPositionVisibleInFog(drop.position);\n        });\n        this.players_models.forEach((model, playerId) => {\n            model.visible = this.IsPlayerVisibleInFog(playerId);\n        });\n        this.players_minimap_markers.forEach((group, playerId) => {\n            group.visible = this.IsPlayerVisibleInFog(playerId);\n        });\n        this.minimap_resource_markers.forEach(group => {\n            group.visible = true;\n        });\n    }\n    GetMinimapResourceIdsFromObject(object) {\n        var _a;\n        let node = object;\n        while (node) {\n            if (typeof ((_a = node.userData) === null || _a === void 0 ? void 0 : _a.ids) === 'string')\n                return node.userData.ids;\n            node = node.parent || undefined;\n        }\n        return undefined;\n    }\n    HideMinimapTooltip() {\n        this.hoveredMinimapResourceIds = undefined;\n        const tooltip = this.minimapTooltipRef.current;\n        if (tooltip)\n            tooltip.style.display = 'none';\n    }\n    GetMinimapWorldPoint(clientX, clientY) {\n        const canvasMini = this.canvasRefMini.current;\n        if (!canvasMini || !this.cameraMini)\n            return undefined;\n        const rect = canvasMini.getBoundingClientRect();\n        if (clientX < rect.left ||\n            clientX > rect.right ||\n            clientY < rect.top ||\n            clientY > rect.bottom) {\n            return undefined;\n        }\n        this.mouse.x = ((clientX - rect.left) / rect.width) * 2 - 1;\n        this.mouse.y = -((clientY - rect.top) / rect.height) * 2 + 1;\n        this.minimapRaycaster.layers.set(0);\n        this.minimapRaycaster.setFromCamera(this.mouse, this.cameraMini);\n        const point = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3();\n        this.minimapRaycaster.ray.intersectPlane(new three__WEBPACK_IMPORTED_MODULE_9__.Plane(new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(0, 1, 0), 0), point);\n        if (!Number.isFinite(point.x) || !Number.isFinite(point.z)) {\n            return undefined;\n        }\n        return point;\n    }\n    UpdateMinimapResourceHover(clientX, clientY) {\n        var _a;\n        const canvasMini = this.canvasRefMini.current;\n        const tooltip = this.minimapTooltipRef.current;\n        if (!canvasMini || !tooltip || !this.cameraMini)\n            return;\n        const rect = canvasMini.getBoundingClientRect();\n        if (clientX < rect.left ||\n            clientX > rect.right ||\n            clientY < rect.top ||\n            clientY > rect.bottom) {\n            this.HideMinimapTooltip();\n            return;\n        }\n        const world = this.GetMinimapWorldPoint(clientX, clientY);\n        if (!world) {\n            this.HideMinimapTooltip();\n            return;\n        }\n        const cell = {\n            x: Math.round(world.x / _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize),\n            z: Math.round(world.z / _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize),\n        };\n        if (!(0,_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsIsPointInsideMap)(cell, this.state.gameData.mapSize)) {\n            this.HideMinimapTooltip();\n            return;\n        }\n        const build = this.GetBuildingInPoint(cell);\n        if (build && this.IsBuildingVisibleInFog(build)) {\n            const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(q => q.id === build.id);\n            if (cfg) {\n                const maxHp = Math.max(1, Number(build.data.maxHp || build.data.hp || cfg.maxHealth || 1));\n                const hp = Math.max(0, Math.min(maxHp, Number((_a = build.data.hp) !== null && _a !== void 0 ? _a : maxHp)));\n                const teamOrder = this.GetTeamOrder(build.team);\n                const teamName = this.GetTeamDisplayName(build.team);\n                const teamColor = teamOrder >= 0 ? this.GetTeamColorByOrder(teamOrder) : '#ffffff';\n                const extraLine = build.id === 'hub'\n                    ? this.LangString('FactoryWarsMinimapTeamCore', teamName)\n                    : this.LangString('FactoryWarsMinimapTeam', teamName);\n                tooltip.innerHTML =\n                    `<div style=\"display:flex;flex-direction:column;gap:2px;padding:8px 10px;border-radius:10px;background:rgba(7,12,20,0.9);border:1px solid ${teamColor};box-shadow:0 10px 20px rgba(0,0,0,0.35);\">` +\n                        `<span style=\"font:600 13px/1.1 Oswald, sans-serif;color:#ffffff;\">${this.EscapeHtml(this.LocalizeConfigName(cfg.name, cfg.nameLang))}</span>` +\n                        `<span style=\"font:600 11px/1.2 Oswald, sans-serif;color:#dbeafe;\">${this.EscapeHtml(extraLine)}</span>` +\n                        `<span style=\"font:600 11px/1.2 Oswald, sans-serif;color:#dbeafe;\">${this.EscapeHtml(this.LangString('FactoryWarsHpCompact', Math.round(hp), Math.round(maxHp)))}</span>` +\n                        `</div>`;\n                tooltip.style.display = 'block';\n                tooltip.style.left = `${clientX + 14}px`;\n                tooltip.style.top = `${clientY + 14}px`;\n                this.hoveredMinimapResourceIds = undefined;\n                return;\n            }\n        }\n        const mapPoint = this.state.gameData.map.find(q => q.x === cell.x && q.z === cell.z);\n        if (!(mapPoint === null || mapPoint === void 0 ? void 0 : mapPoint.resource)) {\n            this.HideMinimapTooltip();\n            return;\n        }\n        const ids = mapPoint.ids;\n        const item = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.items.find(q => { var _a; return q.id === ((_a = mapPoint.resource) === null || _a === void 0 ? void 0 : _a.id); });\n        if (!item) {\n            this.HideMinimapTooltip();\n            return;\n        }\n        const isVisible = this.IsMapPointVisibleInFog(mapPoint);\n        const amountLine = isVisible\n            ? this.LangString('FactoryWarsResourceAmountKnown', `${mapPoint.resource.amount}${typeof mapPoint.resource.total === 'number' ? ` / ${mapPoint.resource.total}` : ''}`)\n            : this.LangString('FactoryWarsResourceAmountHidden');\n        tooltip.innerHTML =\n            `<div style=\"display:flex;flex-direction:column;gap:2px;padding:8px 10px;border-radius:10px;background:rgba(7,12,20,0.9);border:1px solid ${item.color || '#4cc9f0'};box-shadow:0 10px 20px rgba(0,0,0,0.35);\">` +\n                `<span style=\"font:600 13px/1.1 Oswald, sans-serif;color:#ffffff;\">${this.EscapeHtml(item.name)}</span>` +\n                `<span style=\"font:600 11px/1.2 Oswald, sans-serif;color:#dbeafe;\">${this.EscapeHtml(amountLine)}</span>` +\n                `</div>`;\n        tooltip.style.display = 'block';\n        tooltip.style.left = `${clientX + 14}px`;\n        tooltip.style.top = `${clientY + 14}px`;\n        this.hoveredMinimapResourceIds = ids;\n    }\n    GetMapPointSnapshot(point) {\n        var _a;\n        return `${point.x}:${point.z}:${((_a = point.resource) === null || _a === void 0 ? void 0 : _a.id) || ''}`;\n    }\n    RecreateMapPoint(ids, mapData = this.state.gameData.map) {\n        const objs = this.mapItems.get(ids);\n        if (objs) {\n            objs.forEach(obj => this.DisposeObject(obj));\n            this.mapItems.delete(ids);\n        }\n        this.DrawMapPoint(ids, mapData);\n    }\n    UpdateSunLightForMap(mapSize) {\n        if (!this.sunLight)\n            return;\n        const xSize = mapSize.x * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n        const zSize = mapSize.z * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n        const centerX = xSize / 2;\n        const centerZ = zSize / 2;\n        const halfSpan = Math.max(xSize, zSize) / 2;\n        // Light placed at oblique angle above map center so shadows fall diagonally\n        const lightOffset = halfSpan * 0.8;\n        const lightHeight = halfSpan * 1.2;\n        this.sunLight.position.set(centerX + lightOffset, lightHeight, centerZ + lightOffset);\n        this.sunLight.target.position.set(centerX, 0, centerZ);\n        this.sunLight.target.updateMatrixWorld();\n        // Frustum covers full map with 20% padding\n        const pad = halfSpan * 0.2 + 200;\n        const cam = this.sunLight.shadow.camera;\n        cam.left = -(halfSpan + pad);\n        cam.right = halfSpan + pad;\n        cam.top = halfSpan + pad;\n        cam.bottom = -(halfSpan + pad);\n        cam.far = lightHeight + halfSpan * 2 + 500;\n        cam.updateProjectionMatrix();\n        if (this.renderer)\n            this.renderer.shadowMap.needsUpdate = true;\n    }\n    EnsureMapGround(mapSize, force = false) {\n        const signature = `${mapSize.x}:${mapSize.z}`;\n        if (!force && signature === this.mapGroundSignature)\n            return;\n        if (this.mapGroundMini) {\n            this.DisposeObject(this.mapGroundMini);\n            this.mapGroundMini = undefined;\n        }\n        const xSize = mapSize.x * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n        const zSize = mapSize.z * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n        const geometry = new three__WEBPACK_IMPORTED_MODULE_9__.PlaneGeometry(xSize * 4, zSize * 4);\n        const material = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n            color: '#292727',\n            roughness: 1,\n            metalness: 0,\n        });\n        const plane = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(geometry, material);\n        plane.position.set(Math.floor(xSize / 2), 0, Math.floor(zSize / 2));\n        plane.rotation.x = -Math.PI / 2;\n        plane.layers.set(0);\n        this.ApplyShadowForObject(plane);\n        this.scene.add(plane);\n        this.mapGroundMini = plane;\n        this.mapGroundSignature = signature;\n        this.UpdateSunLightForMap(mapSize);\n        this.CreateArenaWalls(mapSize);\n    }\n    MapTick() {\n        const intervalByQuality = {\n            high: 140,\n            medium: 220,\n            low: 360,\n        };\n        const tickInterval = intervalByQuality[this.graphicsSettings.vfxQuality];\n        const now = Date.now();\n        if (now - this.lastMapTick < tickInterval)\n            return;\n        this.lastMapTick = now;\n        const q = this.PickMapPoints();\n        if (!q)\n            return;\n        const points = q.map(q => q.ids);\n        const pointSet = new Set(points);\n        this.RemoveMapPoint(...[...this.mapItems.keys()].filter(id => !pointSet.has(id)));\n        points.forEach(id => this.DrawMapPoint(id));\n        this.SyncMinimapResourceMarkers();\n        this.UpdateFogOfWar(performance.now());\n        this.TickTeams();\n        this.TickBuilds();\n        this.TickAutomatons();\n        this.TrimOldVfx();\n    }\n    CreateArenaWalls(mapSize) {\n        var _a;\n        (_a = this.arenaWallsGroup) === null || _a === void 0 ? void 0 : _a.removeFromParent();\n        this.arenaWallsGroup = undefined;\n        this.arenaWallMaterials = [];\n        const wallHeight = 90;\n        const wallThickness = 14;\n        const wallColorPrimary = '#ff3b30';\n        const wallColorSecondary = '#6e0000';\n        const wallBlinkDurationMs = 1200;\n        const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n        const grid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n        const halfGrid = grid / 2;\n        const wallY = wallHeight / 2;\n        const initialColor = new three__WEBPACK_IMPORTED_MODULE_9__.Color(wallColorPrimary);\n        const myTeamOrder = this.myTeamOrder;\n        const teamCount = this.teamCount;\n        if (myTeamOrder < 0 || teamCount <= 0) {\n            this.arenaWallsGroup = group;\n            this.scene.add(group);\n            return;\n        }\n        const createWall = (width, depth, x, z) => {\n            const material = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: initialColor.clone(),\n                emissive: initialColor.clone().multiplyScalar(0.35),\n                transparent: true,\n                opacity: 0.35,\n                metalness: 0.05,\n                roughness: 0.45,\n            });\n            const wall = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(width, wallHeight, depth), material);\n            wall.position.set(x, wallY, z);\n            group.add(wall);\n            this.arenaWallMaterials.push(material);\n        };\n        const getZone = (x, z) => {\n            if (!(0,_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsIsPointInsideMap)({ x, z }, mapSize))\n                return -1;\n            return (0,_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsGetTeamZoneIndexForPoint)({ x, z }, mapSize, teamCount);\n        };\n        for (let x = 0; x < mapSize.x; x++) {\n            for (let z = 0; z < mapSize.z; z++) {\n                if (getZone(x, z) !== myTeamOrder)\n                    continue;\n                const centerX = x * grid;\n                const centerZ = z * grid;\n                if (getZone(x, z - 1) !== myTeamOrder) {\n                    createWall(grid + wallThickness, wallThickness, centerX, centerZ - halfGrid);\n                }\n                if (getZone(x, z + 1) !== myTeamOrder) {\n                    createWall(grid + wallThickness, wallThickness, centerX, centerZ + halfGrid);\n                }\n                if (getZone(x - 1, z) !== myTeamOrder) {\n                    createWall(wallThickness, grid + wallThickness, centerX - halfGrid, centerZ);\n                }\n                if (getZone(x + 1, z) !== myTeamOrder) {\n                    createWall(wallThickness, grid + wallThickness, centerX + halfGrid, centerZ);\n                }\n            }\n        }\n        group.userData.wallColorPrimary = new three__WEBPACK_IMPORTED_MODULE_9__.Color(wallColorPrimary);\n        group.userData.wallColorSecondary = new three__WEBPACK_IMPORTED_MODULE_9__.Color(wallColorSecondary);\n        group.userData.wallBlinkDurationMs = wallBlinkDurationMs;\n        this.arenaWallsGroup = group;\n        this.scene.add(group);\n    }\n    CreateBuild(uid) {\n        var _a, _b;\n        const build = this.state.gameData.buildings.find(b => b.uid === uid);\n        if (!build)\n            return;\n        const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(b => b.id === build.id);\n        if (!cfg)\n            return;\n        const model = this.GetModel(cfg.id);\n        if (!model)\n            return;\n        const pos = this.GetBuildWorldCenter(build);\n        const spawnBox = new three__WEBPACK_IMPORTED_MODULE_9__.Box3().setFromObject(model);\n        const spawnSize = spawnBox.getSize(new three__WEBPACK_IMPORTED_MODULE_9__.Vector3());\n        const spawnStartY = -spawnSize.y - 1;\n        model.position.set(pos.x, spawnStartY, pos.z);\n        model.rotation.y = build.rotation * (Math.PI / 2);\n        this.ApplyShadowForObject(model);\n        const resource = this.GetResouceInPoint(build.pos);\n        if (resource) {\n            const item = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.items.find(i => i.id === resource.id);\n            if (item) {\n                model.traverse((obj) => {\n                    if (obj.isMesh && obj.material) {\n                        const mats = Array.isArray(obj.material)\n                            ? obj.material\n                            : [obj.material];\n                        mats.forEach(m => {\n                            if (m.name === 'Material.016')\n                                m.color.set(item.color);\n                        });\n                    }\n                });\n                const clip = model.animations[0];\n                if (clip) {\n                    const mixer = new three__WEBPACK_IMPORTED_MODULE_9__.AnimationMixer(model);\n                    const action = mixer.clipAction(clip);\n                    action.setEffectiveTimeScale(1);\n                    action.setLoop(three__WEBPACK_IMPORTED_MODULE_9__.LoopRepeat, Infinity);\n                    action.play();\n                    this.animationBuildings.set(uid, mixer);\n                }\n            }\n        }\n        model.userData.buildingUid = uid;\n        model.traverse(child => {\n            child.userData.buildingUid = uid;\n        });\n        this.ConfigureBuildingWorkAnimation(model, build.id);\n        this.scene.add(model);\n        this.buildings.set(uid, model);\n        this.buildingTransitions.set(uid, {\n            type: 'spawn',\n            elapsed: 0,\n            duration: 0.2,\n            startY: spawnStartY,\n            targetY: 0,\n        });\n        this.UpdateBuildProgress(uid);\n        // Play build sound once the game is fully loaded (not during initial map render)\n        if (this.threeJsLoadedFull) {\n            const buildSoundFile = (_b = (_a = cfg.sounds) === null || _a === void 0 ? void 0 : _a.build) !== null && _b !== void 0 ? _b : _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.sounds.build;\n            if (buildSoundFile) {\n                this.PlayPositionalSoundAt(pos, buildSoundFile, this.GetBuildingEffectiveVolume(cfg));\n            }\n        }\n    }\n    SpawnBuildingDebris(uid) {\n        const build = this.buildings.get(uid);\n        if (!build || !this.scene)\n            return;\n        const scale = 2.0;\n        const worldPos = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3();\n        build.getWorldPosition(worldPos);\n        const debrisColor = new three__WEBPACK_IMPORTED_MODULE_9__.Color('#6d5f57');\n        const count = this.GetDebrisCount();\n        for (let index = 0; index < count; index += 1) {\n            const debris = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.randFloat(8, 18), three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.randFloat(4, 10), three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.randFloat(8, 18)), new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({\n                color: debrisColor,\n                roughness: 1,\n                metalness: 0,\n            }));\n            debris.scale.set(scale, scale, scale);\n            debris.position.set(worldPos.x + three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.randFloatSpread(40), 4 + three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.randFloat(2, 14), worldPos.z + three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.randFloatSpread(40));\n            debris.rotation.set(Math.random() * Math.PI, Math.random() * Math.PI, Math.random() * Math.PI);\n            this.scene.add(debris);\n            this.buildingDebris.push({\n                mesh: debris,\n                velocity: new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.randFloatSpread(70), three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.randFloat(35, 90), three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.randFloatSpread(70)),\n                angularVelocity: new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.randFloatSpread(6), three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.randFloatSpread(6), three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.randFloatSpread(6)),\n                life: 0,\n                maxLife: three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.randFloat(0.35, 0.6),\n            });\n        }\n    }\n    TickBuildingDebris(delta) {\n        if (this.buildingDebris.length === 0)\n            return;\n        const gravity = 220;\n        this.buildingDebris = this.buildingDebris.filter(entry => {\n            entry.life += delta;\n            entry.velocity.y -= gravity * delta;\n            entry.mesh.position.addScaledVector(entry.velocity, delta);\n            entry.mesh.rotation.x += entry.angularVelocity.x * delta;\n            entry.mesh.rotation.y += entry.angularVelocity.y * delta;\n            entry.mesh.rotation.z += entry.angularVelocity.z * delta;\n            const material = entry.mesh.material;\n            material.transparent = true;\n            material.opacity = three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.clamp(1 - entry.life / entry.maxLife, 0, 1);\n            if (entry.life >= entry.maxLife || entry.mesh.position.y <= -2) {\n                this.DisposeObject(entry.mesh);\n                return false;\n            }\n            return true;\n        });\n    }\n    GetTurretAmmoCount(build) {\n        var _a;\n        if (!build)\n            return 0;\n        const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(b => b.id === build.id);\n        const ammoId = (_a = cfg === null || cfg === void 0 ? void 0 : cfg.turret) === null || _a === void 0 ? void 0 : _a.ammoItemId;\n        if (!ammoId)\n            return 0;\n        return (build.data.input || [])\n            .filter(slot => slot && slot.id === ammoId)\n            .reduce((sum, slot) => sum + Number(slot.amount || 0), 0);\n    }\n    /** Combined volume for a building: global sounds × in-game soundVolume × camera-height factor × building volume multiplier */\n    GetBuildingEffectiveVolume(cfg) {\n        var _a, _b, _c;\n        const global = _controllers_state__WEBPACK_IMPORTED_MODULE_7__.Store.getSoundsVolume() / 100;\n        const inGame = ((_a = this.graphicsSettings.soundVolume) !== null && _a !== void 0 ? _a : 100) / 100;\n        const buildingMul = (_c = (_b = cfg === null || cfg === void 0 ? void 0 : cfg.sounds) === null || _b === void 0 ? void 0 : _b.volume) !== null && _c !== void 0 ? _c : 1;\n        const [minH, maxH] = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.cameraLimit;\n        const t = Math.max(0, Math.min(1, (this.cameraOffset - minH) / (maxH - minH)));\n        const cameraFactor = 1.0 - t * 0.9; // 1.0 at min height (max zoom), 0.1 at max height\n        return global * inGame * cameraFactor * buildingMul;\n    }\n    /** Refresh volumes of all active looping building sounds (call after settings/zoom change). */\n    UpdateAllIdleSoundVolumes() {\n        this.typeIdleSounds.forEach((entry, buildId) => {\n            var _a, _b;\n            const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(b => b.id === buildId);\n            const volume = this.GetBuildingEffectiveVolume(cfg);\n            (_a = entry.active) === null || _a === void 0 ? void 0 : _a.setVolume(volume);\n            (_b = entry.inactive) === null || _b === void 0 ? void 0 : _b.setVolume(volume);\n        });\n    }\n    /** Play a one-shot positional sound at a world xz position */\n    PlayPositionalSoundAt(worldPos, soundFile, volume = 1) {\n        if (!this.scene)\n            return;\n        const fileNoExt = String(soundFile || '').replace(/\\.(mp3|ogg|wav)$/i, '');\n        const buffer = this.sounds.get(soundFile) ||\n            this.sounds.get(`${fileNoExt}.mp3`) ||\n            this.sounds.get(`${fileNoExt}.ogg`) ||\n            this.sounds.get(`${fileNoExt}.wav`) ||\n            this.sounds.get(fileNoExt);\n        if (!buffer)\n            return;\n        const sound = new three__WEBPACK_IMPORTED_MODULE_9__.PositionalAudio(this.audio_listener);\n        sound.setBuffer(buffer);\n        sound.setRefDistance(400);\n        sound.setMaxDistance(_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.sounds.max_distance);\n        sound.setRolloffFactor(1.5);\n        sound.setDistanceModel('exponential');\n        sound.setVolume(volume);\n        sound.setLoop(false);\n        const anchor = new three__WEBPACK_IMPORTED_MODULE_9__.Object3D();\n        anchor.position.set(worldPos.x, 10, worldPos.z);\n        this.scene.add(anchor);\n        anchor.add(sound);\n        sound.play();\n        const cleanupMs = Math.ceil(buffer.duration * 1000) + 300;\n        setTimeout(() => {\n            try {\n                sound.stop();\n                anchor.removeFromParent();\n            }\n            catch (_a) {\n                // already cleaned up\n            }\n        }, cleanupMs);\n    }\n    /** Create a looping PositionalAudio placed directly in the scene (not bound to any model). */\n    CreateBuildingLoopSound(soundFile, volume) {\n        if (!this.scene)\n            return null;\n        const buffer = this.sounds.get(soundFile);\n        if (!buffer)\n            return null;\n        const sound = new three__WEBPACK_IMPORTED_MODULE_9__.PositionalAudio(this.audio_listener);\n        sound.setBuffer(buffer);\n        sound.setRefDistance(400);\n        sound.setMaxDistance(_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.sounds.max_distance);\n        sound.setRolloffFactor(1.5);\n        sound.setDistanceModel('exponential');\n        sound.setVolume(volume);\n        sound.setLoop(true);\n        // Add directly to scene — we reposition it each tick to the nearest instance.\n        this.scene.add(sound);\n        return sound;\n    }\n    /**\n     * No-op: per-instance sound setup is gone.\n     * The shared type-level nodes are created lazily by TickIdleSounds.\n     */\n    SetupBuildingIdleSounds(_uid, _model, _cfg) {\n        // intentionally empty — see TickIdleSounds\n    }\n    /**\n     * No-op: shared type nodes are managed by TickIdleSounds and ClearThreeJs.\n     * Removing a single instance never destroys the shared node.\n     */\n    StopBuildingIdleSounds(_uid) {\n        // intentionally empty — see TickIdleSounds\n    }\n    /**\n     * Reposition and play/stop the shared idle-sound nodes for every building\n     * type that currently has instances in the scene.  Called once per\n     * animation tick from TickBuildingWorkAnimations.\n     *\n     * For each type we track:\n     *   • active  node → plays at the nearest *working* building to the camera\n     *   • inactive node → plays at the nearest *idle* building to the camera\n     *\n     * When no buildings of that type remain in the scene both nodes are stopped.\n     */\n    TickIdleSounds() {\n        var _a, _b;\n        if (!this.scene || !this.camera)\n            return;\n        const camPos = this.camera.position;\n        const byType = new Map();\n        for (const build of this.state.gameData.buildings) {\n            const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(b => b.id === build.id);\n            if (!((_a = cfg === null || cfg === void 0 ? void 0 : cfg.sounds) === null || _a === void 0 ? void 0 : _a.idle))\n                continue;\n            if (!cfg.sounds.idle.active && !cfg.sounds.idle.inactive)\n                continue;\n            if (!byType.has(build.id)) {\n                byType.set(build.id, {\n                    cfg,\n                    nearestActivePos: null,\n                    nearestInactivePos: null,\n                });\n            }\n            const summary = byType.get(build.id);\n            const worldCenter = this.GetBuildWorldCenter(build);\n            const pos = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(worldCenter.x, 10, worldCenter.z);\n            const progress = Number((_b = build.data) === null || _b === void 0 ? void 0 : _b.progress);\n            const isWorking = Number.isFinite(progress) && progress >= 1 && progress <= 99;\n            if (isWorking) {\n                if (!summary.nearestActivePos ||\n                    pos.distanceTo(camPos) < summary.nearestActivePos.distanceTo(camPos)) {\n                    summary.nearestActivePos = pos;\n                }\n            }\n            else {\n                if (!summary.nearestInactivePos ||\n                    pos.distanceTo(camPos) < summary.nearestInactivePos.distanceTo(camPos)) {\n                    summary.nearestInactivePos = pos;\n                }\n            }\n        }\n        // Ensure shared nodes exist for every live type; update position + play state.\n        for (const [buildId, summary] of byType) {\n            if (!this.typeIdleSounds.has(buildId)) {\n                const volume = this.GetBuildingEffectiveVolume(summary.cfg);\n                this.typeIdleSounds.set(buildId, {\n                    active: summary.cfg.sounds.idle.active\n                        ? this.CreateBuildingLoopSound(summary.cfg.sounds.idle.active, volume)\n                        : null,\n                    inactive: summary.cfg.sounds.idle.inactive\n                        ? this.CreateBuildingLoopSound(summary.cfg.sounds.idle.inactive, volume)\n                        : null,\n                });\n            }\n            const entry = this.typeIdleSounds.get(buildId);\n            // Active node\n            if (entry.active) {\n                if (summary.nearestActivePos) {\n                    entry.active.position.copy(summary.nearestActivePos);\n                    if (!entry.active.isPlaying)\n                        entry.active.play();\n                }\n                else {\n                    if (entry.active.isPlaying)\n                        entry.active.stop();\n                }\n            }\n            // Inactive node\n            if (entry.inactive) {\n                if (summary.nearestInactivePos) {\n                    entry.inactive.position.copy(summary.nearestInactivePos);\n                    if (!entry.inactive.isPlaying)\n                        entry.inactive.play();\n                }\n                else {\n                    if (entry.inactive.isPlaying)\n                        entry.inactive.stop();\n                }\n            }\n        }\n        // Stop nodes for types that have no buildings left in the scene.\n        this.typeIdleSounds.forEach((entry, buildId) => {\n            var _a, _b;\n            if (byType.has(buildId))\n                return;\n            try {\n                if ((_a = entry.active) === null || _a === void 0 ? void 0 : _a.isPlaying)\n                    entry.active.stop();\n                if ((_b = entry.inactive) === null || _b === void 0 ? void 0 : _b.isPlaying)\n                    entry.inactive.stop();\n            }\n            catch (_c) {\n                // ignore\n            }\n        });\n    }\n    /**\n     * Play the `custom` sound of a building at its world position.\n     * Intended for turret shots, manipulator arm movements, etc.\n     */\n    PlayBuildingCustomSound(uid) {\n        var _a;\n        const build = this.state.gameData.buildings.find(b => b.uid === uid);\n        if (!build)\n            return;\n        const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(b => b.id === build.id);\n        if (!((_a = cfg === null || cfg === void 0 ? void 0 : cfg.sounds) === null || _a === void 0 ? void 0 : _a.custom))\n            return;\n        const pos = this.GetBuildWorldCenter(build);\n        this.PlayPositionalSoundAt(pos, cfg.sounds.custom, this.GetBuildingEffectiveVolume(cfg));\n    }\n    SpawnTurretBulletFx(from, to) {\n        if (!this.scene)\n            return;\n        const dir = to.clone().sub(from);\n        const dist = dir.length();\n        if (dist < 1)\n            return;\n        const speed = 3200;\n        dir.normalize();\n        const seg = this.GetVfxSegments();\n        const mesh = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.SphereGeometry(5, seg, seg), new three__WEBPACK_IMPORTED_MODULE_9__.MeshBasicMaterial({ color: '#ffd07a' }));\n        mesh.position.copy(from);\n        this.scene.add(mesh);\n        this.combatProjectiles.push({\n            mesh,\n            velocity: dir.multiplyScalar(speed),\n            life: 0,\n            maxLife: dist / speed,\n        });\n    }\n    SpawnLaserFx(from, to) {\n        if (!this.scene)\n            return;\n        const geometry = new three__WEBPACK_IMPORTED_MODULE_9__.BufferGeometry().setFromPoints([from, to]);\n        const material = new three__WEBPACK_IMPORTED_MODULE_9__.LineBasicMaterial({\n            color: '#76f7ff',\n            transparent: true,\n            opacity: 0.95,\n        });\n        const line = new three__WEBPACK_IMPORTED_MODULE_9__.Line(geometry, material);\n        this.scene.add(line);\n        this.combatLasers.push({ line, life: 0, maxLife: 0.08 });\n    }\n    SpawnImpactFx(pos, color = '#ffcf88', radius = 16) {\n        if (!this.scene)\n            return;\n        const seg = this.GetVfxSegments();\n        const mesh = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.SphereGeometry(Math.max(4, radius * 0.35), seg + 2, seg), new three__WEBPACK_IMPORTED_MODULE_9__.MeshBasicMaterial({\n            color,\n            transparent: true,\n            opacity: 0.9,\n        }));\n        mesh.position.copy(pos);\n        this.scene.add(mesh);\n        this.combatImpacts.push({ mesh, life: 0, maxLife: 0.18 });\n    }\n    SpawnAutomatonDestroyFx(pos, team) {\n        const c = this.GetTeamColorById(team);\n        this.SpawnImpactFx(new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(pos.x, 20, pos.z), c, 40);\n        this.SpawnImpactFx(new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(pos.x, 20, pos.z), '#ff895a', 28);\n    }\n    ActionAutomatonShot(data) {\n        var _a, _b;\n        this.automatonLastShotTargetByUid.set(data.automatonUid, data.targetBuildingUid);\n        const fireHeight = this.GetAutomatonFireHeight(data.automatonId);\n        const from = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(data.from.x, fireHeight, data.from.z);\n        const target = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(data.targetCenter.x, 20, data.targetCenter.z);\n        const impact = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(data.impact.x, 20, data.impact.z);\n        this.SpawnLaserFx(from, target);\n        this.SpawnImpactFx(impact, '#76f7ff', 32);\n        const unitCfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsAutomatonUnits.find(u => u.id === data.automatonId);\n        if (!((_a = unitCfg === null || unitCfg === void 0 ? void 0 : unitCfg.shootSound) === null || _a === void 0 ? void 0 : _a.name))\n            return;\n        const targetBuild = this.state.gameData.buildings.find(build => build.uid === data.targetBuildingUid);\n        const targetBuildCfg = targetBuild\n            ? _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(buildCfg => buildCfg.id === targetBuild.id)\n            : undefined;\n        this.PlayPositionalSoundAt({ x: data.from.x, z: data.from.z }, unitCfg.shootSound.name, ((_b = unitCfg.shootSound.volume) !== null && _b !== void 0 ? _b : 1) *\n            this.GetBuildingEffectiveVolume(targetBuildCfg));\n    }\n    ConfigureBuildingWorkAnimation(model, buildId) {\n        var _a, _b;\n        const animation = {\n            type: buildId,\n            time: 0,\n        };\n        const collectByPrefix = (prefix) => {\n            const result = [];\n            model.traverse(node => {\n                if (node instanceof three__WEBPACK_IMPORTED_MODULE_9__.Mesh && node.name.startsWith(prefix)) {\n                    result.push(node);\n                }\n            });\n            return result;\n        };\n        if (['miner', 'well', 'lumberjack'].includes(buildId)) {\n            const rotor = model.getObjectByName('MinerTopRotor');\n            if (!rotor)\n                return;\n            animation.rotor = rotor;\n            animation.rotorBaseY = Number((_a = rotor.userData.minerHitBaseY) !== null && _a !== void 0 ? _a : rotor.position.y);\n            animation.rotorAmplitude = Math.max(1, Number(rotor.userData.minerHitAmplitude || 20));\n        }\n        else if (buildId === 'smelter') {\n            const windowMesh = model.getObjectByName('SmelterWindow');\n            const smokeMeshes = collectByPrefix('SmelterSmoke');\n            if (!windowMesh && smokeMeshes.length === 0)\n                return;\n            animation.windowMesh = windowMesh;\n            animation.smokeMeshes = smokeMeshes;\n            const windowMaterial = windowMesh === null || windowMesh === void 0 ? void 0 : windowMesh.material;\n            animation.baseLight = (windowMaterial === null || windowMaterial === void 0 ? void 0 : windowMaterial.emissiveIntensity) || 0;\n        }\n        else if (buildId === 'fabric') {\n            const windowMesh = model.getObjectByName('FabricWindow');\n            const smokeMeshes = collectByPrefix('FabricSmoke');\n            if (!windowMesh && smokeMeshes.length === 0)\n                return;\n            animation.windowMesh = windowMesh;\n            animation.smokeMeshes = smokeMeshes;\n            const windowMaterial = windowMesh === null || windowMesh === void 0 ? void 0 : windowMesh.material;\n            animation.baseLight = (windowMaterial === null || windowMaterial === void 0 ? void 0 : windowMaterial.emissiveIntensity) || 0;\n        }\n        else if (buildId === 'laboratory') {\n            const orb = model.getObjectByName('LaboratoryCoreOrb');\n            if (!orb)\n                return;\n            animation.orb = orb;\n            animation.baseLight =\n                ((_b = orb.material) === null || _b === void 0 ? void 0 : _b.emissiveIntensity) || 0;\n            animation.baseScale = orb.scale.clone();\n        }\n        else {\n            return;\n        }\n        model.userData.factoryWorkAnimation = animation;\n    }\n    UpdateSmokeAnimation(smokeMeshes, active, time, rise = 60, maxOpacity = 0.32) {\n        // On low quality hide all smoke particles entirely\n        if (!this.IsSmokeEnabled()) {\n            smokeMeshes.forEach(mesh => {\n                mesh.material.opacity = 0;\n            });\n            return;\n        }\n        // On medium quality use reduced opacity\n        const qualityOpacity = this.graphicsSettings.vfxQuality === 'medium'\n            ? maxOpacity * 0.5\n            : maxOpacity;\n        smokeMeshes.forEach(mesh => {\n            const material = mesh.material;\n            const baseY = Number(mesh.userData.smokeBaseY || mesh.position.y);\n            const baseScale = Number(mesh.userData.smokeBaseScale || 1);\n            const phase = Number(mesh.userData.smokePhase || 0);\n            if (!active) {\n                mesh.position.y = baseY;\n                mesh.scale.setScalar(baseScale);\n                material.opacity = 0;\n                return;\n            }\n            const cycle = (time * 0.7 + phase) % 1;\n            const alpha = Math.sin(cycle * Math.PI);\n            mesh.position.y = baseY + cycle * rise;\n            mesh.scale.setScalar(baseScale * (0.9 + cycle * 1.1));\n            material.opacity = Math.max(0, alpha) * qualityOpacity;\n        });\n    }\n    TickBuildingWorkAnimations(delta) {\n        // Update shared-per-type idle sounds (repositions 1 node per type to nearest building)\n        this.TickIdleSounds();\n        this.buildings.forEach((model, uid) => {\n            var _a;\n            const animation = model.userData.factoryWorkAnimation;\n            if (!animation)\n                return;\n            const build = this.state.gameData.buildings.find(item => item.uid === uid);\n            const progress = Number((_a = build === null || build === void 0 ? void 0 : build.data) === null || _a === void 0 ? void 0 : _a.progress);\n            const active = Number.isFinite(progress) && progress >= 1 && progress <= 99;\n            if (active) {\n                animation.time = Number(animation.time || 0) + delta;\n            }\n            const time = Number(animation.time || 0);\n            if (['miner', 'well', 'lumberjack'].includes(animation.type) &&\n                animation.rotor) {\n                const baseY = Number(animation.rotorBaseY || animation.rotor.position.y);\n                const amplitude = Number(animation.rotorAmplitude || 16);\n                if (!active) {\n                    animation.rotor.position.y = baseY;\n                    return;\n                }\n                const strike = Math.abs(Math.sin(time * 8.5));\n                const rebound = Math.sin(time * 17) * 0.06;\n                animation.rotor.position.y =\n                    baseY - strike * amplitude + rebound * amplitude;\n                return;\n            }\n            if ((animation.type === 'smelter' || animation.type === 'fabric') &&\n                animation.windowMesh) {\n                const windowMesh = animation.windowMesh;\n                const material = windowMesh.material;\n                // On low quality skip the animated pulse, keep a static dim glow\n                if (this.IsSmokeEnabled()) {\n                    const pulse = active ? 0.65 + (Math.sin(time * 10) + 1) * 0.35 : 0;\n                    material.emissiveIntensity = (animation.baseLight || 0) * pulse;\n                }\n                else {\n                    material.emissiveIntensity = active ? animation.baseLight || 0 : 0;\n                }\n                this.UpdateSmokeAnimation((animation.smokeMeshes || []), active, time, animation.type === 'fabric' ? 70 : 55, animation.type === 'fabric' ? 0.26 : 0.34);\n                return;\n            }\n            if (animation.type === 'laboratory' && animation.orb) {\n                const orb = animation.orb;\n                const material = orb.material;\n                if (this.IsSmokeEnabled()) {\n                    const glow = active ? 0.55 + (Math.sin(time * 7) + 1) * 0.55 : 0.18;\n                    material.emissiveIntensity = (animation.baseLight || 0) * glow;\n                    const baseScale = animation.baseScale ||\n                        new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(1, 1, 1);\n                    const scalePulse = active ? 1 + Math.sin(time * 6) * 0.12 : 1;\n                    orb.scale.copy(baseScale).multiplyScalar(scalePulse);\n                }\n                else {\n                    material.emissiveIntensity = active ? animation.baseLight || 0 : 0.18;\n                }\n            }\n        });\n    }\n    TickCombatVfx(delta) {\n        if (this.combatProjectiles.length > 0) {\n            this.combatProjectiles = this.combatProjectiles.filter(entry => {\n                entry.life += delta;\n                entry.mesh.position.addScaledVector(entry.velocity, delta);\n                if (entry.life >= entry.maxLife) {\n                    this.SpawnImpactFx(entry.mesh.position.clone(), '#ffcf88', 12);\n                    this.DisposeObject(entry.mesh);\n                    return false;\n                }\n                return true;\n            });\n        }\n        if (this.combatLasers.length > 0) {\n            this.combatLasers = this.combatLasers.filter(entry => {\n                entry.life += delta;\n                const material = entry.line.material;\n                material.opacity = three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.clamp(1 - entry.life / entry.maxLife, 0, 1);\n                if (entry.life >= entry.maxLife) {\n                    this.DisposeObject(entry.line);\n                    return false;\n                }\n                return true;\n            });\n        }\n        if (this.combatImpacts.length > 0) {\n            this.combatImpacts = this.combatImpacts.filter(entry => {\n                entry.life += delta;\n                const t = three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.clamp(entry.life / entry.maxLife, 0, 1);\n                const material = entry.mesh.material;\n                material.opacity = 1 - t;\n                const scale = 1 + t * 1.8;\n                entry.mesh.scale.set(scale, scale, scale);\n                if (entry.life >= entry.maxLife) {\n                    this.DisposeObject(entry.mesh);\n                    return false;\n                }\n                return true;\n            });\n        }\n    }\n    TickBuildingTransitions(delta) {\n        if (this.buildingTransitions.size === 0)\n            return;\n        this.buildingTransitions.forEach((transition, uid) => {\n            const model = this.buildings.get(uid);\n            if (!model) {\n                this.buildingTransitions.delete(uid);\n                return;\n            }\n            transition.elapsed = Math.min(transition.elapsed + delta, transition.duration);\n            const progress = three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.clamp(transition.elapsed / transition.duration, 0, 1);\n            const eased = three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.smoothstep(progress, 0, 1);\n            model.position.y = three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.lerp(transition.startY, transition.targetY, eased);\n            if (progress < 1)\n                return;\n            if (transition.type === 'despawn') {\n                this.building_progress.delete(uid);\n                this.building_progress_fill.delete(uid);\n                const mixer = this.animationBuildings.get(uid);\n                if (mixer) {\n                    mixer.stopAllAction();\n                    mixer.uncacheRoot(model);\n                    this.animationBuildings.delete(uid);\n                }\n                this.DisposeObject(model);\n                this.buildings.delete(uid);\n            }\n            this.buildingTransitions.delete(uid);\n        });\n    }\n    UpdateBuildProgress(uid) {\n        const oldProgressGroup = this.building_progress.get(uid);\n        if (oldProgressGroup) {\n            oldProgressGroup.removeFromParent();\n            this.building_progress.delete(uid);\n            this.building_progress_fill.delete(uid);\n        }\n    }\n    RemoveBuild(uid) {\n        var _a, _b;\n        const old = this.buildings.get(uid);\n        if (!old)\n            return;\n        if (((_a = this.buildingTransitions.get(uid)) === null || _a === void 0 ? void 0 : _a.type) === 'despawn')\n            return;\n        // Play destroy sound\n        const buildData = this.state.gameData.buildings.find(b => b.uid === uid);\n        const cfg = buildData\n            ? _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(b => b.id === buildData.id)\n            : undefined;\n        this.building_output_snapshots.delete(uid);\n        if ((_b = cfg === null || cfg === void 0 ? void 0 : cfg.sounds) === null || _b === void 0 ? void 0 : _b.destroy) {\n            const pos = old.position;\n            this.PlayPositionalSoundAt({ x: pos.x, z: pos.z }, cfg.sounds.destroy, this.GetBuildingEffectiveVolume(cfg));\n        }\n        const box = new three__WEBPACK_IMPORTED_MODULE_9__.Box3().setFromObject(old);\n        const size = box.getSize(new three__WEBPACK_IMPORTED_MODULE_9__.Vector3());\n        this.SpawnBuildingDebris(uid);\n        this.buildingTransitions.set(uid, {\n            type: 'despawn',\n            elapsed: 0,\n            duration: 0.2,\n            startY: old.position.y,\n            targetY: -size.y - 1,\n        });\n    }\n    UpdateBuild(uid) {\n        var _a, _b;\n        const build = this.state.gameData.buildings.find(b => b.uid === uid);\n        if (!build)\n            return;\n        const model = this.buildings.get(uid);\n        if (!model)\n            return;\n        const pos = this.GetBuildWorldCenter(build);\n        model.position.x = pos.x;\n        model.position.z = pos.z;\n        if (((_a = this.buildingTransitions.get(uid)) === null || _a === void 0 ? void 0 : _a.type) !== 'spawn') {\n            model.position.y = 0;\n        }\n        if (build.id === 'turret' &&\n            typeof ((_b = build.data.additional) === null || _b === void 0 ? void 0 : _b.turretAimYaw) === 'number') {\n            model.rotation.y = Number(build.data.additional.turretAimYaw);\n        }\n        else {\n            if (build.id === 'underground_conveyor') {\n                const connection = this.GetUndergroundConnection(build);\n                const isExit = !!connection && connection.exit.uid === build.uid;\n                model.rotation.y =\n                    build.rotation * (Math.PI / 2) + (isExit ? Math.PI : 0);\n                const indicatorRoot = model.getObjectByName('UndergroundDirectionIndicator');\n                const indicatorArrow = model.getObjectByName('UndergroundDirectionIndicatorArrow');\n                const indicatorCross = model.getObjectByName('UndergroundDirectionIndicatorCross');\n                if (indicatorRoot && indicatorArrow && indicatorCross) {\n                    const hasConnection = !!connection;\n                    indicatorArrow.visible = hasConnection;\n                    indicatorCross.visible = !hasConnection;\n                    if (hasConnection && connection) {\n                        const worldFlowYaw = connection.entry.rotation * (Math.PI / 2);\n                        indicatorRoot.rotation.y =\n                            worldFlowYaw - model.rotation.y + Math.PI;\n                    }\n                }\n            }\n            else {\n                model.rotation.y = build.rotation * (Math.PI / 2);\n            }\n        }\n        this.UpdateBuildProgress(uid);\n    }\n    TickBuilds() {\n        const exist = new Set(this.state.gameData.buildings.map(b => b.uid));\n        this.state.gameData.buildings.forEach(build => {\n            if (!this.buildings.has(build.uid)) {\n                this.CreateBuild(build.uid);\n            }\n            else {\n                this.UpdateBuild(build.uid);\n            }\n            const model = this.buildings.get(build.uid);\n            if (model)\n                model.visible = this.IsBuildingVisibleInFog(build);\n        });\n        this.buildings.forEach((model, uid) => {\n            if (!exist.has(uid)) {\n                this.RemoveBuild(uid);\n            }\n        });\n    }\n    TickAutomatons() {\n        if (!this.scene)\n            return;\n        const automatons = this.state.gameData.automatons || [];\n        const existUids = new Set(automatons.map(a => a.uid));\n        this.automatonLastShotTargetByUid.forEach((_, uid) => {\n            if (!existUids.has(uid)) {\n                this.automatonLastShotTargetByUid.delete(uid);\n            }\n        });\n        // Remove stale models\n        this.automatons_models.forEach((model, uid) => {\n            if (!existUids.has(uid)) {\n                this.scene.remove(model);\n                this.automatons_models.delete(uid);\n                this.automatonVisualTargets.delete(uid);\n            }\n        });\n        for (const automaton of automatons) {\n            const teamColor = this.GetTeamColorById(automaton.team);\n            const unitCfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsAutomatonUnits.find(unit => unit.id === automaton.id);\n            const flyHeight = Number((unitCfg === null || unitCfg === void 0 ? void 0 : unitCfg.fly) || 0);\n            const targetPos = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(automaton.position.x, flyHeight, automaton.position.z);\n            this.automatonVisualTargets.set(automaton.uid, targetPos);\n            let model = this.automatons_models.get(automaton.uid);\n            if (!model) {\n                const generated = this.GenerateModel(automaton.id, teamColor);\n                if (!generated)\n                    continue;\n                generated.userData.automatonUid = automaton.uid;\n                generated.traverse(child => {\n                    child.userData.automatonUid = automaton.uid;\n                });\n                generated.position.copy(targetPos);\n                this.ApplyShadowForObject(generated);\n                this.scene.add(generated);\n                this.automatons_models.set(automaton.uid, generated);\n                model = generated;\n            }\n            if (model.position.distanceTo(targetPos) >\n                _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize * 3) {\n                model.position.copy(targetPos);\n            }\n            model.visible = this.IsAutomatonVisibleInFog(automaton);\n        }\n    }\n    TickAutomatonVisuals(delta) {\n        if (delta <= 0)\n            return;\n        this.automatons_models.forEach((model, uid) => {\n            const target = this.automatonVisualTargets.get(uid);\n            if (!target)\n                return;\n            const before = model.position.clone();\n            const alpha = Math.min(1, delta * 8);\n            model.position.lerp(target, alpha);\n            const vx = model.position.x - before.x;\n            const vz = model.position.z - before.z;\n            if (Math.abs(vx) > 0.001 || Math.abs(vz) > 0.001) {\n                model.rotation.y = Math.atan2(vx, vz);\n            }\n        });\n    }\n    // Только создание/удаление модели и телепорт при большом расхождении\n    ActionDrop(uid) {\n        const drop = this.state.gameData.drop.find(d => d.uid === uid);\n        if (!drop)\n            return;\n        const item = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.items.find(i => i.id === drop.id);\n        if (!item)\n            return;\n        let model = this.dropItems.get(uid);\n        if (!model) {\n            model = this.GetModel(item.model, item.color);\n            if (!model)\n                return;\n            model.scale.set(_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.dropModelScale, _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.dropModelScale, _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.dropModelScale);\n            this.ApplyResouceColor(model, item.color);\n            model.userData.dropUid = uid;\n            model.traverse(child => {\n                child.userData.dropUid = uid;\n            });\n            this.scene.add(model);\n            this.dropItems.set(uid, model);\n            // Первый спавн — сразу на серверную позицию\n            model.position.set(drop.position.x, 4, drop.position.z);\n            return;\n        }\n        if (this.dropManipulatorAnimations.has(uid))\n            return;\n        // Мягкая коррекция: если визуальная позиция слишком далеко от серверной\n        const serverPos = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(drop.position.x, 4, drop.position.z);\n        if (model.position.distanceTo(serverPos) >\n            _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize * 2) {\n            model.position.copy(serverPos);\n        }\n    }\n    // Батч-резолвер: тот же алгоритм что на сервере, но по визуальным позициям\n    ResolveDropMovements(delta) {\n        const drops = this.state.gameData.drop;\n        if (drops.length === 0)\n            return;\n        const movers = [];\n        for (const drop of drops) {\n            if (this.dropManipulatorAnimations.has(drop.uid))\n                continue;\n            const model = this.dropItems.get(drop.uid);\n            if (!model)\n                continue;\n            const vel = this.CalculateVectorOffset(model.position);\n            if (!vel || (vel.x === 0 && vel.z === 0))\n                continue;\n            const speed = Math.sqrt(vel.x * vel.x + vel.z * vel.z);\n            const dirX = vel.x / speed;\n            const dirZ = vel.z / speed;\n            movers.push({\n                uid: drop.uid,\n                model,\n                velX: vel.x,\n                velZ: vel.z,\n                dirX,\n                dirZ,\n                latDirX: -dirZ,\n                latDirZ: dirX,\n            });\n        }\n        // 2. Для каждого движущегося предмета находим блокирующий его предмет\n        const minDist = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.drop_move_distance;\n        const minDistSq = minDist * minDist;\n        const laneHalfWidth = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize * 0.75;\n        // uid → uid предмета, который его блокирует (если есть)\n        const blockedBy = new Map();\n        for (const mover of movers) {\n            const mx = mover.model.position.x;\n            const mz = mover.model.position.z;\n            for (const drop of drops) {\n                if (drop.uid === mover.uid)\n                    continue;\n                const other = this.dropItems.get(drop.uid);\n                if (!other)\n                    continue;\n                const dx = other.position.x - mx;\n                const dz = other.position.z - mz;\n                const dotForward = dx * mover.dirX + dz * mover.dirZ;\n                if (dotForward <= 0)\n                    continue;\n                const dotLateral = Math.abs(dx * mover.latDirX + dz * mover.latDirZ);\n                if (dotLateral >= laneHalfWidth)\n                    continue;\n                const distSq = dx * dx + dz * dz;\n                if (distSq < minDistSq) {\n                    blockedBy.set(mover.uid, drop.uid);\n                    break;\n                }\n            }\n        }\n        // 3. Разрешаем взаимные блокировки: если A мешает B, а B мешает A —\n        //    оба игнорируют блокировку (предметы с разных лент встретились на стыке).\n        const finalCanMove = new Set();\n        for (const mover of movers) {\n            const blocker = blockedBy.get(mover.uid);\n            if (blocker === undefined) {\n                finalCanMove.add(mover.uid);\n                continue;\n            }\n            // Взаимная блокировка — пропускаем только того, чей uid меньше\n            if (blockedBy.get(blocker) === mover.uid && mover.uid < blocker) {\n                finalCanMove.add(mover.uid);\n            }\n        }\n        // 3. Применяем движение\n        for (const mover of movers) {\n            if (!finalCanMove.has(mover.uid))\n                continue;\n            mover.model.position.x += mover.velX * delta;\n            mover.model.position.z += mover.velZ * delta;\n        }\n    }\n    TickDrops(delta) {\n        const exist = new Set(this.state.gameData.drop.map(d => d.uid));\n        this.state.gameData.drop.forEach(drop => this.ActionDrop(drop.uid));\n        this.dropItems.forEach((_, uid) => {\n            if (!exist.has(uid))\n                this.RemoveDrop(uid);\n        });\n        this.state.gameData.drop.forEach(drop => {\n            const model = this.dropItems.get(drop.uid);\n            if (!model)\n                return;\n            model.visible = this.IsWorldPositionVisibleInFog(drop.position);\n        });\n        if (this.hoveredDropUid && !this.dropItems.has(this.hoveredDropUid)) {\n            this.hoveredDropUid = undefined;\n        }\n        this.ResolveDropMovements(delta);\n    }\n    RemoveDrop(uid) {\n        this.dropManipulatorAnimations.delete(uid);\n        const model = this.dropItems.get(uid);\n        if (model) {\n            this.DisposeObject(model);\n            this.dropItems.delete(uid);\n        }\n    }\n    TickTeams() {\n        const signature = JSON.stringify({\n            map: this.state.gameData.mapSize,\n            teamCount: this.teamCount,\n            myTeamOrder: this.myTeamOrder,\n        });\n        if (signature === this.arenaWallsSignature)\n            return;\n        this.arenaWallsSignature = signature;\n        if (!this.threeJsReady)\n            return;\n        this.CreateArenaWalls(this.state.gameData.mapSize);\n    }\n    TickPlayers() {\n        const exist = new Set();\n        this.state.players.forEach(player => {\n            this.SpawnPlayer(player.id);\n            exist.add(player.id);\n        });\n        this.players_models.forEach((model, playerid) => {\n            if (!exist.has(playerid)) {\n                this.RemovePlayer(playerid);\n                return;\n            }\n            model.visible = this.IsPlayerVisibleInFog(playerid);\n            const marker = this.players_minimap_markers.get(playerid);\n            if (marker)\n                marker.visible = this.IsPlayerVisibleInFog(playerid);\n        });\n        this.TickTeams();\n    }\n    CloneObject(obj) {\n        if (!obj)\n            return undefined;\n        const cloneObj = (0,three_examples_jsm_utils_SkeletonUtils_js__WEBPACK_IMPORTED_MODULE_10__.clone)(obj);\n        cloneObj.traverse((child) => {\n            if (child.isMesh) {\n                // Mark geometry as shared so DisposeObject won't free the underlying\n                // GPU buffer that belongs to the template (and all other instances).\n                if (child.geometry) {\n                    child.userData.sharedGeometry = true;\n                }\n                if (child.material) {\n                    if (child.material instanceof Array) {\n                        child.material = child.material.map(mat => mat.clone());\n                    }\n                    else {\n                        child.material = child.material.clone();\n                    }\n                    child.material.needsUpdate = true;\n                }\n            }\n        });\n        return cloneObj;\n    }\n    GetMinimapPlayerMarkerScale() {\n        var _a;\n        const mapSize = (_a = this.state.gameData) === null || _a === void 0 ? void 0 : _a.mapSize;\n        if (!mapSize)\n            return 42;\n        const worldSize = Math.max(mapSize.x, mapSize.z) * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n        return three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.clamp(worldSize * 0.026, 42, 96);\n    }\n    GetMinimapResourceMarkerScale() {\n        var _a;\n        const mapSize = (_a = this.state.gameData) === null || _a === void 0 ? void 0 : _a.mapSize;\n        if (!mapSize)\n            return 24;\n        const worldSize = Math.max(mapSize.x, mapSize.z) * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n        return three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.clamp(worldSize * 0.015, 22, 54);\n    }\n    GetMinimapPlayerColor(playerid) {\n        if (playerid === _api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id)\n            return '#39d353';\n        const player = this.state.players.find(q => q.id === playerid);\n        if (this.GetTeam(playerid) === this.myTeamId)\n            return '#4cc9f0';\n        return '#ff5d5d';\n    }\n    EscapeHtml(value) {\n        return value.replace(/[&<>\"']/g, char => {\n            switch (char) {\n                case '&':\n                    return '&amp;';\n                case '<':\n                    return '&lt;';\n                case '>':\n                    return '&gt;';\n                case '\"':\n                    return '&quot;';\n                case \"'\":\n                    return '&#39;';\n                default:\n                    return char;\n            }\n        });\n    }\n    CreateMinimapResourceMarker(ids, color) {\n        const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n        const outline = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(2.5, 0.3, 2.5), new three__WEBPACK_IMPORTED_MODULE_9__.MeshBasicMaterial({\n            color: '#111111',\n            depthTest: false,\n        }));\n        const fill = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(1.8, 0.55, 1.8), new three__WEBPACK_IMPORTED_MODULE_9__.MeshBasicMaterial({\n            color,\n            depthTest: false,\n        }));\n        outline.position.y = 0.1;\n        fill.position.y = 0.3;\n        group.add(outline);\n        group.add(fill);\n        group.position.y = 10;\n        group.traverse((child) => child.layers.set(2));\n        group.userData.fillMaterial = fill.material;\n        group.userData.ids = ids;\n        this.scene.add(group);\n        this.minimap_resource_markers.set(ids, group);\n        return group;\n    }\n    SyncMinimapResourceMarkers() {\n        if (!this.scene)\n            return;\n        const resourcePoints = this.state.gameData.map.filter(point => point.resource);\n        const existingIds = new Set(resourcePoints.map(point => point.ids));\n        const scale = this.GetMinimapResourceMarkerScale();\n        this.minimap_resource_markers.forEach((group, ids) => {\n            if (existingIds.has(ids))\n                return;\n            this.DisposeObject(group);\n            this.minimap_resource_markers.delete(ids);\n        });\n        resourcePoints.forEach(point => {\n            const item = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.items.find(i => { var _a; return i.id === ((_a = point.resource) === null || _a === void 0 ? void 0 : _a.id); });\n            if (!item)\n                return;\n            let marker = this.minimap_resource_markers.get(point.ids);\n            if (!marker) {\n                marker = this.CreateMinimapResourceMarker(point.ids, item.color);\n            }\n            const fillMaterial = marker.userData\n                .fillMaterial;\n            fillMaterial.color.set(item.color);\n            const coords = this.SnapToGrid({\n                x: point.x * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize,\n                z: point.z * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize,\n            });\n            marker.position.set(coords.x, 10, coords.z);\n            marker.scale.setScalar(scale);\n        });\n    }\n    BuildPlayerHudBadge(player, color, left, top, arrowAngle) {\n        const safeName = this.EscapeHtml(player.name);\n        const safeAvatar = this.EscapeHtml(player.avatar || '');\n        const arrow = typeof arrowAngle === 'number'\n            ? `<div style=\"width:0;height:0;transform:rotate(${arrowAngle}deg);border-top:10px solid transparent;border-bottom:10px solid transparent;border-left:18px solid ${color};filter:drop-shadow(0 0 8px rgba(0,0,0,0.45));\"></div>`\n            : '';\n        return (`<div style=\"position:absolute;left:${left}px;top:${top}px;transform:translate(-50%,-50%);display:flex;flex-direction:column;align-items:center;gap:6px;pointer-events:none;z-index:4;\">` +\n            arrow +\n            `<div style=\"display:flex;align-items:center;gap:8px;padding:6px 10px;border-radius:999px;background:rgba(7,12,20,0.82);border:1px solid ${color};box-shadow:0 8px 18px rgba(0,0,0,0.22);\">` +\n            `<img src=\"${safeAvatar}\" style=\"width:24px;height:24px;border-radius:50%;object-fit:cover;display:block;border:1px solid rgba(255,255,255,0.3);background:#1f2937;\" />` +\n            `<span style=\"color:#ffffff;font:600 12px/1.2 Oswald, sans-serif;white-space:nowrap;text-shadow:0 1px 2px rgba(0,0,0,0.5);\">${safeName}</span>` +\n            `</div>` +\n            `</div>`);\n    }\n    ProjectWorldToScreen(worldPos, width, height, pad = 18) {\n        if (!this.camera)\n            return undefined;\n        const cameraPos = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3();\n        const cameraDirection = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3();\n        this.camera.getWorldPosition(cameraPos);\n        this.camera.getWorldDirection(cameraDirection);\n        const projected = worldPos.clone().project(this.camera);\n        const sx = (projected.x * 0.5 + 0.5) * width;\n        const sy = (projected.y * -0.5 + 0.5) * height;\n        const isBehind = cameraDirection.dot(worldPos.clone().sub(cameraPos)) < 0;\n        const visibleOnScreen = !isBehind &&\n            projected.z >= -1 &&\n            projected.z <= 1 &&\n            sx >= pad &&\n            sx <= width - pad &&\n            sy >= pad &&\n            sy <= height - pad;\n        if (!visibleOnScreen)\n            return undefined;\n        return {\n            x: sx,\n            y: sy,\n        };\n    }\n    BuildWorldInfoBadge(options) {\n        const safeTitle = this.EscapeHtml(options.title);\n        const safeSubtitle = options.subtitle\n            ? this.EscapeHtml(options.subtitle)\n            : '';\n        const safeLines = (options.lines || []).map(line => this.EscapeHtml(line));\n        const accent = options.accent || '#40baff';\n        const progress = typeof options.progress === 'number'\n            ? three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.clamp(options.progress, 0, 100)\n            : undefined;\n        const progressHtml = typeof progress === 'number'\n            ? `<div style=\"display:flex;align-items:center;gap:6px;margin-top:2px;\"><div style=\"flex:1;height:6px;border-radius:999px;background:rgba(255,255,255,0.14);overflow:hidden;\"><div style=\"width:${progress}%;height:100%;border-radius:999px;background:linear-gradient(90deg,${accent},#ffffff);box-shadow:0 0 10px rgba(255,255,255,0.25);\"></div></div><span style=\"font:600 11px/1 Oswald, sans-serif;color:#dbeafe;opacity:0.95;white-space:nowrap;\">${Math.round(progress)}%</span></div>`\n            : '';\n        const linesHtml = safeLines.length\n            ? `<div style=\"display:flex;flex-direction:column;gap:2px;margin-top:2px;\">${safeLines\n                .map(line => `<span style=\"font:600 11px/1.2 Oswald, sans-serif;color:#ffffffd9;text-shadow:0 1px 2px rgba(0,0,0,0.45);\">${line}</span>`)\n                .join('')}</div>`\n            : '';\n        return (`<div style=\"position:absolute;left:${options.left}px;top:${options.top}px;transform:translate(-50%,-50%);pointer-events:none;z-index:5;\">` +\n            `<div style=\"display:flex;flex-direction:column;gap:4px;min-width:140px;max-width:220px;padding:7px 10px;border-radius:12px;background:rgba(7,12,20,0.82);border:1px solid ${accent};box-shadow:0 8px 18px rgba(0,0,0,0.3);\">` +\n            `<span style=\"font:600 13px/1.1 Oswald, sans-serif;color:#ffffff;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;text-shadow:0 1px 2px rgba(0,0,0,0.5);\">${safeTitle}</span>` +\n            (safeSubtitle\n                ? `<span style=\"font:600 11px/1.1 Oswald, sans-serif;color:#dbeafe;opacity:0.95;text-shadow:0 1px 2px rgba(0,0,0,0.45);\">${safeSubtitle}</span>`\n                : '') +\n            linesHtml +\n            progressHtml +\n            `</div>` +\n            `</div>`);\n    }\n    BuildHoveredWorldInfo(width, height) {\n        var _a, _b, _c, _d, _e, _f, _g, _h, _j;\n        if (!this.cursorInsideGame)\n            return '';\n        if (this.hoveredDropUid) {\n            const drop = this.state.gameData.drop.find(d => d.uid === this.hoveredDropUid);\n            const model = this.dropItems.get(this.hoveredDropUid);\n            if (!drop || !model)\n                return '';\n            const item = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.items.find(i => i.id === drop.id);\n            if (!item)\n                return '';\n            const box = new three__WEBPACK_IMPORTED_MODULE_9__.Box3().setFromObject(model);\n            const center = box.getCenter(new three__WEBPACK_IMPORTED_MODULE_9__.Vector3());\n            const topY = box.max.y + 30;\n            const screen = this.ProjectWorldToScreen(new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(center.x, topY, center.z), width, height);\n            if (!screen)\n                return '';\n            const dropSubtitle = drop.amount > 1\n                ? this.LangString('FactoryWarsGroundAmount', drop.amount)\n                : undefined;\n            return this.BuildWorldInfoBadge({\n                left: screen.x,\n                top: screen.y,\n                title: item.name,\n                subtitle: dropSubtitle,\n                accent: item.color,\n            });\n        }\n        if (!this.isValidPoint)\n            return '';\n        const build = this.GetBuildingInPoint(this.hoverCell);\n        const resource = this.GetResouceInPoint(this.hoverCell);\n        if (build) {\n            if (!this.IsBuildingVisibleInFog(build))\n                return '';\n            const buildCfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(cfg => cfg.id === build.id);\n            if (!buildCfg)\n                return '';\n            let worldPos = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(this.hoverCell.x * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize, 40, this.hoverCell.z * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize);\n            const center = this.GetBuildWorldCenter(build);\n            const buildHeightById = {\n                conveyor: 38,\n                underground_conveyor: 38,\n                miner: 72,\n                chest: 66,\n                hub: 320,\n            };\n            const topOffset = buildHeightById[build.id] ||\n                Math.max(52, (((_a = buildCfg.size) === null || _a === void 0 ? void 0 : _a.x) || 1) * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize * 0.22);\n            worldPos = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(center.x, topOffset, center.z);\n            const screen = this.ProjectWorldToScreen(worldPos, width, height);\n            if (!screen)\n                return '';\n            const outputLines = (((_b = build.data) === null || _b === void 0 ? void 0 : _b.output) || [])\n                .filter(item => item.amount > 0)\n                .map(item => {\n                const itemCfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.items.find(cfg => cfg.id === item.id);\n                const itemName = this.LocalizeConfigName(itemCfg === null || itemCfg === void 0 ? void 0 : itemCfg.name, itemCfg === null || itemCfg === void 0 ? void 0 : itemCfg.nameLang) ||\n                    item.id;\n                return `x${item.amount} ${itemName}`;\n            });\n            const maxHp = Math.max(1, Number(((_c = build.data) === null || _c === void 0 ? void 0 : _c.maxHp) || buildCfg.maxHealth || 1));\n            const hp = Math.max(0, Math.min(maxHp, Number((_e = (_d = build.data) === null || _d === void 0 ? void 0 : _d.hp) !== null && _e !== void 0 ? _e : maxHp)));\n            const extraLines = [];\n            if ((_f = buildCfg.recipes) === null || _f === void 0 ? void 0 : _f.length) {\n                const recipeIndex = (_h = (_g = build.data) === null || _g === void 0 ? void 0 : _g.additional) === null || _h === void 0 ? void 0 : _h.recipeIndex;\n                const recipe = Number.isInteger(recipeIndex) && recipeIndex >= 0\n                    ? buildCfg.recipes[recipeIndex]\n                    : undefined;\n                if (recipe) {\n                    const outputStr = recipe.output\n                        .map(o => {\n                        const itemCfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.items.find(cfg => cfg.id === o.id);\n                        const name = this.LocalizeConfigName(itemCfg === null || itemCfg === void 0 ? void 0 : itemCfg.name, itemCfg === null || itemCfg === void 0 ? void 0 : itemCfg.nameLang) ||\n                            o.id;\n                        return `${name} x${o.amount}`;\n                    })\n                        .join(', ');\n                    extraLines.push(`⚙ ${outputStr}`);\n                }\n                else {\n                    extraLines.push(this.LangString('FactoryWarsRecipeNotSelected'));\n                }\n            }\n            let html = this.BuildWorldInfoBadge({\n                left: screen.x,\n                top: screen.y,\n                title: this.LocalizeConfigName(buildCfg.name, buildCfg.nameLang),\n                lines: [\n                    this.LangString('FactoryWarsHpCompact', Math.round(hp), Math.round(maxHp)),\n                    ...extraLines,\n                    ...outputLines,\n                ],\n                progress: typeof ((_j = build.data) === null || _j === void 0 ? void 0 : _j.progress) === 'number'\n                    ? build.data.progress\n                    : undefined,\n            });\n            if (resource) {\n                const resourceItem = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.items.find(i => i.id === resource.id);\n                if (resourceItem) {\n                    const isVisibleInFog = this.IsMapPointVisibleInFog(this.hoverCell);\n                    html += this.BuildWorldInfoBadge({\n                        left: screen.x,\n                        top: screen.y + 58,\n                        title: resourceItem.name,\n                        subtitle: isVisibleInFog\n                            ? this.LangString('FactoryWarsResourceAmountKnown', `${resource.amount}${typeof resource.total === 'number' ? ` / ${resource.total}` : ''}`)\n                            : this.LangString('FactoryWarsResourceAmountHidden'),\n                        accent: resourceItem.color,\n                    });\n                }\n            }\n            return html;\n        }\n        if (resource) {\n            const resourceItem = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.items.find(i => i.id === resource.id);\n            if (!resourceItem)\n                return '';\n            const worldPos = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(this.hoverCell.x * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize, 48, this.hoverCell.z * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize);\n            const screen = this.ProjectWorldToScreen(worldPos, width, height);\n            if (!screen)\n                return '';\n            return this.BuildWorldInfoBadge({\n                left: screen.x,\n                top: screen.y,\n                title: resourceItem.name,\n                subtitle: this.IsMapPointVisibleInFog(this.hoverCell)\n                    ? this.LangString('FactoryWarsResourceAmountKnown', `${resource.amount}${typeof resource.total === 'number' ? ` / ${resource.total}` : ''}`)\n                    : this.LangString('FactoryWarsResourceAmountHidden'),\n                accent: resourceItem.color,\n            });\n        }\n        return '';\n    }\n    UpdatePlayerHudIndicators(width, height) {\n        const container = this.offscreenLayerRef.current;\n        if (!container)\n            return;\n        if (!this.camera) {\n            container.innerHTML = '';\n            return;\n        }\n        const cx = width * 0.5;\n        const cy = height * 0.5;\n        const pad = 42;\n        const badgePad = 72;\n        const cameraPos = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3();\n        const cameraDirection = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3();\n        this.camera.getWorldPosition(cameraPos);\n        this.camera.getWorldDirection(cameraDirection);\n        let html = '';\n        html += this.BuildHoveredWorldInfo(width, height);\n        this.state.players.forEach(player => {\n            if (player.id === _api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id)\n                return;\n            const model = this.players_models.get(player.id);\n            if (!model)\n                return;\n            const color = this.GetMinimapPlayerColor(player.id);\n            const isTeammate = this.GetTeam(player.id) === this.myTeamId;\n            if (!isTeammate && !this.IsPlayerVisibleInFog(player.id))\n                return;\n            const worldPos = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3();\n            model.getWorldPosition(worldPos);\n            worldPos.y += (this.players_models_top.get(player.id) || 0) + 42;\n            const projected = worldPos.clone().project(this.camera);\n            const sx = (projected.x * 0.5 + 0.5) * width;\n            const sy = (projected.y * -0.5 + 0.5) * height;\n            const isBehind = cameraDirection.dot(worldPos.clone().sub(cameraPos)) < 0;\n            const visibleOnScreen = !isBehind &&\n                projected.z >= -1 &&\n                projected.z <= 1 &&\n                sx >= pad &&\n                sx <= width - pad &&\n                sy >= pad &&\n                sy <= height - pad;\n            if (visibleOnScreen) {\n                const labelX = three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.clamp(sx, badgePad, width - badgePad);\n                const labelY = three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.clamp(sy - 28, 28, height - 28);\n                html += this.BuildPlayerHudBadge(player, color, labelX, labelY);\n                return;\n            }\n            if (!isTeammate)\n                return;\n            let dx = sx - cx;\n            let dy = sy - cy;\n            if (!Number.isFinite(dx) || !Number.isFinite(dy)) {\n                dx = worldPos.x - cameraPos.x;\n                dy = worldPos.z - cameraPos.z;\n            }\n            if (isBehind) {\n                dx *= -1;\n                dy *= -1;\n            }\n            if (Math.abs(dx) < 1e-3 && Math.abs(dy) < 1e-3) {\n                dy = -1;\n            }\n            const t = Math.min((cx - pad) / Math.max(Math.abs(dx), 1e-6), (cy - pad) / Math.max(Math.abs(dy), 1e-6));\n            const ex = cx + dx * t;\n            const ey = cy + dy * t;\n            const angle = Math.atan2(dy, dx) * (180 / Math.PI);\n            html += this.BuildPlayerHudBadge(player, color, ex, ey, angle);\n        });\n        container.innerHTML = html;\n    }\n    OnMouse(cursor) {\n        if (this.openedInterfaces.length)\n            return;\n        if (cursor.scrollUp) {\n            this.cameraOffset = Math.max(_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.cameraLimit[0], this.cameraOffset - 20);\n            this.UpdateAllIdleSoundVolumes();\n        }\n        if (cursor.scrollDown) {\n            this.cameraOffset = Math.min(_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.cameraLimit[1], this.cameraOffset + 20);\n            this.UpdateAllIdleSoundVolumes();\n        }\n    }\n    BuildAnimationTick(delta) {\n        this.animationBuildings.forEach((mixer, i) => {\n            const build = this.state.gameData.buildings.find(b => b.uid === i);\n            if (build) {\n                const obj = this.buildings.get(build.uid);\n                if (obj && obj.visible) {\n                    mixer.update(delta);\n                }\n            }\n        });\n        this.TickBuildingWorkAnimations(delta);\n        this.TickBuildingTransitions(delta);\n        this.TickBuildingDebris(delta);\n        this.TickManipulatorAnimations(delta);\n        this.TickDropManipulatorAnimations(delta);\n        this.TickCombatVfx(delta);\n    }\n    UpdateConveyorArrowsForObject(object) {\n        if (!object)\n            return;\n        object.traverse(node => {\n            var _a, _b, _c;\n            const baseZ = (_a = node.userData) === null || _a === void 0 ? void 0 : _a.conveyorArrowBaseZ;\n            if (typeof baseZ !== 'number')\n                return;\n            const travel = ((_c = (_b = node.parent) === null || _b === void 0 ? void 0 : _b.userData) === null || _c === void 0 ? void 0 : _c.conveyorArrowTravel) ||\n                this.conveyorArrowTravel;\n            if (travel <= 0)\n                return;\n            const wrappedOffset = this.conveyorArrowOffset % travel;\n            const half = travel * 0.5;\n            let z = baseZ + wrappedOffset;\n            if (z > half)\n                z -= travel;\n            node.position.z = z;\n        });\n    }\n    TickConveyorArrowAnimation(delta) {\n        if (!this.graphicsSettings.animatedConveyors)\n            return;\n        this.conveyorArrowOffset += delta * this.conveyorArrowSpeed;\n        this.buildings.forEach(model => {\n            this.UpdateConveyorArrowsForObject(model);\n        });\n        this.dropItems.forEach(model => {\n            this.UpdateConveyorArrowsForObject(model);\n        });\n        this.conveyorDragGhostObjects.forEach(model => {\n            this.UpdateConveyorArrowsForObject(model);\n        });\n        this.UpdateConveyorArrowsForObject(this.ghostObject);\n    }\n    OnFrame(cursor, delta, keyPress) {\n        var _a, _b;\n        this.TickGameNotifications();\n        this.TickConveyorArrowAnimation(delta);\n        if (!this.renderer)\n            return;\n        this.UpdateCursorInsideGame(cursor);\n        this.ValidateOpenedBuildingDistance();\n        this.TryPickupNearestByHold();\n        this.handleKeyPress(keyPress);\n        this.MapTick();\n        this.UpdateFogOfWar(performance.now());\n        this.TickDrops(delta);\n        this.BuildAnimationTick(delta);\n        this.TickAutomatonVisuals(delta);\n        const world = this.GetWorldPoint(cursor);\n        if (world) {\n            this.SnapToGrid(world);\n            this.UpdateHover(cursor);\n            this.UpdateHoveredDrop(cursor);\n            this.UpdateHoverMesh();\n            this.UpdateGhost();\n            this.UpdateAutomatonTooltip();\n            this.UpdateHoverCombatVisuals();\n        }\n        else {\n            this.hoveredDropUid = undefined;\n            this.HideCombatHoverObjects();\n        }\n        // THREEJS GAME TICK\n        // console.log(`OnFrame: ${delta.toFixed(3)}s`);\n        this.players_models.forEach((model, playerid) => {\n            const playerData = this.state.players.find(q => q.id === playerid);\n            if (!playerData)\n                return;\n            // console.log(`Player ${playerid} position:`, model.position);\n            const velocityData = this.players_velocities.get(playerid) || {\n                x: 0,\n                z: 0,\n                shift: false,\n            };\n            const velocity = {\n                x: velocityData.x,\n                z: velocityData.z,\n                shift: velocityData.shift,\n            };\n            const posStored = this.players_positions.get(playerid) || { x: 0, z: 0 };\n            const dataPos = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(posStored.x, 1, posStored.z);\n            const isMove = velocity.x !== 0 || velocity.z !== 0;\n            const isSprint = velocity.shift;\n            if (playerid !== _api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id &&\n                dataPos.distanceTo(model.position) > (isMove ? 100 : 10)) {\n                model.position.x = dataPos.x;\n                model.position.z = dataPos.z;\n                // console.log(`Correcting position for player ${playerid} to`, model.position);\n            }\n            const nextPosition = {\n                x: model.position.x,\n                z: model.position.z,\n            };\n            nextPosition.x +=\n                velocity.x *\n                    delta *\n                    _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.playerSpeedMultipler *\n                    (isSprint ? _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.playerSpeedMultiplerShift : 1);\n            nextPosition.z +=\n                velocity.z *\n                    delta *\n                    _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.playerSpeedMultipler *\n                    (isSprint ? _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.playerSpeedMultiplerShift : 1);\n            if (isMove) {\n                const targetAngle = Math.atan2(velocity.x, velocity.z);\n                const targetQuat = new three__WEBPACK_IMPORTED_MODULE_9__.Quaternion().setFromEuler(new three__WEBPACK_IMPORTED_MODULE_9__.Euler(0, targetAngle, 0));\n                model.quaternion.slerp(targetQuat, (isSprint ? 10 : 5) * delta);\n            }\n            const addOffset = this.CalculateVectorOffset(nextPosition, 0);\n            if (addOffset.x !== 0 || addOffset.z !== 0) {\n                nextPosition.x += addOffset.x * delta;\n                nextPosition.z += addOffset.z * delta;\n            }\n            const origin = { x: model.position.x, z: model.position.z };\n            if (this.CanPlayerStandAtWorldPosition(playerid, nextPosition)) {\n                model.position.x = nextPosition.x;\n                model.position.z = nextPosition.z;\n            }\n            else {\n                const byX = { x: nextPosition.x, z: origin.z };\n                if (this.CanPlayerStandAtWorldPosition(playerid, byX)) {\n                    model.position.x = byX.x;\n                }\n                const byZ = { x: model.position.x, z: nextPosition.z };\n                if (this.CanPlayerStandAtWorldPosition(playerid, byZ)) {\n                    model.position.z = byZ.z;\n                }\n            }\n            const mixer = this.players_mixers.get(playerid);\n            const animation = isMove ? 'run' : 'idle';\n            const anim = this.character_animations.get(animation);\n            if (mixer && anim) {\n                const action = mixer.clipAction(anim[0]);\n                if (!action.isRunning() || action.getClip().name !== anim[0].name) {\n                    mixer.stopAllAction();\n                    action.setEffectiveTimeScale(isSprint ? 1.2 : 1);\n                    action.setLoop(three__WEBPACK_IMPORTED_MODULE_9__.LoopRepeat, Infinity);\n                    action.play();\n                }\n            }\n            const sound = this.players_sounds.get(playerid);\n            if (isMove) {\n                sound.setVolume(this.GetBuildingEffectiveVolume());\n                sound.setPlaybackRate(isSprint ? 1.2 : 1);\n                if (sound && !sound.isPlaying) {\n                    sound.play();\n                }\n            }\n            else {\n                if (sound && sound.isPlaying) {\n                    sound.stop();\n                }\n            }\n            if (playerid === _api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id) {\n                const positionListener = model.position\n                    .clone()\n                    .add(new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(0, this.players_models_top.get(playerid) || 0, 0));\n                this.audio_listener.position.copy(positionListener);\n            }\n        });\n        this.players_mixers.forEach(mixer => {\n            mixer.update(delta);\n        });\n        // Camera update\n        const playerMesh = this.players_models.get(_api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id);\n        if (playerMesh) {\n            const playerPos = playerMesh.position;\n            const targetCameraPos = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(playerPos.x, playerPos.y + this.cameraOffset, playerPos.z + 200);\n            // FPS-independent smoothing so 20 FPS follows as responsive as 60 FPS.\n            const followAlpha = 1 - Math.exp(-Math.max(0, delta) * 12);\n            const cameraAlpha = 1 - Math.exp(-Math.max(0, delta) * 16);\n            this.cameraTarget.lerp(targetCameraPos, followAlpha);\n            // Cap follow lag to avoid delayed camera turns that can look like 180 flips.\n            const maxFollowLag = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize * 6;\n            const toTarget = targetCameraPos.clone().sub(this.cameraTarget);\n            const lagDistance = toTarget.length();\n            if (lagDistance > maxFollowLag) {\n                toTarget.multiplyScalar((lagDistance - maxFollowLag) / lagDistance);\n                this.cameraTarget.add(toTarget);\n            }\n            this.camera.position.lerp(this.cameraTarget, cameraAlpha);\n            const lookPoint = playerPos.clone();\n            lookPoint.y += 20;\n            const lookAlpha = 1 - Math.exp(-Math.max(0, delta) * 18);\n            this.cameraLookTarget.lerp(lookPoint, lookAlpha);\n            this.camera.lookAt(this.cameraLookTarget);\n        }\n        this.audio_listener.quaternion.copy(this.camera.quaternion);\n        const viewWidth = ((_a = this.canvasRef.current) === null || _a === void 0 ? void 0 : _a.clientWidth) ||\n            this.renderer.domElement.clientWidth;\n        const viewHeight = ((_b = this.canvasRef.current) === null || _b === void 0 ? void 0 : _b.clientHeight) ||\n            this.renderer.domElement.clientHeight;\n        this.UpdatePlayerHudIndicators(viewWidth, viewHeight);\n        if (this.arenaWallsGroup && this.arenaWallMaterials.length > 0) {\n            const primaryColor = this.arenaWallsGroup.userData.wallColorPrimary;\n            const secondaryColor = this.arenaWallsGroup.userData\n                .wallColorSecondary;\n            const wallBlinkDurationMs = this.arenaWallsGroup.userData.wallBlinkDurationMs || 1200;\n            if (primaryColor && secondaryColor) {\n                const elapsed = performance.now() % wallBlinkDurationMs;\n                const blinkPhase = elapsed / wallBlinkDurationMs;\n                const blend = 0.5 - 0.5 * Math.cos(blinkPhase * Math.PI * 2);\n                const blinkColor = new three__WEBPACK_IMPORTED_MODULE_9__.Color().lerpColors(primaryColor, secondaryColor, blend);\n                this.arenaWallMaterials.forEach(material => {\n                    material.color.copy(blinkColor);\n                    material.emissive.copy(blinkColor).multiplyScalar(0.35);\n                });\n            }\n        }\n        this.renderer.render(this.scene, this.camera);\n        // Minimap update — throttled to ~10fps to avoid rendering the full scene twice per frame\n        const minimapIntervalMs = 100;\n        const nowMinimap = performance.now();\n        if (this.graphicsSettings.minimapEnabled &&\n            nowMinimap - this.lastMinimapRender >= minimapIntervalMs) {\n            this.lastMinimapRender = nowMinimap;\n            const mapSize = this.state.gameData.mapSize;\n            const xSize = mapSize.x * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const zSize = mapSize.z * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n            const centerX = xSize / 2;\n            const centerZ = zSize / 2;\n            // Рассчитываем минимальную высоту, при которой карта целиком влезает в кадр\n            const halfFovRad = three__WEBPACK_IMPORTED_MODULE_9__.MathUtils.degToRad(this.cameraMini.fov / 2);\n            const aspect = this.cameraMini.aspect;\n            // Высота, нужная чтобы покрыть Z-ось (вертикаль экрана)\n            const heightForZ = zSize / 2 / Math.tan(halfFovRad);\n            // Высота, нужная чтобы покрыть X-ось (горизонталь, с учётом aspect ratio)\n            const heightForX = xSize / 2 / (Math.tan(halfFovRad) * aspect);\n            // Берём большее значение + небольшой отступ\n            const camHeight = Math.max(heightForZ, heightForX) * 1.05;\n            this.cameraMini.position.set(centerX, camHeight, centerZ);\n            this.cameraMini.lookAt(centerX, 0, centerZ);\n            this.cameraMini.updateProjectionMatrix();\n            // Обновляем маркеры игроков на миникарте\n            this.players_minimap_markers.forEach((group, playerid) => {\n                const model = this.players_models.get(playerid);\n                if (!model)\n                    return;\n                group.visible = this.IsPlayerVisibleInFog(playerid);\n                group.position.x = model.position.x;\n                group.position.z = model.position.z;\n                // Берём только Y-вращение модели (направление движения)\n                const euler = new three__WEBPACK_IMPORTED_MODULE_9__.Euler().setFromQuaternion(model.quaternion, 'YXZ');\n                group.rotation.y = euler.y;\n                group.scale.setScalar(this.GetMinimapPlayerMarkerScale());\n                const material = group.userData.material;\n                material.color.set(this.GetMinimapPlayerColor(playerid));\n            });\n            this.cameraMini.far = Math.max(this.cameraMini.far, camHeight + 5000);\n            this.cameraMini.updateProjectionMatrix();\n            this.rendererMini.render(this.scene, this.cameraMini);\n        }\n    }\n    OnKeyPress(keyPress, pressed, withShift) {\n        if (!pressed)\n            return;\n        console.log(keyPress);\n        if (keyPress === 'Tab') {\n            if (this.openedInterfaces.length > 0 || this.dragAmountModal.open) {\n                this.CloseAllInterfaces();\n            }\n            else {\n                this.TogglePlayerUi();\n            }\n            return;\n        }\n        if (keyPress === 'KeyF') {\n            this.ToggleFullScreen();\n            return;\n        }\n        if (keyPress == 'Escape') {\n            if (this.openCategoryMenuIndex !== undefined) {\n                this.CloseCategoryMenu();\n                return;\n            }\n            const hasActiveInteraction = this.openedInterfaces.length > 0 ||\n                this.dragAmountModal.open ||\n                !!this.openedAutomatonUid ||\n                !!this.editBuildMode ||\n                !!this.ghostObject ||\n                this.conveyorDragBuildActive;\n            if (hasActiveInteraction) {\n                this.graphicsMenuOpen = false;\n                this.CloseAllInterfaces(false);\n                this.CloseAutomatonUi();\n                this.editBuildMode = undefined;\n                this.CancelGhost();\n            }\n            else {\n                this.graphicsMenuOpen = !this.graphicsMenuOpen;\n            }\n            this.forceUpdate();\n            return;\n        }\n        if (this.graphicsMenuOpen)\n            return;\n        if (this.openedInterfaces.length > 0)\n            return;\n        if (keyPress == 'KeyR') {\n            if (!this.RotateGhost()) {\n                const build = this.GetBuildingInPoint(this.hoverCell);\n                if (build) {\n                    this.SendAction('build.rotate', {\n                        uid: build.uid,\n                    });\n                }\n            }\n        }\n        if (keyPress === 'Delete') {\n            this.EnterEditMode('delete');\n        }\n        if (keyPress === 'KeyX') {\n            this.EnterEditMode('cut');\n        }\n        if (keyPress === 'KeyC') {\n            this.EnterEditMode('copy');\n        }\n        // Handle number keys 1-9 for category hotkeys\n        if (keyPress.startsWith('Digit') && keyPress.length === 6) {\n            const digitStr = keyPress.slice(5); // \"Digit1\" -> \"1\"\n            const digit = Number(digitStr);\n            if (digit >= 1 && digit <= 9) {\n                const categoryIndex = digit - 1;\n                if (this.openCategoryMenuIndex !== undefined &&\n                    digit >= 1 &&\n                    digit <= 9) {\n                    // If another category menu is open and we press a number,\n                    // select from that menu (1-indexed)\n                    const menuItemIndex = digit - 1;\n                    this.SelectBuildFromCategory(this.openCategoryMenuIndex, menuItemIndex);\n                }\n                else {\n                    // Open category menu\n                    this.TryOpenCategoryMenu(categoryIndex);\n                }\n            }\n        }\n    }\n    constructor(props) {\n        super(\"110\" /* GameID.FACTORY_WARS */, _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsGameData, _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsPlayerData, props);\n        this.fbx_loader = new three_examples_jsm_loaders_FBXLoader_js__WEBPACK_IMPORTED_MODULE_12__.FBXLoader();\n        this.audio_loader = new three__WEBPACK_IMPORTED_MODULE_9__.AudioLoader();\n        this.audio_listener = new three__WEBPACK_IMPORTED_MODULE_9__.AudioListener();\n        this.texture_loader = new three__WEBPACK_IMPORTED_MODULE_9__.TextureLoader();\n        this.players_models = new Map();\n        this.players_models_top = new Map();\n        this.players_mixers = new Map();\n        this.players_sounds = new Map();\n        this.players_minimap_markers = new Map();\n        this.minimap_resource_markers = new Map();\n        this.sounds = new Map();\n        this.character_models = [];\n        this.character_animations = new Map();\n        this.threeJsInitialized = false;\n        this.threeJsLoadedFull = false;\n        this.canvasRef = react__WEBPACK_IMPORTED_MODULE_5___default().createRef();\n        this.canvasRefMini = react__WEBPACK_IMPORTED_MODULE_5___default().createRef();\n        this.offscreenLayerRef = react__WEBPACK_IMPORTED_MODULE_5___default().createRef();\n        this.automatonTooltipRef = react__WEBPACK_IMPORTED_MODULE_5___default().createRef();\n        this.minimapTooltipRef = react__WEBPACK_IMPORTED_MODULE_5___default().createRef();\n        this.imgObjects = new Map();\n        this.resouceMaterials = new Map();\n        this.models = new Map();\n        this.mapItems = new Map();\n        this.mapPointSnapshots = new Map();\n        this.mapGroundSignature = '';\n        this.fogOfWarMapSignature = '';\n        this.fogOfWarVisibility = new Uint8Array(0);\n        this.fogOfWarMapSize = { x: 0, z: 0 };\n        this.fogOfWarInstanceCapacity = 0;\n        this.fogOfWarLastUpdateAt = 0;\n        this.fogOfWarTickIntervalMS = 120;\n        this.fogOfWarVisibilityStamp = 0;\n        this.fogOfWarVisibilityStampApplied = -1;\n        this.fogOfWarTempMatrix = new three__WEBPACK_IMPORTED_MODULE_9__.Matrix4();\n        this.arenaWallMaterials = [];\n        this.arenaWallsSignature = '';\n        this.cameraTarget = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3();\n        this.cameraLookTarget = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3();\n        this.cameraOffset = Math.floor((_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.cameraLimit[0] + _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.cameraLimit[1]) / 2);\n        this.buildings = new Map();\n        this.automatons_models = new Map();\n        this.automatonVisualTargets = new Map();\n        this.automatonLastShotTargetByUid = new Map();\n        this.building_progress = new Map();\n        this.building_progress_fill = new Map();\n        this.buildingTransitions = new Map();\n        this.buildingDebris = [];\n        this.dropItems = new Map();\n        this.combatProjectiles = [];\n        this.combatLasers = [];\n        this.combatImpacts = [];\n        this.prevTurretAmmoByUid = new Map();\n        this.prevAutomatonByUid = new Map();\n        this.prevBuildingHpByUid = new Map();\n        /**\n         * Shared idle-sound nodes keyed by building TYPE id.\n         * Instead of one pair per instance (N conveyors → 2N nodes), there is\n         * exactly one active node and one inactive node for the whole type.\n         * Each tick the nodes are repositioned to the nearest qualifying building\n         * of that type so the audio feels spatially correct.\n         */\n        this.typeIdleSounds = new Map();\n        /** Stringified output snapshot per building uid, used to detect produce events */\n        this.building_output_snapshots = new Map();\n        this.players_positions = new Map();\n        this.players_velocities = new Map();\n        this.raycaster = new three__WEBPACK_IMPORTED_MODULE_9__.Raycaster();\n        this.minimapRaycaster = new three__WEBPACK_IMPORTED_MODULE_9__.Raycaster();\n        this.mouse = new three__WEBPACK_IMPORTED_MODULE_9__.Vector2();\n        this.hoverPoint = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3();\n        this.hoverCell = { x: 0, z: 0 };\n        this.graphicsMenuOpen = false;\n        this.graphicsMenuRef = react__WEBPACK_IMPORTED_MODULE_5___default().createRef();\n        this.graphicsStorageKey = 'factorywars.graphics.v1';\n        this.graphicsSettings = {\n            renderResolution: 'native',\n            renderQuality: 1.0,\n            shadowsEnabled: false,\n            shadowQuality: 'medium',\n            fpsLimit: 'unlimited',\n            minimapEnabled: true,\n            animatedConveyors: true,\n            vfxQuality: 'medium',\n            soundVolume: 100,\n        };\n        this.ghostObjectBaseScale = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(1, 1, 1);\n        this.ghostObjectRotation = 0;\n        this.animationBuildings = new Map();\n        this.manipulatorAnimations = new Map();\n        this.dropManipulatorAnimations = new Map();\n        this.openedInterfaces = [];\n        this.cursorInsideGame = false;\n        this.hoverCursorPos = { x: 0, y: 0 };\n        this.HandleWindowMouseOut = (e) => {\n            if (e && e.relatedTarget)\n                return;\n            this.cursorInsideGame = false;\n        };\n        this.HandleWindowBlur = () => {\n            console.log(`Blurred window, hiding hover and tooltips`);\n            this.cursorInsideGame = false;\n        };\n        // Drag-select automatons (no editBuildMode required)\n        this.automatonDragSelectActive = false;\n        this.automatonDragSelectScreenStart = { x: 0, y: 0 };\n        this.automatonDragSelectScreenEnd = { x: 0, y: 0 };\n        this.selectedAutomatonUids = [];\n        this.conveyorArrowOffset = 0;\n        this.conveyorArrowSpeed = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.conveyorSpeedMultipler;\n        this.conveyorArrowTravel = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize;\n        this.editBuildMode = undefined;\n        this.editSelectionActive = false;\n        this.cutBuildUids = [];\n        this.ghostSelectionBuilds = [];\n        this.editBuildRange = [\n            { x: -1, z: -1 },\n            { x: -1, z: -1 },\n        ];\n        this.conveyorDragBuilds = [];\n        this.conveyorDragGhostObjects = new Map();\n        this.conveyorDragBuildActive = false;\n        this.suppressNextCanvasClick = false;\n        this.lastPickupNearestAt = 0;\n        this.showPlayerUi = false;\n        this.gameNotifications = [];\n        this.gameNotificationSeq = 0;\n        this.dragAmountModal = { open: false };\n        this.dragCursorPos = { x: 0, y: 0 };\n        this.openCategoryMenuIndex = undefined;\n        // ToTreeJsCoords(pos: { x: number; z: number }) {\n        //   return { x: pos.x * FactoryDataset.gridPointSize, z: pos.z * FactoryDataset.gridPointSize };\n        // }\n        // ToDataCoords(){\n        //     return { x: Math.floor(this.hoverPoint.x / FactoryDataset.gridPointSize), z: Math.floor(this.hoverPoint.z / FactoryDataset.gridPointSize) };\n        // }\n        this.LoadCollections = () => __awaiter(this, void 0, void 0, function* () {\n            var _a, _b;\n            const tm = Date.now();\n            console.log('Loading character models');\n            this.character_models.push(yield this.fbx_loader.loadAsync('/models/factorywars/models/player3.fbx'));\n            this.character_animations.set('idle', (_a = (yield this.fbx_loader.loadAsync('/models/factorywars/anims/idle.fbx'))) === null || _a === void 0 ? void 0 : _a.animations);\n            this.character_animations.set('run', (_b = (yield this.fbx_loader.loadAsync('/models/factorywars/anims/run.fbx'))) === null || _b === void 0 ? void 0 : _b.animations);\n            this.sounds.set('footstep', yield this.audio_loader.loadAsync('/sounds/step_grass_clear.mp3'));\n            this.sounds.set('footstep2', yield this.audio_loader.loadAsync('/sounds/step_city_clear.mp3'));\n            // Load all FactoryWars building sounds\n            const buildingSoundFiles = new Set();\n            [_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.sounds.build, _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.sounds.click_sound]\n                .filter(Boolean)\n                .forEach(f => buildingSoundFiles.add(f));\n            _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.forEach(cfg => {\n                var _a, _b;\n                if (!cfg.sounds)\n                    return;\n                [\n                    cfg.sounds.build,\n                    cfg.sounds.destroy,\n                    cfg.sounds.produce,\n                    (_a = cfg.sounds.idle) === null || _a === void 0 ? void 0 : _a.active,\n                    (_b = cfg.sounds.idle) === null || _b === void 0 ? void 0 : _b.inactive,\n                    cfg.sounds.custom,\n                ]\n                    .filter(Boolean)\n                    .forEach(f => buildingSoundFiles.add(f));\n            });\n            // Load automaton shoot sounds as well.\n            _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsAutomatonUnits.forEach(unit => {\n                var _a;\n                if (!((_a = unit.shootSound) === null || _a === void 0 ? void 0 : _a.name))\n                    return;\n                buildingSoundFiles.add(unit.shootSound.name);\n            });\n            for (const file of buildingSoundFiles) {\n                if (!this.sounds.has(file)) {\n                    try {\n                        this.sounds.set(file, yield this.audio_loader.loadAsync(`/sounds/${file}`));\n                    }\n                    catch (_c) {\n                        console.warn(`[FactoryWars] Could not load sound: ${file}`);\n                    }\n                }\n            }\n            this.resouceGeometry = new three__WEBPACK_IMPORTED_MODULE_9__.BoxGeometry(10, 10, 10);\n            _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.items.forEach(item => {\n                const material = new three__WEBPACK_IMPORTED_MODULE_9__.MeshStandardMaterial({ color: item.color });\n                this.resouceMaterials.set(item.id, material);\n            });\n            // const modelsLoad = [\n            //   ...FactoryDataset.items.map(i => i.model),\n            //   ...FactoryDataset.items.flatMap(i =>\n            //     i.modelResource ? [i.modelResource] : []\n            //   ),\n            //   ...FactoryDataset.builds.map(i => i.model),\n            // ].filter((m, i, arr) => m && arr.indexOf(m) === i) as string[];\n            // for (const model of modelsLoad) {\n            //   if (!this.models.has(model) || !this.models.get(model)) {\n            //     try {\n            //       this.models.set(\n            //         model,\n            //         await this.fbx_loader.loadAsync(\n            //           `/models/factorywars/models/${model}.fbx`\n            //         )\n            //       );\n            //     } catch (error) {\n            //       //\n            //     }\n            //   }\n            // }\n            console.log('Character models loaded', Date.now() - tm, 'ms');\n        });\n        // GenerateModel(id: string) {\n        //   if (id === 'conveyor') {\n        //     const group = new THREE.Group();\n        //     const base = new THREE.Mesh(\n        //       new THREE.BoxGeometry(\n        //         FactoryDataset.gridPointSize,\n        //         4,\n        //         FactoryDataset.gridPointSize\n        //       ),\n        //       new THREE.MeshStandardMaterial({ color: '#555555' })\n        //     );\n        //     base.position.y = 4;\n        //     group.add(base);\n        //     return group;\n        //   }\n        //   return undefined;\n        // }\n        this.ToGameCoords = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsThreeJsToGameCoords;\n        this.GenerateModelStorage = new Map();\n        this.InitThreeJs = () => __awaiter(this, void 0, void 0, function* () {\n            const canvas = this.canvasRef.current;\n            const canvasMini = this.canvasRefMini.current;\n            if (!canvas || !canvasMini) {\n                requestAnimationFrame(this.InitThreeJs.bind(this));\n                return;\n            }\n            if (this.renderer && !canvas)\n                return this.ClearThreeJs();\n            if (!canvas)\n                return;\n            if (!canvasMini)\n                return;\n            console.log('Initializing Three.js');\n            yield this.LoadCollections();\n            this.scene = new three__WEBPACK_IMPORTED_MODULE_9__.Scene();\n            this.camera = new three__WEBPACK_IMPORTED_MODULE_9__.PerspectiveCamera(75, // FOV\n            canvas.clientWidth / canvas.clientHeight, // aspect\n            0.1, // near\n            3000 // far\n            );\n            this.cameraMini = new three__WEBPACK_IMPORTED_MODULE_9__.PerspectiveCamera(75, // FOV\n            canvasMini.clientWidth / canvasMini.clientHeight, // aspect\n            0.1, // near\n            30000 // far\n            );\n            this.camera.layers.enable(0);\n            this.camera.layers.enable(1);\n            this.camera.layers.disable(2);\n            this.camera.layers.enable(0);\n            this.cameraMini.layers.disable(1);\n            this.cameraMini.layers.enable(2);\n            this.renderer = new three__WEBPACK_IMPORTED_MODULE_11__.WebGLRenderer({ canvas, antialias: true });\n            this.rendererMini = new three__WEBPACK_IMPORTED_MODULE_11__.WebGLRenderer({\n                canvas: canvasMini,\n                antialias: false,\n            });\n            this.camera.position.y = 350;\n            this.camera.position.x = 50;\n            this.camera.position.z = 150;\n            this.camera.lookAt(0, 0, 0);\n            this.cameraMini.position.y = 350;\n            this.cameraMini.position.x = 50;\n            this.cameraMini.position.z = 150;\n            this.cameraMini.lookAt(0, 0, 0);\n            this.sunLight = new three__WEBPACK_IMPORTED_MODULE_9__.DirectionalLight(0xffffff, 2);\n            this.sunLight.shadow.mapSize.set(2048, 2048);\n            this.sunLight.shadow.camera.near = 10;\n            // Position and frustum are updated once the map size is known (UpdateSunLightForMap)\n            this.sunLight.shadow.camera.far = 5000;\n            this.sunLight.shadow.camera.left = -1200;\n            this.sunLight.shadow.camera.right = 1200;\n            this.sunLight.shadow.camera.top = 1200;\n            this.sunLight.shadow.camera.bottom = -1200;\n            this.scene.add(this.sunLight.target);\n            this.scene.add(this.sunLight);\n            this.ambientLight = new three__WEBPACK_IMPORTED_MODULE_9__.AmbientLight(0xffffff, 0.3);\n            this.scene.add(this.ambientLight);\n            this.ApplyGraphicsSettings();\n            window.addEventListener('resize', this.OnResize);\n            window.addEventListener('mouseout', this.HandleWindowMouseOut);\n            window.addEventListener('blur', this.HandleWindowBlur);\n            this.threeJsInitialized = true;\n            this.audio_listener = new three__WEBPACK_IMPORTED_MODULE_9__.AudioListener();\n            this.scene.add(this.audio_listener);\n            // Chrome blocks AudioContext until user interaction. Resume it on the first input event.\n            const resumeAudioContext = () => {\n                if (this.audio_listener.context.state === 'suspended') {\n                    this.audio_listener.context.resume();\n                }\n                document.removeEventListener('pointerdown', resumeAudioContext);\n                document.removeEventListener('keydown', resumeAudioContext);\n            };\n            document.addEventListener('pointerdown', resumeAudioContext);\n            document.addEventListener('keydown', resumeAudioContext);\n            const hoverGroup = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            hoverGroup.position.y += 2;\n            hoverGroup.rotation.x = -Math.PI / 2;\n            hoverGroup.layers.set(1);\n            const geo = new three__WEBPACK_IMPORTED_MODULE_9__.PlaneGeometry(_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize, _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize);\n            // Прозрачная заливка\n            const fillMat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshBasicMaterial({\n                transparent: true,\n                opacity: 0, // полностью прозрачный, можно поставить 0.05 для лёгкого тинта\n                depthWrite: false,\n            });\n            const fillMesh = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(geo, fillMat);\n            fillMesh.layers.set(1);\n            hoverGroup.add(fillMesh);\n            // Белая рамка\n            const edgesGeo = new three__WEBPACK_IMPORTED_MODULE_9__.EdgesGeometry(geo);\n            const edgesMat = new three__WEBPACK_IMPORTED_MODULE_9__.LineBasicMaterial({\n                color: '#ffffff77',\n                transparent: true,\n                linewidth: 1, // >1 работает только в WebGL1 на некоторых драйверах\n            });\n            const edgesMesh = new three__WEBPACK_IMPORTED_MODULE_9__.LineSegments(edgesGeo, edgesMat);\n            this.hoverBorder = edgesMesh;\n            edgesMesh.layers.set(1);\n            hoverGroup.add(edgesMesh);\n            hoverGroup.scale.set(1.01, 1.01, 1.01);\n            this.hoverGroup = hoverGroup; // UpdateHoverMesh двигает group через position fillMesh'а — не подойдёт\n            // Поэтому сохраняем group отдельно:\n            this.scene.add(hoverGroup);\n            yield this.GeneratePreviewImages();\n            yield this.RenderMap(true);\n            this.threeJsLoadedFull = true;\n            this.forceUpdate();\n        });\n        this.ClearThreeJs = () => {\n            var _a, _b;\n            console.log('Clearing Three.js');\n            window.removeEventListener('resize', this.OnResize);\n            window.removeEventListener('mouseout', this.HandleWindowMouseOut);\n            window.removeEventListener('blur', this.HandleWindowBlur);\n            (_a = this.offscreenLayerRef.current) === null || _a === void 0 ? void 0 : _a.replaceChildren();\n            this.HideMinimapTooltip();\n            if (!this.threeJsReady)\n                return;\n            this.threeJsInitialized = false;\n            this.threeJsLoadedFull = false;\n            this.forceUpdate();\n            (_b = this.scene) === null || _b === void 0 ? void 0 : _b.traverse((obj) => {\n                var _a, _b;\n                (_b = (_a = obj.geometry) === null || _a === void 0 ? void 0 : _a.dispose) === null || _b === void 0 ? void 0 : _b.call(_a);\n                if (obj.material) {\n                    if (Array.isArray(obj.material)) {\n                        obj.material.forEach(m => m.dispose());\n                    }\n                    else {\n                        obj.material.dispose();\n                    }\n                }\n            });\n            this.renderer.dispose();\n            this.rendererMini.dispose();\n            // Dispose materials held only by the template cache (geometry is already\n            // gone via the scene.traverse above). Clear the map so stale templates\n            // don't survive a reconnect / game restart.\n            this.GenerateModelStorage.forEach(template => {\n                template.traverse((child) => {\n                    if (child.material) {\n                        if (Array.isArray(child.material)) {\n                            child.material.forEach((m) => m.dispose());\n                        }\n                        else {\n                            child.material.dispose();\n                        }\n                    }\n                });\n            });\n            this.GenerateModelStorage.clear();\n            this.mapItems.clear();\n            this.mapPointSnapshots.clear();\n            this.mapGroundMini = undefined;\n            this.mapGroundSignature = '';\n            this.fogOfWarMesh = undefined;\n            this.fogOfWarMapSignature = '';\n            this.fogOfWarVisibility = new Uint8Array(0);\n            this.fogOfWarMapSize = { x: 0, z: 0 };\n            this.fogOfWarInstanceCapacity = 0;\n            this.fogOfWarLastUpdateAt = 0;\n            this.fogOfWarVisibilityStamp = 0;\n            this.fogOfWarVisibilityStampApplied = -1;\n            this.minimap_resource_markers.clear();\n            this.arenaWallMaterials = [];\n            this.arenaWallsGroup = undefined;\n            this.arenaWallsSignature = '';\n            this.buildingTransitions.clear();\n            this.buildingDebris.forEach(entry => this.DisposeObject(entry.mesh));\n            this.buildingDebris = [];\n            this.manipulatorAnimations.clear();\n            this.dropManipulatorAnimations.clear();\n            // Stop and clear all shared type-level idle sounds\n            this.typeIdleSounds.forEach(entry => {\n                var _a, _b, _c, _d;\n                try {\n                    if ((_a = entry.active) === null || _a === void 0 ? void 0 : _a.isPlaying)\n                        entry.active.stop();\n                    if ((_b = entry.inactive) === null || _b === void 0 ? void 0 : _b.isPlaying)\n                        entry.inactive.stop();\n                    (_c = entry.active) === null || _c === void 0 ? void 0 : _c.removeFromParent();\n                    (_d = entry.inactive) === null || _d === void 0 ? void 0 : _d.removeFromParent();\n                }\n                catch (_e) {\n                    // ignore\n                }\n            });\n            this.typeIdleSounds.clear();\n            this.building_output_snapshots.clear();\n            this.scene = undefined;\n            this.camera = undefined;\n            this.renderer = undefined;\n        };\n        this.SnapToGrid = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsSnapToGrid;\n        this.ClearMap = () => {\n            var _a, _b;\n            if (!this.threeJsReady)\n                return;\n            this.mapItems.forEach((objs, ids) => {\n                objs.forEach(obj => {\n                    var _a;\n                    // this.DisposeObject(obj);\n                    (_a = this.scene) === null || _a === void 0 ? void 0 : _a.remove(obj);\n                });\n            });\n            this.mapItems.clear();\n            this.automatons_models.forEach(model => { var _a; return (_a = this.scene) === null || _a === void 0 ? void 0 : _a.remove(model); });\n            this.automatons_models.clear();\n            this.automatonVisualTargets.clear();\n            this.combatProjectiles.forEach(entry => this.DisposeObject(entry.mesh));\n            this.combatProjectiles = [];\n            this.combatLasers.forEach(entry => this.DisposeObject(entry.line));\n            this.combatLasers = [];\n            this.combatImpacts.forEach(entry => this.DisposeObject(entry.mesh));\n            this.combatImpacts = [];\n            if (this.hoverTargetLine) {\n                (_a = this.scene) === null || _a === void 0 ? void 0 : _a.remove(this.hoverTargetLine);\n                this.hoverTargetLine = undefined;\n            }\n            if (this.hoverTurretRangeRing) {\n                (_b = this.scene) === null || _b === void 0 ? void 0 : _b.remove(this.hoverTurretRangeRing);\n                this.hoverTurretRangeRing = undefined;\n            }\n        };\n        this.lastMapTick = 0;\n        this.lastMinimapRender = 0;\n        this.RenderMap = (first = false) => {\n            if (!this.threeJsReady)\n                return;\n            console.log(`Rendering map for game`);\n            const mapData = this.state.gameData.map;\n            const mapSize = this.state.gameData.mapSize;\n            this.EnsureMapGround(mapSize, first);\n            this.EnsureFogOfWarMesh(mapSize);\n            const resourcePoints = mapData.filter(point => !!point.resource);\n            const resourceByIds = new Map(resourcePoints.map(point => [point.ids, point]));\n            [...this.mapPointSnapshots.keys()].forEach(ids => {\n                if (resourceByIds.has(ids))\n                    return;\n                this.mapPointSnapshots.delete(ids);\n                const objs = this.mapItems.get(ids);\n                if (!objs)\n                    return;\n                objs.forEach(obj => this.DisposeObject(obj));\n                this.mapItems.delete(ids);\n            });\n            resourcePoints.forEach(point => {\n                const snapshot = this.GetMapPointSnapshot(point);\n                const prevSnapshot = this.mapPointSnapshots.get(point.ids);\n                if (snapshot === prevSnapshot)\n                    return;\n                this.mapPointSnapshots.set(point.ids, snapshot);\n                if (this.mapItems.has(point.ids)) {\n                    this.RecreateMapPoint(point.ids, mapData);\n                }\n            });\n            this.SyncMinimapResourceMarkers();\n            this.UpdateFogOfWar(performance.now(), true);\n            this.MapTick();\n            this.TickPlayers();\n        };\n        this.DisposeObject = (obj) => {\n            obj === null || obj === void 0 ? void 0 : obj.traverse((child) => {\n                // Skip geometries that are shared across all instances of the same building\n                // type (they live in GenerateModelStorage). Disposing a shared geometry\n                // would corrupt every other building of that type still in the scene.\n                if (child.geometry && !child.userData.sharedGeometry) {\n                    child.geometry.dispose();\n                }\n                if (child.material) {\n                    if (Array.isArray(child.material)) {\n                        child.material.forEach(m => m.dispose());\n                    }\n                    else {\n                        child.material.dispose();\n                    }\n                }\n            });\n            obj.clear();\n            obj.removeFromParent();\n        };\n        this.RemovePlayer = (playerid) => {\n            var _a, _b;\n            const model = this.players_models.get(playerid);\n            if (model) {\n                (_a = this.players_sounds.get(playerid)) === null || _a === void 0 ? void 0 : _a.stop();\n                (_b = this.players_sounds.get(playerid)) === null || _b === void 0 ? void 0 : _b.disconnect();\n                const mixer = this.players_mixers.get(playerid);\n                if (mixer) {\n                    mixer.stopAllAction();\n                    mixer.uncacheRoot(model);\n                }\n                this.players_models.delete(playerid);\n                this.players_mixers.delete(playerid);\n                this.players_sounds.delete(playerid);\n                this.players_models_top.delete(playerid);\n                const markerGroup = this.players_minimap_markers.get(playerid);\n                if (markerGroup) {\n                    this.scene.remove(markerGroup);\n                    this.players_minimap_markers.delete(playerid);\n                }\n                this.scene.remove(model);\n            }\n        };\n        this.CreateMinimapMarker = (playerid) => {\n            if (this.players_minimap_markers.has(playerid))\n                return;\n            const color = this.GetMinimapPlayerColor(playerid);\n            const group = new three__WEBPACK_IMPORTED_MODULE_9__.Group();\n            const scale = this.GetMinimapPlayerMarkerScale();\n            const geo = new three__WEBPACK_IMPORTED_MODULE_9__.BufferGeometry();\n            const vertices = new Float32Array([\n                0,\n                0,\n                1.6, // острый конец (вперёд)\n                -0.85,\n                0,\n                -0.85, // левый угол\n                0.85,\n                0,\n                -0.85, // правый угол\n            ]);\n            geo.setAttribute('position', new three__WEBPACK_IMPORTED_MODULE_9__.BufferAttribute(vertices, 3));\n            const mat = new three__WEBPACK_IMPORTED_MODULE_9__.MeshBasicMaterial({\n                color,\n                side: three__WEBPACK_IMPORTED_MODULE_9__.DoubleSide,\n                depthTest: false,\n            });\n            const triangle = new three__WEBPACK_IMPORTED_MODULE_9__.Mesh(geo, mat);\n            triangle.layers.set(2);\n            group.add(triangle);\n            group.position.y = 15;\n            group.scale.setScalar(scale);\n            group.userData.material = mat;\n            this.scene.add(group);\n            this.players_minimap_markers.set(playerid, group);\n        };\n        this.SpawnPlayer = (playerid) => {\n            if (!this.threeJsInitialized) {\n                console.warn('Three.js not initialized yet');\n                return;\n            }\n            if (this.players_models.has(playerid))\n                return;\n            const playerData = this.state.players.find(q => q.id === playerid);\n            if (!playerData)\n                return;\n            if (!this.character_models) {\n                console.warn('Character models not loaded yet');\n                setTimeout(() => this.SpawnPlayer(playerid), 100);\n                return;\n            }\n            if (this.players_models.has(playerid))\n                return;\n            const position = this.players_positions.get(playerid);\n            if (!position)\n                return;\n            const object = (0,three_examples_jsm_utils_SkeletonUtils_js__WEBPACK_IMPORTED_MODULE_10__.clone)(_shared_system__WEBPACK_IMPORTED_MODULE_6__.System.randomElement(this.character_models));\n            object.position.set(position.x, 11, position.z);\n            this.players_models.set(playerid, object);\n            this.ApplyShadowForObject(object);\n            this.scene.add(object);\n            const mixer = new three__WEBPACK_IMPORTED_MODULE_9__.AnimationMixer(object);\n            this.players_mixers.set(playerid, mixer);\n            const anim = this.character_animations.get('idle');\n            if (anim) {\n                const action = mixer.clipAction(anim[0]);\n                action.setLoop(three__WEBPACK_IMPORTED_MODULE_9__.LoopRepeat, Infinity);\n                action.play();\n            }\n            const sound = playerid === _api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id\n                ? new three__WEBPACK_IMPORTED_MODULE_9__.Audio(this.audio_listener)\n                : new three__WEBPACK_IMPORTED_MODULE_9__.PositionalAudio(this.audio_listener);\n            sound.setBuffer(this.sounds.get('footstep2'));\n            if (sound instanceof three__WEBPACK_IMPORTED_MODULE_9__.PositionalAudio) {\n                sound.setRefDistance(400); // дистанция, где звук “нормальный”\n                sound.setMaxDistance(_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.sounds.max_distance); // дальше — почти не слышно\n                sound.setRolloffFactor(1.5); // насколько быстро затухает\n                sound.setDistanceModel('exponential');\n            }\n            this.players_sounds.set(playerid, sound);\n            object.add(sound);\n            sound.setLoop(true);\n            const box = new three__WEBPACK_IMPORTED_MODULE_9__.Box3().setFromObject(object);\n            this.players_models_top.set(playerid, box.max.y);\n            this.CreateMinimapMarker(playerid);\n        };\n        this.OnResize = () => {\n            if (!this.renderer)\n                return;\n            const canvas = this.renderer.domElement;\n            const canvas2 = this.rendererMini.domElement;\n            this.ResizeRenderer(this.renderer, canvas, this.camera);\n            this.ResizeRenderer(this.rendererMini, canvas2, this.cameraMini, false);\n            this.forceUpdate();\n            // console.log(`Resized to ${w}x${h}`);\n        };\n        this.VerifyCanControl = () => {\n            // if (this.state.stage !== 'ingame') return false;\n            const player = this.state.players.find(q => q.id === _api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id);\n            if (!player)\n                return false;\n            return true;\n        };\n        this.handleKeyPress = (keyPress) => {\n            if (this.graphicsMenuOpen)\n                return;\n            if (this.openedInterfacesBlock)\n                return;\n            if (!this.VerifyCanControl())\n                return;\n            const player = this.state.players.find(q => q.id === _api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id);\n            const velocity = {\n                x: 0,\n                z: 0,\n                shift: false,\n            };\n            if (keyPress.has('KeyW'))\n                velocity.z -= 1;\n            if (keyPress.has('KeyS'))\n                velocity.z += 1;\n            if (keyPress.has('KeyA'))\n                velocity.x -= 1;\n            if (keyPress.has('KeyD'))\n                velocity.x += 1;\n            if (keyPress.has('ShiftLeft') || keyPress.has('ShiftRight'))\n                velocity.shift = true;\n            this.players_velocities.set(player.id, velocity);\n            this.VerifyLocalVelocityAndPos();\n        };\n        this.OnUpdateGameData = (oldState, newState) => {\n            if (!this.threeJsReady)\n                return;\n            const oldBuildingsByUid = new Map(oldState.gameData.buildings.map(build => [build.uid, build]));\n            const newBuildingsByUid = new Map(newState.gameData.buildings.map(build => [build.uid, build]));\n            // Turret shot FX: ammo consumed or cooldown reset to turret value with target.\n            newState.gameData.buildings.forEach(build => {\n                var _a, _b, _c;\n                const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(b => b.id === build.id);\n                if (!(cfg === null || cfg === void 0 ? void 0 : cfg.turret))\n                    return;\n                const oldBuild = oldBuildingsByUid.get(build.uid);\n                const oldAmmo = this.GetTurretAmmoCount(oldBuild);\n                const newAmmo = this.GetTurretAmmoCount(build);\n                const oldCd = Number(((_a = oldBuild === null || oldBuild === void 0 ? void 0 : oldBuild.data.additional) === null || _a === void 0 ? void 0 : _a.turretCooldownLeftMS) || 0);\n                const newCd = Number(((_b = build.data.additional) === null || _b === void 0 ? void 0 : _b.turretCooldownLeftMS) || 0);\n                const shotByAmmo = oldBuild ? newAmmo < oldAmmo : false;\n                const shotByCdReset = oldBuild\n                    ? newCd > oldCd + 100 && newCd >= cfg.turret.cooldownMS * 0.8\n                    : false;\n                if (!shotByAmmo && !shotByCdReset)\n                    return;\n                const targetPos = (_c = build.data.additional) === null || _c === void 0 ? void 0 : _c.turretTargetPos;\n                if (!targetPos)\n                    return;\n                const from = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(build.pos.x * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize, 24, build.pos.z * _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.gridPointSize);\n                const to = new three__WEBPACK_IMPORTED_MODULE_9__.Vector3(targetPos.x, 18, targetPos.z);\n                this.SpawnTurretBulletFx(from, to);\n                // Play turret custom sound on each shot\n                this.PlayBuildingCustomSound(build.uid);\n            });\n            // Produce sound: detect when a building's output gains new items\n            newState.gameData.buildings.forEach(build => {\n                var _a, _b, _c, _d, _e;\n                const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(b => b.id === build.id);\n                if (!((_a = cfg === null || cfg === void 0 ? void 0 : cfg.sounds) === null || _a === void 0 ? void 0 : _a.produce))\n                    return;\n                const oldBuild = oldBuildingsByUid.get(build.uid);\n                if (!oldBuild)\n                    return;\n                const prevSnapshot = (_b = this.building_output_snapshots.get(build.uid)) !== null && _b !== void 0 ? _b : '';\n                const newSnapshot = JSON.stringify((_c = build.data.output) !== null && _c !== void 0 ? _c : []);\n                if (newSnapshot !== prevSnapshot) {\n                    this.building_output_snapshots.set(build.uid, newSnapshot);\n                    // Only play if output count increased (new item produced)\n                    const prevCount = ((_d = oldBuild.data.output) !== null && _d !== void 0 ? _d : []).reduce((s, slot) => { var _a; return s + Number((_a = slot === null || slot === void 0 ? void 0 : slot.amount) !== null && _a !== void 0 ? _a : 0); }, 0);\n                    const newCount = ((_e = build.data.output) !== null && _e !== void 0 ? _e : []).reduce((s, slot) => { var _a; return s + Number((_a = slot === null || slot === void 0 ? void 0 : slot.amount) !== null && _a !== void 0 ? _a : 0); }, 0);\n                    if (newCount > prevCount) {\n                        const pos = this.GetBuildWorldCenter(build);\n                        this.PlayPositionalSoundAt(pos, cfg.sounds.produce, this.GetBuildingEffectiveVolume(cfg));\n                    }\n                }\n            });\n            // Automaton destroy FX\n            oldState.gameData.automatons.forEach(oldAuto => {\n                const exists = newState.gameData.automatons.some(a => a.uid === oldAuto.uid);\n                if (!exists) {\n                    this.SpawnAutomatonDestroyFx(oldAuto.position, oldAuto.team);\n                }\n            });\n            if (JSON.stringify(oldState.gameData.map) !==\n                JSON.stringify(newState.gameData.map)) {\n                this.RenderMap();\n            }\n            if (JSON.stringify(oldState.players) !== JSON.stringify(newState.players)) {\n                this.TickPlayers();\n            }\n            if (JSON.stringify(oldState.gameData.teams) !==\n                JSON.stringify(newState.gameData.teams)) {\n                this.TickTeams();\n            }\n            if (JSON.stringify(oldState.gameData.buildings) !==\n                JSON.stringify(newState.gameData.buildings)) {\n                this.PushRenderedPlayersFromNewBlockedCells(oldState.gameData.buildings, newState.gameData.buildings, newState.gameData);\n                this.TickBuilds();\n            }\n            if (JSON.stringify(oldState.gameData.automatons) !==\n                JSON.stringify(newState.gameData.automatons)) {\n                this.TickAutomatons();\n            }\n        };\n        this.lastLocalSendData = '';\n        this.lastSendTime = 0;\n        this.LoadGraphicsSettings();\n    }\n    GetFrameIntervalMS() {\n        const fpsLimit = this.graphicsSettings.fpsLimit;\n        if (fpsLimit === 'unlimited')\n            return 0;\n        return Math.round(1000 / Math.max(1, Number(fpsLimit)));\n    }\n    LoadGraphicsSettings() {\n        try {\n            const raw = localStorage.getItem(this.graphicsStorageKey);\n            if (!raw)\n                return;\n            const parsed = JSON.parse(raw);\n            this.graphicsSettings = Object.assign(Object.assign({}, this.graphicsSettings), parsed);\n        }\n        catch (_a) {\n            // ignore corrupted local settings and keep defaults\n        }\n    }\n    SaveGraphicsSettings() {\n        try {\n            localStorage.setItem(this.graphicsStorageKey, JSON.stringify(this.graphicsSettings));\n        }\n        catch (_a) {\n            // ignore localStorage write failures\n        }\n    }\n    SetGraphicsSettings(patch) {\n        const merged = Object.assign(Object.assign({}, this.graphicsSettings), patch);\n        // Keep shadowsEnabled in sync with shadowQuality\n        if (patch.shadowQuality !== undefined) {\n            merged.shadowsEnabled = patch.shadowQuality !== 'off';\n        }\n        else if (patch.shadowsEnabled !== undefined) {\n            if (!patch.shadowsEnabled)\n                merged.shadowQuality = 'off';\n            else if (merged.shadowQuality === 'off')\n                merged.shadowQuality = 'medium';\n        }\n        this.graphicsSettings = merged;\n        this.SaveGraphicsSettings();\n        this.ApplyGraphicsSettings();\n        if (patch.soundVolume !== undefined)\n            this.UpdateAllIdleSoundVolumes();\n        this.forceUpdate();\n    }\n    GetTargetRenderHeight() {\n        switch (this.graphicsSettings.renderResolution) {\n            case '4k':\n                return 2160;\n            case '2k':\n                return 1440;\n            case '1080p':\n                return 1080;\n            case '720p':\n                return 720;\n            case '480p':\n                return 480;\n            case '240p':\n                return 240;\n            case '144p':\n                return 144;\n            default:\n                return 0;\n        }\n    }\n    ResizeRenderer(renderer, canvas, mapCamera, applyResolutionScale = true) {\n        const width = Math.max(1, canvas.clientWidth);\n        const height = Math.max(1, canvas.clientHeight);\n        const targetHeight = applyResolutionScale\n            ? this.GetTargetRenderHeight()\n            : 0;\n        const targetQuality = this.graphicsSettings.renderQuality;\n        if (targetHeight > 0) {\n            const scale = targetHeight / height;\n            renderer.setPixelRatio(targetQuality);\n            renderer.setSize(Math.max(1, Math.round(width * scale)), Math.max(1, Math.round(height * scale)), false);\n        }\n        else {\n            renderer.setPixelRatio(targetQuality);\n            renderer.setSize(width, height, false);\n        }\n        mapCamera.aspect = width / height;\n        mapCamera.updateProjectionMatrix();\n    }\n    UpdateShadowsForScene() {\n        var _a;\n        const shadowsEnabled = this.graphicsSettings.shadowsEnabled;\n        (_a = this.scene) === null || _a === void 0 ? void 0 : _a.traverse((node) => {\n            if (!(node === null || node === void 0 ? void 0 : node.isMesh))\n                return;\n            node.castShadow = shadowsEnabled;\n            node.receiveShadow = shadowsEnabled;\n        });\n    }\n    ApplyShadowForObject(object) {\n        if (!object)\n            return;\n        const shadowsEnabled = this.graphicsSettings.shadowsEnabled;\n        object.traverse((node) => {\n            if (!(node === null || node === void 0 ? void 0 : node.isMesh))\n                return;\n            node.castShadow = shadowsEnabled;\n            node.receiveShadow = shadowsEnabled;\n        });\n    }\n    GetVfxLimit() {\n        if (this.graphicsSettings.vfxQuality === 'low')\n            return 80;\n        if (this.graphicsSettings.vfxQuality === 'medium')\n            return 160;\n        return 300;\n    }\n    /** Sphere/geometry segment count for combat VFX meshes */\n    GetVfxSegments() {\n        if (this.graphicsSettings.vfxQuality === 'low')\n            return 4;\n        if (this.graphicsSettings.vfxQuality === 'medium')\n            return 7;\n        return 10;\n    }\n    /** Number of debris pieces when a building is destroyed */\n    GetDebrisCount() {\n        if (this.graphicsSettings.vfxQuality === 'low')\n            return 3;\n        if (this.graphicsSettings.vfxQuality === 'medium')\n            return 6;\n        return 10;\n    }\n    /** Whether building smoke/glow animations are rendered */\n    IsSmokeEnabled() {\n        return this.graphicsSettings.vfxQuality !== 'low';\n    }\n    TrimOldVfx(limit = this.GetVfxLimit()) {\n        while (this.combatProjectiles.length > limit) {\n            const old = this.combatProjectiles.shift();\n            if (old)\n                this.DisposeObject(old.mesh);\n        }\n        while (this.combatLasers.length > limit) {\n            const old = this.combatLasers.shift();\n            if (old)\n                this.DisposeObject(old.line);\n        }\n        while (this.combatImpacts.length > limit) {\n            const old = this.combatImpacts.shift();\n            if (old)\n                this.DisposeObject(old.mesh);\n        }\n    }\n    ApplyGraphicsSettings() {\n        var _a, _b;\n        if (this.renderer && this.canvasRef.current && this.camera) {\n            this.ResizeRenderer(this.renderer, this.canvasRef.current, this.camera);\n        }\n        if (this.rendererMini && this.canvasRefMini.current && this.cameraMini) {\n            this.ResizeRenderer(this.rendererMini, this.canvasRefMini.current, this.cameraMini, false // minimap always renders at native resolution\n            );\n        }\n        const shadowsOn = this.graphicsSettings.shadowsEnabled;\n        const shadowQ = this.graphicsSettings.shadowQuality;\n        if (this.renderer) {\n            this.renderer.shadowMap.enabled = shadowsOn;\n            // PCF type: low/medium → PCFSoftShadowMap, high/ultra → VSMShadowMap gives banding so keep PCFSoft, ultra gets higher res\n            this.renderer.shadowMap.type = three__WEBPACK_IMPORTED_MODULE_9__.PCFSoftShadowMap;\n            this.renderer.shadowMap.needsUpdate = true;\n        }\n        if (this.sunLight) {\n            this.sunLight.castShadow = shadowsOn;\n            if (shadowsOn) {\n                // Shadow map resolution: low=512, medium=1024, high=2048, ultra=4096\n                const shadowMapSizes = {\n                    off: 512,\n                    low: 512,\n                    medium: 1024,\n                    high: 2048,\n                    ultra: 4096,\n                };\n                const mapSize = (_a = shadowMapSizes[shadowQ]) !== null && _a !== void 0 ? _a : 1024;\n                this.sunLight.shadow.mapSize.set(mapSize, mapSize);\n                // Radius controls PCF softness: low=1 (hard), medium=2, high=3, ultra=4\n                const shadowRadii = {\n                    off: 1,\n                    low: 1,\n                    medium: 2,\n                    high: 3,\n                    ultra: 4,\n                };\n                this.sunLight.shadow.radius = (_b = shadowRadii[shadowQ]) !== null && _b !== void 0 ? _b : 2;\n                // Recreate shadow map so the new size takes effect\n                if (this.sunLight.shadow.map) {\n                    this.sunLight.shadow.map.dispose();\n                    this.sunLight.shadow.map = null;\n                }\n            }\n        }\n        this.UpdateShadowsForScene();\n        this.TrimOldVfx();\n    }\n    EnterEditMode(mode) {\n        if (this.openedInterfacesBlock)\n            return;\n        this.editBuildMode = mode;\n        this.editBuildRange = [\n            { x: -1, z: -1 },\n            { x: -1, z: -1 },\n        ];\n        this.CancelGhost();\n        this.forceUpdate();\n    }\n    RenderInventorySlot(params, item) {\n        const itemPreview = item ? this.imgObjects.get(item.id) : undefined;\n        const itemName = item ? this.GetItemName(item.id) : undefined;\n        const target = {\n            buildingUid: params.buildingUid,\n            type: params.type,\n            slot: params.slot,\n        };\n        // Определяем, является ли эта ячейка source (тащим отсюда)\n        const isDragSource = !!this.dragSource &&\n            this.dragSource.buildingUid === params.buildingUid &&\n            this.dragSource.type === params.type &&\n            this.dragSource.slot === params.slot;\n        // Определяем, является ли эта ячейка потенциальным target (пустая, можно бросить)\n        let isValidDropTarget = false;\n        if (this.dragSource && params.droppable && (!item || item.amount === 0)) {\n            // Проверяем CanDropToTarget для этой ячейки\n            isValidDropTarget = this.CanDropToTarget(target, this.dragSource);\n        }\n        const slotClass = [\n            'inventory-slot',\n            item ? 'filled' : '',\n            isDragSource ? 'drag-source' : '',\n            isValidDropTarget ? 'drag-target' : '',\n        ]\n            .filter(Boolean)\n            .join(' ');\n        const block = ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: slotClass, draggable: !!item && !!params.draggable, onContextMenu: e => {\n                e.preventDefault();\n                e.stopPropagation();\n            }, onDragStart: e => {\n                if (!item)\n                    return;\n                this.OnInventoryDragStart({\n                    buildingUid: params.buildingUid,\n                    type: params.type,\n                    slot: params.slot,\n                    itemId: item.id,\n                    amount: item.amount,\n                }, e, {\n                    itemName: itemName || this.GetItemName(item.id),\n                    itemPreview,\n                });\n            }, onDrag: e => {\n                this.UpdateInventoryDragCursor(e.clientX, e.clientY);\n            }, onDragOver: e => {\n                this.UpdateInventoryDragCursor(e.clientX, e.clientY);\n                if (!params.droppable)\n                    return;\n                const source = this.GetDragSourceFromEvent(e);\n                if (!this.CanDropToTarget(target, source))\n                    return;\n                e.preventDefault();\n                e.dataTransfer.dropEffect = 'move';\n            }, onDrop: e => {\n                if (!params.droppable)\n                    return;\n                this.HandleInventoryDrop(target, e);\n            }, onDragEnd: () => {\n                this.ResetInventoryDrag();\n                this.forceUpdate();\n            }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"slot-number\", children: params.slot + 1 }), item && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.Fragment, { children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"slot-item-preview\", children: itemPreview ? ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"img\", { src: itemPreview, alt: itemName, draggable: false })) : ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"slot-item-fallback\", children: itemName })) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"slot-amount\", children: [\"x\", item.amount] })] }))] }, `${params.type}-${params.slot}`));\n        return itemName ? ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(_components_tooltip__WEBPACK_IMPORTED_MODULE_8__.DrawToolTip, { text: itemName, trigger: `${!isDragSource ? 'hover' : 'none'}`, placement: 'top', children: block })) : (block);\n    }\n    RenderInventoryDragPreview() {\n        if (!this.dragPreview || !this.dragSource)\n            return null;\n        return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"inventory-drag-preview\", style: {\n                left: `${this.dragCursorPos.x + 14}px`,\n                top: `${this.dragCursorPos.y + 12}px`,\n            }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"slot-item-preview\", children: this.dragPreview.itemPreview ? ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"img\", { src: this.dragPreview.itemPreview, alt: this.dragPreview.itemName })) : ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"slot-item-fallback\", children: this.dragPreview.itemName })) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"slot-amount\", children: [\"x\", this.dragPreview.amount] })] }));\n    }\n    RenderPlayerUi(buildingUid = this.openedBuildingUid || 'self-panel') {\n        if (!this.showPlayerUi && !this.openedBuildingUid)\n            return null;\n        const player = this.me;\n        if (!player)\n            return null;\n        const activeCraft = player.data.craft;\n        const craftBuilds = this.builds.filter(build => !!build.craft);\n        return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"factory-overlay-panel player-panel\", onContextMenu: e => {\n                e.preventDefault();\n            }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"panel-head\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"h3\", { children: player.name }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { children: this.LangString('FactoryWarsTeamNameFallback', this.myTeamId + 1) })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_13__[\"default\"], { size: \"xs\", appearance: \"ghost\", onClick: () => {\n                                this.showPlayerUi = false;\n                                this.SyncOpenedInterfaces();\n                                this.forceUpdate();\n                            }, children: this.LangString('FactoryWarsUiClose') })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"craft-block\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"craft-title\", children: this.LangString('FactoryWarsCraftTitle') }), activeCraft && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"craft-active\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { children: this.LangString('FactoryWarsCraftActive', this.GetItemName(`build.${activeCraft.buildId}`), activeCraft.outputAmount) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_14__[\"default\"], { percent: Math.floor(this.GetPlayerCraftProgressPercent()) })] })), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"craft-list\", children: craftBuilds.map(build => {\n                                const recipe = build.craft;\n                                const outputAmount = Math.max(1, (recipe === null || recipe === void 0 ? void 0 : recipe.outputAmount) || 1);\n                                const buildItemId = `build.${build.id}`;\n                                const lockingRecipe = this.GetItemLockingRecipe(buildItemId);\n                                const isLocked = !!lockingRecipe;\n                                const canCraft = !isLocked && this.CanStartBuildCraft(build.id);\n                                return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: `craft-item${isLocked ? ' craft-item-locked' : ''}`, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"craft-item-head\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"span\", { children: [isLocked && (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { className: \"lock-icon\", children: \"\\uD83D\\uDD12 \" }), this.LocalizeConfigName(build.name, build.nameLang), \" x\", outputAmount] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_13__[\"default\"], { size: \"xs\", appearance: \"primary\", disabled: !canCraft, onClick: () => this.StartBuildCraft(build.id), children: this.LangString('FactoryWarsCraftButton') })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"craft-item-req\", children: ((recipe === null || recipe === void 0 ? void 0 : recipe.input) || [])\n                                                .map(req => `${this.GetItemName(req.id)} x${req.amount}`)\n                                                .join(', ') }), isLocked ? ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"craft-item-locked-hint\", children: this.LangString('FactoryWarsCraftRequiresResearch', this.LocalizeConfigName(lockingRecipe.name, lockingRecipe.nameLang) || lockingRecipe.name) })) : ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"craft-item-time\", children: this.LangString('FactoryWarsCraftTime', Math.round(((recipe === null || recipe === void 0 ? void 0 : recipe.durationMS) || 0) / 1000)) }))] }, build.id));\n                            }) })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"inventory-grid\", children: this.GetPlayerInventorySlots().map(({ slot, item }) => this.RenderInventorySlot({\n                        type: 'player',\n                        slot,\n                        buildingUid,\n                        draggable: true,\n                        droppable: true,\n                    }, item)) })] }));\n    }\n    RenderBuildingUi() {\n        var _a, _b, _c, _d, _e, _f;\n        const build = this.openedBuilding;\n        const cfg = this.openedBuildingConfig;\n        if (!build || !cfg)\n            return null;\n        const pveVoteState = this.GetPveVoteState();\n        const pveStatusText = this.GetPveWaveStatusText();\n        const recipeIndex = Number.isInteger((_a = build.data.additional) === null || _a === void 0 ? void 0 : _a.recipeIndex)\n            ? Number(build.data.additional.recipeIndex)\n            : 0;\n        const recipe = (_b = cfg.recipes) === null || _b === void 0 ? void 0 : _b[recipeIndex];\n        return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"factory-overlay-modal-wrap\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"factory-overlay-backdrop\", onClick: () => this.CloseBuildingUi(), onContextMenu: e => {\n                        e.preventDefault();\n                    } }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"factory-overlay-layout\", onContextMenu: e => {\n                        e.preventDefault();\n                    }, children: [this.RenderPlayerUi(build.uid), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"factory-overlay-modal building-modal\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"modal-head\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"h3\", { children: this.LocalizeConfigName(cfg.name, cfg.nameLang) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"span\", { children: [\"#\", build.uid, \" | \", this.GetTeamDisplayName(build.team)] })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_13__[\"default\"], { size: \"xs\", appearance: \"ghost\", onClick: () => this.CloseBuildingUi(), children: this.LangString('FactoryWarsUiClose') })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"modal-content\", children: build.id === 'laboratory' ? (this.RenderLaboratoryContent(build)) : ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.Fragment, { children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"modal-info\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { children: [this.LangString('FactoryWarsUiIdLabel'), \": \", cfg.id] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { children: [this.LangString('FactoryWarsUiHpLabel'), \":\", ' ', Math.floor((_d = (_c = build.data) === null || _c === void 0 ? void 0 : _c.hp) !== null && _d !== void 0 ? _d : cfg.maxHealth), \" /\", ' ', Math.floor((_f = (_e = build.data) === null || _e === void 0 ? void 0 : _e.maxHp) !== null && _f !== void 0 ? _f : cfg.maxHealth)] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { children: [this.LangString('FactoryWarsUiProgressLabel'), \":\", ' ', (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_14__[\"default\"], { percent: Math.floor(build.data.progress || 0) })] }), build.id === 'hub' && this.gameMode === 'pve' && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"pve-wave-vote-box\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"pve-wave-vote-title\", children: this.LangString('FactoryWarsPveWaveTitle') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { children: pveStatusText }), pveVoteState.canVote &&\n                                                                this.state.stage === 'ingame' ? ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.Fragment, { children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { children: this.LangString('FactoryWarsPveVotesEarlyStart', `${pveVoteState.votes}/${pveVoteState.totalPlayers}`) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_13__[\"default\"], { size: \"xs\", appearance: \"primary\", disabled: pveVoteState.hasVoted ||\n                                                                            pveVoteState.totalPlayers <= 0, onClick: () => this.SendAction('pve.wave.vote_start', true), children: pveVoteState.hasVoted\n                                                                            ? this.LangString('FactoryWarsPveAlreadyVoted')\n                                                                            : this.LangString('FactoryWarsPveStartWaveEarly') })] })) : ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { children: this.state.stage !== 'ingame'\n                                                                    ? this.LangString('FactoryWarsPveVoteOnlyDuringRunningGame')\n                                                                    : this.LangString('FactoryWarsPveVoteOnlyBetweenWaves') }))] })), cfg.recipes && cfg.recipes.length > 0 && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"recipe-box\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { children: this.LangString('FactoryWarsUiRecipeCounter', recipeIndex + 1, cfg.recipes.length) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { children: [this.LangString('FactoryWarsUiRecipeIn'), \":\", ' ', (recipe === null || recipe === void 0 ? void 0 : recipe.input.map(q => `${this.GetItemName(q.id)} x${q.amount}`).join(', ')) || '-'] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { children: [this.LangString('FactoryWarsUiRecipeOut'), \":\", ' ', (recipe === null || recipe === void 0 ? void 0 : recipe.output.map(q => `${this.GetItemName(q.id)} x${q.amount}`).join(', ')) || '-'] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_13__[\"default\"], { size: \"xs\", appearance: \"primary\", onClick: () => this.SelectNextBuildingRecipe(), children: this.LangString('FactoryWarsUiChangeRecipe') })] }))] }), (cfg.input || cfg.output) && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"io-columns\", children: [cfg.input && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"h4\", { children: this.LangString('FactoryWarsUiInput') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"inventory-grid compact\", children: this.GetBuildingSlots('input').map(({ slot, item }) => this.RenderInventorySlot({\n                                                                    type: 'input',\n                                                                    slot,\n                                                                    buildingUid: build.uid,\n                                                                    draggable: true,\n                                                                    droppable: true,\n                                                                }, item)) })] })), cfg.output && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"h4\", { children: this.LangString('FactoryWarsUiOutput') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"inventory-grid compact\", children: this.GetBuildingSlots('output').map(({ slot, item }) => this.RenderInventorySlot({\n                                                                    type: 'output',\n                                                                    slot,\n                                                                    buildingUid: build.uid,\n                                                                    draggable: true,\n                                                                    droppable: false,\n                                                                }, item)) })] }))] })), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"drag-hint\", children: this.LangString('FactoryWarsUiDragHint') })] })) })] })] })] }));\n    }\n    RenderLaboratoryContent(build) {\n        var _a, _b, _c, _d, _e, _f, _g, _h;\n        const teamData = this.myTeamData;\n        const openedRecipes = (teamData === null || teamData === void 0 ? void 0 : teamData.openedRecipes) || [];\n        const currentResearch = teamData === null || teamData === void 0 ? void 0 : teamData.currentResearch;\n        const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(q => q.id === build.id);\n        const maxHp = Math.floor((_c = (_b = (_a = build.data) === null || _a === void 0 ? void 0 : _a.maxHp) !== null && _b !== void 0 ? _b : cfg === null || cfg === void 0 ? void 0 : cfg.maxHealth) !== null && _c !== void 0 ? _c : 1);\n        const hp = Math.floor((_e = (_d = build.data) === null || _d === void 0 ? void 0 : _d.hp) !== null && _e !== void 0 ? _e : maxHp);\n        return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.Fragment, { children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"modal-info lab-research-info\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"h4\", { children: this.LangString('FactoryWarsLabTitle') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { children: [this.LangString('FactoryWarsUiHpLabel'), \": \", hp, \" / \", maxHp] }), currentResearch ? ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"lab-active-research\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { children: [this.LangString('FactoryWarsLabResearching'), \":\", ' ', (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"strong\", { children: (() => {\n                                                const recipe = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.repicePermissions.find(r => r.id === currentResearch.id);\n                                                return (this.LocalizeConfigName(recipe === null || recipe === void 0 ? void 0 : recipe.name, recipe === null || recipe === void 0 ? void 0 : recipe.nameLang) ||\n                                                    currentResearch.id);\n                                            })() })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { children: [this.LangString('FactoryWarsLabTicks'), \":\", ' ', currentResearch.ticks, \" /\", ' ', (_g = (_f = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.repicePermissions.find(r => r.id === currentResearch.id)) === null || _f === void 0 ? void 0 : _f.requireTicks) !== null && _g !== void 0 ? _g : '?'] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_14__[\"default\"], { percent: Math.floor(build.data.progress || 0) }), currentResearch.tickProgress > 0 && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"lab-tick-progress\", children: [this.LangString('FactoryWarsLabCurrentTick'), \":\", ' ', (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_14__[\"default\"], { percent: Math.floor(currentResearch.tickProgress), status: \"active\" })] }))] })) : ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"lab-no-research\", children: this.LangString('FactoryWarsLabNoResearchSelected') })), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"lab-recipe-list\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"h4\", { children: this.LangString('FactoryWarsLabAvailableRecipes') }), _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.repicePermissions.map(recipe => {\n                                    var _a;\n                                    const isOpened = openedRecipes.includes(recipe.id);\n                                    const isResearching = (currentResearch === null || currentResearch === void 0 ? void 0 : currentResearch.id) === recipe.id;\n                                    const isResearchable = this.IsRecipeResearchable(recipe.id);\n                                    const prereqsMissing = (_a = recipe.requireRecipes) === null || _a === void 0 ? void 0 : _a.filter(rId => !openedRecipes.includes(rId));\n                                    return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: `lab-recipe-item ${isOpened ? 'recipe-opened' : ''} ${isResearching ? 'recipe-active' : ''} ${!isOpened && !isResearchable ? 'recipe-blocked' : ''}`, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"lab-recipe-head\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"span\", { className: \"lab-recipe-name\", children: [isOpened ? ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { className: \"lab-recipe-icon-opened\", children: \"\\u2713 \" })) : isResearching ? ('⚗ ') : !isResearchable ? ('🔒 ') : (''), this.LocalizeConfigName(recipe.name, recipe.nameLang)] }), !isOpened && isResearchable && !isResearching && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_13__[\"default\"], { size: \"xs\", appearance: \"primary\", onClick: () => this.SelectLabResearch(recipe.id), children: this.LangString('FactoryWarsLabResearchButton') })), isResearching && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { className: \"lab-recipe-badge\", children: this.LangString('FactoryWarsLabInProgress') })), isOpened && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { className: \"lab-recipe-badge opened\", children: this.LangString('FactoryWarsLabOpened') }))] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"lab-recipe-desc\", children: [this.LangString('FactoryWarsLabUnlocks'), \":\", ' ', recipe.items\n                                                        .map(itemId => this.GetItemName(itemId))\n                                                        .join(', ')] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"lab-recipe-req\", children: [this.LangString('FactoryWarsLabRequires', recipe.requireTicks, recipe.requireTickSeconds), \":\", ' ', recipe.require\n                                                        .map(r => `${this.GetItemName(r.id)} x${r.amount} ${this.LangString('FactoryWarsLabPerTick')}`)\n                                                        .join(', ')] }), prereqsMissing && prereqsMissing.length > 0 && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"lab-recipe-prereq-warn\", children: [this.LangString('FactoryWarsLabRequiresRecipe'), \":\", ' ', prereqsMissing\n                                                        .map(rId => {\n                                                        const prereq = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.repicePermissions.find(r => r.id === rId);\n                                                        return (this.LocalizeConfigName(prereq === null || prereq === void 0 ? void 0 : prereq.name, prereq === null || prereq === void 0 ? void 0 : prereq.nameLang) || rId);\n                                                    })\n                                                        .join(', ')] }))] }, recipe.id));\n                                })] })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"io-columns\", children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"h4\", { children: [this.LangString('FactoryWarsLabResearchResources'), currentResearch && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"span\", { className: \"lab-input-hint\", children: [' ', \"(\", this.LangString('FactoryWarsLabLoadHint'), \":\", ' ', ((_h = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.repicePermissions\n                                                .find(r => r.id === currentResearch.id)) === null || _h === void 0 ? void 0 : _h.require.map(r => `${this.GetItemName(r.id)} x${r.amount}`).join(', ')) || '—', \")\"] }))] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"inventory-grid compact\", children: this.GetBuildingSlots('input').map(({ slot, item }) => this.RenderInventorySlot({\n                                    type: 'input',\n                                    slot,\n                                    buildingUid: build.uid,\n                                    draggable: true,\n                                    droppable: true,\n                                }, item)) })] }) })] }));\n    }\n    RenderAutomatonDragSelectOverlay() {\n        return null;\n    }\n    RenderGameNotifications() {\n        if (this.gameNotifications.length === 0)\n            return null;\n        return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"factory-notifications\", children: this.gameNotifications.map(notification => ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: `factory-notification ${notification.type}`, children: notification.text }, notification.id))) }));\n    }\n    RenderAutomatonGroupUi() {\n        if (this.selectedAutomatonUids.length === 0)\n            return null;\n        const automatons = this.state.gameData.automatons.filter(a => this.selectedAutomatonUids.includes(a.uid));\n        if (automatons.length === 0) {\n            this.selectedAutomatonUids = [];\n            return null;\n        }\n        const enemyHubs = this.state.gameData.buildings.filter(b => {\n            if (b.team === this.myTeamId)\n                return false;\n            const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(x => x.id === b.id);\n            return !!(cfg === null || cfg === void 0 ? void 0 : cfg.isTeamCore);\n        });\n        const sendAll = (targetUid) => {\n            automatons.forEach(a => this.SendAutomatonTarget(a.uid, targetUid));\n            this.selectedAutomatonUids = [];\n            this.forceUpdate();\n        };\n        return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"factory-overlay-modal-wrap\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"factory-overlay-backdrop\", onClick: () => {\n                        this.selectedAutomatonUids = [];\n                        this.forceUpdate();\n                    }, onContextMenu: e => e.preventDefault() }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"factory-overlay-layout\", onContextMenu: e => e.preventDefault(), children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"factory-overlay-modal building-modal\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"modal-head\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"h3\", { children: this.LangString('FactoryWarsAutomatonsTitle', automatons.length) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { children: this.LangString('FactoryWarsAutomatonsSelected', automatons.length) })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_13__[\"default\"], { size: \"xs\", appearance: \"ghost\", onClick: () => {\n                                            this.selectedAutomatonUids = [];\n                                            this.forceUpdate();\n                                        }, children: this.LangString('FactoryWarsUiClose') })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"modal-content\", children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"modal-info\", children: [automatons.map(a => {\n                                            const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsAutomatonUnits.find(u => u.id === a.id);\n                                            const targetBuild = a.targetUid\n                                                ? this.state.gameData.buildings.find(b => b.uid === a.targetUid)\n                                                : undefined;\n                                            const statusText = targetBuild\n                                                ? this.LangString('FactoryWarsAutomatonStatusAttack', this.GetTeamDisplayName(targetBuild.team))\n                                                : this.LangString('FactoryWarsAutomatonStatusIdle');\n                                            return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { style: {\n                                                    display: 'flex',\n                                                    alignItems: 'center',\n                                                    gap: 8,\n                                                    marginBottom: 4,\n                                                }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { style: { minWidth: 80, fontWeight: 600 }, children: this.LocalizeConfigName(cfg === null || cfg === void 0 ? void 0 : cfg.name, cfg === null || cfg === void 0 ? void 0 : cfg.nameLang) ||\n                                                            a.id }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { style: { minWidth: 100, opacity: 0.8 }, children: this.LangString('FactoryWarsHpCompact', Math.floor(a.hp), Math.floor(a.maxHp)) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { style: { opacity: 0.7, fontSize: 11 }, children: statusText })] }, a.uid));\n                                        }), automatons.some(a => !!a.targetUid) && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { style: { marginTop: 8 }, children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_13__[\"default\"], { size: \"xs\", appearance: \"ghost\", onClick: () => sendAll(null), children: this.LangString('FactoryWarsAutomatonsStopAll') }) })), enemyHubs.length > 0 && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { style: { marginTop: 12 }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { style: { marginBottom: 6, fontWeight: 600 }, children: this.LangString('FactoryWarsAutomatonsChooseAttackTeam') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { style: {\n                                                        display: 'flex',\n                                                        flexDirection: 'column',\n                                                        gap: 4,\n                                                    }, children: enemyHubs.map(b => {\n                                                        var _a, _b, _c;\n                                                        const cfg = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(bd => bd.id === b.id);\n                                                        const teamColor = this.GetTeamColorById(b.team);\n                                                        const teamName = this.GetTeamDisplayName(b.team);\n                                                        const hpPct = ((_a = b.data) === null || _a === void 0 ? void 0 : _a.hp) != null && ((_b = b.data) === null || _b === void 0 ? void 0 : _b.maxHp) != null\n                                                            ? Math.round((b.data.hp / b.data.maxHp) * 100)\n                                                            : 100;\n                                                        return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(rsuite__WEBPACK_IMPORTED_MODULE_13__[\"default\"], { size: \"xs\", appearance: \"subtle\", style: {\n                                                                textAlign: 'left',\n                                                                justifyContent: 'flex-start',\n                                                            }, onClick: () => sendAll(b.uid), children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { style: {\n                                                                        display: 'inline-block',\n                                                                        width: 10,\n                                                                        height: 10,\n                                                                        borderRadius: '50%',\n                                                                        background: teamColor,\n                                                                        marginRight: 6,\n                                                                    } }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { style: { color: teamColor, marginRight: 4 }, children: teamName }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { style: { marginRight: 4 }, children: (_c = cfg === null || cfg === void 0 ? void 0 : cfg.name) !== null && _c !== void 0 ? _c : b.id }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"span\", { style: { opacity: 0.6 }, children: [hpPct, \"% HP\"] })] }, b.uid));\n                                                    }) })] }))] }) })] }) })] }));\n    }\n    RenderDragAmountModal() {\n        if (!this.dragAmountModal.open)\n            return null;\n        return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"factory-overlay-modal-wrap top-layer\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"factory-overlay-backdrop\", onClick: () => {\n                        this.dragAmountModal = { open: false };\n                        this.forceUpdate();\n                    }, onContextMenu: e => {\n                        e.preventDefault();\n                    } }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"factory-overlay-modal amount-modal\", onContextMenu: e => {\n                        e.preventDefault();\n                    }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"h4\", { children: this.LangString('FactoryWarsTransferAmountTitle') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"amount-row\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { children: this.LangString('FactoryWarsTransferAmountMax', this.dragAmountModal.max) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_15__[\"default\"], { min: 1, max: this.dragAmountModal.max, value: this.dragAmountModal.amount, onChange: value => {\n                                        if (!this.dragAmountModal.open)\n                                            return;\n                                        this.dragAmountModal = Object.assign(Object.assign({}, this.dragAmountModal), { amount: Number(value || 1) });\n                                        this.forceUpdate();\n                                    } })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"modal-actions\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_13__[\"default\"], { appearance: \"ghost\", onClick: () => {\n                                        this.dragAmountModal = { open: false };\n                                        this.forceUpdate();\n                                    }, children: this.LangString('FactoryWarsTransferCancel') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_13__[\"default\"], { appearance: \"primary\", onClick: () => this.ConfirmDragAmount(), children: this.LangString('FactoryWarsTransferMove') })] })] })] }));\n    }\n    RenderGraphicsMenu() {\n        if (!this.graphicsMenuOpen)\n            return null;\n        const resolutionOptions = [\n            {\n                label: this.LangString('FactoryWarsGfxResolutionNative'),\n                value: 'native',\n            },\n            { label: '4K (2160p)', value: '4k' },\n            { label: '2K (1440p)', value: '2k' },\n            { label: 'FullHD (1080p)', value: '1080p' },\n            { label: 'HD (720p)', value: '720p' },\n            { label: 'SD (480p)', value: '480p' },\n            { label: 'SD (240p)', value: '240p' },\n            { label: 'SD (144p)', value: '144p' },\n        ];\n        const qualityOptions = [\n            { label: '200%', value: 2.0 },\n            { label: '150%', value: 1.5 },\n            { label: '120%', value: 1.2 },\n            { label: '100%', value: 1.0 },\n            { label: '80%', value: 0.8 },\n            { label: '60%', value: 0.6 },\n            { label: '40%', value: 0.4 },\n            { label: '20%', value: 0.2 },\n        ];\n        const fpsOptions = [\n            {\n                label: this.LangString('FactoryWarsGfxFpsUnlimited'),\n                value: 'unlimited',\n            },\n            { label: '120 FPS', value: 120 },\n            { label: '90 FPS', value: 90 },\n            { label: '60 FPS', value: 60 },\n            { label: '30 FPS', value: 30 },\n            { label: '20 FPS', value: 20 },\n        ];\n        const vfxOptions = [\n            { label: this.LangString('FactoryWarsGfxQualityHigh'), value: 'high' },\n            {\n                label: this.LangString('FactoryWarsGfxQualityMedium'),\n                value: 'medium',\n            },\n            { label: this.LangString('FactoryWarsGfxQualityLow'), value: 'low' },\n        ];\n        const shadowOptions = [\n            { label: this.LangString('FactoryWarsGfxOff'), value: 'off' },\n            { label: this.LangString('FactoryWarsGfxShadowLow'), value: 'low' },\n            { label: this.LangString('FactoryWarsGfxShadowMedium'), value: 'medium' },\n            { label: this.LangString('FactoryWarsGfxShadowHigh'), value: 'high' },\n            { label: this.LangString('FactoryWarsGfxShadowUltra'), value: 'ultra' },\n        ];\n        const boolToggle = (title, value, onChange) => ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"gfx-toggle-row\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { children: title }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"gfx-toggle-buttons\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_13__[\"default\"], { size: \"xs\", appearance: value ? 'primary' : 'ghost', onClick: () => onChange(true), children: this.LangString('FactoryWarsGfxOn') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_13__[\"default\"], { size: \"xs\", appearance: !value ? 'primary' : 'ghost', onClick: () => onChange(false), children: this.LangString('FactoryWarsGfxOffShort') })] })] }));\n        return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"factory-overlay-modal-wrap top-layer\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"factory-overlay-backdrop\", onClick: () => {\n                        this.graphicsMenuOpen = false;\n                        this.forceUpdate();\n                    }, onContextMenu: e => {\n                        e.preventDefault();\n                    } }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { ref: this.graphicsMenuRef, className: \"factory-overlay-modal graphics-menu\", onContextMenu: e => {\n                        e.preventDefault();\n                    }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"modal-head\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"h3\", { children: this.LangString('FactoryWarsGfxTitle') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { children: this.LangString('FactoryWarsGfxEscToClose') })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_13__[\"default\"], { size: \"xs\", appearance: \"ghost\", onClick: () => {\n                                        this.graphicsMenuOpen = false;\n                                        this.forceUpdate();\n                                    }, children: this.LangString('FactoryWarsUiClose') })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"graphics-grid\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"gfx-field\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"label\", { children: this.LangString('FactoryWarsGfxRenderResolution') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_16__[\"default\"], { data: resolutionOptions, value: this.graphicsSettings.renderResolution, cleanable: false, searchable: false, style: { width: '100%' }, container: () => { var _a; return (_a = this.graphicsMenuRef.current) !== null && _a !== void 0 ? _a : document.body; }, onChange: value => {\n                                                if (!value)\n                                                    return;\n                                                this.SetGraphicsSettings({\n                                                    renderResolution: value,\n                                                });\n                                            } })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"gfx-field\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"label\", { children: this.LangString('FactoryWarsGfxRenderQuality') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_16__[\"default\"], { data: qualityOptions, value: this.graphicsSettings.renderQuality, cleanable: false, searchable: false, style: { width: '100%' }, container: () => { var _a; return (_a = this.graphicsMenuRef.current) !== null && _a !== void 0 ? _a : document.body; }, onChange: value => {\n                                                if (!value)\n                                                    return;\n                                                this.SetGraphicsSettings({\n                                                    renderQuality: value,\n                                                });\n                                            } })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"gfx-field\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"label\", { children: this.LangString('FactoryWarsGfxFpsLimit') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_16__[\"default\"], { data: fpsOptions, value: this.graphicsSettings.fpsLimit, cleanable: false, searchable: false, style: { width: '100%' }, container: () => { var _a; return (_a = this.graphicsMenuRef.current) !== null && _a !== void 0 ? _a : document.body; }, onChange: value => {\n                                                if (value === undefined || value === null)\n                                                    return;\n                                                this.SetGraphicsSettings({\n                                                    fpsLimit: value,\n                                                });\n                                            } })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"gfx-field\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"label\", { children: this.LangString('FactoryWarsGfxVfxQuality') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_16__[\"default\"], { data: vfxOptions, value: this.graphicsSettings.vfxQuality, cleanable: false, searchable: false, style: { width: '100%' }, container: () => { var _a; return (_a = this.graphicsMenuRef.current) !== null && _a !== void 0 ? _a : document.body; }, onChange: value => {\n                                                if (!value)\n                                                    return;\n                                                this.SetGraphicsSettings({\n                                                    vfxQuality: value,\n                                                });\n                                            } })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"gfx-field\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"label\", { children: this.LangString('FactoryWarsGfxShadowQuality') }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_16__[\"default\"], { data: shadowOptions, value: this.graphicsSettings.shadowQuality, cleanable: false, searchable: false, style: { width: '100%' }, container: () => { var _a; return (_a = this.graphicsMenuRef.current) !== null && _a !== void 0 ? _a : document.body; }, onChange: value => {\n                                                if (!value)\n                                                    return;\n                                                this.SetGraphicsSettings({\n                                                    shadowQuality: value,\n                                                });\n                                            } })] }), boolToggle(this.LangString('FactoryWarsGfxMinimap'), this.graphicsSettings.minimapEnabled, next => this.SetGraphicsSettings({ minimapEnabled: next })), boolToggle(this.LangString('FactoryWarsGfxConveyorAnimations'), this.graphicsSettings.animatedConveyors, next => this.SetGraphicsSettings({ animatedConveyors: next })), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"gfx-field gfx-field-full\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"label\", { children: [this.LangString('FactoryWarsGfxSoundVolume'), \":\", ' ', this.graphicsSettings.soundVolume, \"%\"] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_17__[\"default\"], { value: this.graphicsSettings.soundVolume, min: 0, max: 100, step: 10, graduated: true, progress: true, renderMark: mark => `${mark}%`, onChange: value => {\n                                                this.SetGraphicsSettings({ soundVolume: value });\n                                            } })] })] })] })] }));\n    }\n    MainScreen() {\n        var _a, _b, _c, _d;\n        const meHp = (_b = (_a = this.me) === null || _a === void 0 ? void 0 : _a.data.hp) !== null && _b !== void 0 ? _b : 0;\n        const playerHpMax = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsPlayerData.hp || 1;\n        const hpPercent = (meHp / playerHpMax) * 100;\n        const pveStatusText = this.GetPveWaveStatusText();\n        const teamRows = this.activeTeamIndexes.map((teamId, order) => {\n            const teamData = this.state.gameData.teams[teamId];\n            const hp = this.GetTeamHp(teamId);\n            return {\n                teamId,\n                hp: hp.current,\n                maxHp: hp.max,\n                percent: Math.max(0, Math.min(100, (hp.current / hp.max) * 100)),\n                mine: teamId === this.myTeamId,\n                name: this.GetTeamDisplayName(teamId),\n                color: this.GetTeamColorByOrder(order),\n            };\n        });\n        return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { id: \"factorywars-main\", className: \"factorywars-main\", onMouseEnter: () => {\n                this.cursorInsideGame = true;\n            }, onMouseLeave: () => {\n                this.cursorInsideGame = false;\n                this.hoveredDropUid = undefined;\n                if (this.hoverGroup)\n                    this.hoverGroup.visible = false;\n                this.HideCombatHoverObjects();\n                this.CancelConveyorDragBuild();\n            }, onContextMenu: e => {\n                e.preventDefault();\n            }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"hud\", style: {\n                        width: `${(_c = this.canvasRef.current) === null || _c === void 0 ? void 0 : _c.clientWidth}px`,\n                        height: `${(_d = this.canvasRef.current) === null || _d === void 0 ? void 0 : _d.clientHeight}px`,\n                        opacity: this.threeJsLoadedFull ? 1 : 0,\n                        // position: 'relative',\n                    }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { ref: this.offscreenLayerRef, style: {\n                                position: 'absolute',\n                                inset: 0,\n                                overflow: 'hidden',\n                                pointerEvents: 'none',\n                                zIndex: 3,\n                            } }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"header\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"fps\", children: this.LangString('FactoryWarsHudFps', this.state.fps) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"timer\", children: _shared_system__WEBPACK_IMPORTED_MODULE_6__.System.secondsToString(0) }), pveStatusText && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"wave-status\", children: pveStatusText })), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"fullscreen\", onClick: () => this.ToggleFullScreen(), children: this.LangString(document.fullscreenElement\n                                        ? 'FactoryWarsHudFullScreenExit'\n                                        : 'FactoryWarsHudFullScreen') })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"teams-hp-board\", children: teamRows.map(row => ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: `team-hp-row ${row.mine ? 'mine' : ''}`, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"team-hp-head\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"span\", { className: \"team-name\", style: { color: row.color }, children: [row.teamId === this.myTeamId ? `👉 ` : '', row.name] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"span\", { children: [row.hp, \"/\", row.maxHp] })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"team-hp-track\", children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"team-hp-fill\", style: {\n                                                width: `${row.percent}%`,\n                                                backgroundColor: row.color,\n                                            } }) })] }, row.teamId))) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"control\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"left\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"me\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"inventory\" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"hp\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_14__[\"default\"], { percent: hpPercent, status: `${hpPercent > 25 ? 'active' : 'fail'}` }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { children: this.LangString('FactoryWarsHpShort', meHp, playerHpMax) })] })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"controls\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"keys info start\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { children: \"Esc\" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { children: this.LangString('FactoryWarsControlGraphicsMenu') })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"keys\", children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { children: \"W\" }) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"keys\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { children: \"A\" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { children: \"S\" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { children: \"D\" })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"info\", children: this.LangString('FactoryWarsControlMove') })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"keys info start\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { children: \"Shift\" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { children: this.LangString('FactoryWarsControlSprint') })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"keys info start\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { children: \"R\" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { children: this.LangString('FactoryWarsControlRotate') })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"keys info start\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { children: \"V\" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { children: this.LangString('FactoryWarsControlPickupNearest') })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"keys info start\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { children: \"Tab\" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { children: this.LangString('FactoryWarsControlPlayerInventory') })] })] })] })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"middle\", children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"builds\", children: _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryBuildCategories.map((category, categoryIndex) => {\n                                            const hotkey = categoryIndex + 1;\n                                            const isOpen = this.openCategoryMenuIndex === categoryIndex;\n                                            const buildsInCategory = this.GetBuildsInCategory(category.id);\n                                            const categoryName = this.LangString(category.name);\n                                            return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"category-hotkey-slot\", style: { position: 'relative' }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(_components_tooltip__WEBPACK_IMPORTED_MODULE_8__.DrawToolTip, { text: categoryName, trigger: 'hover', placement: isOpen ? 'leftStart' : 'top', children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: `build category-button ${isOpen ? 'active' : ''}`, onClick: () => this.TryOpenCategoryMenu(categoryIndex), children: [category.svg ? ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"img\", { className: \"category-icon\", src: `/images/${category.svg}`, alt: categoryName, draggable: false })) : ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"build-empty\", children: categoryName })), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { className: `hotkey ${typeof this.openCategoryMenuIndex === 'number' ? 'hide' : ''}`, children: hotkey })] }) }), isOpen && buildsInCategory.length > 0 && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"category-menu\", style: {\n                                                            position: 'absolute',\n                                                            bottom: '100%',\n                                                            left: 0,\n                                                            right: 0,\n                                                            display: 'flex',\n                                                            flexDirection: 'column',\n                                                            backgroundColor: 'rgba(0, 0, 0, 0.8)',\n                                                            border: '2px solid rgba(255, 255, 255, 0.3)',\n                                                            borderRadius: '4px',\n                                                            marginBottom: '4px',\n                                                            zIndex: 1000,\n                                                            minWidth: '100%',\n                                                        }, children: buildsInCategory.map((build, buildIndex) => {\n                                                            const buildItemId = `build.${build.id}`;\n                                                            const availableAmount = this.GetBuildInventoryAmount(build.id);\n                                                            const preview = this.imgObjects.get(buildItemId);\n                                                            const buildNumber = buildIndex + 1;\n                                                            return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"category-menu-item\", onClick: () => this.SelectBuildFromCategory(categoryIndex, buildIndex), title: this.LocalizeConfigName(build.name, build.nameLang), style: {\n                                                                    display: 'flex',\n                                                                    alignItems: 'center',\n                                                                    padding: '4px',\n                                                                    borderBottom: '1px solid rgba(255, 255, 255, 0.1)',\n                                                                    cursor: 'pointer',\n                                                                    transition: 'background-color 0.2s',\n                                                                }, onMouseEnter: e => {\n                                                                    const el = e.currentTarget;\n                                                                    el.style.backgroundColor =\n                                                                        'rgba(255, 255, 255, 0.1)';\n                                                                }, onMouseLeave: e => {\n                                                                    const el = e.currentTarget;\n                                                                    el.style.backgroundColor = 'transparent';\n                                                                }, children: [preview ? ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"img\", { src: preview, alt: build.name, style: {\n                                                                            width: '24px',\n                                                                            height: '24px',\n                                                                            marginRight: '4px',\n                                                                        }, draggable: false })) : ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { style: {\n                                                                            width: '24px',\n                                                                            height: '24px',\n                                                                            marginRight: '4px',\n                                                                            backgroundColor: 'rgba(255, 255, 255, 0.1)',\n                                                                        } })), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { style: {\n                                                                            flex: 1,\n                                                                            fontSize: '12px',\n                                                                            color: '#fff',\n                                                                        }, children: this.LocalizeConfigName(build.name, build.nameLang) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"span\", { style: {\n                                                                            marginRight: '4px',\n                                                                            fontSize: '11px',\n                                                                            color: '#999',\n                                                                        }, children: [\"x\", availableAmount] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { style: {\n                                                                            minWidth: '16px',\n                                                                            textAlign: 'center',\n                                                                            fontSize: '11px',\n                                                                            fontWeight: 'bold',\n                                                                            color: '#0f0',\n                                                                        }, children: buildNumber })] }, `category-item-${build.id}`));\n                                                        }) }))] }, `category-${category.id}`));\n                                        }) }) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"div\", { className: \"right\", children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { className: \"keys\", children: [\n                                                { icon: '🗃️', mode: 'copy', hotkey: 'C' },\n                                                { icon: '✂️', mode: 'cut', hotkey: 'X' },\n                                                { icon: '🗑️', mode: 'delete', hotkey: 'Delete' },\n                                            ].map(btn => {\n                                                return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(\"span\", { className: this.editBuildMode === btn.mode ? `active` : '', onClick: e => {\n                                                        e.preventDefault();\n                                                        this.EnterEditMode(btn.mode);\n                                                    }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { className: \"hotkey\", children: btn.hotkey }), btn.icon] }));\n                                            }) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"span\", { className: \"r\", children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"canvas\", { ref: this.canvasRefMini, style: {\n                                                    display: this.graphicsSettings.minimapEnabled\n                                                        ? 'block'\n                                                        : 'none',\n                                                }, onMouseMove: e => {\n                                                    this.UpdateMinimapResourceHover(e.clientX, e.clientY);\n                                                }, onMouseLeave: () => {\n                                                    this.HideMinimapTooltip();\n                                                } }) })] })] }), !this.openedBuildingUid && this.RenderPlayerUi('self-panel')] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"canvas\", { ref: this.canvasRef, style: {\n                        opacity: this.threeJsLoadedFull ? 1 : 0,\n                    }, onClick: e => {\n                        e.preventDefault();\n                        this.HandleCanvasClick();\n                    }, onMouseDown: e => {\n                        e.preventDefault();\n                        this.HandleCanvasMouseDown(e);\n                    }, onContextMenu: e => {\n                        e.preventDefault();\n                    }, onMouseMove: e => {\n                        this.HandleCanvasMouseMove(e);\n                    }, onMouseUp: e => {\n                        e.preventDefault();\n                        this.HandleCanvasMouseUp();\n                    }, onMouseEnter: e => {\n                        this.cursorInsideGame = true;\n                    }, onMouseLeave: e => {\n                        this.cursorInsideGame = false;\n                        this.CancelConveyorDragBuild();\n                    } }), this.RenderInventoryDragPreview(), this.RenderBuildingUi(), this.RenderAutomatonGroupUi(), this.RenderAutomatonDragSelectOverlay(), this.RenderDragAmountModal(), this.RenderGraphicsMenu(), this.RenderGameNotifications(), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { ref: this.automatonTooltipRef, className: \"automaton-tooltip\", style: { display: 'none', position: 'fixed', pointerEvents: 'none' } }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { ref: this.minimapTooltipRef, className: \"automaton-tooltip minimap-tooltip\", style: {\n                        display: 'none',\n                        position: 'fixed',\n                        pointerEvents: 'none',\n                        zIndex: 20,\n                    } })] }));\n    }\n    SettingsScreen() {\n        var _a, _b;\n        if (!this.isHost()) {\n            return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { style: { color: '#999', fontSize: '14px' }, children: this.LangString('FactoryWarsSettingHostOnly') }));\n        }\n        if (this.state.stage !== 'waiting') {\n            return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(\"div\", { style: { color: '#999', fontSize: '14px' }, children: this.LangString('FactoryWarsSettingWaitingOnly') }));\n        }\n        const gamemodes = [\n            { value: 'pvp', label: 'PvP' },\n            { value: 'pve', label: 'PvE' },\n        ];\n        const warFogEnabled = this.state.gameData.warFog !== false;\n        const mapSizeValue = Math.max(20, Math.min(200, Math.floor(Number(((_a = this.state.gameData.mapSize) === null || _a === void 0 ? void 0 : _a.x) ||\n            ((_b = this.state.gameData.mapSize) === null || _b === void 0 ? void 0 : _b.z) ||\n            100))));\n        const visibleScaleValue = Math.max(0.25, Math.min(3, Number(this.state.gameData.visibleScale || 1)));\n        const warFogOptions = [\n            {\n                label: this.LangString('FactoryWarsSettingWarFogOn'),\n                value: true,\n            },\n            {\n                label: this.LangString('FactoryWarsSettingWarFogOff'),\n                value: false,\n            },\n        ];\n        return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.Fragment, { children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(rsuite__WEBPACK_IMPORTED_MODULE_18__[\"default\"], { gutter: 10, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_19__[\"default\"], { xs: 24, sm: 12, children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_16__[\"default\"], { data: gamemodes, value: this.state.gameData.gamemode, placeholder: this.LangString('FactoryWarsSettingSelectGamemode'), label: this.LangString('FactoryWarsSettingGamemode'), cleanable: false, searchable: false, style: { width: '100%' }, onChange: e => {\n                                    if (!e)\n                                        return;\n                                    this.SendAction('set.gamemode', e);\n                                } }) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_19__[\"default\"], { xs: 24, sm: 12, children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_15__[\"default\"], { size: \"lg\", value: mapSizeValue, min: 20, max: 200, step: 1, formatter: value => `${this.LangString('FactoryWarsSettingMapSize')}: ${value}`, onChange: value => {\n                                    const parsed = Math.floor(Number(value) || mapSizeValue);\n                                    const next = Math.max(20, Math.min(200, parsed));\n                                    this.SendAction('set.map_size', { x: next, z: next });\n                                } }) })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(rsuite__WEBPACK_IMPORTED_MODULE_18__[\"default\"], { gutter: 10, style: { marginTop: 10 }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_19__[\"default\"], { xs: 24, sm: 12, children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_16__[\"default\"], { data: warFogOptions, value: warFogEnabled, placeholder: this.LangString('FactoryWarsSettingWarFog'), label: this.LangString('FactoryWarsSettingWarFog'), cleanable: false, searchable: false, style: { width: '100%' }, onChange: value => {\n                                    if (typeof value !== 'boolean')\n                                        return;\n                                    this.SendAction('set.war_fog', value);\n                                } }) }), warFogEnabled && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_19__[\"default\"], { xs: 24, sm: 12, children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_15__[\"default\"], { size: \"lg\", value: visibleScaleValue, min: 0.25, max: 3, step: 0.05, formatter: value => `${this.LangString('FactoryWarsSettingVisibleScale')}: ${value}`, onChange: value => {\n                                    const parsed = Number(value);\n                                    const next = Math.max(0.25, Math.min(3, parsed || 1));\n                                    this.SendAction('set.visible_scale', next);\n                                } }) }))] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_18__[\"default\"], { style: { marginTop: 12 }, children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_19__[\"default\"], { sm: 24, children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(rsuite__WEBPACK_IMPORTED_MODULE_13__[\"default\"], { appearance: \"ghost\", color: \"blue\", disabled: this.state.stage !== 'waiting', onClick: e => this.SendAction('generateMap'), children: this.LangString('FactoryWarsSettingGenerateMap') }) }) })] }));\n    }\n    IsBuildBlockingCell(buildId) {\n        const config = _shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryDataset.builds.find(build => build.id === buildId);\n        return !(config === null || config === void 0 ? void 0 : config.allowPlayerMove);\n    }\n    PushRenderedPlayersFromNewBlockedCells(oldBuildings, newBuildings, gameData) {\n        const oldBlocked = new Set(oldBuildings\n            .filter(build => this.IsBuildBlockingCell(build.id))\n            .flatMap(build => this.GetBuildOccupiedCells(build).map(cell => `${cell.x}:${cell.z}`)));\n        const newBlocked = new Set(newBuildings\n            .filter(build => this.IsBuildBlockingCell(build.id))\n            .flatMap(build => this.GetBuildOccupiedCells(build).map(cell => `${cell.x}:${cell.z}`)));\n        const justBlocked = new Set();\n        newBlocked.forEach(key => {\n            if (!oldBlocked.has(key))\n                justBlocked.add(key);\n        });\n        if (justBlocked.size === 0)\n            return;\n        this.players_models.forEach((model, playerid) => {\n            const playerCell = this.ToGameCoords(model.position);\n            const key = `${playerCell.x}:${playerCell.z}`;\n            if (!justBlocked.has(key))\n                return;\n            if (this.CanPlayerStandAtWorldPosition(playerid, model.position))\n                return;\n            const nearest = (0,_shared_games_params_factory__WEBPACK_IMPORTED_MODULE_4__.FactoryWarsFindNearestStandablePosition)(gameData, model.position);\n            if (!nearest)\n                return;\n            model.position.x = nearest.x;\n            model.position.z = nearest.z;\n            this.players_positions.set(playerid, { x: nearest.x, z: nearest.z });\n        });\n    }\n    OnJoinLobby() {\n        this.InitThreeJs();\n    }\n    OnLeaveLobby() {\n        this.ClearThreeJs();\n    }\n    VerifyLocalVelocityAndPos() {\n        var _a;\n        const velocity = this.players_velocities.get(_api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id) || { x: 0, z: 0 };\n        const pos = (_a = this.players_models.get(_api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id)) === null || _a === void 0 ? void 0 : _a.position;\n        if (!velocity || !pos)\n            return;\n        const position = { x: Math.floor(pos.x), z: Math.floor(pos.z) };\n        const sendData = {\n            velocity,\n            position,\n        };\n        const sendDataStr = JSON.stringify(sendData);\n        const tm = Date.now();\n        if (tm - this.lastSendTime < 100)\n            return;\n        if (sendDataStr === this.lastLocalSendData)\n            return;\n        this.lastSendTime = tm;\n        this.lastLocalSendData = sendDataStr;\n        this.SendActionSocket('velocity', sendData);\n    }\n    OnJoin() {\n        this.lastLocalSendData = '';\n        this.lastPickupNearestAt = 0;\n        this.OnMultiplayerAction('playersMove', (data) => {\n            data.players.forEach(player => {\n                this.players_positions.set(player.id, player.position);\n                if (player.id === _api__WEBPACK_IMPORTED_MODULE_2__.Api.user_id && !data.important)\n                    return;\n                // console.log(\n                //   `Received move data for player ${player.id}:`,\n                //   player.position,\n                //   player.velocity\n                // );\n                this.players_velocities.set(player.id, player.velocity);\n                if (data.important) {\n                    const model = this.players_models.get(player.id);\n                    if (model) {\n                        model.position.x = player.position.x;\n                        model.position.z = player.position.z;\n                    }\n                }\n            });\n        });\n        this.OnMultiplayerAction('manipulator.action', (data) => {\n            this.ActionManipulator(data);\n        });\n        this.OnMultiplayerAction('automaton.shot', (data) => {\n            this.ActionAutomatonShot(data);\n        });\n        this.velocityTimeout = setInterval(() => {\n            this.VerifyLocalVelocityAndPos();\n        }, 100);\n        // window.addEventListener('resize', this.OnResize);\n    }\n    OnLeave() {\n        this.CloseAllInterfaces(false);\n        this.HideMinimapTooltip();\n        if (this.velocityTimeout) {\n            clearInterval(this.velocityTimeout);\n            this.velocityTimeout = undefined;\n        }\n        // window.removeEventListener('resize', this.OnResize);\n    }\n}\n\n\n//# sourceURL=webpack:///./src/frontend/pages/games/multiplayer/FactoryWars.tsx?")},"./src/shared/games_params/factory.ts":(__unused_webpack_module,__webpack_exports__,__webpack_require__)=>{eval("/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   FactoryAutomatonAttackIgnores: () => (/* binding */ FactoryAutomatonAttackIgnores),\n/* harmony export */   FactoryBuildCategories: () => (/* binding */ FactoryBuildCategories),\n/* harmony export */   FactoryDataset: () => (/* binding */ FactoryDataset),\n/* harmony export */   FactoryWarsAutomatonUnits: () => (/* binding */ FactoryWarsAutomatonUnits),\n/* harmony export */   FactoryWarsCanPlayerStandAt: () => (/* binding */ FactoryWarsCanPlayerStandAt),\n/* harmony export */   FactoryWarsConveyorMoveVelocity: () => (/* binding */ FactoryWarsConveyorMoveVelocity),\n/* harmony export */   FactoryWarsDoesBuildingOccupyCell: () => (/* binding */ FactoryWarsDoesBuildingOccupyCell),\n/* harmony export */   FactoryWarsFaceCell: () => (/* binding */ FactoryWarsFaceCell),\n/* harmony export */   FactoryWarsFindNearestStandablePosition: () => (/* binding */ FactoryWarsFindNearestStandablePosition),\n/* harmony export */   FactoryWarsGameData: () => (/* binding */ FactoryWarsGameData),\n/* harmony export */   FactoryWarsGetBuildingFootprintSize: () => (/* binding */ FactoryWarsGetBuildingFootprintSize),\n/* harmony export */   FactoryWarsGetBuildingInPoint: () => (/* binding */ FactoryWarsGetBuildingInPoint),\n/* harmony export */   FactoryWarsGetBuildingOccupiedCells: () => (/* binding */ FactoryWarsGetBuildingOccupiedCells),\n/* harmony export */   FactoryWarsGetBuildingWorldCenter: () => (/* binding */ FactoryWarsGetBuildingWorldCenter),\n/* harmony export */   FactoryWarsGetTeamZoneIndexForPoint: () => (/* binding */ FactoryWarsGetTeamZoneIndexForPoint),\n/* harmony export */   FactoryWarsIsPointInsideMap: () => (/* binding */ FactoryWarsIsPointInsideMap),\n/* harmony export */   FactoryWarsPlayerData: () => (/* binding */ FactoryWarsPlayerData),\n/* harmony export */   FactoryWarsPveConfig: () => (/* binding */ FactoryWarsPveConfig),\n/* harmony export */   FactoryWarsPveDirectionLangKeys: () => (/* binding */ FactoryWarsPveDirectionLangKeys),\n/* harmony export */   FactoryWarsSnapToGrid: () => (/* binding */ FactoryWarsSnapToGrid),\n/* harmony export */   FactoryWarsThreeJsToGameCoords: () => (/* binding */ FactoryWarsThreeJsToGameCoords)\n/* harmony export */ });\n/* unused harmony exports FactoryWarsPveSpawnDirections, FactoryWarsGetPveWaveUnitAmount, FactoryWarsTeamDataDefault, FactoryWarsPveStateDefault, FactoryWarsGetTeamSpawnPoints, FactoryWarsIsPointInTeamZone, FactoryWarsGetCellProgress, FactoryWarsNormalizeRotation, FactoryWarsCanAutomatonStandAt, FactoryWarsAllowedRotations, GenerateFactoryMap */\n/* harmony import */ var _system__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../system */ \"./src/shared/system.ts\");\n\nconst FactoryWarsAutomatonUnits = [\n    {\n        id: 'scout',\n        name: 'Разведчик',\n        nameLang: 'FactoryWarsAutomatonScout',\n        craft: {\n            input: [\n                { id: 'ingot', amount: 2 },\n                { id: 'ammo_round', amount: 4 },\n            ],\n            durationMS: 8000,\n        },\n        damage: 16,\n        cooldownMS: 950,\n        range: 580,\n        maxHealth: 90,\n        speed: 290,\n        visibleRange: 900,\n        fly: 160,\n        shootSound: {\n            name: 'laser1.mp3',\n            volume: 0.8,\n        },\n    },\n    {\n        id: 'assault',\n        name: 'Штурмовик',\n        nameLang: 'FactoryWarsAutomatonAssault',\n        craft: {\n            input: [\n                { id: 'ingot', amount: 4 },\n                { id: 'ammo_round', amount: 10 },\n                { id: 'copper', amount: 4 },\n            ],\n            durationMS: 13000,\n        },\n        damage: 28,\n        cooldownMS: 1200,\n        range: 320,\n        maxHealth: 180,\n        speed: 240,\n        visibleRange: 700,\n        shootSound: {\n            name: 'laser2.mp3',\n            volume: 0.8,\n        },\n    },\n    {\n        id: 'siege',\n        name: 'Осадный автоматон',\n        nameLang: 'FactoryWarsAutomatonSiege',\n        craft: {\n            input: [\n                { id: 'ingot', amount: 7 },\n                { id: 'ammo_round', amount: 16 },\n                { id: 'zinc', amount: 6 },\n            ],\n            durationMS: 19000,\n        },\n        damage: 52,\n        cooldownMS: 1850,\n        range: 360,\n        maxHealth: 320,\n        speed: 190,\n        visibleRange: 750,\n        shootSound: {\n            name: 'laser3.mp3',\n            volume: 0.8,\n        },\n    },\n];\nconst FactoryWarsPveSpawnDirections = [\n    'north',\n    'north_east',\n    'east',\n    'south_east',\n    'south',\n    'south_west',\n    'west',\n    'north_west',\n];\nconst FactoryWarsPveDirectionLangKeys = {\n    north: 'FactoryWarsDirectionNorth',\n    north_east: 'FactoryWarsDirectionNorthEast',\n    east: 'FactoryWarsDirectionEast',\n    south_east: 'FactoryWarsDirectionSouthEast',\n    south: 'FactoryWarsDirectionSouth',\n    south_west: 'FactoryWarsDirectionSouthWest',\n    west: 'FactoryWarsDirectionWest',\n    north_west: 'FactoryWarsDirectionNorthWest',\n};\nconst FactoryWarsPveConfig = {\n    /** Через сколько секунд стартует первая волна */\n    firstWaveDelaySeconds: 90,\n    /** Сколько длится перерыв после полной зачистки волны */\n    intermissionSeconds: 45,\n    /** Набор направлений, из которых может прийти следующая волна */\n    directions: [...FactoryWarsPveSpawnDirections],\n    units: [\n        {\n            id: 'scout',\n            startWave: 1,\n            startAmount: 6,\n            perWaveAmount: 7,\n            amountIncreasePerWave: 1,\n        },\n        {\n            id: 'assault',\n            startWave: 3,\n            startAmount: 3,\n            perWaveAmount: 4,\n            amountIncreasePerWave: 1,\n        },\n        {\n            id: 'siege',\n            startWave: 5,\n            startAmount: 2,\n            perWaveAmount: 3,\n            amountIncreasePerWave: 1,\n        },\n    ],\n};\nconst FactoryWarsGetPveWaveUnitAmount = (waveNumber, config) => {\n    const wave = Math.max(1, Math.floor(Number(waveNumber) || 0));\n    if (wave < config.startWave)\n        return 0;\n    if (wave === config.startWave)\n        return Math.max(0, config.startAmount);\n    const offset = wave - config.startWave - 1;\n    return Math.max(0, config.perWaveAmount + offset * config.amountIncreasePerWave);\n};\nconst FactoryAutomatonAttackIgnores = [\n    \"conveyors\" /* FactoryBuildCategory.CONVEYORS */,\n    \"manipulators\" /* FactoryBuildCategory.MANIPULATORS */,\n];\nconst FactoryBuildCategories = [\n    {\n        id: \"conveyors\" /* FactoryBuildCategory.CONVEYORS */,\n        name: 'FactoryWarsCategoryConveyors',\n        svg: 'factorywars_category_conveyors.svg',\n    },\n    {\n        id: \"manipulators\" /* FactoryBuildCategory.MANIPULATORS */,\n        name: 'FactoryWarsCategoryManipulators',\n        svg: 'factorywars_category_manipulators.svg',\n    },\n    {\n        id: \"miners\" /* FactoryBuildCategory.MINERS */,\n        name: 'FactoryWarsCategoryMiners',\n        svg: 'factorywars_category_miners.svg',\n    },\n    {\n        id: \"productions\" /* FactoryBuildCategory.PRODUCTIONS */,\n        name: 'FactoryWarsCategoryProductions',\n        svg: 'factorywars_category_productions.svg',\n    },\n    {\n        id: \"military\" /* FactoryBuildCategory.MILITARY */,\n        name: 'FactoryWarsCategoryMilitary',\n        svg: 'factorywars_category_military.svg',\n    },\n    {\n        id: \"other\" /* FactoryBuildCategory.OTHER */,\n        name: 'FactoryWarsCategoryOther',\n        svg: 'factorywars_category_other.svg',\n        hideBuilds: ['hub'], // Скрываем хаб в категории прочее, так как он не является строением, а скорее базой, и его можно строить только в начале игры\n    },\n];\nconst FactoryDataset = {\n    minersData: [\n        {\n            // Обычная шахта: добывает все твёрдые ресурсы (руды, камень, уголь, сера, кристалл)\n            types: [\"item\" /* FactoryResourceType.ITEM */],\n            ids: ['miner'],\n            // Исключение: не добывает дерево и жидкости\n        },\n        {\n            // Скважина: добывает только жидкости\n            types: [\"liquid\" /* FactoryResourceType.LIQUID */],\n            ids: ['well'],\n        },\n        {\n            // Лесопилка: добывает только дерево\n            types: [\"item\" /* FactoryResourceType.ITEM */],\n            ids: ['lumberjack'],\n        },\n    ],\n    repicePermissions: [\n        {\n            id: 'miner',\n            name: 'Шахта',\n            nameLang: 'FactoryWarsResearchMiner',\n            items: ['build.miner'],\n            require: [{ id: 'iron', amount: 10 }],\n            requireTicks: 5,\n            requireTickSeconds: 10,\n        },\n        {\n            id: 'coal_smelting',\n            name: 'Угольная плавка',\n            nameLang: 'FactoryWarsResearchCoalSmelting',\n            items: ['carbon_steel'],\n            require: [\n                { id: 'coal', amount: 15 },\n                { id: 'ingot', amount: 5 },\n            ],\n            requireTicks: 6,\n            requireTickSeconds: 10,\n        },\n        {\n            id: 'alloy_metallurgy',\n            name: 'Сплавная металлургия',\n            nameLang: 'FactoryWarsResearchAlloyMetallurgy',\n            items: ['copper_ingot', 'zinc_ingot'],\n            require: [\n                { id: 'copper', amount: 10 },\n                { id: 'zinc', amount: 8 },\n            ],\n            requireTicks: 5,\n            requireTickSeconds: 10,\n        },\n        {\n            id: 'crystal_optics',\n            name: 'Кристальная оптика',\n            nameLang: 'FactoryWarsResearchCrystalOptics',\n            items: ['crystal_lens', 'crystal'],\n            requireRecipes: ['alloy_metallurgy'],\n            require: [\n                { id: 'crystal', amount: 5 },\n                { id: 'ingot', amount: 8 },\n            ],\n            requireTicks: 8,\n            requireTickSeconds: 12,\n        },\n        {\n            id: 'laser_technology',\n            name: 'Лазерные технологии',\n            nameLang: 'FactoryWarsResearchLaserTechnology',\n            items: ['laser_emitter'],\n            requireRecipes: ['crystal_optics', 'coal_smelting'],\n            require: [\n                { id: 'crystal_lens', amount: 3 },\n                { id: 'carbon_steel', amount: 4 },\n            ],\n            requireTicks: 10,\n            requireTickSeconds: 15,\n        },\n        {\n            id: 'research_lab',\n            name: 'Исследовательская лаборатория',\n            nameLang: 'FactoryWarsResearchResearchLab',\n            items: ['build.laboratory'],\n            require: [\n                { id: 'ingot', amount: 8 },\n                { id: 'stone', amount: 12 },\n                { id: 'copper', amount: 5 },\n            ],\n            requireTicks: 6,\n            requireTickSeconds: 12,\n        },\n        {\n            id: 'turret_defense',\n            name: 'Турельная оборона',\n            nameLang: 'FactoryWarsResearchTurretDefense',\n            items: ['build.turret'],\n            require: [\n                { id: 'ingot', amount: 10 },\n                { id: 'copper', amount: 8 },\n                { id: 'shell_casing', amount: 4 },\n            ],\n            requireTicks: 7,\n            requireTickSeconds: 12,\n        },\n        {\n            id: 'automaton_production',\n            name: 'Производство автоматонов',\n            nameLang: 'FactoryWarsResearchAutomatonProduction',\n            items: ['build.automaton_factory'],\n            requireRecipes: ['turret_defense'],\n            require: [\n                { id: 'ingot', amount: 12 },\n                { id: 'copper', amount: 12 },\n                { id: 'ammo_round', amount: 8 },\n            ],\n            requireTicks: 8,\n            requireTickSeconds: 14,\n        },\n    ],\n    items: [\n        {\n            id: 'wood',\n            type: \"item\" /* FactoryResourceType.ITEM */,\n            tags: [\"from_ground\" /* FactoryItemCategory.FROM_GROUND */],\n            name: 'Дерево',\n            nameLang: 'FactoryWarsItemWood',\n            color: '#7a4d2a',\n            model: 'wood',\n            modelResource: 'woodResource',\n            spawn: {\n                chance: 80,\n                min_distance: 2,\n                amount: [100, 500],\n                count: [10, 10],\n                size: [5, 10],\n            },\n            max_stack: 100,\n        },\n        {\n            id: 'stone',\n            type: \"item\" /* FactoryResourceType.ITEM */,\n            tags: [\"from_ground\" /* FactoryItemCategory.FROM_GROUND */],\n            name: 'Камень',\n            nameLang: 'FactoryWarsItemStone',\n            color: '#888780',\n            model: 'resource',\n            spawn: {\n                chance: 60,\n                min_distance: 2,\n                amount: [100, 500],\n                count: [10, 10],\n                size: [5, 10],\n            },\n            max_stack: 100,\n        },\n        {\n            id: 'iron',\n            type: \"item\" /* FactoryResourceType.ITEM */,\n            tags: [\"from_ground\" /* FactoryItemCategory.FROM_GROUND */, \"can_smelt\" /* FactoryItemCategory.CAN_SMELT */],\n            name: 'Железо',\n            nameLang: 'FactoryWarsItemIronOre',\n            color: '#D85A30',\n            model: 'resource',\n            spawn: {\n                chance: 40,\n                min_distance: 2,\n                amount: [100, 500],\n                count: [10, 10],\n                size: [5, 10],\n            },\n            max_stack: 100,\n        },\n        {\n            id: 'copper',\n            type: \"item\" /* FactoryResourceType.ITEM */,\n            tags: [\"from_ground\" /* FactoryItemCategory.FROM_GROUND */, \"can_smelt\" /* FactoryItemCategory.CAN_SMELT */],\n            name: 'Медь',\n            nameLang: 'FactoryWarsItemCopperOre',\n            color: '#C87941',\n            model: 'resource',\n            spawn: {\n                chance: 30,\n                min_distance: 2,\n                amount: [100, 500],\n                count: [8, 8],\n                size: [5, 10],\n            },\n            max_stack: 100,\n        },\n        {\n            id: 'zinc',\n            type: \"item\" /* FactoryResourceType.ITEM */,\n            tags: [\"from_ground\" /* FactoryItemCategory.FROM_GROUND */, \"can_smelt\" /* FactoryItemCategory.CAN_SMELT */],\n            name: 'Цинк',\n            nameLang: 'FactoryWarsItemZincOre',\n            color: '#7F77DD',\n            model: 'resource',\n            spawn: {\n                chance: 20,\n                min_distance: 2,\n                amount: [100, 500],\n                count: [5, 5],\n                size: [5, 10],\n            },\n            max_stack: 100,\n        },\n        {\n            id: 'water',\n            type: \"liquid\" /* FactoryResourceType.LIQUID */,\n            tags: [\"from_ground\" /* FactoryItemCategory.FROM_GROUND */],\n            name: 'Вода',\n            nameLang: 'FactoryWarsItemWater',\n            color: '#378ADD',\n            model: 'liquidResource',\n            modelResource: 'liquid',\n            spawn: {\n                chance: 20,\n                min_distance: 2,\n                amount: [100, 500],\n                count: [5, 5],\n                size: [5, 10],\n            },\n            max_stack: 100,\n        },\n        // Добавить сюда другие жидкости, если появятся, с modelResource: 'liquidResource'\n        {\n            id: 'ingot',\n            type: \"item\" /* FactoryResourceType.ITEM */,\n            tags: [],\n            name: 'Железный слиток',\n            nameLang: 'FactoryWarsItemIngot',\n            color: '#AAAAAA',\n            model: 'ingot',\n            max_stack: 100,\n        },\n        {\n            id: 'sulfur',\n            type: \"item\" /* FactoryResourceType.ITEM */,\n            tags: [\"from_ground\" /* FactoryItemCategory.FROM_GROUND */],\n            name: 'Сера',\n            nameLang: 'FactoryWarsItemSulfur',\n            color: '#d7c65a',\n            model: 'resource',\n            spawn: {\n                chance: 25,\n                min_distance: 2,\n                amount: [80, 260],\n                count: [5, 7],\n                size: [4, 7],\n            },\n            max_stack: 100,\n        },\n        {\n            id: 'gunpowder',\n            type: \"item\" /* FactoryResourceType.ITEM */,\n            tags: [],\n            name: 'Порох',\n            nameLang: 'FactoryWarsItemGunpowder',\n            color: '#626262',\n            model: 'resource',\n            max_stack: 100,\n        },\n        {\n            id: 'shell_casing',\n            type: \"item\" /* FactoryResourceType.ITEM */,\n            tags: [],\n            name: 'Оболочка патрона',\n            nameLang: 'FactoryWarsItemShellCasing',\n            color: '#be8747',\n            model: 'resource',\n            max_stack: 100,\n        },\n        {\n            id: 'ammo_round',\n            type: \"item\" /* FactoryResourceType.ITEM */,\n            tags: [],\n            name: 'Патрон',\n            nameLang: 'FactoryWarsItemAmmoRound',\n            model: 'resource',\n            color: '#d5b37c',\n            max_stack: 100,\n        },\n        {\n            id: 'coal',\n            type: \"item\" /* FactoryResourceType.ITEM */,\n            tags: [\"from_ground\" /* FactoryItemCategory.FROM_GROUND */],\n            name: 'Уголь',\n            nameLang: 'FactoryWarsItemCoal',\n            color: '#2a2a2a',\n            model: 'coal',\n            spawn: {\n                chance: 35,\n                min_distance: 2,\n                amount: [80, 400],\n                count: [8, 8],\n                size: [4, 8],\n            },\n            max_stack: 100,\n        },\n        {\n            id: 'crystal',\n            type: \"item\" /* FactoryResourceType.ITEM */,\n            tags: [\"from_ground\" /* FactoryItemCategory.FROM_GROUND */],\n            name: 'Кристалл',\n            nameLang: 'FactoryWarsItemCrystal',\n            color: '#7de8e8',\n            model: 'crystal',\n            spawn: {\n                chance: 12,\n                min_distance: 3,\n                amount: [40, 180],\n                count: [4, 6],\n                size: [3, 6],\n            },\n            max_stack: 50,\n        },\n        {\n            id: 'copper_ingot',\n            type: \"item\" /* FactoryResourceType.ITEM */,\n            tags: [],\n            name: 'Медный слиток',\n            nameLang: 'FactoryWarsItemCopperIngot',\n            color: '#C87941',\n            model: 'ingot',\n            max_stack: 100,\n        },\n        {\n            id: 'zinc_ingot',\n            type: \"item\" /* FactoryResourceType.ITEM */,\n            tags: [],\n            name: 'Цинковый слиток',\n            nameLang: 'FactoryWarsItemZincIngot',\n            color: '#7F77DD',\n            model: 'ingot',\n            max_stack: 100,\n        },\n        {\n            id: 'carbon_steel',\n            type: \"item\" /* FactoryResourceType.ITEM */,\n            tags: [],\n            name: 'Углеродистая сталь',\n            nameLang: 'FactoryWarsItemCarbonSteel',\n            color: '#505860',\n            model: 'resource',\n            max_stack: 100,\n        },\n        {\n            id: 'crystal_lens',\n            type: \"item\" /* FactoryResourceType.ITEM */,\n            tags: [],\n            name: 'Кристальная линза',\n            nameLang: 'FactoryWarsItemCrystalLens',\n            color: '#aaf0f0',\n            model: 'resource',\n            max_stack: 50,\n        },\n        {\n            id: 'laser_emitter',\n            type: \"item\" /* FactoryResourceType.ITEM */,\n            tags: [],\n            name: 'Лазерный излучатель',\n            nameLang: 'FactoryWarsItemLaserEmitter',\n            color: '#ff4444',\n            model: 'resource',\n            max_stack: 20,\n        },\n    ],\n    builds: [\n        {\n            id: 'chest',\n            category: \"other\" /* FactoryBuildCategory.OTHER */,\n            model: 'chest',\n            name: 'Сундук',\n            nameLang: 'FactoryWarsBuildChest',\n            size: { x: 2, y: 1 },\n            maxHealth: 100,\n            craft: {\n                input: [{ id: 'wood', amount: 10 }],\n                durationMS: 1000,\n            },\n            input: true,\n            canTakeFromInput: true,\n            progressTick: 1,\n            maxInputSlots: 5,\n            visibleRange: 380,\n        },\n        {\n            id: 'miner',\n            category: \"miners\" /* FactoryBuildCategory.MINERS */,\n            model: 'miner',\n            name: 'Шахта',\n            nameLang: 'FactoryWarsBuildMiner',\n            size: { x: 1, y: 1 },\n            maxHealth: 210,\n            craft: {\n                input: [\n                    { id: 'wood', amount: 12 },\n                    { id: 'stone', amount: 10 },\n                ],\n                durationMS: 8000,\n            },\n            input: false,\n            output: true,\n            progressTick: 1,\n            maxOutputSlots: 1,\n            autoOutput: true,\n            visibleRange: 520,\n        },\n        {\n            id: 'well',\n            category: \"miners\" /* FactoryBuildCategory.MINERS */,\n            model: 'well',\n            name: 'Скважина',\n            nameLang: 'FactoryWarsBuildWell',\n            size: { x: 1, y: 1 },\n            maxHealth: 180,\n            craft: {\n                input: [\n                    { id: 'stone', amount: 14 },\n                    { id: 'iron', amount: 8 },\n                ],\n                durationMS: 9000,\n            },\n            input: false,\n            output: true,\n            progressTick: 1,\n            maxOutputSlots: 1,\n            autoOutput: true,\n            visibleRange: 520,\n        },\n        {\n            id: 'lumberjack',\n            category: \"miners\" /* FactoryBuildCategory.MINERS */,\n            model: 'lumberjack',\n            name: 'Лесопилка',\n            nameLang: 'FactoryWarsBuildLumberjack',\n            size: { x: 1, y: 1 },\n            maxHealth: 160,\n            craft: {\n                input: [\n                    { id: 'stone', amount: 10 },\n                    { id: 'wood', amount: 8 },\n                ],\n                durationMS: 7000,\n            },\n            input: false,\n            output: true,\n            progressTick: 1,\n            maxOutputSlots: 1,\n            autoOutput: true,\n            visibleRange: 520,\n        },\n        {\n            id: 'smelter',\n            category: \"productions\" /* FactoryBuildCategory.PRODUCTIONS */,\n            model: 'smelter',\n            name: 'Плавильня',\n            nameLang: 'FactoryWarsBuildSmelter',\n            size: { x: 1, y: 1 },\n            maxHealth: 300,\n            craft: {\n                input: [\n                    { id: 'stone', amount: 14 },\n                    { id: 'iron', amount: 8 },\n                ],\n                durationMS: 12000,\n            },\n            input: true,\n            output: true,\n            progressTick: 5,\n            maxOutputSlots: 1,\n            maxInputSlots: 1,\n            autoOutput: false,\n            visibleRange: 420,\n            recipes: [\n                {\n                    input: [{ id: 'iron', amount: 1 }],\n                    output: [{ id: 'ingot', amount: 1 }],\n                    progressTick: 5,\n                },\n                {\n                    input: [{ id: 'sulfur', amount: 1 }],\n                    output: [{ id: 'gunpowder', amount: 2 }],\n                    progressTick: 5,\n                },\n                {\n                    input: [\n                        { id: 'copper', amount: 1 },\n                        { id: 'ingot', amount: 1 },\n                    ],\n                    output: [{ id: 'shell_casing', amount: 2 }],\n                    progressTick: 5,\n                },\n                {\n                    input: [\n                        { id: 'gunpowder', amount: 1 },\n                        { id: 'shell_casing', amount: 1 },\n                    ],\n                    output: [{ id: 'ammo_round', amount: 2 }],\n                    progressTick: 5,\n                },\n                {\n                    input: [{ id: 'copper', amount: 1 }],\n                    output: [{ id: 'copper_ingot', amount: 1 }],\n                    progressTick: 5,\n                },\n                {\n                    input: [{ id: 'zinc', amount: 1 }],\n                    output: [{ id: 'zinc_ingot', amount: 1 }],\n                    progressTick: 5,\n                },\n                {\n                    input: [\n                        { id: 'ingot', amount: 2 },\n                        { id: 'coal', amount: 3 },\n                    ],\n                    output: [{ id: 'carbon_steel', amount: 1 }],\n                    progressTick: 4,\n                },\n                {\n                    input: [{ id: 'crystal', amount: 1 }],\n                    output: [{ id: 'crystal_lens', amount: 1 }],\n                    progressTick: 3,\n                },\n            ],\n        },\n        {\n            id: 'fabric',\n            category: \"productions\" /* FactoryBuildCategory.PRODUCTIONS */,\n            model: 'fabric',\n            name: 'Фабрика',\n            nameLang: 'FactoryWarsBuildFabric',\n            size: { x: 1, y: 1 },\n            maxHealth: 300,\n            craft: {\n                input: [\n                    { id: 'stone', amount: 14 },\n                    { id: 'iron', amount: 8 },\n                ],\n                durationMS: 12000,\n            },\n            input: true,\n            output: true,\n            progressTick: 5,\n            maxOutputSlots: 2,\n            maxInputSlots: 5,\n            autoOutput: false,\n            visibleRange: 420,\n            recipes: [\n                {\n                    input: [{ id: 'iron', amount: 1 }],\n                    output: [{ id: 'ingot', amount: 1 }],\n                    progressTick: 5,\n                },\n                {\n                    input: [{ id: 'sulfur', amount: 1 }],\n                    output: [{ id: 'gunpowder', amount: 2 }],\n                    progressTick: 5,\n                },\n                {\n                    input: [\n                        { id: 'copper', amount: 1 },\n                        { id: 'ingot', amount: 1 },\n                    ],\n                    output: [{ id: 'shell_casing', amount: 2 }],\n                    progressTick: 5,\n                },\n                {\n                    input: [\n                        { id: 'gunpowder', amount: 1 },\n                        { id: 'shell_casing', amount: 1 },\n                    ],\n                    output: [{ id: 'ammo_round', amount: 2 }],\n                    progressTick: 5,\n                },\n                {\n                    input: [\n                        { id: 'carbon_steel', amount: 2 },\n                        { id: 'copper_ingot', amount: 1 },\n                    ],\n                    output: [{ id: 'laser_emitter', amount: 1 }],\n                    progressTick: 3,\n                },\n                {\n                    input: [\n                        { id: 'crystal_lens', amount: 2 },\n                        { id: 'ingot', amount: 3 },\n                    ],\n                    output: [{ id: 'laser_emitter', amount: 2 }],\n                    progressTick: 3,\n                },\n            ],\n        },\n        {\n            id: 'conveyor',\n            category: \"conveyors\" /* FactoryBuildCategory.CONVEYORS */,\n            model: 'conveyor',\n            name: 'Конвейер',\n            nameLang: 'FactoryWarsBuildConveyor',\n            size: { x: 1, y: 1 },\n            maxHealth: 130,\n            craft: {\n                input: [\n                    { id: 'wood', amount: 3 },\n                    { id: 'ingot', amount: 1 },\n                ],\n                durationMS: 3500,\n                outputAmount: 2,\n            },\n            input: true,\n            output: true,\n            progressTick: 0,\n            allowPlayerMove: true,\n            visibleRange: 0,\n            sounds: {\n                volume: 0.1,\n                idle: {\n                    inactive: 'transport-belt.mp3',\n                },\n            },\n        },\n        {\n            id: 'underground_conveyor',\n            category: \"conveyors\" /* FactoryBuildCategory.CONVEYORS */,\n            model: 'underground_conveyor',\n            name: 'Подземный конвейер',\n            nameLang: 'FactoryWarsBuildUndergroundConveyor',\n            size: { x: 1, y: 1 },\n            maxHealth: 160,\n            maxCells: 5,\n            craft: {\n                input: [\n                    { id: 'stone', amount: 6 },\n                    { id: 'ingot', amount: 2 },\n                ],\n                durationMS: 6500,\n            },\n            input: false,\n            output: false,\n            progressTick: 0,\n            allowPlayerMove: true,\n            visibleRange: 0,\n            sounds: {\n                volume: 0.2,\n                idle: {\n                    inactive: 'transport-belt.mp3',\n                },\n            },\n        },\n        {\n            id: 'manipulator',\n            category: \"manipulators\" /* FactoryBuildCategory.MANIPULATORS */,\n            model: 'manipulator',\n            name: 'Манипулятор',\n            nameLang: 'FactoryWarsBuildManipulator',\n            maxHealth: 240,\n            craft: {\n                input: [\n                    { id: 'ingot', amount: 3 },\n                    { id: 'copper', amount: 4 },\n                ],\n                durationMS: 10000,\n            },\n            input: false,\n            output: false,\n            progressTick: 0,\n            visibleRange: 260,\n            sounds: {\n                custom: 'inserters2.mp3', // В момент переноса предмета\n            },\n        },\n        {\n            id: 'laboratory',\n            category: \"other\" /* FactoryBuildCategory.OTHER */,\n            model: 'laboratory',\n            name: 'Лаборатория',\n            nameLang: 'FactoryWarsBuildLaboratory',\n            size: { x: 3, y: 3 },\n            maxHealth: 360,\n            craft: {\n                input: [\n                    { id: 'stone', amount: 8 },\n                    { id: 'ingot', amount: 5 },\n                    { id: 'copper', amount: 3 },\n                ],\n                durationMS: 20000,\n            },\n            input: true,\n            output: false,\n            progressTick: 0,\n            maxInputSlots: 8,\n            visibleRange: 700,\n            sounds: {\n                idle: {\n                    active: 'factory.mp3',\n                },\n            },\n        },\n        {\n            id: 'hub',\n            category: \"other\" /* FactoryBuildCategory.OTHER */,\n            model: 'hub',\n            name: 'Хаб',\n            nameLang: 'FactoryWarsBuildHub',\n            size: { x: 5, y: 5 },\n            maxHealth: 2000,\n            input: true,\n            output: false,\n            maxInputSlots: 30,\n            progressTick: 0,\n            allowManualBuild: false,\n            allowManualDestroy: false,\n            isTeamCore: true,\n            visibleRange: 900,\n        },\n        {\n            id: 'turret',\n            category: \"military\" /* FactoryBuildCategory.MILITARY */,\n            model: 'turret',\n            name: 'Туррель',\n            nameLang: 'FactoryWarsBuildTurret',\n            size: { x: 3, y: 3 },\n            maxHealth: 450,\n            craft: {\n                input: [\n                    { id: 'ingot', amount: 6 },\n                    { id: 'copper', amount: 6 },\n                    { id: 'shell_casing', amount: 6 },\n                ],\n                durationMS: 14000,\n            },\n            input: true,\n            output: true,\n            maxInputSlots: 1,\n            maxOutputSlots: 2,\n            progressTick: 0,\n            autoOutput: false,\n            visibleRange: 780,\n            turret: {\n                range: 1600,\n                damage: 34,\n                cooldownMS: 650,\n                ammoItemId: 'ammo_round',\n                spentAmmoOutputItemId: 'shell_casing',\n                spentAmmoOutputChance: 0.1,\n            },\n            sounds: {\n                custom: 'turret.mp3', // В момент выстрела\n            },\n        },\n        {\n            id: 'automaton_factory',\n            category: \"military\" /* FactoryBuildCategory.MILITARY */,\n            model: 'automaton_factory',\n            name: 'Завод автоматонов',\n            nameLang: 'FactoryWarsBuildAutomatonFactory',\n            size: { x: 3, y: 3 },\n            maxHealth: 700,\n            craft: {\n                input: [\n                    { id: 'ingot', amount: 8 },\n                    { id: 'copper', amount: 8 },\n                    { id: 'ammo_round', amount: 10 },\n                ],\n                durationMS: 20000,\n            },\n            input: true,\n            output: true,\n            maxInputSlots: 10,\n            maxOutputSlots: 6,\n            progressTick: 5,\n            autoOutput: false,\n            visibleRange: 680,\n            automatonFactory: {\n                pvpOnly: true,\n                unitIds: FactoryWarsAutomatonUnits.map(unit => unit.id),\n            },\n            recipes: FactoryWarsAutomatonUnits.map(unit => ({\n                input: unit.craft.input,\n                output: [{ id: `automaton.${unit.id}`, amount: 1 }],\n                progressTick: Math.max(1, Math.round(500 / Math.max(1000, unit.craft.durationMS))),\n            })),\n            sounds: {\n                idle: {\n                    active: 'factory.mp3',\n                },\n            },\n        },\n    ],\n    gridPointSize: 100, // Размер клетки в игровых единицах\n    playerSpeedMultipler: 500.0,\n    playerSpeedMultiplerShift: 2.0,\n    conveyorSpeedMultipler: 100.0,\n    undergroundConveyorSpeedMultipler: 650.0,\n    dropModelScale: 0.5,\n    /** Сколько секунд надо на перенос предмета манипулятором */\n    manipulatorSpeed: 1,\n    /** Количество слотов в инвентаре игрока */\n    playerMaxSlots: 20,\n    /** Дистанция для подбора предметов на земле, расстояние в threejs координатах */\n    pickupRange: 200,\n    /** Интервал времени для подбора предметов на земле в миллисекундах */\n    pickupIntervalMS: 100,\n    /** Лимит камеры */\n    cameraLimit: [600, 1400],\n    /** Радиус видимости игрока */\n    playerVisibleRange: 500,\n    /** Стандартные звуки игры */\n    sounds: {\n        /** Максимальное расстояние, на котором можно услышать звук */\n        max_distance: 1200,\n        /** Звук при клике игрока (взаимодействие с интерфейсом или entity в игре) */\n        click_sound: 'button-click.mp3',\n        /** Звук при постройке здания */\n        build: 'placing.mp3',\n    },\n    /** Расстояние между выброшенными предметами при перемещении на конвеерной ленте */\n    drop_move_distance: 20,\n};\nFactoryDataset.builds.forEach(build => {\n    if (build.allowManualBuild === false)\n        return;\n    FactoryDataset.items.push({\n        id: `build.${build.id}`,\n        name: build.name,\n        nameLang: build.nameLang,\n        tags: [],\n        type: \"building\" /* FactoryResourceType.BUILDING */,\n        model: build.model,\n        max_stack: 10,\n    });\n});\nconst FactoryWarsAutomatonBlueprintNameLangKeys = {\n    scout: 'FactoryWarsItemAutomatonBlueprintScout',\n    assault: 'FactoryWarsItemAutomatonBlueprintAssault',\n    siege: 'FactoryWarsItemAutomatonBlueprintSiege',\n};\nFactoryWarsAutomatonUnits.forEach(unit => {\n    FactoryDataset.items.push({\n        id: `automaton.${unit.id}`,\n        name: `Чертеж: ${unit.name}`,\n        nameLang: FactoryWarsAutomatonBlueprintNameLangKeys[unit.id] ||\n            'FactoryWarsItemAutomatonBlueprint',\n        tags: [],\n        type: \"item\" /* FactoryResourceType.ITEM */,\n        model: 'resource',\n        max_stack: 20,\n        color: '#8ea0b8',\n    });\n});\nconst FactoryWarsTeamDataDefault = {\n    inventory: {},\n    score: 0,\n    /** Список открытых рецептов */\n    openedRecipes: [],\n    /** Текущее исследование */\n    currentResearch: undefined,\n    spawnPoint: { x: 0, z: 0 },\n};\nconst FactoryWarsPveStateDefault = {\n    status: 'countdown',\n    currentWave: 0,\n    countdownEndsAt: 0,\n    direction: undefined,\n    allocatedAutomatons: 0,\n    aliveAutomatons: 0,\n    votedPlayerIds: [],\n};\nconst FactoryWarsGameData = {\n    mapSize: { x: 100, z: 100 },\n    map: [],\n    warFog: false,\n    visibleScale: 1.0,\n    /** Текущий игровой режим */\n    gamemode: 'pvp',\n    pve: Object.assign({}, FactoryWarsPveStateDefault),\n    buildings: [],\n    automatons: [],\n    teams: [],\n    /** Список вещей на земле. Координаты в threejs сетке */\n    drop: [],\n};\nconst FactoryWarsPlayerData = {\n    score: 0,\n    craft: undefined,\n    inventory: {\n        [0]: { id: 'build.miner', amount: 2 },\n        [1]: { id: 'build.smelter', amount: 2 },\n        [2]: { id: 'build.conveyor', amount: 5 },\n        [3]: { id: 'build.manipulator', amount: 2 },\n        [4]: { id: 'build.laboratory', amount: 1 },\n        [5]: { id: 'build.chest', amount: 2 },\n    },\n    hp: 1000,\n};\nconst ClampCell = (value, maxExclusive) => {\n    if (maxExclusive <= 0)\n        return 0;\n    return Math.min(maxExclusive - 1, Math.max(0, Math.round(value)));\n};\nconst FactoryWarsIsPointInsideMap = (point, mapSize) => {\n    return (point.x >= 0 && point.z >= 0 && point.x < mapSize.x && point.z < mapSize.z);\n};\nconst FactoryWarsGetTeamSpawnPoints = (mapSize, teamCount) => {\n    const normalizedTeams = Math.max(0, Math.floor(teamCount));\n    if (normalizedTeams === 0)\n        return [];\n    const centerX = (mapSize.x - 1) / 2;\n    const centerZ = (mapSize.z - 1) / 2;\n    if (normalizedTeams === 1) {\n        return [\n            {\n                x: ClampCell(centerX, mapSize.x),\n                z: ClampCell(centerZ, mapSize.z),\n            },\n        ];\n    }\n    if (normalizedTeams === 2) {\n        return [\n            {\n                x: ClampCell((mapSize.x - 1) * 0.25, mapSize.x),\n                z: ClampCell(centerZ, mapSize.z),\n            },\n            {\n                x: ClampCell((mapSize.x - 1) * 0.75, mapSize.x),\n                z: ClampCell(centerZ, mapSize.z),\n            },\n        ];\n    }\n    const radiusX = Math.max(1, (mapSize.x - 1) * 0.32);\n    const radiusZ = Math.max(1, (mapSize.z - 1) * 0.32);\n    const used = new Set();\n    const points = [];\n    for (let i = 0; i < normalizedTeams; i++) {\n        const angle = -Math.PI / 2 + (i * Math.PI * 2) / normalizedTeams;\n        const baseX = ClampCell(centerX + Math.cos(angle) * radiusX, mapSize.x);\n        const baseZ = ClampCell(centerZ + Math.sin(angle) * radiusZ, mapSize.z);\n        let selected = { x: baseX, z: baseZ };\n        if (used.has(`${baseX}:${baseZ}`)) {\n            let found = false;\n            const maxRadius = Math.max(mapSize.x, mapSize.z);\n            for (let r = 1; r <= maxRadius && !found; r++) {\n                for (let dx = -r; dx <= r && !found; dx++) {\n                    for (let dz = -r; dz <= r; dz++) {\n                        const x = ClampCell(baseX + dx, mapSize.x);\n                        const z = ClampCell(baseZ + dz, mapSize.z);\n                        const key = `${x}:${z}`;\n                        if (!used.has(key)) {\n                            selected = { x, z };\n                            found = true;\n                            break;\n                        }\n                    }\n                }\n            }\n        }\n        used.add(`${selected.x}:${selected.z}`);\n        points.push(selected);\n    }\n    return points;\n};\nconst FactoryWarsGetTeamZoneIndexForPoint = (point, mapSize, teamCount) => {\n    const normalizedTeams = Math.max(0, Math.floor(teamCount));\n    if (normalizedTeams <= 0)\n        return -1;\n    if (!FactoryWarsIsPointInsideMap(point, mapSize))\n        return -1;\n    if (normalizedTeams === 1)\n        return 0;\n    if (normalizedTeams === 2) {\n        return point.x < mapSize.x / 2 ? 0 : 1;\n    }\n    const spawns = FactoryWarsGetTeamSpawnPoints(mapSize, normalizedTeams);\n    let bestIndex = 0;\n    let bestDistance = Number.POSITIVE_INFINITY;\n    for (let i = 0; i < spawns.length; i++) {\n        const dx = point.x - spawns[i].x;\n        const dz = point.z - spawns[i].z;\n        const distSq = dx * dx + dz * dz;\n        if (distSq < bestDistance) {\n            bestDistance = distSq;\n            bestIndex = i;\n        }\n    }\n    return bestIndex;\n};\nconst FactoryWarsIsPointInTeamZone = (point, mapSize, teamCount, teamIndex) => {\n    if (!Number.isFinite(teamIndex) || teamIndex < 0)\n        return false;\n    const zoneIndex = FactoryWarsGetTeamZoneIndexForPoint(point, mapSize, teamCount);\n    return zoneIndex === teamIndex;\n};\nconst FactoryWarsSnapToGrid = (position) => {\n    return {\n        x: Math.round(position.x / FactoryDataset.gridPointSize) *\n            FactoryDataset.gridPointSize,\n        z: Math.round(position.z / FactoryDataset.gridPointSize) *\n            FactoryDataset.gridPointSize,\n    };\n};\nconst FactoryWarsThreeJsToGameCoords = (position) => {\n    return {\n        x: Math.round(position.x / FactoryDataset.gridPointSize),\n        z: Math.round(position.z / FactoryDataset.gridPointSize),\n    };\n};\n/**\n * Прогресс объекта внутри текущей клетки по направлению движения ленты.\n * 0 = только въехал с \"правильной\" стороны, 1 = у выхода.\n * Может быть > 1 (overshoot) или < 0 (въехал с обратной стороны).\n */\nconst FactoryWarsGetCellProgress = (position, rotation) => {\n    const gridSize = FactoryDataset.gridPointSize;\n    const cellCenterX = Math.round(position.x / gridSize) * gridSize;\n    const cellCenterZ = Math.round(position.z / gridSize) * gridSize;\n    if (rotation === 0)\n        return (position.z - (cellCenterZ - gridSize / 2)) / gridSize; // +Z\n    if (rotation === -2)\n        return (cellCenterZ + gridSize / 2 - position.z) / gridSize; // -Z\n    if (rotation === -1)\n        return (cellCenterX + gridSize / 2 - position.x) / gridSize; // -X\n    if (rotation === -3)\n        return (position.x - (cellCenterX - gridSize / 2)) / gridSize; // +X\n    return 0;\n};\nconst FactoryWarsGetBuildingInPoint = (gameData, position) => {\n    return gameData.buildings.find(build => FactoryWarsDoesBuildingOccupyCell(build, position));\n};\nconst FactoryWarsNormalizeRotation = (rotation) => {\n    return ((Math.round(Number(rotation) || 0) % 4) + 4) % 4;\n};\nconst FactoryWarsGetBuildingFootprintSize = (buildId, rotation = 0) => {\n    const cfg = FactoryDataset.builds.find(build => build.id === buildId);\n    const base = (cfg === null || cfg === void 0 ? void 0 : cfg.size) || { x: 1, y: 1 };\n    const normalizedRotation = FactoryWarsNormalizeRotation(rotation);\n    if (normalizedRotation % 2 === 1) {\n        return { x: base.y, z: base.x };\n    }\n    return { x: base.x, z: base.y };\n};\nconst FactoryWarsGetBuildingOccupiedCells = (build) => {\n    const anchor = build.pos || build.position;\n    if (!anchor)\n        return [];\n    const size = FactoryWarsGetBuildingFootprintSize(build.id, build.rotation);\n    const cells = [];\n    for (let dx = 0; dx < size.x; dx++) {\n        for (let dz = 0; dz < size.z; dz++) {\n            cells.push({\n                x: anchor.x + dx,\n                z: anchor.z + dz,\n            });\n        }\n    }\n    return cells;\n};\nconst FactoryWarsDoesBuildingOccupyCell = (build, point) => {\n    return FactoryWarsGetBuildingOccupiedCells(build).some(cell => cell.x === point.x && cell.z === point.z);\n};\nconst FactoryWarsGetBuildingWorldCenter = (build) => {\n    const anchor = build.pos || build.position;\n    if (!anchor)\n        return { x: 0, z: 0 };\n    const size = FactoryWarsGetBuildingFootprintSize(build.id, build.rotation);\n    return {\n        x: (anchor.x + (size.x - 1) / 2) * FactoryDataset.gridPointSize,\n        z: (anchor.z + (size.z - 1) / 2) * FactoryDataset.gridPointSize,\n    };\n};\nconst FactoryWarsCanPlayerStandAt = (gameData, position) => {\n    const gamepos = FactoryWarsThreeJsToGameCoords(position);\n    const build = FactoryWarsGetBuildingInPoint(gameData, gamepos);\n    if (!build)\n        return true;\n    const buildConfig = FactoryDataset.builds.find(cfg => cfg.id === build.id);\n    return !!(buildConfig === null || buildConfig === void 0 ? void 0 : buildConfig.allowPlayerMove);\n};\nconst FactoryWarsCanAutomatonStandAt = (gameData, position) => {\n    const gamepos = FactoryWarsThreeJsToGameCoords(position);\n    const build = FactoryWarsGetBuildingInPoint(gameData, gamepos);\n    if (!build)\n        return true;\n    return build.id === 'conveyor';\n};\nconst FactoryWarsFindNearestStandablePosition = (gameData, position, maxRadius = 24) => {\n    const origin = FactoryWarsThreeJsToGameCoords(position);\n    const toWorld = (x, z) => ({\n        x: x * FactoryDataset.gridPointSize,\n        z: z * FactoryDataset.gridPointSize,\n    });\n    const inBounds = (x, z) => {\n        if (!(gameData === null || gameData === void 0 ? void 0 : gameData.mapSize))\n            return true;\n        return x >= 0 && z >= 0 && x < gameData.mapSize.x && z < gameData.mapSize.z;\n    };\n    if (inBounds(origin.x, origin.z) &&\n        FactoryWarsCanPlayerStandAt(gameData, position)) {\n        return FactoryWarsSnapToGrid(position);\n    }\n    for (let radius = 1; radius <= maxRadius; radius++) {\n        let best;\n        for (let dx = -radius; dx <= radius; dx++) {\n            for (let dz = -radius; dz <= radius; dz++) {\n                const onBorder = Math.abs(dx) === radius || Math.abs(dz) === radius;\n                if (!onBorder)\n                    continue;\n                const tx = origin.x + dx;\n                const tz = origin.z + dz;\n                if (!inBounds(tx, tz))\n                    continue;\n                const candidate = toWorld(tx, tz);\n                if (!FactoryWarsCanPlayerStandAt(gameData, candidate))\n                    continue;\n                const distSq = dx * dx + dz * dz;\n                if (!best || distSq < best.distSq) {\n                    best = { x: candidate.x, z: candidate.z, distSq };\n                }\n            }\n        }\n        if (best) {\n            return best;\n        }\n    }\n    return undefined;\n};\nconst FactoryWarsConveyorMoveVelocity = (position, gameData, includeBuildings = ['conveyor', 'underground_conveyor'], \n/** Сила притяжения к центру ленты (0 = выключено, рекомендуется 2–5) */\ncenteringStrength = 1) => {\n    if (!includeBuildings)\n        includeBuildings = ['conveyor', 'underground_conveyor'];\n    const gamepos = FactoryWarsThreeJsToGameCoords(position);\n    const build = FactoryWarsGetBuildingInPoint(gameData, gamepos);\n    if (!build || !includeBuildings.includes(build.id))\n        return { x: 0, z: 0, rotation: 0 };\n    const rot = build.rotation % 4;\n    const multipler = FactoryDataset.conveyorSpeedMultipler;\n    const gridSize = FactoryDataset.gridPointSize;\n    const centerX = gamepos.x * gridSize;\n    const centerZ = gamepos.z * gridSize;\n    // Нормализует вектор (vMain, vLateral) так, чтобы его длина равнялась multipler\n    const normalizeToSpeed = (vMain, vLateral) => {\n        const mag = Math.sqrt(vMain * vMain + vLateral * vLateral);\n        if (mag === 0)\n            return { main: 0, lateral: 0 };\n        const scale = multipler / mag;\n        return { main: vMain * scale, lateral: vLateral * scale };\n    };\n    if (rot === 0 || rot === -2) {\n        // Движение вдоль Z → центрируем по X\n        const vMain = (rot === 0 ? 1 : -1) * multipler;\n        const vLateral = centeringStrength > 0 ? (centerX - position.x) * centeringStrength : 0;\n        const { main, lateral } = normalizeToSpeed(vMain, vLateral);\n        return { x: lateral, z: main, rotation: rot };\n    }\n    else {\n        // Движение вдоль X → центрируем по Z\n        const vMain = (rot === -1 ? -1 : 1) * multipler;\n        const vLateral = centeringStrength > 0 ? (centerZ - position.z) * centeringStrength : 0;\n        const { main, lateral } = normalizeToSpeed(vMain, vLateral);\n        return { x: main, z: lateral, rotation: rot };\n    }\n};\nconst FactoryWarsAllowedRotations = [0, -1, -2, -3];\nconst FactoryWarsFaceCell = (position, rot) => {\n    if (rot === 0)\n        return { x: position.x + 0, z: position.z + 1 };\n    if (rot === -1)\n        return { x: position.x - 1, z: position.z + 0 };\n    if (rot === -2)\n        return { x: position.x + 0, z: position.z - 1 };\n    if (rot === -3)\n        return { x: position.x + 1, z: position.z + 0 };\n    return position;\n};\n/**\n * Генерирует карту ресурсов с учётом зон (кластеров) и размера пятна.\n * @param mapSize Размер карты в клетках (x, z).\n * @returns Массив точек карты с ресурсами.\n */\nconst GenerateFactoryMap = (mapSize) => {\n    const points = [];\n    // Вспомогательная функция: проверка доступности позиции для ресурса заданного типа\n    const isPositionFree = (x, z, resourceId, minDistance, excludeSelf = false) => {\n        var _a;\n        // Выход за границы карты\n        if (x < 0 || x >= mapSize.x || z < 0 || z >= mapSize.z)\n            return false;\n        // Проверка на уже существующую точку с такими же координатами\n        if (points.some(p => p.x === x && p.z === z))\n            return false;\n        // Проверка расстояния до точек того же ресурса\n        for (const p of points) {\n            if (((_a = p.resource) === null || _a === void 0 ? void 0 : _a.id) === resourceId) {\n                const dist = Math.hypot(p.x - x, p.z - z);\n                if (dist < minDistance)\n                    return false;\n            }\n        }\n        return true;\n    };\n    // Генерация связного кластера (пятна) вокруг центра\n    const generateCluster = (centerX, centerZ, targetSize, resourceId, minDistance) => {\n        const cluster = [];\n        const queue = [];\n        // Добавляем центр, если он свободен\n        if (!isPositionFree(centerX, centerZ, resourceId, minDistance))\n            return null;\n        cluster.push({ x: centerX, z: centerZ });\n        queue.push({ x: centerX, z: centerZ });\n        const directions = [\n            [0, 1],\n            [1, 0],\n            [0, -1],\n            [-1, 0], // 4 соседа (можно добавить диагонали, но для пятна достаточно)\n        ];\n        let attempts = 0;\n        const maxAttempts = 200;\n        while (cluster.length < targetSize && attempts < maxAttempts) {\n            // Берём случайную точку из уже добавленных\n            const seed = queue[Math.floor(Math.random() * queue.length)];\n            // Перемешиваем направления для случайного порядка\n            const shuffledDirs = [...directions];\n            for (let i = shuffledDirs.length - 1; i > 0; i--) {\n                const j = Math.floor(Math.random() * (i + 1));\n                [shuffledDirs[i], shuffledDirs[j]] = [shuffledDirs[j], shuffledDirs[i]];\n            }\n            let added = false;\n            for (const [dx, dz] of shuffledDirs) {\n                const nx = seed.x + dx;\n                const nz = seed.z + dz;\n                if (nx >= 0 && nx < mapSize.x && nz >= 0 && nz < mapSize.z) {\n                    // Не должна быть уже в кластере\n                    if (!cluster.some(c => c.x === nx && c.z === nz)) {\n                        if (isPositionFree(nx, nz, resourceId, minDistance)) {\n                            cluster.push({ x: nx, z: nz });\n                            queue.push({ x: nx, z: nz });\n                            added = true;\n                            break;\n                        }\n                    }\n                }\n            }\n            if (!added) {\n                // Если не удалось добавить соседа, убираем текущий seed из очереди, чтобы не мешал\n                const idx = queue.findIndex(p => p.x === seed.x && p.z === seed.z);\n                if (idx !== -1)\n                    queue.splice(idx, 1);\n            }\n            attempts++;\n        }\n        if (cluster.length < targetSize) {\n            // Не удалось вырастить кластер нужного размера\n            return null;\n        }\n        return cluster;\n    };\n    // Перебираем каждый тип ресурса\n    for (const item of FactoryDataset.items) {\n        const spawn = item.spawn;\n        if (!spawn)\n            continue;\n        // Количество зон для этого ресурса\n        const zoneCount = _system__WEBPACK_IMPORTED_MODULE_0__.System.getRandomInt(spawn.count[0], spawn.count[1]);\n        // Размер одной зоны (количество клеток)\n        const zoneSize = _system__WEBPACK_IMPORTED_MODULE_0__.System.getRandomInt(spawn.size[0], spawn.size[1]);\n        let generatedZones = 0;\n        let zoneAttempts = 0;\n        const maxZoneAttempts = 50; // чтобы избежать бесконечного цикла\n        while (generatedZones < zoneCount && zoneAttempts < maxZoneAttempts) {\n            zoneAttempts++;\n            // Пытаемся найти подходящий центр зоны\n            let centerX, centerZ;\n            let centerAttempts = 0;\n            const maxCenterAttempts = 100;\n            let centerFound = false;\n            while (!centerFound && centerAttempts < maxCenterAttempts) {\n                centerX = _system__WEBPACK_IMPORTED_MODULE_0__.System.getRandomInt(0, mapSize.x);\n                centerZ = _system__WEBPACK_IMPORTED_MODULE_0__.System.getRandomInt(0, mapSize.z);\n                // Центр должен быть свободен и соблюдать min_distance до точек того же ресурса\n                if (isPositionFree(centerX, centerZ, item.id, spawn.min_distance)) {\n                    centerFound = true;\n                    break;\n                }\n                centerAttempts++;\n            }\n            if (!centerFound) {\n                // Не удалось найти место для центра – пробуем следующую зону\n                continue;\n            }\n            // Генерируем кластер (пятно) вокруг найденного центра\n            const cluster = generateCluster(centerX, centerZ, zoneSize, item.id, spawn.min_distance);\n            if (cluster && cluster.length >= Math.min(zoneSize, 1)) {\n                // Успешно сгенерировали кластер – добавляем точки на карту\n                for (const pos of cluster) {\n                    const amount = _system__WEBPACK_IMPORTED_MODULE_0__.System.getRandomInt(spawn.amount[0], spawn.amount[1]);\n                    points.push({\n                        x: pos.x,\n                        z: pos.z,\n                        ids: _system__WEBPACK_IMPORTED_MODULE_0__.System.randomString(8), // увеличил длину для уникальности\n                        resource: {\n                            id: item.id,\n                            amount: amount,\n                            total: amount,\n                        },\n                    });\n                }\n                generatedZones++;\n            }\n        }\n        // Если после всех попыток не удалось сгенерировать нужное количество зон – просто пропускаем оставшиеся\n    }\n    return points;\n};\n\n\n//# sourceURL=webpack:///./src/shared/games_params/factory.ts?")}}]);